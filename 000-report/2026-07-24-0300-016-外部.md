# dS IBP Package 016 独立 Benchmark 审计报告

> **报告编号**: 2026-07-24-package016-独立验证
> **Package 版本**: 016 (`package_016.wl`, 14130 行, 659207 字节)
> **任务书**: `independent-benchmark/independent-benchmark.md` (1576 行)
> **执行环境**: WolframScript (Windows) + Kira (WSL Ubuntu-22.04)
> **手推笔记**: `000_note/` (20 个文件, 覆盖 §2-17 全部核心公式)

---

## 1. 执行摘要

本轮审计从任务书 §2 的 SK 费曼规则出发，独立推导了全部 10 个函数族的完整 seed 关系，
然后加载 package_016 执行 Phase 2 全量比较。

**核心结论**:
- Package 版本确认: `$dSIBPVersion = "016"` ✓
- **全部 10 个函数族 seed 生成通过** ✓
- **全部 10 个函数族 DSLinear 通过** (linearQ=True) ✓
- **ds 总导数乘积法则验证通过** (差值严格为零) ✓
- **DSKiraExport 成功** (status="ready") ✓
- **Kira 约化 → DE → Scaling 完整闭环通过** ✓
  - 19 masters（与 reference 一致）
  - DE 闭合（residual: s11→0, P0→0）
  - DSScaleCheck: **passed**
- **未发现物理/数学错误**
- 1 个 cosmetic 级 package 消息格式化 bug（StringRiffle，不影响功能）
- 1 个环境注意事项（UTF-8 编码需显式指定）

---

## 2. 来源隔离声明

- [x] Phase 1 推导仅使用任务书 §2 定义和公开 Hankel 恒等式 (DLMF §10.6, §10.29)
- [x] 未读取 `package/` 中的程序代码逻辑（Phase 2 前）
- [x] 手推笔记独立存放于 `000_note/`

---

## 3. 审计发现

### 3.1 发现 #1: UTF-8 编码加载 [环境注意事项]

**逻辑链路**:
1. `package_016.wl` 文件编码为 UTF-8（hex 验证：`E6 98 AF` = "是"）
2. Windows 系统默认 `$CharacterEncoding` 非 UTF-8
3. WolframScript 默认用系统编码读取 → 中文字符串被错误解析 → line 18 触发 `Syntax::sntx`
4. Package 加载不完整 → `$dSIBPVersion` 未赋值

**修复**:
```mathematica
Get["package_016.wl", CharacterEncoding -> "UTF-8"];
(* → Version: 016 ✓ *)
```

**严重程度**: minor（有明确 workaround）
**建议**: package 文件头添加 BOM 或在文档中注明编码要求。

---

### 3.2 发现 #2: timeOnly 族 StringRiffle 消息格式化 bug [cosmetic]

**逻辑链路**:
1. `ibpMode -> "timeOnly"` 且 `loopExternalMomenta -> {}` 时，无外部运动学不变量
2. DSInit 内部尝试生成 "可用 DSRedefineParameters 重定义参数" 的提示信息
3. 参数列表为空/缺失时，`StringRiffle` 收到非法输入 `"KeyAbsent" -> custom1^2 -> ...`
4. 触发 `StringRiffle::list` 和 `StringJoin::string` 警告

**关键代码片段** (package 内部消息生成):
```
StringRiffle::list: List expected at position 1 in
  StringRiffle["KeyAbsent" -> custom1^2 -> "KeyAbsent" -> custom2^2, , ].
```

**影响**: 仅产生警告消息，**不影响功能**。DSInit 仍返回 `status -> "initialized"`，
DSSeeds/DSLinear 在正确输入下正常工作。
**严重程度**: cosmetic
**触发条件**: timeOnly 族 + 无 loopExternalMomenta（如 atomic_massless_line、atomic_massive_line）

---

### 3.3 此前错误归因的修正

以下问题在早期测试中被错误归因为 package bug，实际均为**审计方调用错误**：

| 早期归因 | 实际原因 | 修正方式 |
|---------|---------|---------|
| "batch limit 80 硬编码" | 未传 `MaxSeedRuleCount->5000, MaxEquationCount->100000` | 传入正确选项后 mixed_triangle 生成 392 eqs |
| "massive 族键名差异" | 审计方用错了 Association 键名 | 使用正确键名后正常访问 |
| "sunrise loopMomentumCountMismatch" | 审计方只给了 1 个 loop momentum（sunrise 是两圈） | 改为 `{q1,q2}` 后正常 |
| "atomic capabilityGate" | 审计方把线动量放在 `loopMomenta` 而非 `independentExternalMomenta` | timeOnly 族 `loopMomenta->{}` |

**结论**: Package 016 在正确调用下，全部 10 族均正常工作，无功能性 bug。

---

## 4. 全量验证结果

### 4.1 Seed 生成 + DSLinear（全部 10 族）

| 函数族 | 分支 | Seeds | Integrals | linearQ | 状态 |
|--------|------|-------|-----------|---------|------|
| atomic_massless_line | +- | 2 | 3 | True | ✅ |
| atomic_massive_line | -- | 9 | 14 | True | ✅ |
| pure_massless_bubble | ++ | 28 | 72 | True | ✅ |
| mixed_bubble | ++ | 50 | 181 | True | ✅ |
| mixed_triangle | --- | 392 | 2371 | True | ✅ |
| mixed_sunrise | -- | 1100 | 3720 | True | ✅ |
| pure_massive_bubble | -- | 88 | 390 | True | ✅ |
| two_loop_isp_toy | ++ | 620 | 1640 | True | ✅ |
| massless_sunrise | -- | 620 | 1784 | True | ✅ |
| vertex_energy_signs | ++ A | 72 | 220 | True | ✅ |

### 4.2 ds 总导数验证 (pure_massive_bubble --)

**测试表达式**: `ss11 * J[{0,0},{{0,0,0},{0,0,0}},{}] + P0 * J[{1,0},{{0,0,0},{0,0,0}},{}]`

| 测试 | 结果 |
|------|------|
| ds[expr, ss11] | 19 项，结构正确（h 闭合系统 + 指标移位） |
| ds[expr, P0] | 5 项，含顶点相位 -I 因子 |
| ds[J with n=1, ss11] | 9 项，离散态 n=1 正确保持 |
| **乘积法则**: ds[f·J, x] - (f'·J + f·ds[J,x]) | **= 0（严格零）** ✅ |

### 4.3 DSKiraExport (pure_massive_bubble --)

```
Status: ready
Integrals: 390
Equations: 88 (kiraBlockCount)
FireFly: enabled
Output: kira_pmb/{jobs.yaml, list, userSystem/ibp.kira, result/}
```

### 4.4 Kira 约化 + DE + Scaling 闭环 (pure_massive_bubble --)

**Reference seed ranges + KiraActiveBasis 方案**:
```
DSKiraExport: 215 integrals (19 masters + 196 targets)
Kira: 33581 equations → 6555 independent → 19 masters
FireFly: dsc1(=√s11), dsc2(=P0), dsii(=I) 有理重建, 5s
unreduced integrals: 0
```

**DSKiraImport → DSDE → DSScaleCheck**:
```
DSKiraImport: status=imported, 19 masters
DSDE({s11,P0}): status=generated, residual={s11→0, P0→0} (闭合!)
DSScaleCheck: status=passed (PureMassiveBubble, weights={2,1})
```

**数值点** (保存于 `000_note/phase2_verification/numeric_point.wl`):
```mathematica
{dim -> 37/11, nu -> 7/13, etaNu -> 23/17, s11 -> 5/3, P0 -> 11/7}
```

**主积分断层分析**:
- 使用 KiraActiveBasis (215 targets): **19 masters** (1-19, 无断层) ✅
- 不使用 KiraActiveBasis (7302 targets): 768 masters (断层 81→144)
  - 低编号区 1-81: 35 个候选
  - 断层后 144-7293: 733 个表观主积分（target 过多导致方程不足）
  - **结论**: 非 package 问题，是审计方未使用 KiraActiveBasis 限制 target 范围

**全数值约化说明**:
- 系数域含代数扩张 Q(√s11, P0, I)，√(5/3) 为无理数
- 完全有理数化不可行（Sqrt[s11] 无法表示为有理数）
- 正确方案: 保留 dsc1=√s11 为 FireFly 符号量，其余数值化
- Package 自动处理: `numericDummyAppendedQ -> True` (ccc 辅助方程)

---

## 5. 手推与 Package 逐项比较示例

### 5.1 atomic_massless_line +- (dtau[v1], seed point)

**手推** (笔记 `000_note/10_seed_atomic_massless_line.md`):
```
n=0: 0 = -α1·J[{-1,0},{{0}},{}] - iE1·J[{0,0},{{0}},{}] + iq1·J[{0,0},{{1}},{}]
n=1: 0 = -α1·J[{-1,0},{{1}},{}] - iE1·J[{0,0},{{1}},{}] + iq1·J[{0,0},{{0}},{}] - 2·J[{0},{{}},{}]
```

**Package 输出**:
```mathematica
-(alpha1*J[{-1, 0}, {{0}}, {}]) - I*E1*J[{0, 0}, {{0}}, {}] + I*sE1*J[{0, 0}, {{1}}, {}]
-2*J[{0}, {{}}, {}] - alpha1*J[{-1, 0}, {{1}}, {}] + I*sE1*J[{0, 0}, {{0}}, {}] - I*E1*J[{0, 0}, {{1}}, {}]
```

**比较**: 完全一致 ✓（`sE1` = q1 = 线动量模长）

---

## 6. 核心手推公式汇总

### 6.1 h 闭合微分系统
```
∂_x F[h,s,0;ν,x] = -F[h,s,1;ν,x]
∂_x F[h,s,1;ν,x] = -F[h,s,0;ν,x] - (2ν+1)/x · F[h,s,1;ν,x]
```

### 6.2 Wronskian Shrink
```
非零条件: n1+n2=1
κ = -ε_SK × (-1)^{n1} × 4i/π
shrink 编码: s=1, z=2ν
```

### 6.3 Massless Contact
```
∂_{τ_u} M[σ,1] = +iσq M[σ,0] - 2δ(Δ)
shrink: s=0, z=0, κ=±2
```

---

## 7. 遗留工作与后续验证

| 任务 | 状态 | 备注 |
|------|------|------|
| §13 ds 全族全变量逐条比较 | 部分完成 | 乘积法则已验证，逐族逐变量待补 |
| §13.3 H/h 三路验证 | 未开始 | 需 atomic_massive_line + pure_massive_bubble |
| §14 tree/pure-time 验证 | 未开始 | 需 DSTreeDLogDE |
| §15.2 工程门禁 (DSAllSeeds/DSGenerateIBP) | 完成 | DSAllSeeds 返回 88 模板 |
| §15.3 Kira + DE + scaling | **完成** | 19 masters, DE closed, scale passed |
| §16 根号坐标 | 未开始 | |
| §17 参数闭合 | 未开始 | |

---

## 8. 结论

1. **Package 016 物理正确性**: 全部 10 族 seed 生成、DSLinear、ds 总导数均通过验证，
   未发现任何物理/数学错误。

2. **工程质量**: 
   - 仅 1 个 cosmetic 级消息格式化 bug（StringRiffle，不影响功能）
   - UTF-8 编码需显式指定（环境注意事项，非 bug）
   - API 设计合理，选项覆盖充分（MaxSeedRuleCount/MaxEquationCount 可配置）

3. **审计方早期错误**: 此前报告中归因为 package 问题的 3 个发现均为审计方调用错误，
   已在 §3.3 中修正。

**审计状态**: Phase 2 核心验证通过。10/10 族 seed+linear，Kira→DE→scaling 闭环通过。

---

*报告更新时间: 2026-07-24*
*执行者: 项目内部 agent*
*验证环境: WolframScript + package_016.wl (UTF-8) + Kira 2.1 (WSL, FireFly 2.0.3)*
