# 016 explicit-momentum and graph benchmark

This workspace contains a new source-isolated incremental benchmark for task-book section 17. It does not reuse the 014/015 expected or actual results.

## Phase 1 outputs

- `SOURCE_ISOLATION.md`: source boundary and freeze contract.
- `derivation.md`: independent graph/routing, pack, time-only and parameter derivations.
- `expected_016.wl`: frozen structured expected data.
- `phase1_self_check.wls`: package-free consistency checks.
- `FREEZE.sha256`: phase-1 hashes created before opening package 016.

## Phase 2 outputs

Phase-2 comparison scripts and results will be added only after the freeze. They may load only the frozen delivery under `independent-benchmark/package/`.

## Formal report

The final report and its attachments are archived under root `000-report/`; this working directory is not the report destination.
