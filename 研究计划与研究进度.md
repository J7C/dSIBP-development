# dS IBP Package 研究计划与研究进度

> 本文件是项目的当前任务清单、研究进度和交接入口。每次收到新的开发、推导、验证或文档任务时，必须先更新本文件，再修改代码或其它文档。
>
> `package-dSibp/Documentation/dS_IBP_package_plan.md` 是长期总体规划，说明目标架构和物理约定；它不是逐任务 todolist。两者不得混用。
>
> 归档区保留历史任务事实，可能使用当时的目录名或已清理报告名；这些字符串是历史身份，不是当前可加载路径。当前入口以本文件顶部状态和各 `package-*/README.md` 为准，已删除报告从 Git history 追溯。

## 1. 当前权威状态

更新日期：2026-08-21。

### 当前任务：MadStree v0.15 / FlintNDE 0.5.0 奇点节点覆盖与继续输运（进行中）

- [x] 无外腿或 `externalLegEnergy -> 0` 时生成私有辅助能量坐标；解析 dlog 保留该坐标，
  数值路线自动把目标补为 0，且用户的 `pointSequence`/`ParameterRules` 不需要输入私有符号。
- [x] 末端用户点命中 dlogDE 奇点时区分 removable、true pole 与零阶 log 发散；可去奇点返回
  数值，真实发散返回文本 `Infinity`，并输出 `singularityClassifications` 证据表。
- [x] 独立 FlintNDE 0.5.0 已按奇点局部收敛半径实现双侧用户点 bucket、奇点解点归属和
  奇点后的继续输运；计划 JSON round-trip 后不重新规划，主/参考链分类必须一致。
- [x] 完成末端奇点隐藏匹配点与主/参考两阶核验；逐文件同步 MadStree Vendor，并从空运行
  目录完成两包 Python/Wolfram 专项、MadStree Examples 01--07 与两项独立验证。
- [ ] 修正提升环境下 `wolframscript -file` 在脚本内 `$CharacterEncoding` 生效前已按单字节
  解析中文报告字面量的问题；重跑 MadStree Validation-03，验证 Markdown 原始字节与显式
  UTF-8 读取均为中文，再完成最终生成物、文档计数和 Git 门禁。
- [x] 所有 Mathematica/Wolfram 命令固定第一次就以提升权限串行启动；不再先用普通权限试跑。
  每轮后检查残留 `wolframscript`、`WolframKernel` 和后端 Python 进程。
- [x] 通用性复核：表观/可去奇点与真实奇点只能由每段实际拉回的完整 DE、局部解和用户
  向量分类，不得依赖 `k0`、零坐标或某个固定 letter 名称；用平移到非零位置且含多个
  affine letters 的正反例覆盖同一入口。
- [x] 在 massive 三顶点 Example 05 中保留原数值点列，并另建一次独立
  `MSEvaluatePath` 调用，计算中间顶点 `externalLegEnergy=0` 的一组点；两组只复用同一个
  topology/context，不混合规划、结果或导出，并显式检查这些点的实际 letter 分类。
- [x] 同步中英文 README/手册和 Example 05 说明；从空运行目录提权串行重跑新增通用性回归
  与 Example 05，记录点、分类、数值和主/参考误差，重编手册并清理可重跑产物。

本轮完成记录（2026-08-21）：独立 FlintNDE 与 MadStree Vendor 的通用奇点目标专项均
`7/7`，其中非零复目标为 `2+i`、另一有限奇点为 `5+i`，同一二维系统分别得到 removable
与 true-pole 分量。Example 05 从空输出目录 fresh 退出 `0`，新增 `k2=0` 点列为
`{(8i,0),(7i,0),(6i,0),(9i,0)}`；25 个主积分全部保存，四点完整 dlog letters 均非零，
分类表为空，主/参考相对差均约 `2.3007194469693847e-31`。MadStree 中英文手册与 FlintNDE
手册均完成多遍编译和新增页渲染检查。

### 当前任务：v0.15/v0.5.0 提权 fresh 验收与旧版本清理（已完成）

- [x] 使用提升权限的单一串行 WolframScript 会话，从空的 `results*` 和 Python cache 开始，完成 MadStree v0.15 全部 tests 与 6 个 examples；Wolfram 入口均退出码 `0`，核心 `58/58`、路径 `27/27`、制品 `24/24`、周期/图表 `22/22`、顶点约化 `19/19`，正规化 example `16/16`。
- [x] 独立 FlintNDE v0.5.0 通过 `PYTHONPATH` 指向版本根的完整回归 `173/173`；根目录未设置包路径导致的 14 个 `ModuleNotFoundError` 仅为启动方式错误，不计入功能失败。
- [x] 清理本轮生成的 `results`、`results_temp`、`results_test`、`__pycache__` 和日志；同步修正当前 README、示例和文档入口中的版本路径。
- [x] 验收固定使用提升权限启动 WolframScript；普通权限可能导致 Kernel/configuration 启动失败，不能作为回归环境。

### 当前任务：2026-08-21 MadStree v0.14 参数规则与点序列表数值接口（已完成）

本任务把数值输入明确拆成一次性固定替换与可跑动坐标表。公开入口仍只有
`MSEvaluatePath`；单点仍是 `pointSequence` 表格只有一行数值的特殊情形，不增加第二入口。v0.14 不保留
v0.13 的逐点规则列表 schema、解析分支、示例或兼容测试。

- [x] N1. 从当前 v0.13 建立 v0.14，统一版本目录、loader、索引、任务书、更新说明和文档版本号。
- [x] N2. 新增 `ParameterRules`：在边界生成、DE 拉回和完整性检查前只应用一次；固定符号
  不得出现在坐标表头，坐标表头只接受当前 dlog DE 的可跑动符号。
- [x] N3. `MSEvaluatePath[context,pointSequence,...]` 只接受首行为坐标符号、后续为等宽值行的
  新 schema；单点是一行值，多点是多行值，临时点写成 `{{values...},"tmp"}`。
- [x] N4. 同步正规化批量求值，使 regulator 生产点继续复用同一固定参数与坐标表合同，不在每个
  点重复构造规则列表。
- [x] N5. 重做现行 examples、开发测试、中英文 README、手册典型例子章节和独立验证任务书，
  清除当前文档中的旧逐点规则调用法。
- [x] N6. 从空输出目录运行单点/多点、树图、单顶点和正规化受影响回归；重编并目视检查两本
  手册，执行严格 UTF-8、旧 schema、生成物和 `git diff --check` 门禁。
- [x] N7. 把完整解析 dlog DE 的正式落盘从 examples 约定提升为所有数值入口的强制前置门禁：
  同一 context/调用根复用同一份公式资产，写出失败时不得启动 Python/FlintNDE；返回值携带
  公式目录与文件路径，参数化 DE 仍只存在于函数局部临时对象。
- [x] N8. 更新中英文 README、手册、examples 和回归，重新运行受影响数值/正规化测试并清理
  `results/`、`results_temp/`、cache 与 TeX 中间物，复核 UTF-8 和 `git diff --check`。

接口合同：

```wl
MSEvaluatePath[
  context,
  {{k0,k1},{1,1},{11/10,1},{6/5,1}},
  ParameterRules -> {nu -> 1/2, a -> 2}
]
```

固定规则先同时作用于 dlog connection、边界输入和其它非坐标参数；每行坐标值随后只替换
表头符号。固定规则与表头重叠、表头重复或含非 DE 坐标、值行宽度不符、固定后仍缺必需符号、
空坐标表及旧逐点规则 schema 均 fail closed。Association 的普通额外选项仍遵循现有 option
检查，不建立旧输入 denylist 或 wrapper。

完成记录（2026-08-21）：v0.14 的数值入口现只接受坐标表 `pointSequence` 与一次性
`ParameterRules`；缺失/非数值参数在 Python/FlintNDE 启动前返回列出符号的 Failure，路径回归
同时证明 MSEvaluatePath 的缺参数负例不创建 runtime。`MSDLogDE` 和
`results/madstree_formula/run-<UUID>/dlog_de.wl` 保持完整解析形式，数值阶段只使用函数内
参数化副本。fresh 回归为 core `58/58`、artifact `24/24`、point/planning `27/27`、二维 900 点
复平面分组 `8/8`、
vertex-family NDE `13/13`、runtime/export `10/10`、massless `10/10`、massive Full `6/6`、
massive vertex `8/8`、mixed 三顶点 `8/8`；六个 examples 均退出 `0`，Example 06 为 `16/16`，
使用 3 个生产点、2 个独立验证点和实际 5 个并行任务。并发冷启动曾有两个 Windows Python/FLINT
进程以空输出和 `$Failed` 退出；删除对应 runtime 后串行冷启动均恢复通过，未把该环境竞争计为
数值通过。中英文 PDF 经 BibTeX 与两遍 LaTeX 生成，各 33 页；目录、新接口、正式输出、正规化
与典型例子页已渲染检查，无乱码、截断、重叠、overfull 或未定义引用。105 个 MadStree 文本文件
严格 UTF-8 解码通过且无 BOM。旧 v0.13 源码、任务书、报告/结果及 v0.14 运行产物、cache、TeX
中间物均已删除；v0.14 独立验证任务书已更新，但本轮未执行独立验证，不把旧结果冒充现行证据。

补充门禁完成记录（2026-08-21）：`MSEvaluatePath` 与 `MSReconstructEpSeries` 在启动任何
Python/FlintNDE 数值任务前自动写出或复用同一 context/调用根的完整解析资产；返回值新增
`"formulaArtifacts"` 文件引用，但发给后端的 `madstree_flintnde_evaluate_v1` schema 保持不变。
四文件部分写出返回 `FormulaArtifactWriteFailed`，专项实测后端未启动；固定参数 DE 仍只存在于
函数局部。fresh 结果为 artifact `24/24`、point/planning `27/27`、core `58/58`、runtime/export
`10/10`；六个 examples 逐个冷启动均退出 `0`，Example 06 为 `16/16`、3 个生产点、2 个验证点、
实际并行 5。一次批量 shell 启动在读取用户级 `WolframScript.conf` 前即以 Windows 进程码退出，
未进入例子且不计为验证；随后六次独立冷启动全部通过。中英文手册均为 33 页，最终日志无
overfull、未定义引用/文献或 fatal error，受影响页已渲染检查。

### 当前任务：2026-08-21 MadStree 统一单点/多点输入与单顶点函数族手册例子（已完成）

本任务检查 MadStree 数值入口是否把单点和多点错误地分成两套输入格式，并补齐单顶点函数族
的公开定义说明与独立例子。单点必须表达为统一 `pointSequence` 的长度为 1 的特殊情形；
树图和单顶点函数族的初始化 schema 可以不同，但数值评估、返回结构和路径选项不得分叉。

- [x] M1. 审计 `MSEvaluatePath`、`MSInitTree`、`MSInitVertexFamily` 的当前输入契约及手册/例子覆盖。
- [x] M2. 确认现行代码原本只有统一 point-sequence 入口；补强公开合同和回归，不新增单点入口或第二返回格式。
- [x] M3. 在 MadStree 手册中单独介绍单顶点函数族的紧凑/显式定义、与树图输入的差异及单点数值调用。
- [x] M4. 以 `Examples/02_single_vertex_family.wl` 破坏性替换旧 Example 02，并补充共同首点互检。
- [x] M5. 从空结果目录运行受影响例子/检查，执行 UTF-8、无临时产物和 `git diff --check` 门禁。

完成记录（2026-08-21）：数值入口确认只有 `MSEvaluatePath[context,pointSequence]`；单点用
`{pointRules}`，多点用 `{pointRules1,pointRules2,...}`，两者共用选项、FlintNDE 请求、
`pointResults` 和返回结构。单顶点函数族继续使用独立的 `MSInitVertexFamily` 初始化 schema，
但建立 context 后与树图共用 masters、递推、DE、边界和数值接口。Example 02 fresh 通过
`11/11`，vertex-family NDE 通过 `13/13` 且共同首点差为 0，100 点路径回归通过 `21/21`
且 planned/direct 最大差约 `4.2*^-48`；中英文手册均为 33 页，新典型例子章节已目视检查。

### 当前任务：2026-08-20 MadStree v0.13 与 dSIBP 022 树图主积分常系数独立交叉验证（已完成）

本任务只比较两个程序包对同一 tree/time-only 主积分的定义与 sector normalization，不生成
IBP 关系，不调用约化、Kira、DE、边界或数值输运。验证程序、任务书、结果和报告放在仓库
根部独立目录，不进入 `package-MadStree/` 或 `package-dSibp/`，也不成为任一程序包的输入。

- [x] C1. 冻结 MadStree v0.13 与 dSIBP 022 的实际加载路径、源码身份和公开主积分读取入口；
  独立脚本不得读取两包既有 smoke/独立验证 expected。
- [x] C2. 分别构造两顶点单 massive tree、三顶点双 massive chain、三顶点一 massive 一
  massless chain；固定相同顶点顺序、传播子顺序、外腿能量、时间幂、顶点 `+/-`、Hankel
  branch、massless representation 和 sector key。
- [x] C3. 明确记录两边的 `nu` 正负 convention、h/H 基底、state-bit/slot 顺序、contact sector
  编码、裸积分含义及 normalization 方向；先对齐 identity，再比较常系数。
- [x] C4. 每个 case 只调用初始化与主积分/sector metadata 入口，提取全部同序 master；逐项记录
  MadStree master、dSIBP master、两边裸积分外的系数、系数比值和 exact difference。单边系数
  可以依赖固定传播子动量与 `nu`，但对齐后的两包系数比值必须严格为常数 1；无法唯一配对则
  失败，不以约化关系修补。
- [x] C5. 生成机器可读 summary 和自包含 Markdown 报告，执行 fresh-kernel、UTF-8、临时产物
  与 `git diff --check` 门禁；报告明确区分已验证的三类 tree family 与未覆盖图型。

验收标准：三个 case 的 master 数、sector 分块、指标和状态顺序一一对应；对齐 convention 后，
每一对 normalized master 的裸积分外系数严格相等，因而两包系数之比为 1、差为零；系数自身
允许含固定传播子动量与 `nu`，但不得依赖积分变量、离散 state 或同 sector 内的 master 序号。
任何无法消去的两包系数残余均记为失败。本任务不证明 IBP、约化或 DE 正确性。

完成记录（2026-08-20）：从空 `results/` 和已删除旧报告启动 fresh kernel，三个 case 均通过；
两顶点 massive、三顶点双 massive、三顶点 massive+massless 的 master 数分别为 `5/5`、
`25/25`、`15/15`。总计 sector/slot order、dSIBP selector split、sector normalization、master
identity 与 master coefficient 分别通过 `10/10`、`10/10`、`10/10`、`45/45`、`45/45`，
全部两包系数比值为 1、差为 0；wall time 为 `1.59675 s`。验证只读取初始化和 master/sector
metadata，未生成 IBP，未调用约化、Kira、DE、边界或数值输运。自包含报告位于
`independent-validation/MadStree-v0.13-dSIBP-022-master-normalization/000_MadStree-v0.13-dSIBP-022-master-normalization-report.md`，
机器结果位于同目录 `results/summary.wl`。验证目录严格 UTF-8、无 cache/runtime 临时产物，
三个程序包源码均未改动，`git diff --check` 通过。

### 当前任务：2026-08-20 MadStree normalized master 与裸指标积分的公开对应（已完成）

MadStree 的直接公式路线仍依赖 sector normalization：sector 对角 connection 包含
`d log normalization`，contact block 包含 source/target normalization ratio。因此沿用手册
已有完整标识
`J_s(n;a) = MSIntegral[sectorKey,aShifts,stateBits]`，只补足
`J_s(n;a)=calN_s I_s(n;a)` 的公开读取。手册把二态因子数量统一改记为
`n_s^(slot)`，normalization 只使用 `calN_s`；边界权重中误写成普通 `N_s` 的 normalization
同步纠正。

- [x] D1. 在中英文手册用现有 `s/n/a` 记号简洁定义一般裸积分 `I_s(n;a)`；`++/--`
  的 h 组合沿用统一 building block 定义，不在每条 master 输出中展开。
- [x] D2. 新增 `MSIntegralDefinition[MSIntegral[s,n,a],context]`，原样复用三个现有标识，
  返回 `calN_s` 及 `J_s(n;a)=calN_s I_s(n;a)`；非法指标复用现有 `Failure`。
- [x] D3. 让既有 `MSMasterIntegrals[context]` 的每条记录直接带同源定义；不增加重复的
  bulk API，也不改变 `MSIntegral`、master order/digest、recurrence、DE 或 boundary。
- [x] D4. 更新中英文 README、手册、UPDATE_NOTES 和一个简短 example；以 top 的
  `calN_s=1`、massive contact child、massless 表示和非法指标作 exact 回归，并完成 PDF、UTF-8、
  临时产物与 `git diff --check` 门禁。

完成记录（2026-08-20）：新增惰性 `MSBareIntegral` 与单对象查询
`MSIntegralDefinition`；`MSMasterIntegrals` 保持原顺序及 `integral` identity，只在同一记录附加
`bareIntegral/normalization/definition`。专项、core 与 package artifact 回归分别通过 `17/17`、
`58/58`、`18/18`，更新后的 Example 05 从空输出目录执行并以 exit code `0` 完成。中英文手册
重新编译为 31/32 页；编译日志无 fatal error、undefined reference/citation 或 overfull box，受影响
页面已目视检查。14 个本轮文本文件严格 UTF-8 解码均通过，普通 `N_s` 旧歧义扫描为空；临时
结果、cache、TeX 中间文件和 PDF 渲染文件清理后执行 `git diff --check`。

### 当前任务：2026-08-19 三程序包冗余代码审计复核与清理（进行中）

- [x] R1. 复核最新独立审计报告 F-01 至 F-05 的真实调用链，区分可执行旧入口、孤立定义和退休实现。
- [x] R2. 删除 dSIBP 022 的旧运动学字段读取与 `kEPower` metadata fallback，并增加当前 schema 结构负例。
- [x] R3. 删除 MadStree v0.13 的退休单顶点 Frobenius 路线和五个无消费者定义。
- [x] R4. 删除 FlintNDE 0.4.0 的旧 epsilon 局部求解簇及孤立函数，并同步 MadStree Vendor。
- [x] R5. 从新路径运行受影响回归，更新各包 todolist、版本说明、审计报告和独立验证记录。
- [x] R6. 清理临时产物，执行 UTF-8、`git diff --check`、缓存/字节码和远端分歧门禁，提交并推送。
- [x] R7. 纠正 F-01 的过度门禁：从 dSIBP runtime 和现行 smoke 中物理删除四个已退休
  raw-case 字段名及其定向拒绝语义；未知额外键继续按 022 合同忽略。重建候选单文件，
  复验受影响回归并同步版本说明、todolist 与审计闭环后再晋升正式文件。

R1-R7 完成记录（2026-08-20）：F-01 至 F-05 均经真实调用链二次确认并完成物理删除，不增加
alias、wrapper、fallback 或旧名字 denylist。dSIBP 模块/候选/正式 topology `16/16`、normalization `14/14`、
参数导数 `11/11`、死定义 `17/17`；正式单文件 SHA-256 为
`FF8B6F87274C88998D9E38AB31ED27B7FAA2ACC6AAD3B9BCCCF4B5D426C06FF3`。MadStree core、generic
boundary、artifact、Backend 分别为 `58/58`、`10/10`、`18/18`、`11/11`；FlintNDE 独立包和
Vendor Python 各 `170/170`，Wolfram `25/25`。本轮不改变物理公式或数值算法，未重跑独立
validation；审计报告明确保留该边界。所有本轮 runtime/cache/候选均已清理。

### 当前任务：2026-08-19 三程序包现行代码冗余独立审计（已完成）

- [x] A1. 冻结 dSIBP 022、MadStree v0.13、FlintNDE 0.4.0 的实际磁盘入口、Git 工作树和版本边界。
- [x] A2. 沿真实 loader / package init / Python import 路径核对公开 API、内部调用图和测试消费者，定位死定义、不可达分支、旧接口兼容层、旧 schema fallback 与重复实现。
- [x] A3. 核对现行目录中的旧版本资产、runtime/cache/生成物和已被替代的验证证据，区分已删除但未提交的历史文件与仍会被当前入口消费的冗余。
- [x] A4. 运行不覆盖 retained result 的静态检查和受影响低风险测试；无法用当前证据证明触发或下游影响的项目只列为风险/覆盖缺口。
- [x] A5. 在根目录 `000_report/` 写入自包含独立审计报告，给出严重度、完整证据链、修复顺序、验收方法和未执行边界；本轮不修改三个程序包源码。

验收标准：每项已确认发现都有现行契约、实现位置、触发条件、直接后果、下游传播、复核命令、修复边界与验收方法；旧名称命中必须排除更新说明、历史说明、拒绝测试和外部文献语境；报告不依赖聊天或旧 memory 才能复核。

完成记录：报告位于 `000_report/2026-08-19-三程序包最新版内部冗余代码独立审计.md`。确认 5 组清理项：dSIBP 1 个可触发旧运动学 schema（中）及 1 个旧 metadata fallback（低）；MadStree 1 个已退休 189 行实现簇及 5 个孤立定义（低）；FlintNDE 1 个 610 行旧 epsilon 实现簇及 2 个孤立函数（低），后者同时存在于 MadStree vendor。定向验证为 dSIBP `17/17`、`16/16`，MadStree `58/58`、Backend `11/11`，FlintNDE Python `170/170`；一次性探针已删除，未修改三包源码或覆盖 retained result。

### 当前任务：2026-08-19 三程序包最终状态提交 GitHub（已完成）

- [x] P1. 核对 `main` 与 `origin/main` 分歧、远端地址和待提交快照。
- [x] P2. 清理并确认没有 cache、pyc、TeX 中间物、`results_test/` 或其它临时产物进入暂存区。
- [x] P3. 执行定向 `git diff --check`，保留已知的 Example 04 生成 manifest 尾随空格记录。
- [x] P4. 提交本轮三包最新版、文档、独立验证报告和旧版本清理，并推送 `origin/main`。

P1-P4 完成记录（2026-08-19）：远端地址为 `git@github.com:J7C/dSIBP-development.git`，
fetch 与 SSH 认证通过；提交 `1282a02` 已推送到 `origin/main`，推送后重新核对本地/远端
commit 和工作树状态。


### 当前任务：2026-08-19 dSIBP 022 / MadStree v0.13 干净拓扑接口与外腿能量命名（进行中）

本轮为破坏性新版本，不设迁移期，不保留旧函数名、字段别名、wrapper、fallback、
旧 schema 解析分支或兼容测试。dSIBP 从 `021` 升为 `022`，MadStree 从 `v0.12`
升为 `v0.13`；验收后工作树只保留两包各自的新版本。

两包的顶点物理输入统一为 `"vertexType" -> "+"|"-"` 和
`"externalLegEnergy" -> E_v`。该量表示连在该顶点的外腿指数因子参数，不是顶点
本身的能量，也不是所有内外线能量之和。指数约定唯一由顶点类型生成：
顶点 `+` 使用 `Exp[-I externalLegEnergy tau]`，顶点 `-` 使用
`Exp[+I externalLegEnergy tau]`；用户不自行把分支符号塞入能量。

dSIBP 022 的公开顶点用 Association 列表表示，整合旧 `vertexData` 和
`vertexEnergies`。公开 line 只保留 `id/massType/endpoints/momentum`，massive 线再给
`nu`；高级 massive 线可给唯一 `functionSystem`。删除旧五元组 line，以及
`bbType/eomCoefficients/shrinkPrefactor/skType/packType/state/thetaConvention/`
`thetaBoundarySignOffset` 等公开覆盖入口。massless 线不接收 `nu/functionSystem`。
内部 `skType/packType/state`、active vertices、root zero points 和 sector representative map
仍是计算所需元数据，但只由 root topology/contact producer 生成，不再作为用户 case
字段或重新送入公开 parser。

dSIBP 同时删除旧运动学入口 `externalMomenta/externalLegMomenta/`
`externalInvariantRules/rawExternalInvariantRules/externalLegInvariantRules/`
`rawExternalLegInvariantRules`。公开输入只使用 `loopExternalMomenta`、
`independentExternalMomenta` 和统一 `kinematicRules`；内部有效圈外向量基改用新名，
不再混用 `externalMomenta`。每条线的 signed linear `momentum` routing 继续保留，
用于圈动量 IBP、ISP 闭合和参数求导；整体 `Q -> -Q` 依然等价，不将
routing 符号误解为传播子物理箭头。

- [x] S1. 核对两包当前 schema、兼容 fallback、内部 sector 消费者与顶点相位符号。
- [x] S2. 在根进度表和两个新版本 `todolist_temp` 记录接口、数据流、删除边界与验收项。
- [x] S3. 建立 dSIBP 022：重写公开 case parser 和内部 sector constructor，删除全部旧 schema 读取。
- [x] S4. 建立 MadStree v0.13：将公开/内部顶点字段统一改为 `externalLegEnergy`，删除 `energy` 旧入口。
- [x] S5. 更新两包全部 examples、tests、验证任务书、README、VERSION_INDEX、UPDATE_NOTES 和手册。
- [x] S6. 增加破坏性接口回归：新 schema 通过；缺物理必需字段失败；顶点 `+/-`
  分别给 `-I/+I` 外腿能量项；line 不能覆盖 SK/pack/state；contact sector 仍正确继承
  root zero points 和 representative map；routing/ISP/DE 闭合不退化。
- [x] S7. 删除两包旧版本目录、旧任务书/报告/结果；清空中间产物后从新路径 fresh 运行受影响回归与例子。
- [x] S8. 重编两包手册并检查实际 PDF；运行 UTF-8、静态旧名残留、生成物和 `git diff --check` 门禁。
- [x] S9. 增加 FeynCalc 风格的简洁加载引用提醒并做链接二次核验：MadStree Wolfram 入口
  显示完整可点击引用块，Python 后端在协议元数据与直接 CLI 中提供同一内容；dSIBP 列出
  三篇指定 arXiv 论文和 dSIBP 程序包论文；FlintNDE Python 首次导入只提示 FlintNDE
  程序包论文。三个 arXiv 链接只允许逐字使用用户给出的
  `https://arxiv.org/abs/2401.00129`、`https://arxiv.org/abs/2411.03088`、
  `https://arxiv.org/abs/2604.14549`，最终以源码扫描和实际加载输出双重核验；三包手册的
  安装/加载章节同步说明提醒行为，能做超链接的 PDF 对指定 arXiv 号使用同一白名单 URL。
- [x] S10. 修复 022 积分重排的 Kira ordering 解析 pattern；Example 01 必须在启用显式
  `DSReorderIntegrals` 后无 `Join::incpt`，只输出进度与紧凑 summary，stdout 不得再展开
  `linearData` 或 5516 条方程。增加 ordering Association 与消息洁净度 smoke，并从新路径
  fresh 重跑 Example 01。
- [x] S11. 修复 MadStree v0.13 massive contact dlog source 的符号 authority：外腿指数继续读取
  `vertexSign`，contact block 必须只读取 `contourSign`。论文两顶点 G++ 验证须在
  `Exp[+I k tau]` 到公开输入 `externalLegEnergy=-k` 的映射下，完整通过三个 5x5 DE、contact
  leading vector、五个绝对边界系数和五维收敛域普通点值；不得以事后乘相位代替源码修复。
- [x] S12. 重新审计 15x15 失败的真实调用链，撤销“该脚本直接执行了 dSIBP contact producer”
  的错误归因。脚本由 dSIBP `ds` 生成 shifted integrals 后，统一交给 MadStree `MSReduce`；
  speculative 修改 dSIBP atomic contact 后结果仍严格不变，证明该失败不能作为该 producer 的证据。
- [x] S13. 修复 MadStree v0.13 recurrence 与已由论文验证的 dlog contact phase 不一致：
  `MSReduce` 当前直接消费不含该 phase 的 raw contact map；结合既有 `M0/M1` 递推符号后，纯
  massive lower-sector block 与 dlog 整体反号，而含一条 massless contact 的 block 恰好已经对齐。
  新增完全不调用 dSIBP 的 `externalLegEnergy derivative + MSReduce` 对
  `D[MSDLogDE[...], externalLegEnergy]` exact 自检，覆盖 `+/-` 两支；随后 15x15 比较须恢复 `9/9`。
- [x] S14. 修复 v0.13 vertex-family `exponentType` 被错误绑定到 `vertexType` 新符号的问题。
  `exponentType` 是额外指数块进入 effective external-leg energy 的直接代数号，必须独立保持
  `+ -> +1`、`- -> -1`；不得复用顶点外腿指数的 `vertexType + -> -1` 映射。重跑
  `test_vertex_family_reduce.wls`，目标由 fresh 暴露的 `18/19` 恢复为 `19/19`。
- [x] S15. 清除 dSIBP 022 仍残留的公开兼容入口与路径 fallback：物理删除
  `metaSeedRange`、`generateIBP` 的 usage、Options、wrapper、API/coverage 条目和现行调用；
  全部迁移到唯一公开入口 `DSMetaSeedRange`、`DSGenerateIBP`。`DSKiraImport` 的自动文件发现
  只接受 Kira 当前 family 目录 `results/Tuserweight/kira_list.m` 与
  `results/Tuserweight/masters`，不再回退读取 `results/kira_list.m` 或 `results/masters`。
   显式 `KiraReductionFile`、`KiraMasterFile` 继续作为当前接口保留。
- [x] S16. 收口 MadStree v0.13 fresh 全回归暴露的正规化结构与边界数值失败：先在清空共享
  `test/results_temp` 后串行复跑 `test_ep_structure.wls` 与 `test_flintnde_boundary.wls`，排除并行
  runtime/cache 竞争；若仍失败，沿 `MSEpsilonStructure -> MSAsymptoticBoundary` 的真实调用链
  修正零 component boundary energy 的失败传播，禁止把 `Return[Failure[...]]` 列表继续交给
  `Lookup`，并以低阶 primary transport 对解析值的比较恢复 boundary `10/10`。不得通过放宽
  tolerance、改 expected、交换 primary/reference order 或复用旧 cache 消除失败。
- [x] S17. 判定并修复 dSIBP 022 第 15.6 节 normalized child DE 的 `(5,5)=2 nu1/ks`
  差异。结论不能只以论文矩阵为前提，必须先从缺省 h/H 函数系统、Wronskian shrink、zero-point
  传播和 naive 指标基的有理闭合独立推导：所有形如 `ks^(c nu1)` 的连续幂必须完整吸收到 child
  master 的 sector normalization，使 top/subsector 的 IBP 和 naive DE 系数保持参数与动力学量的
  多项式分式；允许 `(-2 nu1-1)/ks` 这类有理对数导数，不允许在 contact coefficient 中残留
  `ks^(-2 nu1)`。若该独立判据确认现行公开对象是 `J_s=N_s I_s`，则 naive/direct 两路必须从
  同一 complete normalization authority 消费 `D Log[N_s]`，禁止对第五项或 `ks` 写专用补丁；
  保持 master 顺序、top 四项、contact phase 和 H/h adapter 不变。修复后先做不读取论文 expected
  的有理闭合、Wronskian、静态 normalization 和乘积法则 exact 回归，再以论文为外部交叉检查，
  重建正式 022 并 fresh 重跑第 15.6 节至三变量两路线全部 `25/25`、全部 residual 为零。

S15/S16 fresh 完成记录（2026-08-19）：dSIBP 022 topology/API/dead-definition/Kira-path 分别
`16/16`、`7/7`、`17/17`、`7/7`，module ownership 为 23 个 Kernel 文件、361 个顶层符号且
无重复签名；正式 022 六个 examples 全部从唯一 `package_022.0.wl/pdf` 路径运行。MadStree
修复了 `Table` 内未生效的 `Return[Failure]`，零 leading-energy 现在返回单一
`VanishingComponentBoundaryEnergy`；ep 结构证书与 boundary 冷启动分别通过，后者低阶 primary
对解析值相对差约 `1.51*^-39`、primary/reference 约 `1.51*^-39`。开发回归、六个 examples、
Vendor FlintNDE `170/170` 及三项独立验证 `18/18`、`26/26`、`16/16` 均 fresh 通过。

S9 运行证据（2026-08-19）：使用正式 `Needs` 文件入口在同一 Wolfram kernel 内连续加载两次，
dSIBP 与 MadStree 各只显示一次引用块；headless 输出分别严格包含用户给出的三条/两条完整
arXiv URL，Notebook 构造中的 `Hyperlink` 计数均为 `1`。FlintNDE 的独立 Python 进程执行
`import` 后再 `reload` 只向 stderr 显示一次 `FlintNDE package paper` 提醒；MadStree Python
直接 CLI 输出两条完整 URL，文件协议测试确认 citation metadata 存在且不向 JSON stdout
混入依赖包提醒。未给 arXiv 号的三包论文都只写 `arXiv identifier pending`，未生成 URL。
三包手册和实际 PDF 均已同步该提醒合同；指定 arXiv 链接目标复核通过，S9 已关闭。

现行目录清理核验（2026-08-19）：三包 `versions/` 分别只保留 022、v0.13 和 0.4.0；旧
版本源码、被替代任务书/报告、未引用的 MadStree v0.11 `eff_bench.md` 和 example 运行输出
均已删除。根 `tmp/`、dSIBP 空 `test/` 与 `check-smoke/results_temp/` 也已清除；当前未发现
`results_temp`、`results_test`、cache、Python 字节码或 TeX 中间文件。独立验证 `results/`
和正式报告属于当前保留证据，不在本次清理中删除。

S17 首轮差异证据（2026-08-19）：独立执行者先冻结论文 oracle 后才加载正式
`package_022.0.wl`；Eq. (4.2) child 两层 normalization 静态差为 `0`，naive IBP 为
`9` equations / `9` unknowns 且 solve residual 为 `0`，naive/direct 彼此三变量全同。
两路实际 child `ks` 对角项均为 `-1/ks`，论文为 `(-2 nu1-1)/ks`。因此错误不在 contact
常系数或单一路线。现行技术合同同时规定 `J_s=N_s I_s`，且 massive child 的连续 `nu1` 幂应
吸收到 sector normalization 以维持 naive IBP/DE 的有理闭合；这使缺失的 `-2 nu1/ks` 可由
函数族数学性质独立推出，而不是因为论文必须正确。失败报告为
`package-dSibp/000-report/2026-08-19-1510-022-内部.md`；完成上述独立 convention 证明、源码修复
和 fresh 验收后，必须由成功报告取代。

S17 实施要点：

1. 从缺省 h/H 的 compiled Wronskian term 逐条导出 shrink 的常数、整数模长幂和 continuous
   zero-point 幂；证明 massive fixed child 所需 `ks^(-2 nu1)` 属于 master 定义，不属于
   top-to-child reduction coefficient。不得以论文 Eq. (4.2) 代替此推导。
2. 对每个 tree master 建立唯一 complete normalization record，明确区分 bare integral、dlog
   选择系数、physical sector prefactor 及其乘积。naive 的乘积法则、direct dlog 的对角块、
   tagged master 输出和 contact sector ratio 必须读取同一 record；已有 ratio 中已经吸收的因子
   不得再次计入。
3. 验收 naive 指标 basis 的全部 IBP/DE coefficient 都是参数与动力学量的有理函数；专门扫描
   `Power[kinematic_, expression containing nu]` 残留。用户显式选择含根号或其它代数 prefactor 的
   master basis 不属于该默认门禁，但其 basis 变换必须单独记录。
4. 独立于论文的 exact 检查先证明 `N_child` 的连续幂、`D Log[N_child]`、Euler scaling 和
   top/child contact coefficient 同时闭合；随后论文矩阵只作为第二 authority 交叉验证。

S17 源码与候选阶段证据（2026-08-19）：已把 tree master normalization 明确拆为有理
`selectorCoefficient`、冻结 metadata 给出的 `physicalSectorPrefactor` 及唯一乘积
`completeNormalization`。direct dlog 对角块消费 `Log[completeNormalization]`；naive derivative
同时消费 selector 的乘积法则与 physical prefactor 的对数导数；off-diagonal contact 只继续消费
selector ratio，避免重复乘入 producer 已使用的 physical ratio。模块路径和重新构建的候选
`test/results_test/package_022.0.wl` 均通过 normalization/有理闭合 `13/13`；候选同时通过参数
导数与 Euler scaling `11/11`、死定义清理 `17/17`。child selector 为 `-1/sE1`，physical
prefactor 为 `(4 I/Pi) Exp[Pi Im[nu1]] sE1^(-2 nu1)`，完整 normalization 的对数导数及两条
DE 的 child 对角项均为 `-(1+2 nu1)/sE1`；naive IBP reduction 与两条 DE 中
`Power[kinematic, exponent containing nu1]` 残留均为零。候选 SHA-256 为
`15393749586EB2D515801003765DBD2C19B27CC2E6E11157C049AD25961C1CCE`。S17 仍等待清空
独立 `check/` 后按第 15.6 节重跑、同字节晋升正式包和正式路径复验，因此暂不关闭。

S17 独立重跑启动记录（2026-08-19）：用户已直接授权完整删除
`package-dSibp/check/`；删除后 `Test-Path` 为 `False`，旧验证脚本、论文副本和结果未复用。
新的独立会话已从空白 Phase 1 开始，只测试上述候选单文件；候选结果返回前主会话不读取
`check/` 中间文件，也不晋升正式包。

S17 正式路径复验启动记录（2026-08-19）：候选完整检查已两次得到 `allPassed=True`，且候选
与正式 `independent-benchmark/package/package_022.0.wl` 的 SHA-256 均为
`15393749586EB2D515801003765DBD2C19B27CC2E6E11157C049AD25961C1CCE`。独立执行者将从冻结的
候选 runner 新建 formal runner，仅改为加载正式包并写入独立 `formal_summary.wl`；不得读取
`candidate_summary.wl`，正式路径 fresh 结果返回前 S17 仍不关闭。

S17 正式路径完成记录（2026-08-19）：独立 formal runner 从冻结候选 runner 复制，仅更换正式
包路径及独立 probe/summary 输出，运行退出码为 `0`、`status=passed`、`allPassed=True`，且未读取
`candidate_summary.wl`。naive/reference、direct/reference、naive/direct 在 `k12,k34,ks` 上
各为 `25/25`；child `(5,5)` 在 `k12,k34` 上为
`(-1-2 nu0+2 nu1)/(k12+k34)`，在 `ks` 上为 `(-1-2 nu1)/ks`。naive IBP 为 `9` 方程、
`9` 未知量、`9/9` solve residual 为零；naive DE source 为 `15/15` 零且无 residual object；
direct dlog residual 为 `25/25` 零。naive IBP reduction、naive DE、direct DE 中含 `nu1` 的
动力学连续幂残留均为空。四段耗时依次为 DSInit `0.1519952 s`、DSTreeDLogDE `0.8325325 s`、
DSTreeNaiveIBP `1.3540736 s`、DSTreeNaiveDE `2.0995986 s`。formal runner 与 summary 的
SHA-256 分别为 `D7B59C78670D2D345E6E7228E33B354223AF999D952A71455D67EFF573346816` 和
`DB4D4589DBDF6D2AEA7D91E74979FC2D82A3E8BE76C5F50A7DEC9F09050BBDC5`；候选 runner 与 summary
仍分别为 `1F8DCF6B545361E3373EC5277D922D40B52C35369FB77AD4212177EF85622E74` 和
`E4B74006DC04AEFAA4A5807ACB07AD3BFA0D7B9CF58C8CAA4725FA7297EE7B01`，未被修改。

S17 发布收口（2026-08-19）：同一独立执行环境显式令 `DSIBP_PACKAGE_FILE` 指向唯一正式
`package_022.0.wl`，串行复跑 normalization closure、参数导数/Euler scaling、死定义清理，
分别通过 `13/13`、`11/11`、`17/17`，退出码均为 `0`。正式用户手册与候选 SHA-256 同为
`08A9B79804268C06D4077E10367A08503A89BF7B4CCC8F9C08F3DFBD11018E09`，正式技术笔记与候选
同为 `FB54ADF26A0EF279577FF47933B782A888611A5733D4DA0DABE19F52F4F165FF`；PDF 结构复核为
33/57 页，提取文本无 replacement character，用户手册三条指定 arXiv hyperlink 目标逐字正确。
dSIBP 121 个最终保留文本文件严格 UTF-8 解码全部通过；当前 3 个 BOM 文件仅为两份中文 TeX 源和正式
单文件入口。成功报告及轻量附件回收后，`check/`、`test/results_test/`、021 源码和 021 报告均
删除；工作树只保留 022。定向 `git diff --check` 通过；仓库全量检查仍只被既有 Example 04
生成 manifest 的尾随空格阻断，本轮不机械改写该 10,155 行 reduction artifact。

S17 候选独立结果（2026-08-19）：Phase 1 先冻结 arXiv e-print，archive SHA-256 为
`409457E1609AFE389D70BF86495F36B8A1C97D64F12B8159DE2C282472C5D37F`，再开放
候选与仓库 reference。首次手工 oracle 的 `R[[4]]` 抄反 `k34` 两个对数符号；原文件及
SHA `82D0685AD8FE74E48F9203AB2BDBD5062A9F3C78FCDAE8830CBE53A5BC56264D` 保持原样，
勘误直接依据同一冻结 TeX 的 `dS_DEsol.tex:1180`，corrected oracle SHA 为
`C744D61DE911151B21C118B2D8D7E245F6D4C4C6B905B98EDFE4D4374D80E334`；其独立自检
通过。完整候选任务 `allPassed=True`：naive/direct 各自对论文 `k12/k34/ks` 均 `25/25`，
两路互比也均 `25/25`；child `(5,5)` 的 `ks` 项均为 `(-1-2 nu1)/ks`；naive IBP
`9/9` residual、naive DE `15/15` source、direct dlog `25/25` residual 均为零；三类
`Power[kinematic, exponent containing nu1]` 残留计数均为零。该句记录候选阶段状态；正式路径复验
已按上文完成记录通过。

引用提醒实施要点：

1. Wolfram 前端使用 `Hyperlink[label,url]` 生成可点击 arXiv 号；headless kernel 同时保留完整
   URL 文本，保证终端和日志仍可访问。每个 Wolfram kernel 只显示一次，且必须在全部模块
   成功加载后显示，失败加载不能先打印成功或引用提醒。
2. Python 文本终端不伪造超链接，只给完整 URL。`import flintnde` 每个 Python 进程只提示一次；
   MadStree JSON 后端不得向协议 stdout 混入提示，其响应 metadata 保存同一引用清单，只有
   人工直接 CLI 查询/启动时才可显示文本。
3. MadStree 引用项严格为 2401.00129、2411.03088 和 MadStree 程序包论文；dSIBP 严格为
   2401.00129、2411.03088、2604.14549 和 dSIBP 程序包论文；FlintNDE 严格为 FlintNDE
   程序包论文。未上 arXiv 的程序包论文写 `arXiv identifier pending`，不生成题名、作者、
   arXiv 号或 URL。
4. 三个现有论文题名与作者只从仓库根 README 的已核对条目复制；三个 URL 只从用户给出的
   白名单复制。验收同时检查源码字符串精确相等、禁止多余 arXiv 链接，并实际捕获一次加载
   输出确认条目数、顺序、一次性行为和 UTF-8 中文显示。

实施顺序与代码边界：

1. 021 模块化源码与 v0.12 在本轮只作为只读行为基线；先在 022/v0.13 完成 parser、内部
   数据合同和定向测试。`independent-benchmark/package/` 的正式 loader 要求程序/PDF 版本
   唯一，因此 022 候选与正式文件 hash、最小正式路径 smoke 通过后，必须先删除 021 的正式
   交付程序、PDF 和更新说明，才能从正式 examples fresh 验收 022；021 模块化源码继续只读
   保留到 022 源码回归通过，随后再删除。不得从旧目录加载测试，也不得把旧 parser 当作
   fallback。
2. dSIBP root parser 只解析新的 `vertices/lines` 公开 schema。Association 键顺序任意；
   额外键不参与计算。旧字段即使随新必需字段一起出现也只能作为无效额外信息被忽略，不能
   替代缺失的新字段、覆盖派生 metadata 或触发兼容分支。
3. dSIBP contact sector 使用专用内部 constructor，从已解析 root topology 继承 routing、
   kinematics、normalization、zero points、active vertices 和 representative map；禁止重新
   拼用户 case 后调用 root parser。seed、numeric 和 Kira 配置由各自工作流入口持有。
4. MadStree 只做 `energy -> externalLegEnergy` 的单一新字段迁移；line 继续不设公开 id，
   内部按输入顺序编号。所有 boundary、dlog、artifact、vertex-family 和数值坐标消费者必须
   读取同一新键，不能混留两个 authority。
5. 先运行最小加载/schema/相位检查，再运行受影响 examples 与独立验证；所有运行前删除其
   旧结果、runtime 和 cache。手册只在源码与 examples 稳定后更新、编译并目检。
6. 引用提醒单独验证两种环境：Notebook 中检查 `Hyperlink` 表达式，headless/CLI 中检查完整
   URL；JSON 协议只检查 metadata，不允许 stdout 前缀破坏解析。
7. `vertexSign` 仅表示 `externalLegEnergy` 指数导数号，`contourSign` 仅表示 SK 顶点权重。
   massive/massless contact producer、dlog lower-sector block 和 endpoint 数据不得读取前者替代
   后者；通过论文 DE 的第 5 列及 contact leading vector 对该边界作 exact 回归。
8. 15x15 脚本必须把两个职责分开报告：dSIBP 只生成参数导数，shifted-integral 约化由
   MadStree `MSReduce` 完成。先用 MadStree 自身 recurrence-vs-dlog identity 判断 reduction
   是否闭合；该自检未过时，不得把跨包残差直接归因于 dSIBP。
9. recurrence 与 dlog 共用一个 contact phase helper。dlog 继续按论文使用纯 massive
   `contourSign`、含 massless event 使用 `(-1)^N0`；recurrence 只在把 contact map 送入
   `M0/M1` 递推时使用由同一 phase 推出的 correction。当前代数追踪给出的候选是对每个 event
   乘 `-contactPhase`：单 massless event 保持 `+1`，纯 massive `+` 支翻转为 `-1`；该候选必须先
   通过完全不调用 dSIBP 的 `+/-` exact identity 才可作为实现结论。若不通过，停止并回查递推
   推导，禁止修改论文 DE、expected、master order、normalization 或 H/h basis 来消差。dSIBP
   atomic contact 保持现有已验证 convention，另由其独立论文任务验收。
10. vertex-family 单独定义 `exponentType` 的代数号映射，只供额外 exponential blocks 生成
     `effectiveExternalLegEnergy = baseExternalLegEnergy + Sum[sign_i momentum_i]`。该 helper 不得
     读取 `vertexType`、`vertexSign` 或 `contourSign`；现有 `p0+q1-q2` exact 回归作为验收。
11. dSIBP 的 seed metadata 与撒点入口只保留 `DSMetaSeedRange`、`DSGenerateIBP`。删除别名时
    同步迁移模块 ownership、`DSPublicAPI[]`、coverage manifest、Examples 01/04、smoke、长期
    plan/design/tech note、用户手册和独立任务书；不得留下 forwarding definition 或 Options
    复制。旧名只允许出现在明确标注的历史记录或删除说明中，不得出现在当前调用合同。
12. `DSKiraImport[workspace]` 的缺省目录布局只有 Kira family 路径
    `results/Tuserweight/{kira_list.m,masters}`。用户采用其它文件名或位置时必须显式传入
    `KiraReductionFile`、`KiraMasterFile`；缺省路径缺失应直接报告缺失的当前路径，不探测旧目录。
    新增隔离 smoke：只放旧根目录文件时必须失败，只放当前 family 文件时必须成功解析。

022 阶段进展（2026-08-19）：root parser 和 contact-sector constructor 已按新 schema
拆分，schema/相位 smoke fresh 通过 16/16。tree family 内部字段已统一为
`vertexType/externalLegEnergy/signedExternalLegEnergy/momentumMagnitude`，删除
`treeEnergy` 覆盖与旧 `energy/sign` 消费。Example 05 删除旧 `init/` 后 fresh 运行退出 0；
传播子变量使用由 `momentum -> p12` 自动绑定的 `sE1`，naive DE 与 dlog DE 精确一致。
Example 04 fresh 生成 57160 条 IBP 方程并成功序列化 Kira 输入，退出 0、状态为
`awaitingExternalKira`；本轮未运行外部 Kira reduction，不能把该状态写成 reduction 通过。
Example 01 首次 022 fresh 运行虽退出 0，但 stdout 为 104,959,796 字节；定位到
`resolveKiraOrderingSpec` 第二参数误写为 `_topoSpec_`，导致该调用保持未求值，随后
`DSReorderIntegrals` 的 `Join` 错误消息把完整 `linearData` 展开。该次运行只作缺陷证据，
修复并清理日志后必须重跑，不能作为 Example 01 验收证据。
完成记录（2026-08-19）：已把错误的 `_topoSpec_` 改为合法匿名参数 pattern；更新后的
积分顺序 smoke 从 022 模块路径通过 `10/10`，包括 `kiraOrdering` 为 Association、重排
无消息、方程 ID 与 plan 顺序一致。Example 01 在新临时目录 fresh 退出 `0`，stdout
由 104,959,796 字节降为 8,924 字节，stderr 为 0 字节，未出现 `Join::incpt`，最终 summary
确认 5516 条方程、uniform/detailed exact 相等且 Kira plan 状态为 `planned`。

022 smoke 迁移记录（2026-08-19）：旧测试输入中的 `vertexData/lineData/vertexEnergies`、
massless `nu`、`bbType/skType` 以及 topology 内 `numericRules/seedPreset/seedRanges` 已按新
schema 物理删除。参数导数 `11/11`、图圈数 `7/7`、Kira 能量 convention `16/16`、
example API coverage `6/6`、ISP numerator `13/13` 和 UserMI `17/17` 均从 022 模块路径
通过；sunrise parity 的 2 个 time 与 6 个 momentum generators、5 个 sector parity 也全部
通过。UserMI smoke 不再读取 Kira manifest，而由 reference basis 支撑与同一导数 producer
两阶段构造闭包；module ownership 022 扫描 23 个 Kernel 文件、360 个顶层符号，无跨文件
同签名重复。

MadStree v0.13 论文 targeted case 的首轮 fresh 结果（2026-08-19）定位到 contact 符号残留：
在正确的论文 `k` 到包坐标 `externalLegEnergy=-k` 映射下，child normalization、绝对边界
coefficient 和普通点 child 值均对齐，但三个 DE 的差值各仅剩第 5 列四项，且 package
top-to-child block 与论文整体反号；contact leading vector 的两个 top 分量也整体反号。
源码追踪确认 `DLog.wl` 错读了 exponent `vertexSign`，修复后必须 fresh 重跑本 case。

MadStree contact 修复完成记录（2026-08-19）：`DLog.wl` 已物理删除错误 helper，并让 contact
source 只读取 `contourSign`。论文两顶点 G++ targeted validation fresh 通过 `16/16`，五维普通点
最大相对差约 `2.71*^-28`、child ratio 为 `1`；core `56/56`、massive full edge `6/6`、
massless edge `10/10`、mixed three-vertex `8/8`。随后 022-v0.13 的 15x15 exact DE 比较由旧的
共同错误下假通过变为 `8/9`。进一步调用链审计确认，该脚本的 reduction consumer 仍是
MadStree `MSReduce`；纯 massive 的 `11->01` 与 `10->00` blocks 反号，而 massless 的
`11->10` block 已精确为零差。对 dSIBP atomic contact 的 speculative 修改未改变任何一项
15x15 输出，已判定不是该失败的执行路径并必须撤销。S13 完成前不得把 MadStree recurrence
与 dlog 的一致性写成已验收；dSIBP 是否存在独立缺陷只认其自身论文 oracle，不从该脚本外推。

S13/S14 完成记录（2026-08-19）：contact phase helper 已移入 `Recurrence.wl` 成为唯一内部
authority，`DLog.wl` 删除本地实现；recurrence 在 raw contact map 进入 `M0/M1` 递推时消费
`-contactPhase`。完全不加载 dSIBP 的纯 massive `++/--` 两组外腿能量导数 5x5 exact identity
均通过，core 由 `56/56` 增至 `58/58`。跨包 15x15 比较恢复 `9/9`，`k1/k2/k3/sE1/sE2`
五个矩阵的全部 block 非零差数均为 `0`。受影响回归为 massive `6/6`、massless `10/10`、
mixed three-vertex `8/8`、simultaneous/cycle/chart `22/22`；论文 G++ case 在 runner 自动删除
旧报告与结果后 fresh 通过 `16/16`，最大相对差约 `2.71*^-28`、child ratio 为 `1`。
dSIBP 022 topology/phase smoke 在撤销 speculative 改动后保持 `16/16`。

同轮 fresh 回归还暴露 v0.13 的 vertex-family 额外指数块误复用新 `vertexType` 映射：
`p0+q1-q2` 被反转，首次为 `18/19`。现已新增独立 `msExponentTypeSign`，恢复字面
`+1/-1` 而不改变顶点分支；vertex/reduce 恢复 `19/19`，对应 FlintNDE 数值路线 `8/8`。

验收定义：当前源码、单文件交付、examples、测试与现行文档中不存在上述旧入口或
兼容逻辑；两包仅保留 dSIBP 022 与 MadStree v0.13。独立验证的论文原文引用中如果
出现历史符号不做文字篡改，但不得被 package 作为旧接口读取。

### 当前任务：2026-08-18 MadStree v0.12 顶点轮廓支与拓扑输入重构（已完成）

从当前 v0.11 建立唯一工作版本 v0.12，删除拓扑公开输入中重复且可能互相矛盾的
`phaseSign`、`skType`、`sigma` 和 `phaseSigns`。用户只在顶点上用
`"vertexType" -> "+"|"-"` 指定 Schwinger--Keldysh 轮廓支；传播子的 Full/Cross/External
类别、SK 标签、整体符号、端点支符号及缺省 Hankel branches 全部由端点顶点自动推导。
传播子公开类型收缩为 `"massive"|"massless"`。顶点 ID 允许直接使用整数；传播子公开接口
不定义或读取 ID，内部严格按 `"lines"` 输入顺序编号为 `1,2,...`。不保留旧字段、
旧线型、wrapper、fallback 或 schema 兼容入口。`"energy"` 字段暂不改名，但全部用户文档
必须准确说明它是附着在顶点上的无 theta 指数 `Exp[I s_v k_v tau_v]` 的参数，不把它称作
顶点自身具有的能量。

拓扑输入的 Association 键顺序不受限制；顶层、顶点和传播子只检查公式所需字段是否齐全，
额外字段忽略。传播子即使额外带有 `"id"` 也不读取，内部编号仍唯一取自 `"lines"` 位置。

dSIBP 021 的只读审计确认存在同类双 authority：`vertexData` 已给出每个顶点 `+/-`，
`completeLineMetadata` 却允许显式 line `skType` 覆盖由端点推导的值，现有门禁只检查其属于
`++/--/+-/-+`，不检查与端点一致。若后续升版修正，应删除公开 line `skType` 和旧五元组
兼容入口，由端点唯一派生 `skType`，再由 `massType+skType+state` 唯一派生 `packType`；
当前本任务不修改 dSIBP 源码。

- [x] V12-1. 建立唯一 `versions/MadStree-v0.12/` 并同步版本入口、索引、任务书和验证路径。
- [x] V12-2. 重构拓扑 schema，从 `vertexType` 与端点自动派生全部内部 SK 数据。
- [x] V12-3. 支持整数顶点 ID；删除公开传播子 ID 并固定内部顺序编号，验证额外字段不覆盖派生值。
- [x] V12-4. 更新全部当前 examples、开发测试、独立验证入口、中英文 README、更新说明和中英文手册。
- [x] V12-5. 审计 dSIBP 是否存在相同的顶点/传播子重复分支输入，并只提交修改方案。
- [x] V12-6. 从清空临时产物的 v0.12 路径运行定向回归、全部 examples、全部当前独立验证、文档编译和发布前门禁。

完成记录（2026-08-18）：删除旧独立验证报告、结果和 runtime 后，仅从 v0.12 新路径 fresh
重跑。开发回归为 core `56/56`、simultaneous/cycle/chart `22/22`、vertex/reduce `19/19`；
六个 examples 全部通过，其中正规化 Example 06 为 `15/15`。三个当前独立验证依次为
`18/18`、`26/26`、`16/16`。900 点规划路线只用 `8` 个节点并以 fast multipoint 计算
`894` 点，严格用户节点路线使用 `904` 个节点；端到端墙钟分别为 `3.75709 s` 与
`5.65041 s`，后端阶段分别为 `0.923019 s` 与 `4.44204 s`，两路线 900 点乘 3 masters
最大相对差约 `1.67707e-31`。中英文手册均完成带 BibTeX 的多遍编译和关键页目检，引用、
中文显示和 v0.12 拓扑接口均正常。examples 的运行结果和临时产物已删除；独立验证仅保留
最新正式报告与轻量 `results/` 证据。

### 当前任务：2026-08-18 NDE 主链结果与参考链精度核验语义修正（已完成）

统一 MadStree v0.11 与 FlintNDE 0.4.0 的双阶输运合同：`TransportOrder`/`primary_order`
产生用户结果，`ReferenceTransportOrder`/`reference_order` 只运行独立高阶链并用于相对差和
精度门禁。节点值、dense 用户点、保存点、正规化样本和 MadStree 各段末点均不得以参考链
替换主链；跨多个 MadStree 复仿射段时，两条链必须分别续传自己的段末状态。

- [x] P1. 修正 FlintNDE 普通路径、规划路径、保存点和正规化样本的主链输出语义。
- [x] P2. 修正 MadStree 正则奇点边界及跨复仿射段的主/参考双状态传播。
- [x] P3. 增加可区分低阶/高阶值的节点、dense、保存点、正规化和多段回归。
- [x] P4. 同步独立 FlintNDE 与 MadStree Vendor、README/手册/更新说明并执行受影响测试。

完成记录：FlintNDE Python 完整回归 `170/170`，MadStree adapter `10/10`；新增测试用
可显著区分的低阶/高阶值确认 dense、保存点、正规化和连续两段均消费 primary。MadStree
`TransportOrder -> 80`、`ReferenceTransportOrder -> 104` 的 massless edge 定向测试
`10/10`，路径求值 `21/21`。独立 FlintNDE 与 Vendor 的 9 个受影响源码/测试文件 SHA-256
逐文件一致；三份手册重编译并目检，运行 cache 和临时结果已清理。本轮未提交、未推送。

### 当前任务：2026-08-18 两包分别对论文的 massive 两顶点独立检验（已完成）

针对 MadStree v0.11 massive contact lower-sector 重复相位，建立两个互不读取对方结果的
独立任务。MadStree 单独对照 2411.03088 的 exact 五维 DE、Eq. (4.2)/(4.11) 绝对系数，
并以收敛域内的论文 Eqs. (4.8)--(4.10) 超几何/多重级数直接检查完整五维值和 child 分量；dSIBP 单独对照论文有序主积分
定义、每项 coefficient/sector normalization 与由 time-IBP 生成的五维 DE。G++ 情形不要求
把论文超几何函数实现成程序包功能；该公式只存在于独立验证目录，也不把共享 FlintNDE 输运
冒充独立常点 oracle，禁止改用缓慢的暴力 `NIntegrate`。

- [x] X1. 登记历史验证缺口；论文 Eq. (4.2)/(4.11) targeted oracle 已推翻“`boundaryContactPhase` 重复”的初步诊断，并把所需 contact 相位限定在边界 producer。
- [x] X2. 审计 dSIBP normalization 数据流；targeted oracle 定位到 loop-to-tree 与实际 direct compiled-theta contact 路线都对 `(-tau)^a` 再乘 `(-1)^Delta a` 的重复连续幂相位。
- [x] X3. 从论文公式和参考代码保存各包独立持有的 exact 五维论文 oracle。
- [x] X4. 拆分 MadStree 与 dSIBP 任务书，删除跨包互证和三路共享验收。
- [x] X5. 从 MadStree sector schema 物理删除旧相位字段；论文证明边界相位必需，因此仅在边界 coefficient producer 内由实际 massive contractions 生成，不留兼容入口。
- [x] X6. 只运行新增两顶点 targeted validation；不重跑三包全量回归。
- [x] X7. 执行 UTF-8、临时产物与 `git diff --check` 门禁，回收两包各自报告。

验收要求：两包分别以论文为外部 authority。dSIBP 没有边界生成能力，不要求数值输运；
其 IBP-DE 与直接 dlog 路线都必须各自对论文逐项为零，且 normalized master 的 coefficient 与
`N_s` 映射逐项保存。MadStree 的 DE-only 检查不能替代绝对值检查；定义积分路线必须独立于
`MSBoundaryData`，直接从论文收敛级数求值并单列第五个 child 分量。删除单条 massive contact
边界相位会使该分量相对正确值少乘 `I`，ratio 为 `-I`；正确实现的 ratio 必须为 `1`。
dSIBP 的 DE 对常数 basis phase 不敏感，必须另用 Eq. (4.2) 检查绝对 normalized-master 定义，
才能暴露 loop-to-tree 的 `(-1)^Delta a` 重复相位。

完成记录（2026-08-18）：MadStree v0.11 定向验证通过 `16/16`；论文级数只在独立验证目录中
作为常点 oracle，所选精确点为 `{k12->30,k34->6,ks->1,nu0->2,nu1->1/5}`，对应
`x=1/5,y=1/6`，位于论文收敛域内，未调用 `NIntegrate`。完整五维值最大相对差为
`2.71038877143649*^-28`，child ratio 为 `1`；删除必要的 massive contact 边界相位时独立
反例为 `-I`。dSIBP 021 定向验证通过 `12/12`，Eq. (4.2) child normalization residual 为
`0`，direct-vs-paper、naive-vs-paper 与 naive-vs-direct 的三个 DE 矩阵非零差值均为 `0`。
正式单文件 `package_021.0.wl` SHA-256 为
`E03E16348584A62837183F56984B74F742F78ADB38387ABADDCAF84F7916D80B`；正式用户手册两处
交付均为 32 页、961430 bytes，SHA-256 为
`5CB373B868E5CCAE3592A81B375201B89C0F333375130D2F6FBB6C837CF82DC5`；技术笔记 PDF
SHA-256 为 `32BA7E942FE2A8F6EAB30C1813D30B3A9437DB7DD76034D3E6D917CD70A4C1E2`。
本轮只运行上述新增 targeted validation，没有运行三包全量回归；被成功报告取代的失败报告、
独立工作区、候选 PDF、TeX 辅助文件和渲染图片均已清理。

### 当前任务：2026-08-16 三程序包单最新版与验证资产清理（已完成）

按用户本轮明确要求，把工作树的版本保留策略从“最近三个版本”收紧为“只保留当前最新版”。
当前唯一源码版本分别为 dSIBP `021_dSIBP`、MadStree `v0.11` 和 FlintNDE `0.4.0`；更早
源码、旧版本独立检验任务书、旧版本报告与结果只从 Git 历史追溯。当前最新版下互不替代的
正式 validation case、轻量机器证据、发布 examples 和回归测试继续保留；运行 cache、临时
结果、临时 benchmark、过时审查报告、空 `test/check/tmp` 工作区和依赖已删除版本的 smoke
脚本清除。

- [x] C1. 核对 Git 状态、活动进程、三包直接规则、当前入口和 tracked/ignored 资产。
- [x] C2. 删除旧源码版本、旧版本任务书/报告及无消费者临时测试、结果和 cache。
- [x] C3. 同步根与三包 AGENTS、README、VERSION_INDEX 和当前文档入口的单版本规则。
- [x] C4. 从保留的最新版路径运行受影响回归，确认 validation-01/02 正式证据仍完整可读。
- [x] C5. 执行旧路径残留、UTF-8、临时产物、`git diff --check` 和最终 Git 状态门禁。

C4 首轮回归暴露三类旧依赖并纳入本任务修正：MadStree 跨包测试硬编码 dSIBP 020；dSIBP
通用 smoke 默认加载 018 且 harness 保留旧环境变量兼容分支；021 的
`DSScaleCheck["LoopTopology"]` 在 time-only 模式下没有把公开
`J[sectorKey,timeShifts,stateBits]` 转回内部三槽 `J` 后再计算物理次数。前两类只修当前测试与
入口；第三类在 021 当前实现中增加唯一的 public-to-internal 边界转换，并由现有 normalized
fixed-line prefactor smoke 验证 `D Log[N_s]`、乘积法则、integrand 回乘和 Euler 次数。

验收边界：不删除 MadStree v0.11 与 FlintNDE 0.4.0 的两个互补独立验证 case；不删除 dSIBP
三个固定典型 example、当前 021 smoke、当前版本正式 package/PDF 或 reference result。删除
目录前已确认工作树干净、无 Python/Wolfram/FORM/Kira 活动进程；本轮不创建版本、不保留旧
路径兼容入口，也不自动推送 GitHub。

完成记录：三个 `versions/` 目录均只保留一个当前版本：dSIBP `021_dSIBP`、MadStree
`v0.11`、FlintNDE `0.4.0`。共删除 245 个 tracked 文件、6,931,042 bytes（6.61 MiB）；旧源码、
旧任务书/报告、无消费者测试脚本和全部可重跑 cache/临时结果均已清除，活动旧版本路径消费者
扫描为 0。FlintNDE 0.4.0 Python 回归 `166/166`；MadStree v0.11 的 16 个 Wolfram 脚本全部
退出 0，Python adapter `9/9`，MadStree/dSIBP 跨包 DE `9/9`；dSIBP 021 当前 12 项 smoke
全部通过，normalized fixed-line 回归 `11/11`。保留的 FlintNDE validation-01/02
`overall_passed=true`，MadStree validation-01/02 分别为 `18/18`、`26/26` 且状态 `passed`。

dSIBP 候选与正式单文件 SHA-256 均为
`AD5A413A49D25210521897A1201A54E6DDF8AF869E1EF81D6991595A86FC2666`。重编译并目检后的
32 页正式用户手册 SHA-256 为
`3CB1A8A5AE3640A4F49F88395F83FF33AB2C8F54BACBA29CB44CED4B8BD5E056`；57 页技术笔记
SHA-256 为 `8F8248CDE32FBD98603E9995F87D82D35FB13667291A8CC1B49A55F8D0ED0EE7`。最终 332 个候选
文本严格 UTF-8 解码 `332/332`，replacement/C1 控制字符均为 0；`results_test`、
`results_temp`、`__pycache__`、`.pyc`、TeX 中间文件扫描为 0，`git diff --check` 通过，
本地 `main` 与本地记录的 `origin/main` 分歧为 `0/0`。本轮未提交、未推送。

### 当前任务：2026-08-16 regulator 复角域采样与容量不足保留结果（进行中）

扩展 FlintNDE 与 MadStree 的显式正规化取值控制：候选池耗尽且精度未达标时不丢弃当前
最佳方阵拟合结果，而返回明确的未达标状态、残差和“候选点容量不足”原因；用户还可给出
正规化参数的复角开区间；程序在内部均匀选择最多三条射线，模长仍由目标精度自动决定且
不设置上限。MadStree 路线把高精度复点 exact rationalize 为 Q(i)，保持 dlog 拉回合同。
缺省仍为 `Automatic`，既有调用和缺省 schema 不变。

- [x] H1. 审计 Python/MadStree 失败传播、结果 schema、任务书与独立验证 runner。
- [x] H2. 实现容量不足保留结果和复角域自动采样；模长继续由目标精度自动决定且不设置上限。
- [x] H3. 增加 Python/Wolfram 正反例、默认兼容、复角域范围与缓存复用测试。
- [x] H4. 更新 examples、README、手册、更新说明和两包独立检验任务书。
- [x] H5. fresh 执行新增独立检验、完整回归、PDF 与 Git 门禁并推送 `main`。

H1--H5 完成记录（2026-08-16）：FlintNDE validation-02 从空结果执行通过，默认四点逐项
保持旧正实轴公式，开角域使用三条内部射线，Laurent 系数最大误差分别为
`1.16667e-38` 与 `3.38258e-42`；候选池耗尽仍返回未认证系数且无池外点。MadStree
validation-02 fresh 为 `26/26`，默认 JSON 不含 `sampleAngleRange`，角域路线 `3` 个生产点加
`2` 个验证点、实际 `26` 节点并通过 refinement；候选容量路线返回
`computed_with_warning/candidate_pool_exhausted`。完整回归为独立包/Vendor Python 各
`166/166`、Wolfram 接口各 `25/25`、MadStree adapter `9/9`，16 个 Wolfram 开发脚本逐个
启动全部退出 `0`。Vendor 非缓存文件 `42/42` SHA-256 一致；严格 UTF-8 扫描 558 个文本
无 decode/replacement/C1 问题，cache、临时结果、TeX 中间文件和目检截图残留为 0。手册已
编译并检查相关页面；本次不升版。功能提交 `949c3d1` 已推送到 GitHub `main`。

### 当前任务：2026-08-16 regulator 冗余自定义点与首轮内部阶数（进行中）

保持 FlintNDE、Wolfram 与 MadStree 缺省输入格式和缺省行为不变，只扩展用户显式配置路线：
用户可提供多于首轮方阵拟合所需数量的 regulator 候选点；程序每轮只按当前内部最高幂所需的
生产点数取用前缀，验证不通过时复用已算点并从剩余候选点增量取点，不得自动越出用户给定
范围。用户还可显式指定首轮内部最高 regulator 幂；它必须不低于用户要求返回的最高幂，缺省
仍由既有 `fit_extra_order=2` 规则确定。若候选点不足以完成首轮或后续所需扩阶，必须给出独立、
可诊断的输入/容量错误，不回退到自动点。

- [x] G1. 审计 Python、Wolfram、MadStree 现有 regulator 规划、缓存和 schema 数据流。
- [x] G2. 实现冗余候选点前缀取用、增量复用与显式首轮内部最高幂。
- [x] G3. 增加缺省兼容、点数容量、增量扩阶复用及用户范围边界测试。
- [x] G4. 更新 Python/Wolfram/MadStree examples、README、手册与更新说明。
- [x] G5. 编译手册、运行完整受影响回归、检查临时产物并推送 GitHub `main`。

G1--G4 与 G5 发布前门禁完成记录（2026-08-16）：独立 FlintNDE 与 MadStree Vendor
实现/测试逐字节同步；Python 完整回归各 `165/165`，Wolfram 接口各 `25/25`，MadStree
adapter `8/8`，16 个 Wolfram 开发脚本逐个独立启动全部退出 `0`。Python 正规化 example
首轮从 7 个候选点取 4 个，验证失败后新增 2 个并复用 4 个生产值与 2 个验证值，最终保留
1 个未使用候选点；Example 06 缺省自动路线 fresh 通过 `14/14`，仍为 3 个自动生产点、
2 个独立验证点，`productionEpCandidateValues === Automatic`，最大验证相对残差约
`1.84278e-36`。三份手册完成 BibTeX/双轮编译和新增页渲染检查；Vendor 42/42 非缓存文件
SHA-256 相同，临时 cache、测试结果和 TeX 中间文件已删除。

### 当前任务：2026-08-15 examples/独立验证零产物 fresh 重跑与旧路径暴露（进行中）

为避免运行目录重构后误复用旧结果、cache 或报告，先删除当前 MadStree v0.11 与 FlintNDE
0.4.0 examples、独立验证产生的全部中间和最终运行产物，再从空产物状态 fresh 重跑。源码、
任务书和验证程序保留；正式报告及机器 summary 由验证程序重新生成。当前代码继续执行零兼容
策略：扫描并删除旧路径、旧 schema、wrapper、fallback 和参数别名，使未同步修改的消费者直接
失败并在 fresh 门禁中暴露。

- [x] F1. 盘点 examples/独立验证全部生成物、tracked/ignored 状态及进程占用。
- [x] F2. 删除中间产物、cache、保存点、日志及既有最终运行结果/报告。
- [x] F3. 从空产物状态运行 FlintNDE 0.4.0 与 MadStree v0.11 当前 examples。
- [x] F4. 从空产物状态运行两套当前独立验证，复核路径、节点、效率和 planned/direct 互检。
- [x] F5. 扫描旧目录/schema/兼容入口并清除残留；运行完整受影响回归和 Git 门禁。
- [x] F6. 更新 fresh 报告与本进度记录，提交并推送 GitHub `main`。

F5 扫描发现 MadStree v0.11 当前 adapter 仍通过 shell `Run`、命令字符串引号 helper 和重定向
启动 Python。该入口不是冻结历史或兼容层，仍位于生产调用链；本轮将物理删除这些 helper，
统一改为参数列表 `RunProcess`，并在无重试/fallback 的条件下重新执行 MadStree 数值回归与 examples。

fresh 重跑还确认：MadStree 独立验证 runner 本身为无 BOM UTF-8，但 Windows
`wolframscript -file` 按系统代码页解析其中的中文报告模板，导致生成报告成为合法 UTF-8 字节
中的双重乱码。实测 UTF-8 BOM 不改变当前 `-file` 解析行为，因此不保留无效 BOM；runner
将在写出前只对可严格 UTF-8 往返的低字节字符段恢复源码文本，已正确解析的 Unicode 与用户
路径保持原样，并以报告原始字节严格解码、中文标题和乱码 marker 扫描作为验收。

发布前证据链复核要求报告除顶层 `Kernel/MadStree.wl` 外，单独记录本轮实际修改并加载的
`Kernel/Numerics/FlintNDE.wl` SHA-256，避免顶层 loader 哈希掩盖数值 launcher 的源码变化。
暂存门禁进一步确认正式 `evidence.json` 的 `RawJSON` 导出会产生 CRLF；现已修正 runner
producer，统一通过二进制 writer 输出 UTF-8/LF，并在写出失败时立即退出，未对结果文件做
一次性格式化。

F1--F5 完成记录（2026-08-15）：FlintNDE 七个 examples 与 MadStree 六个 examples 均从空
`results/`/`results_temp/` fresh 退出 `0`；MadStree Example 06 为 `13/13`，3 个生产点、
2 个独立验证点、并行 requested/effective `12/5`，最大验证相对残差约 `1.84278e-36`。
FlintNDE 独立验证为 128/901 planned/direct 节点、774 个 dense 点、1800 分量全检，后端和
总墙钟加速分别约 `5.505/5.120`。MadStree 独立验证为 `18/18`，900 点乘 3 masters 全分量
互检，最大绝对/相对差约 `5.82618e-43`/`4.49876e-34`，自动规划路线端到端和 backend-only
墙钟为 `2.124148/0.8818735 s`，严格用户节点路线为 `5.4019755/4.2349896 s`，因此约快
`2.54313/4.80226` 倍；报告保存实际五段路径、节点、覆盖桶和完整 evidence。最终 report、
summary、evidence 均为严格 UTF-8、无 BOM、LF，`evidence.json` 含 baseline commit、数值模块与
runner digest；验证结束后唯一 `results_temp/` 已删除。

最终全仓字符级扫描另发现当前正式 dSIBP `package_021.0.wl` 的单文件私有实现章节标题为
UTF-8 字节误解码文本。根因是 021 builder 在 Windows `wolframscript -file` 下直接解析中文
字符串字面量；本轮将改为由 Unicode 字符码构造该标题，在 `test/results_test/` 生成候选并按
正式单文件交付流程复验后，以同一候选字节覆盖正式包。该修复只改注释编码，不改变接口或公式。

完成记录（2026-08-15）：UTF-8 launcher、浅运行目录、fresh examples/独立验证、正式 evidence
及 dSIBP 单文件中文标题修复由功能提交 `bf717d9` 推送到 GitHub `origin/main`。推送前暂存区
`git diff --cached --check` 通过，29 个文件中无 cache、pyc、日志、TeX 中间文件或临时结果。

发布回归为独立包/Vendor Python 各 `162/162`、Wolfram 各 `25/25`、MadStree adapter
`8/8`、16 个开发脚本逐个直接启动全部退出 `0`，独立包/Vendor 42/42 非缓存文件 SHA-256
一致。一次把 16 个 WolframScript 放入 PowerShell 子循环的尝试在 kernel 启动前统一因
WolframScript 配置文件不可读退出，未执行 package 代码且不计入回归；随后逐个直接启动的
结果才作为正式证据。严格扫描 556 个 tracked/待提交文本：UTF-8 解码失败、UTF-16、替换字符
和乱码 marker 均为 0；4 个 BOM 全为本轮前已有文件。最终报告 UTF-8 无 BOM、LF、中文标题
可读；example/test runtime、cache、`__pycache__`、`.pyc` 和空 `results_temp` 已清理。

### 当前任务：2026-08-15 MadStree/FlintNDE 浅运行目录与文件门禁（已完成）

统一 MadStree 到 FlintNDE 的运行目录职责：公开选项直接指定临时运行目录，缺省值才解析为
调用目录下的 `results_temp/`，内部固定使用短目录 `nde/`、`cache/` 和 `bridge/`；删除重复层级、长任务
目录名、完整 UUID 文件名和摘要子目录。路径过长必须单独返回 `RuntimePathTooLong`，只报告
具体路径、实际长度、安全上限与短根目录设置方法，不得混入 Python、cache 或后端输出错误。

- [x] S1. 复现 Windows/Wolfram 259 成功、260 失败及公共导出假 written。
- [x] S2. 重构 MadStree 通用运行器、Example 06 和 FlintNDE Wolfram bridge 为浅目录合同。
- [x] S3. 增加路径预检、逐文件写入/导入检查和准确 Failure 分类。
- [x] S4. 修复 Notebook 命令行读取、公共导出 PASSED 门禁和 package-loaded 完整性门禁。
- [x] S5. 同步 Vendor、README/手册/更新说明，运行深路径、完整回归、PDF 与 Git 门禁。
- [x] S6. 提交并推送 GitHub `main`。

本轮 fresh 证据：MadStree 浅目录/导出门禁 10/10、adapter 8/8、当前 v0.11 全部 Wolfram
开发测试退出 0；FlintNDE 独立包与 Vendor Python 各 162/162，Wolfram 接口 25/25；
两个 FlintNDE Wolfram examples 与 MadStree Example 06 均通过。Example 06 使用 3 个生产点、
2 个独立验证点，最大相对残差约 `1.84e-36`。三份手册重新编译，最终日志无 undefined/
Overfull/Fatal 条目，新增段落页面目检无裁切、重叠或乱码；Vendor 42/42 非缓存文件 SHA-256
一致，`git diff --check` 通过。

完成记录（2026-08-15）：浅运行目录、独立错误分类、示例与文档修订由功能提交
`10786e4` 推送到 GitHub `origin/main`；推送前后均确认本地与远端无分叉。

验收要求：临时运行目录语义在全部公开入口一致；Example 不自行重复拼 `results_temp`；传统
Windows 路径限制下给出独立的真实失败文件和长度；任意请求格式未写成时公共导出返回
`Failure`；模块加载失败时 package 加载失败且不显示成功提示；不保留旧目录/schema 兼容读取。

### 当前任务：2026-08-14 MadStree/FlintNDE 缺省工作精度统一为 200 位（已完成）

把当前 MadStree v0.11 与 FlintNDE 0.4.0 的公开数值入口缺省工作精度统一提高到 200 位。
普通边界、路径规划、路径执行和固定 `ep` 批量使用 200 位固定缺省；正规化自动规划仍可按问题
规模继续提高精度，但不得低于 200 位。用户显式指定工作精度时继续使用指定值，不设置隐式
下限。同步 MadStree 内嵌 FlintNDE、README/手册/更新说明，并增加默认值和自动下限回归。

- [x] W1. 盘点当前公开入口、自动规划和 Vendor 同步边界。
- [x] W2. 修改 MadStree/FlintNDE 当前源码及 Vendor，使所有缺省工作精度达到至少 200 位。
- [x] W3. 更新回归测试、README、手册和当前版本更新说明。
- [x] W4. fresh 运行精度默认值、Python/Wolfram 回归、Vendor 精度合同与 Git 文本门禁。

完成记录（2026-08-14）：MadStree `MSBoundaryData`、`MSEvaluatePath` 的缺省
`WorkingPrecision` 已改为 200；MadStree 自适应 `ep` production/validation 取 200 与原规划值的
较大者。FlintNDE Python 导入及无参 `configure_working_precision()` 缺省为 200 位十进制
精度，即 `ceil(200 log2(10))+32=697` bit；Wolfram 的计划、执行和固定 `ep` 批量入口均缺省
200 位。正规化自动规划取 200 与 AMFlow 式估计的较大者；所有显式精度继续直接覆盖缺省。
fresh 验收：FlintNDE Python 全套 `162/162`，正规化专项 `20/20`，Wolfram `21/21`；
MadStree adapter `8/8`、路径 Wolfram `15/15`。三份手册完成 XeLaTeX/BibTeX 编译，相关页
渲染目检无裁切、重叠或缺字，正式 PDF 与验收件 SHA-256 一致；`git diff --check` 通过。
MadStree Vendor 的 200 位定向测试通过；其旧基线全套正规化测试为 `10/18`，其余 8 项在
既有 `parallel_task_count=1` 仍要求 pickle 局部闭包的分支提前失败，与本次精度默认无关，
不得表述为 Vendor 全套通过，留待当前发布任务完成独立包/Vendor 整体同步后重跑。

### 当前任务：2026-08-14 自适应正规化发布收尾（已完成）

将 MadStree 自适应 `ep` Laurent 重构、FlintNDE/MadStree 并行入口、真实三顶点 900 点性能
证据及相关中英文文档统一提交并推送 GitHub `main`。按用户后续要求，不再把
`ParallelTaskCount=1` 串行基准作为发布测试；现有并行证据只报告已经实际执行的配置。

- [x] R1. 盘点发布范围、根/子项目当前版本入口和现有性能证据。
- [x] R2. Example 06 保留可选并行数及端到端墙钟输出；取消已被用户否决的并行 1 补测。
- [x] R3. 同步根 README、MadStree/FlintNDE README、版本说明、效率记录和本进度表。
- [x] R4. fresh 运行受影响回归、示例、PDF、UTF-8、Vendor 一致性和 Git hygiene 门禁。
- [x] R5. 核对本地/远端分歧，提交全部相关文件并推送 GitHub `main`。

新增正规化验收要求（2026-08-14）：用户只给返回最高幂；内部拟合缺省至少多算两阶。
独立验证未达到 `EpGoalDigits` 时不得降低容差，也不得丢弃已有 NDE 点值后整批重算；程序应
每轮增加内部拟合阶数和生产点，只计算新增点，并复用此前生产点及始终不参与拟合的独立验证
点重新认证。该增量扩阶、点值复用、终止上限和失败原因必须进入 MadStree/FlintNDE 手册、
README、example 与回归测试。

最低幂结构证书要求（2026-08-14）：`ep` 数值任务启动前，MadStree 必须从实际符号边界
条件与 dlog DE 自动确定 Laurent 最低阶，并把证书直接传给采样规划。DE 在 `ep=0` 正则时，
解析可逆的基本解不改变非零边界数据的最低阶；若 DE 含负 `ep` 阶、边界不是有限 Laurent
型、最低阶系数无法证明非零，或正规化参数进入当前未认证的路径坐标，则 fail closed。删除以
终点向量数值 pilot 猜最低阶的生产流程；pilot 不再是后续运算的输入或 fallback。

本次 fresh 验收记录（2026-08-14）：FlintNDE 独立包和 MadStree Vendor 的 Python 全套均
`162/162`；两处 Wolfram 接口均 `21/21`，MadStree Python adapter `8/8`。Laurent valuation
八个正反例通过；真实无质量三顶点结构证书得到 9 masters、9 边界分支、DE 最低阶 0、
`leadingPower=0`，三个 Frobenius 降秩只记录为 resonance 诊断。Example 06 fresh 为 `13/13`，
请求/有效并行 `12/5`，3 个生产点、2 个独立验证点，最大相对残差约 `1.8428e-36`，无 pole。
新增 FlintNDE `ep_series_reconstruction.py` 用解析证书 `leading_power=-1`：首轮拟合到
`ep^2` 后验证失败，第二轮只新增 2 点并复用 4 个生产点与 2 个验证点，拟合到 `ep^4` 后
恢复 pole 系数 1、有限项 2。三份 PDF 分别为 33/30/30 页，最终日志无未定义引用或 overfull，
新增章节渲染目检无裁切、重叠或缺字。

Git 发布（2026-08-14）：发布前两次 fetch 均确认远端无新增提交；功能提交 `2285d0d`
已成功推送到 `origin/main`，包含符号最低阶证书、增量扩阶、并行批处理、examples、回归、
手册 PDF、独立检验引用和效率记录，且 staged 集合不含 cache、pyc、TeX 中间文件或临时结果。

### 当前任务：2026-08-13--14 同一 ep 正规化的无质量三顶点自适应 Laurent 重构（已完成）

为 MadStree 增加各顶点时间幂共享同一 `ep` 正规化的真实无质量三顶点 example，并先盘点此前
是否已有正规化 example。对一组不同 `ep` 数值的独立 NDE 任务建立程序控制的有界任务池：
公开并行数量选项缺省为 12，实际并发数为 `Min[并行数量, ep 取值数]`；当取值数超过并发数时，
任一任务完成后自动续交下一个，禁止依赖用户手动分批。FlintNDE Python、FlintNDE Wolfram
入口和 MadStree Wolfram 入口均提供同语义选项，并在 usage、README 和 example 中说明设置方法
与缺省值。并行数只属于执行配置，不进入数学结果认证门禁。

- [x] P1. 审计现有正规化/ep 输入、examples、MMA/Python 调用链和当前进程模型。
- [x] P2. 实现 Python 有界 ep 任务池及 FlintNDE Wolfram 入口，覆盖少于/等于/多于并行数。
- [x] P3. 实现 MadStree 跨 ep 批量入口或选项透传，并增加真实无质量三顶点正规化 example。
- [x] P4. 增加结果同序、失败传播、并发上限、串并行数值一致性和中文提示测试。
- [x] P5. 更新三包当前文档/usage，fresh 运行受影响回归、example、UTF-8 和 Git 检查。
- [x] P6. 将 MadStree 公开合同改为只指定所需最高 `ep` 幂，不要求用户提供采样点。
- [x] P7. 用符号边界条件与 dlog DE 自动认证最低整数幂和 pole 深度，删除数值 pilot。
- [x] P8. 自动确定生产/验证 `ep` 点、内部缓冲幂和工作精度，并以缺省 12 有界并行执行。
- [x] P9. 以 Acb 方阵 Laurent 重构返回 pole/finite part；独立点验证失败时 fail closed。
- [x] P10. 将 Example 06 改为最高阶 `0` 的实战入口，fresh 运行测试、文档和一致性门禁。

初始固定点阶段记录（2026-08-13，已由下方自适应入口取代）：此前 MadStree 只有理论文档讨论 analytic regulator，没有可运行的
共同 `ep` 正规化 example；现新增 Example 06，使用三个顶点共同
`a1=a2=a3=1+ep`、两条 `masslessFull` 传播子、9 个 masters，并在
`ep={1/5,1/6,1/7}` 上由 `MSEvaluateEpBatch` 自动执行，缺省 requested/effective 为 `12/3`，
检查 `8/8`。FlintNDE Python `run_ep_tasks`、正规化重构、Wolfram
`FlintNDEEvaluateEpBatch` 与 MadStree `MSEvaluateEpBatch` 均以 12 为缺省外层进程上限，
实际并发为任务数与上限的较小者，超出部分由执行器自动续交；该设置独立于单进程
`ctx.threads`。固定 exact `RationalMatrixSystem`、普通边界和 `AdaptivePath` 可结构化跨进程
恢复，任意解析闭包需模块顶层 factory 或显式串行。fresh 验收：FlintNDE Python `158/158`、
Wolfram `20/20`，Python/Wolfram 并行 examples 均通过；MadStree adapter `6/6`、13 个 Wolfram
脚本 `184/184`、Examples 01--06 全部退出 0。该段计数只记录改造前状态；当前验收见下方
`MSReconstructEpSeries` 完成记录。独立包与 Vendor 的 35 个 Python 源/测试文件
逐字节一致。FlintNDE 手册最新源在临时目录经 BibTeX 与双次 XeLaTeX 编译为 33 页，日志无
overfull/underfull/未定义引用，第 3、30--32 页渲染目检通过。

补充审计（2026-08-13）：上述完成记录只认证多个固定非零 `ep` 的 NDE 与有界调度；原
Example 06 的 `{1/5,1/6,1/7}` 未作 Laurent 重构，不能认证 `ep -> 0` 的 pole 或 finite part。
当前任务继续补齐自适应正规化极限入口，完成前不得把固定点批量结果表述为有限项提取。

完成记录（2026-08-14）：MadStree 当前公开入口为
`MSReconstructEpSeries[context,ep,pointTemplate,MaximumEpPower->m]`；用户不提供 `ep` 点。程序用完整
符号边界条件与 dlog DE 证书确定最低整数幂，再自动选择 exact 生产/验证点、工作精度与输运阶数；至少
保留两个未返回的正幂缓冲项，验证失败时把 `ep` 尺度缩小 `10^4` 后重算，最多三轮且不降低
误差门槛。旧公开 `MSEvaluateEpBatch` 已物理删除，固定点任务池只保留为私有阶段执行器。
Example 06 的 `a1=a2=a3=1+ep`、最高阶 `0` fresh 检出最低幂 `0`，自动以 3 个生产点拟合到
内部 `ep^2`，2 个独立验证点的最大相对残差约 `1.97e-16`，检查 `11/11`；该实际 family 无
负幂，不能虚构 `1/ep`。独立人工函数 `1/ep+2+3ep` 回归自动检出 `-1`，以 4 个生产点恢复
pole `1` 和 finite part `2`。Python adapter `7/7`，13 个 Wolfram 开发脚本 fresh 为
`187/187`。

### 当前任务：2026-08-13 Python-FLINT threads=1/10 的真实三顶点 900 点效率测试（已完成）

主 case 使用 MadStree v0.11 Example 04 的真实三顶点、两传播子树图
`v1--masslessFull(l12)--v2--masslessFull(l23)--v3`，所得 dlog 系统有 9 个 master。900 个
exact 用户点沿自动边界 anchor 到 Example 04 目标点的同一复仿射单变量段排列；planned/direct
两路线分别在独立 Python 进程中设置并回读 `ctx.threads=1/10`，按 `1 -> 10`、`10 -> 1`
交错重复两轮。早先 2x2 FlintNDE case 只用于定位底层线程行为，不作为三顶点性能结论。

- [x] H1. 核实 python-flint/FLINT 线程池边界，区分底层算术内核与 Python/MadStree 串行层。
- [x] H2. 在真实三顶点、9 master、900 点 case 完成 threads=1/10 两轮交错 cold-process 测试。
- [x] H3. 汇总 planned/direct 端到端 wall time、process CPU/wall、节点/dense 分配及逐点互检。
- [x] H4. 更新 MadStree/FlintNDE 的 `eff_bench.md`，清理误选 runner 并执行文本与 Git 检查。

完成记录（2026-08-13）：两轮均为 3 顶点、2 传播子、9 master、900 点且全部通过；
planned/direct 的 8100 个复分量最大绝对差均为 `1.08475294e-35`。threads=1 时 planned
端到端均值 11.341965 s（11.289446--11.394483 s），direct 40.779663 s
（40.149104--41.410223 s），planned 快 3.59547 倍；threads=10 时分别为 11.571462 s
（11.486995--11.655929 s）和 40.969189 s（40.646019--41.292360 s），planned 快
3.54054 倍。即 10 线程相对 1 线程 planned 慢 2.02%，direct 慢 0.46%，没有整体加速。
第二轮返回 planned 28 个节点、875 个 dense 点，其中 854 点使用 fast 子积树/余数树、21 点
迭代求值、25 点恰为节点；direct 为 901 个节点且 900 个用户点均逐点输运。补充底层微基准
显示 180x180 `acb_mat` 乘法约快 2.08 倍、120x120 `solve` 约快 1.20 倍，但 fast multipoint
未见线程收益；因此 `ctx.threads=10` 只允许部分 FLINT 内核并行，不并行全部 FlintNDE 计算。

### 当前任务：2026-08-13 FlintNDE 900 点中等覆盖密度重测（已完成）

用户指出原 30x30 网格过密，900 点几乎全部落入同一个 Taylor 展开盘，2 节点极端案例不能
代表一般多节点路径。保持系统、点数、蛇形顺序、精度和 planned/direct 双路线不变，将网格
间距从 0.1 扩大到 0.45，使每个 planned 输运段通常承担 3--20 个用户值；正式报告必须给出
逐段覆盖分布、尾段例外、fast multipoint 桶数、两种墙钟口径和 1800 分量互检。

- [x] G1. 只读扫描候选间距 0.25--0.50；选定 0.45，探针得到 127 段，其中 1 段是
  不承担用户值的纯输运桥段，其余 126 段各承担 3--20 个用户值；20 个桶满足 fast multipoint 门槛。
- [x] G2. 更新 FlintNDE 0.4.0 独立任务书和 runner，增加逐段用户值覆盖统计及正式门禁。
- [x] G3. fresh 重跑 900 点 planned/direct、闭式解互检、fast/Horner oracle 与 fail-closed。
- [x] G4. 更新正式报告、机器 summary 和当前进度结论，核对 UTF-8 与 `git diff --check`。

完成记录（2026-08-13）：正式网格间距为 `9/20=0.45`，输运阶数提高到 64 以保持原
`1e-28` 门禁。planned 实际 128 节点、127 段，其中第 0 段为纯输运桥段；其余 126 个用户
覆盖段承担点数 min/median/mean/max=`4/6/7.142857/20`，严格全部位于 3--20。774 个 dense
点中，20 个 fast 桶共 279 点走 FLINT 子积树/余数树，106 个小桶共 495 点走迭代求值。
planned/direct 后端墙钟为 0.177104/0.976846 s，总路线墙钟为 0.192402/0.989916 s，planned
分别快 5.5157/5.1450 倍。1800 分量的 planned/closed、direct/closed、planned/direct 最大差
分别为 `5.10592e-41`、`1.89628e-30`、`1.89628e-30`，全部通过。

### 当前任务：2026-08-13 MadStree v0.11 单变量段委托式输运重构（已完成）

用户明确要求升版并删除 MadStree 自身的路径规划层。MadStree v0.11 只按输入顺序识别最大连续的同一
复仿射单变量平面段，构造 exact 拉回 DE，并把每段全部用户点一次性交给 FlintNDE。节点选择、
避奇点、绕行、dense output 与实际输运均归 FlintNDE；MadStree 不再生成、保存、恢复或公开
任何路径计划。MMA 侧保留布尔选项，只控制 FlintNDE 对每段启用自动路径规划还是严格按给定
用户点顺序直接输运，关闭时不得隐式回退为自动规划。

节点覆盖多个 dense 用户点时使用 python-flint `acb_poly.evaluate[..., algorithm="fast"]` 的
快速多点求值（子积树/余数树），按节点和向量分量分桶计算；因此同时建立 FlintNDE 0.4.0，
独立包与 MadStree v0.11 Vendor 保持同版本逐文件一致。

- [x] R1. 从 v0.10 建立 v0.11，冻结 v0.10；更新版本入口、版本索引和更新说明，并按最新
  三版本规则清理 v0.8。
- [x] R2. 以单一公开求值接口替代 `MSGeneratePath`、`MSEvaluatePlannedPath`、
  `MSPlannedPathQ`；物理删除旧函数、旧消息、旧 schema、serialized plan、wrapper、fallback、
  兼容测试及现行文档中的旧调用法。
- [x] R3. MadStree 仅保留连续复仿射分组、exact 参数化、dlog 拉回、边界/master 顺序与结果
  映回；一次 Python 调用消费全部段，并在同一进程内完成各段规划或直接输运。
- [x] R4. 增加并验证 MMA 布尔选项：开启时由 FlintNDE 对各段规划节点并 dense 求值；关闭时
  FlintNDE 严格把段内用户点作为顺序节点直接输运，两种模式都返回实际节点/覆盖摘要。
- [x] R4a. FlintNDE 0.4.0 对同一节点 Taylor 系数覆盖的多个 dense 点使用 fast multipoint
  evaluation，并保留逐点 Horner 独立 oracle 互检与小样本回退门槛。
- [x] R5. 重写 v0.11 开发测试、examples、README、开发计划、临时 TODO、手册与独立检验
  任务书；不得保留旧接口示例或迁移 wrapper。
- [x] R6. fresh 运行受影响 Wolfram/Python 回归、examples、UTF-8 扫描、PDF 构建与
  `git diff --check`；随后执行 v0.11 独立检验，使用对称计时比较自动规划 dense 路线与关闭
  规划的逐点路线，并逐点互检。
- [x] R7. 删除 FlintNDE 0.1.0 前，将根目录三个当前示例及其 README 的集中加载路径切换到
  0.4.0 并 fresh 执行；历史报告和冻结版本文档中的旧版本身份不改写。

验收标准：v0.11 当前源码、测试、examples 和现行文档中不存在 MadStree 路径计划对象或
两阶段 plan/execute API；每个连续同复平面段只拉回一次且只经一次总 Python 进程请求；选项
关闭时实际节点严格等于用户给定顺序，开启时节点由 FlintNDE 返回；二维 900 点两模式结果在
阈值内逐点一致，并分别报告 backend-only 与端到端 wall time。

完成记录（2026-08-13）：MadStree v0.11 的 13 个 Wolfram 开发脚本 184/184、Python adapter
4/4、五个 examples 全部通过；FlintNDE 0.4.0 的 Python 回归 146/146、Wolfram `Needs` 18/18，
独立包与 Vendor 的 20 个 Python 实现文件和 14 个测试文件逐文件同字节。MadStree 独立验证
位于 `package-MadStree/independent-validation/MadStree-v0.11-validation-01-flintnde-planned-vs-user-nodes/`，
17/17：900 点乘 3 masters 的 planned/direct 最大绝对差 `5.8262e-43`，自动路线 894 点进入
6 个 fast bucket，最小桶 95 点；端到端 2.12361/5.36613 s，后端 0.951728/4.29495 s，自动
规划分别快 2.5269/4.5128 倍。报告从返回的 assignment 和 Vendor 源码确认 fast 由 FLINT
`acb_poly.evaluate(..., algorithm="fast")` 的子积树/余数树执行，不是 Wolfram 逐点计算。

FlintNDE 独立验证位于
`package-FlintNDE/independent-validation/FlintNDE-0.4.0-validation-01-fast-multipoint-and-direct-path/`：
257 点、64 阶 fast/Horner 最大差 `1.96586e-62`，fast 快 1.31488 倍；扩大间距后的 900 点
planned 为 128 节点、127 段，126 个用户覆盖段各承担 4--20 点，direct 为 901 节点且 planner
sentinel 为 0，后端 direct/planned=5.51566。1800 个分量全部通过闭式解和路线互检。三包版本
目录均只保留最新三个；FlintNDE 三个根示例
已切至 0.4.0 并 fresh 通过，其中 QNM 无穷远边界显式迁移为 `{phi,a,b,C}`。三份 PDF 均完成
XeLaTeX/BibTeX 构建、无未定义引用并通过抽页目检。

### 当前任务：2026-08-13 中文编码审计与 MadStree/FlintNDE 独立验证（已完成）

按当前 `main`（`b8a29ae`）审计三个子项目中脚本、examples、用户可见中文消息和说明文档的
实际字节编码，修复会导致 Windows 默认打开乱码、Wolfram/Python 读写误判或生成报告乱码的
当前入口；冻结历史版本只报告，不为编码整齐而回写。随后从新的独立验证工作区执行当前
MadStree v0.10 与 FlintNDE 0.3.0 任务书，并返还自动报告和机器可读 summary。

- [x] E1. 统计 Git 跟踪文本的 UTF-8 严格解码、BOM、UTF-16/ANSI、替换字符和疑似 mojibake，
  区分当前入口、examples/任务书/验证脚本与冻结历史资产。
- [x] E2. 修复当前入口的编码声明、显式 UTF-8 读写和真实乱码文本；复核中文注释、用户提示和
  自动报告默认打开时均显示正常。
- [x] E2a. 用户调用保持标准 `Needs["程序包`"]`；UTF-8 由包内加载链和报告写入函数承担，
  不要求用户预设 `$CharacterEncoding` 或 Python locale。
- [x] E2b. 修正 dSIBP 021 正式交付，使成品 examples 随程序包进入用户可见的发布结构，并
  从该交付路径 fresh 运行覆盖检查。
- [x] E2c. 三个子项目各自只保留版本索引中的最新三个版本；删除前逐项核对 Git 状态、引用者、
  当前入口和资产用途，同时清理可确认的临时测试、旧检验工作树和运行产物。
- [x] E3. 核对并补齐独立验证任务书：二维复点必须记录实际返回路径、节点规划、节点覆盖分布、
  grouped/dense-output 与 cold-cache naive 逐点路线的 wall time，并逐点双向比较结果。
- [x] E4. fresh 执行 MadStree v0.10 独立验证，生成正式报告与 `results/summary.wl`。
- [x] E5. fresh 执行 FlintNDE 0.3.0 独立验证，生成正式报告与机器可读 summary。
- [x] E6. 复核报告状态、精度/误差、路径/节点/计时证据、临时产物边界和 `git diff --check`。

验收标准：所有当前用户入口均为严格 UTF-8，默认编辑器和运行时无中文乱码；两份独立报告不得
用任务书或开发测试代替实际运行；二维复点报告同时给出实际路径和节点计划、两路线 cold-cache
总时间、效率比、900 点逐点最大误差及通过阈值，任一未执行项必须显式标为未执行。

完成记录（2026-08-13）：本机 Python 源码默认编码为 UTF-8，但 locale/`open()` 与 stdout
分别为 CP936/GBK；因此 Python 文件和报告 I/O 已显式使用 UTF-8。Wolfram 实测
`$CharacterEncoding="UTF8"`、`$SystemCharacterEncoding="CP936"`，三个当前包继续由用户标准
`Needs[...]` 加载，内部 `Get` 显式指定 UTF-8；fresh 加载版本为 dSIBP 021.0、MadStree 0.10、
FlintNDE 0.3.0。清理后 1004 个候选文本严格 UTF-8 解码 1004/1004，replacement/mojibake 均为
0；4 个 BOM 文件均可严格解码，其中正式 `package_021.0.wl` 的 BOM 是 Windows Wolfram
单文件加载兼容措施。dSIBP 021 的模块 Examples 与正式交付 Examples 均已补齐；正式 WL/PDF
SHA-256 分别为 `D7A75440358E64392C4D9923FCD092395FCE07629B8B26F9E01FE8886A3E75AD`、
`B381DC26DA7B7B9E3D7660851A3C46CA4D634743783EE834E0420257D043F024`。三包版本目录各保留三个：
dSIBP 019/020/021、MadStree v0.8/v0.9/v0.10、FlintNDE 0.1.0/0.2.0/0.3.0。

MadStree 正式报告位于
`package-MadStree/independent-validation/MadStree-v0.10-validation-01-complex-grid-grouped-vs-naive/000_MadStree-v0.10-validation-01-complex-grid-grouped-vs-naive-report.md`：
fresh `17/17`，实际 6 组路径，三个固定 `x1` 大组各 300 点、9 个 planned nodes、8 个节点点和
292 个 dense 点；900 点三分量 Route A/B 最大绝对差和相对差均为 0，12 个高阶抽查差为 0，
执行期未重规划。Route A planning/execution/total 为 1.88386/1.87929/3.76315 s，Route B 为
0.0723982/2.40047/2.53109 s；A/B=1.48677，本轻量 case 中 grouped/dense 慢 48.6769%。验证还
确认直接使用很长 `MSRuntimeDirectory` 会在 Windows 传统 MAX_PATH 下失败；runner 以仓库根短
临时 runtime 规避，生产 adapter 尚未内部消除此可移植性风险。

FlintNDE 正式报告位于
`package-FlintNDE/independent-validation/FlintNDE-0.3.0-validation-01-planned-complex-grid/000_FlintNDE-0.3.0-validation-01-planned-complex-grid-report.md`：
900/900 点 grouped/naive/闭式三方互检通过；grouped 实际 2 节点、1 段、899 个 dense sample，
naive 执行 900 次独立 plan/JSON round-trip/execute；grouped/closed 与 naive/closed 最大误差均为
`3.69481826080033e-37`，grouped/naive 最大差为 0，执行期规划 sentinel 调用 0。grouped 总耗时
0.156830 s，naive 1.095004 s，naive/grouped=6.9821。完整 `unittest discover` 的 134 项中
133 项无 failure，唯一 import error 是当前环境未安装 `pytest`，故不记作完整回归全过。

### 当前任务：2026-08-13 MadStree v0.10 同复直线分组与 dense output 修订（源码与开发测试已完成，独立验证待执行）

用户确认 MadStree 不构造多变量高维局部解。节点系数只在同一个单变量拉回
x(s)=x0+s v 的复参数平面内复用：先按输入顺序识别最大连续复仿射直线组，再把该组全部
用户参数点一次性交给 FlintNDE，由 FlintNDE 选择输运节点并用节点 Taylor/Frobenius 系数
生成覆盖范围内的非节点值。遇到不共线转角时在公共端点结束上一组并重新拉回下一组。

- [x] B1. 实现连续点的 exact 复仿射共线判定、最大组划分、组内参数化和转角端点重用。
- [x] B2. 每组只构造一次单变量 dlog 拉回，并把全部组内用户参数点传入 FlintNDE 规划；
  不再把每个普通用户点强制拆成独立段终点。
- [x] B3. 规划保存 userIndex 到 FlintNDE node snapshot 或 dense sample 的完整映射；执行只
  恢复保存计划，并按该映射重建 saved、tmp 和重复点结果。
- [x] B4. 保持缺省避奇点、显式奇点折跃、分支责任、LO 和规划/执行精度门禁；不同复直线
  之间不得共享局部系数。
- [x] B5. 增加 900 点二维复格点开发测试：x1=x1_0+0.1 a（a=0,1,2），
  x2=x2_0+0.1 n+0.1 I m（n=0,...,99；m=0,1,2）。固定 a 时连续排列全部
  300 个 x2 点，并让不同 n 依次出现；复共线判定必须把 m 方向也识别为同一个 x2 复变量
  平面，而不能按固定 m 的实直线拆组。
- [x] B6. 独立验证任务书要求比较分组规划/缓存路线与逐用户点路线的 cold-cache 总时间、节点数、
  覆盖数分布和逐点误差；性能结论只按实测报告。
- [x] B7. 同步中英文 README、UPDATE_NOTES、手册、开发计划和临时 TODO；fresh 运行
  adapter、路径专项及全部受影响 Wolfram 测试和 git diff --check。

验收标准：每个固定 x1 的 300 个 x2 点按一个复仿射单变量平面分组，x1 改变处重新分组；至少一个
FlintNDE 节点覆盖多个组内用户点；全部 900 点与逐点基线在 refinement 阈值内一致，执行期
不重新规划。前两项结构已由开发测试覆盖；900 点数值/计时对比已写入独立验证任务书，尚未执行，
不得把任务书当作验证报告。

Fresh 开发验证：MadStree adapter `11/11`；8 个 `test_flintnde_*.wls` 文件合计
`112/112`，其中二维复格点分组 `7/7`、路径专项 `53/53`；独立 FlintNDE
`144/144`；共同交付文件 `25/25` 哈希一致；`git diff --check` 通过。

### 当前任务：2026-08-11 FlintNDE 0.3.0 与 MadStree v0.10 路径规划、通用 DE 与零兼容残留修订（已完成）

用户决定独立包 `FlintNDE-0.3.0` 与 MadStree v0.10 的 `Vendor/FlintNDE` 保持同一实现和
同一公开版本。FlintNDE 仍是通用单变量矩阵微分方程求解器；MadStree 负责把多变量用户路径
分解并拉回为已规划的单变量 DE 段。所有修改原位更新 0.3.0/v0.10，不新开版本号或分支。

2026-08-12 术语统一：“折跃”指任何经过中途节点的多点输运；只有显式穿过奇点并使用局部基
连接的情形称“奇点折跃”。缺省模式不经过奇点；选择奇点折跃时必须提示多值分支等价于某一
绕行路径并由用户确认。

2026-08-12 消息语言接口统一为 Wolfram `MessageLanguage -> "EN"|"CN"`、Python
`message_language="EN"|"CN"` 和桥接 `messageLanguage`，缺省英文。

2026-08-12 用户进一步明确：未发布的 0.3.0/v0.10 只保留当前唯一接口合同，不保留任何
被替代草案的入口、schema、模式值、字段、别名、wrapper、读取分支、fallback、文档调用法或
测试语料。公开主线只保留 FlintNDE 的系统构造、路径规划、已有计划执行，以及 MadStree 的
多变量路径生成、已有路径执行。数学上的 compatibility condition/residual、测试专用独立
oracle 与 Wolfram 标准包加载文件不是接口兼容层。

- [x] A1. 固定职责：FlintNDE 不读取 MadStree/dSIBP 的图、主积分顺序或 normalization；
  MadStree 只向 FlintNDE 提供拉回后的单变量矩阵系统、边界和已规划段。
- [x] A2. 通用系统与内部奇点发现：FlintNDE 从一般有理矩阵内部提取有限奇点、极点阶数与
  无穷远信息，不要求用户提供奇点；非 dlog 一般有理矩阵继续走通用路线。
- [x] A3. 自动特化：仅在 exact 认证 `A(x)=P(x)+Sum_j R_j/(x-p_j)` 后使用快速递推，
  `P(x)` 支持任意有限次数且有限极点均为一阶；认证失败时回到通用路线或 fail closed。
- [x] A4. 两阶段合同：`FlintNDEPlanPath` / `FlintNDEExecutePath` 与
  `MSGeneratePath` / `MSEvaluatePlannedPath` 严格分离；执行阶段不重新规划，也不增加
  “是否自动规划”的重复选项。
- [x] A5. 缺省避奇点与奇点折跃：规划线段命中奇点时缺省结构化拒绝并报告相邻端点与奇点；
  显式奇点折跃保留局部基、分支和绕行等价类诊断。
- [x] A6. 节点算法：普通节点进入奇点步长限制范围后建立入射/出射匹配；过奇点后依次检查
  后续用户点并取一步可达的最后一点，首点超范围时先向其方向走一步回到普通节点状态。
- [x] A7. 精度合同：工作位数按 `ceil(decimalDigits log2(10))+guardBits` 自适应增长；
  winding、monodromy、匹配几何和序列化节点不经过 binary64；执行精度高于规划精度时拒绝并
  要求重新规划。
- [x] A8. 双语提示：规划/执行、缺省/显式模式和奇点分支责任均使用 `MessageLanguage`；
  当前源码、测试和交付文档不保留此前语言选项名。
- [x] A9. Wolfram 包接口：`Needs["FlintNDE`"]` 可加载标准 context、usage、Options、
  系统构造、规划与执行入口；一般有理矩阵和自动特化均有端到端测试。
- [x] A10. MadStree 多变量路径：完成共线点合并、奇面点报告、仿射拉回、计划序列化、
  坐标化结果与 LO 到达方向记录；全部 letters 为常量的线段按零连接执行。
- [x] A11. 唯一合同清理：当前源码、测试、示例和现行文档只表达唯一的两阶段接口、现行
  schema、现行奇点模式及现行点标签；不保留被替代草案的名称、读取分支或兼容说明，并同步 Vendor。
- [x] A12. 文档、验证与发布：同步 README、UPDATE_NOTES、手册、开发计划和两级临时 TODO；
  fresh 运行两包完整 Python/Wolfram 回归、三份 PDF、旧残留扫描、`git diff --check` 和
  FlintNDE 双份逐文件哈希门禁，随后提交并推送当前 `main`。

最终发布门禁（2026-08-12）：FlintNDE pytest 144/144、unittest 133/133、Wolfram
`Needs["FlintNDE\`"]` 18/18；MadStree Python adapter 10/10、12 个 Wolfram 开发测试
221/221、路径专项 53/53、Examples 01--05 共 5/5 且退出码均为 0。FlintNDE 手册 32 页，
MadStree 中英文手册各 29 页，编译日志无 fatal、undefined reference 或 overfull；全册与关键页
目检通过。独立 FlintNDE 与 Vendor 的共同交付文件 26/26 SHA-256 一致。当前源码、测试、示例
和交付文档中的被替代入口、别名、wrapper、fallback 与旧 schema 读取分支扫描命中 0；未知字段
和非现行值直接拒绝，不提供兼容转发。cache、pyc、TeX 中间文件和临时结果扫描命中 0，
`git diff --check` 通过。

### 当前任务：2026-08-10 MadStree v0.9 后端失败诊断与 examples 失败门禁（已完成）

落实审计 F2 修订版（见 `000_report/2026-08-10-三程序包最新版落地审查.md`），按用户 2026-08-10 确认的设计原则：**零探测**——不探测用户目录、不尝试候选解释器、不硬编码机器路径；Python 安装与 PATH 配置是用户责任。包只保留两条显式通道（`PythonExecutable` 选项、`MADSTREE_PYTHON` 环境变量；缺省 `Automatic` → 环境变量 → `"python"`），包的责任是失败时把问题说清楚。v0.8 冻结不回写，改动进 `package-MadStree/versions/MadStree-v0.9/`；是否提交/推送由用户决定。

- [x] 后端失败诊断增强：`msExecuteFlintNDEAdapter` 把子进程 stdout/stderr 重定向到日志文件，失败时读回放入 `Failure`（`"stderr"` 键），并发一条含解释器命令、Python 原始错误与两条修复通道的 Message；缓存逻辑不动。
- [x] `MADSTREE_PYTHON` 通道下沉到包级：`PythonExecutable` 缺省改 `Automatic`（解析环境变量，否则 `"python"`；显式字符串原样使用）；usage 文案同步。
- [x] Examples 01--05 尾部失败门禁：任一结果为 `Failure` 时打印原因并 `Exit[1]`；调用方式保持缺省（不传选项、不读环境变量）。
- [x] 验证：11 个开发测试 fresh 全过（`MADSTREE_PYTHON` 显式，实测 160/160）；examples 01--05 缺省 fresh 全跑，全部 `Example PASSED`、退出码 0（L4）；负例验证（对无 python-flint 的 venv 解释器显式传 `PythonExecutable`，输出单条 `backendRunFailed` Message：`Backend error: No module named 'flint'.`，example 退出码 1，无刷屏）。
- [x] 版本文档：v0.9 `UPDATE_NOTES.md`（中英文，含零探测、双显式通道原则）与 `VERSION.md`；`VERSION_INDEX.md` 与 `load_current.wl` 指向 v0.9；同步 `todolist_temp.md` 状态（L5：0.2.0/v0.8 升版区段全部勾选收口）与 unittest 口径（L1：102/104 通过，2 项 Windows 短路径环境断言失败除外）。

完成记录（2026-08-10）：v0.8 目录零改动；`$MadStreeVersion` 由 v0.8 遗留的 `"0.7"` 更正为 `"0.9"` 并同步测试断言。Git 发布（2026-08-11，用户确认后执行）：提交 `101df3d` Release MadStree v0.9 已推送到 `origin/main`，tag `MadStree-v0.9` 已推远端。

验收标准：缺省环境（PATH 中 python 有 flint）examples 全过、exit 0；缺 flint 环境单个清晰诊断 Message + exit 1，无 `MapThread::mptd`/`Lookup::invrl` 刷屏；v0.8 目录零改动。

### 历史任务：2026-08-10 三程序包最新版落地审查（已完成）

对照顶部 2026-08-10 性能优化升版任务与 `todolist_temp.md` 验收标准，审查最新落地版本（FlintNDE 0.2.0 / MadStree v0.8 / dSIBP 021）是否完成计划。

- [x] 复核计划完成度：FlintNDE 0.2.0 六项功能点与 MadStree v0.8 五项功能点全部落地；MadStree v0.8 开发测试实测 `160/160` 与文档一致；MMA 接口例子实测 PASSED（`relativeDifference ≈ 6e-31`）；Vendor 19 模块与 pyproject 与源 0 差异。
- [x] 产出审计报告 `000_report/2026-08-10-三程序包最新版落地审查.md`：8 项发现（无高；中 2、低 6）。关键收口项：`flintnde_cache/` 未进 `.gitignore`；example 01 缺省运行失败（缺省 `python` 无 flint，显式 conda 解释器后全链路通过）；研究计划 2026-08-06 提交/推送待办状态滞后。
- [x] 修正 2026-08-06 任务勾选状态：三包 Git 提交/推送与 tag（`FlintNDE-0.1.0`/`MadStree-v0.6`/`dSIBP-021`）已于此前完成，勾选并记录。
- [x] 已收口待办：`.gitignore` 补 `**/flintnde_cache/`（提交 `92de3b1`，check-ignore 实测匹配）；`FlintNDE-0.2.0/UPDATE_NOTES_0.1.0.md` 副本已删除。
- [x] 剩余待办已收口（按零探测原则在顶部 MadStree v0.9 任务中完成）：examples 失败门禁 + 后端失败诊断提示（不探测解释器）；todolist 状态已同步。

验收标准：报告证据链完整（预期契约/实现/触发/后果/传播/证据/修复边界/验收）；只读审计，未修改被审计源码与 retained result。

### 当前任务：2026-08-10 FlintNDE 0.2.0 与 MadStree v0.8 性能优化升版（已完成）

依据合作者性能报告 `MDS-PERF-2026-08-09`（见 `package-MadStree/versions/MadStree-v0.7/MadStree性能问题报告(1).md`）与用户 2026-08-10 确认的修改方案，对 FlintNDE 本体与 MadStree 内嵌 FlintNDE 同时升版实施以下优化：

1. **极点–留数递推（P0-A）**：新增 `PartialFractionSystem`，对 `A(z)=C+Σ R_j/(z-p_j)` 类 dlog 系统用极点状态递推 `u[j,n]=(c[n]-u[j,n-1])/(center-p_j)`、`c[n+1]=(C·c[n]+Σ R_j·u[j,n])/(n+1)` 直接生成解泰勒系数，替代通用 Cauchy–DFT 采样重建；递推全程 acb（保误差球），C/R_j/p_j 一次性预转并缓存。非简单极点系统保留 Cauchy–DFT 回退。
2. **单链 N+5 嵌入式截断认证（P1-1）**：新增 `CertificationMode -> "Embedded"|"Certified"`，缺省 Embedded：只跑 reference_order 一条链，主链结果取前 N 阶前缀（递推保证逐位相同），段末截断差 `r_k=||Y_{N+5}-Y_N||/||Y_{N+5}||` 作局部误差估计，传播加权合成 `E≈Σ r_k·g_k`；`Certified` 保留独立双链用于论文级认证。
3. **dense output 补丁缓存（P0-2）**：保存点不再转为 `detour_points`；自然路径逐段缓存 `{center,radius,order,coefficients}` patch，保存点路由到覆盖 patch 做 Horner 求值；API 区分 waypoint（改路径）与 sample point（只取值）。
4. **通用求值工程优化（P1-3）**：保留的 Cauchy–DFT 路线改为系数一次性预转 acb + 稀疏结构跳过零元；不丢弃误差球。
5. **MMA 嵌入接口（FlintNDE 本体）**：新增 `Mathematica/` 目录提供 Wolfram 加载接口，用户在 MMA 内直接调用 FlintNDE 输运，最终输出（不含中间缓存）读入 MMA 变量；是否落盘由用户自行用 MMA 自带输出命令决定；`examples/` 加入该接口示范案例。
6. **MadStree v0.8**：Vendor 同步 FlintNDE 0.2.0；序列化改走 pole/residue（dlog letters 直接给出）；启用 Embedded 认证与 dense output；**删除 MadStree 内适配非 dlog DE 的模块与回退路径**（MadStree 数值入口一律 `certifiedByFormulaChecks`）。

验收标准：FlintNDE 0.2.0 unittest 102/104 通过（2 项失败为 Windows 短路径环境断言 `ARTIFI~1`，与功能无关；含递推 vs Cauchy–DFT 逐分量对比、Embedded vs Certified 一致性、dense output 与逐点重建一致三项新测试）；MMA 接口示范例子可运行并把结果读入 MMA 变量；MadStree v0.8 开发测试与 examples fresh 全过；两包 VERSION_INDEX、README、UPDATE_NOTES 同步；冻结旧版本不回写。

- [x] **FlintNDE 0.2.0**：PartialFractionSystem 极点状态递推、嵌入式截断认证、dense output 采样点、MMA 嵌入接口（FlintNDELoader.wl + mathematica_bridge.py）；新增 12 项 unittest 全过；MMA 例子端到端 PASSED（embedded 认证，relativeDifference ≈ 6e-31）。
- [x] **MadStree v0.8**：Vendor 同步 FlintNDE 0.2.0；普通段改走 dlog letters 极点–留数序列化（schema v2，PartialFractionSystem 快速路径）；保存点改为 dense output sample point；认证缺省走嵌入式单链截断（certificationMode="embedded"）；后端输出缓存（flintnde_cache/<digest>/）；msAffineLetterData 替换 msAffineConnectionData；RootReduce 处理代数数常量；删除非 dlog 回退标记（fallbackUsedQ、directIntegrationFallbackQ）；relative_difference_inf 改用 matrix_norm_inf 支持多列矩阵。11 个开发测试 fresh 全过：check_core 49/49、test_package_artifacts 18/18、test_flintnde_boundary 9/9、test_flintnde_massive_vertex 7/7、test_flintnde_massless_edge 9/9、test_flintnde_massive_full_edge 5/5、test_flintnde_mixed_three_vertex 7/7、test_flintnde_vertex_family 7/7、test_dsibp_derivative_dlog 9/9、test_simultaneous_cycle_chart 22/22、test_vertex_family_reduce 18/18（合计 160/160）。
- [x] 两包文档同步：VERSION_INDEX.md、load_current.wl、UPDATE_NOTES.md（中英文）、FlintNDE 0.2.0 UPDATE_NOTES.md；冻结 v0.7 不回写。

### 历史任务：2026-08-06 三程序包代码优化审查建议实施（FlintNDE 0.1.0 / MadStree v0.6 / dSIBP 021）

按 `000_report/2026-08-05-三程序包代码优化审查.md` 的修复依赖顺序，实施三包中已判定“建议实施”且边界清晰的优化点，并各自升版（FlintNDE `0.1.0`、MadStree `v0.6`、dSIBP `021`）。全部为纯重构：公开接口、数值合同、convention 与冻结版本均不变。

- [x] **FlintNDE 0.1.0**（H2/H3/H4/H7/H8/H11/M1/M2/M4/M5）：`h2_shared_frobenius_recurrence` 收敛公共 Frobenius 递推、`h3_shared_adaptive_path` 收敛输运路径规划、`h4_shared_ode_struct`/`h8_singularity_metadata_shared`/`h11_acb_schema_contract_tests` 收敛奇点与输出 schema、`h7_decompose_regular_singular` 提取幂级数分解、`m1`/`m2`/`m4`/`m5` 中阈值常量集中与过长函数/死代码清理；`pyproject.toml` 版本 `0.1.0`；`unittest` fresh `88/88`。
- [x] **MadStree v0.6**（F1/F4/F9/F20/F21/F24）：新增 `Core/Paths.wl` 集中调用目录样板、`msSlotPositionByKey` 统一 slot 定位、`msRetainNonEnergyRules` 统一边界规则过滤、`test/_harness.wls` 共享测试样板、Vendor 同步为新版 FlintNDE `0.1.0`；11 个开发测试与三个 examples 全部 fresh 通过。
- [x] **dSIBP 021**（H1/H3/H5）：`LoopCore013.wl` 删除 6 组被覆盖旧定义及失去调用者的死 helper（`integralToInertIntegrand` 等），新增 `check-smoke/_harness.wls` 共享样板与 `check_module_ownership`（静态同左端覆盖检查）/`check_dead_definition_cleanup`（H1 动态专项）；既有 check-smoke 在各自冻结版本上重跑通过，021 上 020 相关 smoke 除版本号断言外全部通过。
- [x] 三包文档同步：各版本 `UPDATE_NOTES.md`、`VERSION_INDEX.md`、包根 README；运行命令与已知限制记录在版本更新说明中。
- [x] 待用户决定：三包 Git 提交/推送（2026-08-10 复核：已完成——提交 `0930921`/`ecf412c`/`6e976b7`/`0fe8900` 已推送，tag `FlintNDE-0.1.0`/`MadStree-v0.6`/`dSIBP-021` 已推远端）；`check_general_ibp_seeds` 的 `topMomentumSeedsPresent` 在冻结 018 上的基线断言问题（该类 seed 的 `ibpClass` 实际为 `qIBP`）不改冻结版本。

验收标准：三个新版本目录只含源代码、文档与测试，不接收运行产物；FlintNDE `unittest` `88/88`；MadStree 11 个测试与 3 个 examples 全过；dSIBP check-smoke 全部按冻结版本与 021 分别重跑，H1/H5 专项检查通过；运行产物只写入 `results_test/`/`results_temp/` 或调用目录。

### 当前任务：2026-08-05 三程序包代码优化审查重整（以 dSIBP 020 为准）

根据当前版本索引和各子项目目录合同，重写
`000_report/2026-08-05-三程序包代码优化审查.md`。本轮只修改研究进度与审查报告，
不修改任何程序包源码、测试、验证程序或结果。

- [x] 将 dSIBP 审查对象由已经冻结的 019 校正为当前开发主线 020，并复核同名 DownValue
  覆盖等关键结论在 020 中是否仍成立。
- [x] 区分“存在重复”“值得抽象”和“已经证明有性能收益”，删除没有 benchmark 或合同依据的
  高收益判断。
- [x] 按“建议实施、条件实施、不建议实施”重排三包建议，排除跨冻结版本单副本、独立验证
  共享可变框架、不同 JSON schema 强行统一等不符合仓库边界的方案。
- [x] 给出按风险排序的实施阶段、逐包验收门禁与明确非目标；完成 Markdown、引用路径和
  `git diff --check` 检查。
- [x] 按用户反馈保留原报告章节、H/F/M/L 编号和逐项发现，在原文基础上校正结论与实施边界，不另建一套建议编号。

验收标准：报告明确以 dSIBP 020、MadStree v0.5、FlintNDE 0.1.0.dev0 为对象；每项建议说明
收益、边界和验证要求；不把静态审查表述成测试结论，不要求修改冻结版本或破坏独立验证的
自包含性；本任务不产生源码和运行结果改动。

完成结论（2026-08-05）：报告已在原文基础上修订，保留三包章节、H/F/M/L 编号、路径证据和逐项发现；dSIBP 对象校正为 020，并新增 FlintNDE H11、MadStree F24、dSIBP H5 三项复核优化点。仅完成静态文档复核，未修改源码或运行测试；本次文档无行尾空白。全工作树 git diff --check 仍被本任务前已有的 FlintNDE 与 MadStree Vendor CRLF 改动阻断，本轮未修改这些文件。

### 当前任务：2026-08-05 合并复制进来的新版 FlintNDE 到版本目录

把从外部复制的 `FlintNDE/` 新版内容与 `package-FlintNDE/versions/FlintNDE-v0.1.0.dev0/` 合并：
以旧版目录结构为蓝本，程序代码用新版，附属 check/example/test 脚本按旧版格式适配新版接口。

- [x] 全量对比新旧两棵源码树：16 个 `flintnde` 模块与 8 个测试文件全部参与比较；其中 14 个模块与
  7 个测试仅行尾符（CRLF/LF）差异，真实内容差异只有 4 处：`__init__.py` 新版增加
  `fit_sampled_series` 导出、`regularization.py` 新版扩展（`fit_sampled_series`、显式
  `regularization_points`/`validation_points` 支持）、新增 `epsilon_jet_de.py` 与
  `regular_point_de.py` 模块、`tests/test_regularization.py` 新版增加 191 行对应测试。
- [x] 确认旧版没有复制件缺失的独有代码改动：除 README 更名说明与 `sympy>=1.12` 依赖外，
  versions 目录旧版相对新版无任何独有代码文件；README 更名说明（2026-07-28 起 `ndes_flint`
  不再兼容）已手工补回合并后版本。
- [x] 以旧版目录为蓝本完成合并：新版 `__init__.py`、`regularization.py`、`epsilon_jet_de.py`、
  `regular_point_de.py`、`tests/test_regularization.py`、`pyproject.toml`、`README.md` 复制覆盖进
  `versions/FlintNDE-v0.1.0.dev0/`，程序使用新版；归一化行尾后与新版源 0 差异。
- [x] 附属脚本适配：`examples/` 三个示例改为基于 `__file__` 的相对路径定位（去掉
  `FLINTNDE_ROOT` 间接层）；`check_01_nde_nlqnm_2x2` 的失效路径 `code/example/qnm_2x2.py`
  修正为 `examples/qnm_2x2.py`；确认全部 `.py/.wls` 无硬编码绝对路径或解释器路径。
- [x] 文档同步：versions README、包根 README 更新为三项依赖（含 `sympy>=1.12`）与
  2026-08-05 fresh `88/88` 测试计数；根 `.gitignore` 已覆盖 `__pycache__`、`**/results/`、
  `**/test/results_test/`，运行产物与 cache 均不会进入提交。
- [x] 验证：合并后 versions 目录 `python -m unittest discover -s tests -v` 通过 `88/88`
  （9.099s）；`regular_singular_save`、`exponential_boundary_save` 两个示例在自带
  `python-flint 0.9.0` 的 `D:\Package\PythonBlackHoleQNM` 解释器下运行成功；`qnm_2x2.py`
  本轮未重跑。
- [x] MadStree 内置 FlintNDE 同步：`package-MadStree/versions/MadStree-v0.5/Vendor/FlintNDE/`
  原有 15 个模块是 76/76 时代冻结副本（缺 `asymptotics.py`、`epsilon_jet_de.py`、
  `regular_point_de.py`），已按新版覆盖 `__init__.py`、`regularization.py`、README、pyproject
  并补齐 3 个缺失模块，归一化后与新版源码 0 差异。MadStree adapter 依赖的 11 个公开接口
  （`NamedPoint`、`RationalMatrixSystem`、`analyze_singularities`、`build_adaptive_path`、
  `classify_point`、`configure_working_precision`、`frobenius_boundary`、`gaussian_rational`、
  `rational_function`、`transport_frobenius_boundaries_refined`、`transport_path_refined`）
  在新版全部保留；Wolfram 层 `FlintNDE.wl` 只经 JSON adapter 通信，`Configuration.wl` 只检查
  `flintnde/__init__.py` 存在，`test_package_artifacts.wls` 只核对相对路径，均不受源码覆盖影响。
  `D:\Package\PythonBlackHoleQNM` 解释器下从 Vendor 内部 import 冒烟通过（全部接口可用、
  新模块可导入）。
- [x] 根目录复制源清理：仓库根 `FlintNDE/`（新版复制源，含 `RESOURCE_PROVENANCE.json`）确认
  无任何脚本引用后已删除；v0.3/v0.4 冻结版本不追溯修改。

验收标准：`versions/FlintNDE-v0.1.0.dev0/` 的源码与新版复制源归一化后逐文件 0 差异，公开接口
兼容旧版；`MadStree-v0.5/Vendor/FlintNDE/` 与新版源码归一化后 0 差异且 MadStree 调用链不受
影响；examples/check/test 全部通过仓库相对结构加载 `flintnde`，不依赖安装位置或盘符；
合并后完整 `unittest` 通过，Git 提交不含 cache、pyc、保存点或临时结果。

发布状态（2026-08-05）：本任务改动已整理完毕待用户环境提交推送，`qnm_2x2.py` 示例按用户指示
本轮不重跑。

### 当前任务：2026-07-31 FlintNDE 渐近尾项公开诊断与 BlackHoleQNM resource 交付

- [x] 把形式渐近级数的五阶尾项诊断抽成可读取的公开结果对象；除既有矢量块比外，提供标量项序列的
  `abs(sum(next five))/sum(abs(previous five))`，阈值由 caller 显式传入，零分母必须结构化失败。
- [x] 增加公开接口、严格 `< 0.1` 门禁、通过/失败/零分母测试，并同步 FlintNDE 根 README、版本
  README、手册和论文规划；完整 `unittest` 通过后才允许下游复制。
- [x] 将经测试的 `FlintNDE-v0.1.0.dev0` 源码作为 BlackHoleQNM 的共享 resource 交付；下游必须通过
  resource-root 相对路径加载，并核对实际 `flintnde.__file__`，不得依赖本机安装副本。

验收标准：公开诊断的分子、分母、比值、阈值和 pass 状态均可直接读取；严格边界不把等于阈值算作
通过；BlackHoleQNM 复制件与本目录源包的受控文件哈希一致，且下游 W producer 的渐近门禁能阻断
已知失效尾项。

### 当前任务：2026-07-30 根 README 项目定位、作者说明与引用

- [x] 在根 README 最前部重新说明 dSIBP、MadStree 与 FlintNDE 的用途、依赖关系和能力边界；dSIBP 明确为任意 dS 圈图的 IBP seed/动力学微分算符 producer。已确认 `04_pure_massive_bubble_closed_loop` 包含 `DSKiraPlan/DSKiraExport`、外部 Kira、`DSKiraImport/DSDE/DSScaleCheck` 的完整对接脚本；README 只称其为完整 example，不把未随仓库保留的外部运行产物写成当前现场验收。
- [x] 说明 MadStree 名称含义、函数族输入到全 sector dlog masters/DE、边界与 FlintNDE 数值输运的数据流，并把加入 massless 额外关系后的任意树图迭代约化和 dlog DE 标为论文未给出的新增结果。
- [x] 说明 FlintNDE 的 FLINT 数值 DE 主线、AMFlow-inspired 高阶 pole 局部化简、有限指数型广义幂级数和 regulator-series 数值重构，同时明确一般不规则/Stokes 问题未系统覆盖。
- [x] 加入纯 AI 编写与作者职责说明、问题/交互建议邮箱，以及三篇 dSIBP 系列论文和 AMFlow 的准确标题与 arXiv 链接。第三篇 arXiv:2604.14549v2 已存在于 `reference/ref_paper/` 并受 Git 跟踪；重新下载的当前 arXiv PDF 与本地文件均为 13 页且全文抽取文本 SHA-256 完全相同，故保留现有正式副本并清理临时比较文件。README 的三篇引用统一只显示 arXiv 链接，不单独标注某一篇的仓库内 PDF。文档格式和链接检查通过。

验收标准：README 的第一屏先回答三个包分别做什么、如何串联、哪些结果来自论文或新增推导、代码如何产生、如何反馈以及使用时建议引用什么；不得把外部 reduction、一般不规则奇点或尚未认证的路线写成 package 已自动完成。

### 当前任务：2026-07-30 dSIBP 020 原生 time-only 公开表示

019 的 `ibpMode->"timeOnly"` 虽然去掉 loop-momentum IBP、把全部 line power 固定为 coefficient，并使用定长 sector 位串，但公开积分仍是 full 模式共用的 `J[aList,linePacks,{}]`；这与 full 模式中 fixed/non-loop line 保留占位 pack 的表示没有本质区别，未完成原生 time-only 指标表示。按积分表示变更规则新建 020：019 冻结；020 内部继续复用 019 已认证的三参数 `J[aList,linePacks,ispList]` producer，不重写 IBP、EOM、contact 或微分算符。初始化一旦选择 time-only，统一 public boundary 依据 sector metadata 把内部对象双向转换为对标 MadStree 的 `J[sectorKey,timeShifts,stateBits]`；其中 `timeShifts` 按当前 compact 时间分量排列，`stateBits` 是全 sector building-block registry 的离散 `n_i` 态，并不要求按顶点重新分组。所有公开输出和公开输入都只出现新表示。

- [x] 冻结 019，复制建立 `versions/020_dSIBP/`；先盘点全部公开 time-only producer/consumer 和序列化字段，固定唯一转换边界。`020.0` 更新说明和正式交付仍在发布步骤完成。
- [x] 定义并实现 metadata 驱动的双向转换：`J[aList,linePacks,{}] <-> J[sectorKey,timeShifts,stateBits]`。`sectorKey` 是 root 顺序定长位串；`timeShifts` 按当前 sector 的 compact active vertex components；`stateBits` 按初始化冻结的 building-block slot registry，其中 massive endpoint 各一位、masslessFull quotient 整边共享一位，cross/exponential 无状态位。转换不得按图名、顶点数或 line id 特判。
- [x] 内部 producer 保持旧表示；`DSSeeds/DSAllSeeds/DSGenerateIBP/DSLinear`、time-IBP/contact relation、`ds` 与初始化 derivative metadata、tree/DE 公开结果在返回前统一转换为新表示。公开 consumer 收到新表示时先用同一 metadata 逆转换，再调用现有内部实现；公开 time-only 路径不得出现旧 `aList/linePacks/ispList` schema。
- [x] 对全 contact-reachable sectors 验证双向转换、不同 subsector 身份、compact component shifts、massive 四态、massless quotient 二态、simultaneous contact 和 lower-sector `ds`。旧 time-only `J[list,list,{}]` 在 020 public boundary 必须 fail closed；full-loop 继续使用原表示且不因本任务重跑。
- [x] 更新 020 技术笔记、plan/design、用户手册、examples、coverage manifest 和正式交付。候选 WL 与 32 页手册的最终日志无未定义引用、重跑提示或 overfull，封面、time-only schema 页和末页渲染目检通过；候选与正式路径均通过 public representation `25/25`、tree formula `14/14`、API/example coverage `6/6`，example 05 均退出 `0`。正式 `package_020.0.wl/pdf` 与候选字节完全一致，SHA-256 分别为 `EAE2A67B498ACF4D0DE99FAFADFBD11B2E2DDBF8E06ED7EABEBDA42F97566145`、`6CBA2A676FBB4C3DDE696C1FDBD078A9C2B3BCA2DF82535977444503BB91B17A`；正式更新说明 SHA-256 为 `C665EF767E048727F7115C98F41956190BE885761F9C43A102EEDFEBEE506884`。正式目录只保留 020 交付，源码只保留 018/019/020。未运行 full-loop、Kira、reduction 或 sunrise。
- [x] 在新表示上完成 dSIBP `ds` -> MadStree `MSReduce` 与 MadStree 直接 dlog DE 的三顶点 mixed 固定点检验；统一 basis、normalization、正 `|nu|` convention、变量 Jacobian和全 sector master 顺序。
- [x] 修复跨包检验暴露的 time-only lower-sector prefactor 查询：公开 `sectorKey` 逆转换成旧 producer pack 后不得重新猜测 normalization，必须读取初始化时冻结的该 sector `sectorPrefactorData`；验证 massive 已缩并 sector 的 `D Log[N_s]` 对角项不丢失，且 full 模式路径不变。
- [x] 修复 MadStree 零传播子单顶点函数族的位宽零 sector key：唯一 key 必须是字符串 `""`，不得由空 `Lookup` 触发 `StringJoin` 消息；在既有 FlintNDE 边界测试中同时验证 key 的 Head、值和无消息执行。

验收标准：020 的任何 public time-only 积分都严格匹配 `J[_String,_List,_List]`，且第一参数满足 context 位宽；seed、方程、linearData、derivative metadata 和 `ds` 输出递归扫描后不存在 `J[_List,_List,_List]`。双向转换对所有初始化可达 sector 和 canonical state exact round trip；full 模式的公开 `J[aList,linePacks,ispList]` 行为保持不变。跨包 mixed 三顶点的每个导数对象均约回 15 个 masters，逐变量矩阵残差为零。

发布状态（2026-07-30）：dSIBP 020、MadStree v0.5、对应独立验证和 active 文档已由主提交 `7833483` 推送到 `origin/main`。

阶段记录（2026-07-30，020 中央转换）：`Kernel/Core/TimeOnlyRepresentation020.wl` 已建立唯一双向转换，sector metadata 已冻结 `timeOnlyStateSlots/timeOnlyStateCount`；massive 传播子每端点一位，massless quotient 整边共享一位且逆转换时被消去的第二端点槽固定为零。`DSSeeds/DSGenerateIBP/DSLinear/dtau/ds` 已在公开边界转换，内部 producer 仍使用既有三槽表示。三顶点链（`massiveFull + masslessFull quotient`）fresh smoke 通过 `19/19`：4 个 reachable sector 的状态位数为 `{3,1,2,0}`，37 个 general seed、506 条撒点 IBP 和 293 个 `linearData` 积分均未泄漏旧公开三槽 `J`；top/child 往返严格相等，旧 time-only 三槽输入被 `ds/dtau` 拒绝。tree 公式入口、其它表示辅助函数、DE/scaling 消费路径和正式发布仍未完成，故对应任务保持未勾选。

阶段记录（2026-07-30，020 tree/辅助入口）：massive-only 的 `DSTreeSeeds/repIterative/DSTreeNaiveIBP/DSTreeNaiveDE/DSTreeDLogDE` 已接到同一中央转换；`rep2innerform/rep2outform/rep2Integrand/symmetry` 已接受原生 time-only `J`，其中只有真正需要 line metadata 的 integrand/symmetry 路径临时还原 producer 表示。两顶点 massive family fresh smoke 通过 `14/14`，得到 5 个全 sector masters；seed、迭代结果、naive IBP/DE、dlog DE 与 master list 均只含 `J[sectorKey,timeShifts,stateBits]`，full 表示审计通过。massless quotient 公式仍保持既有 `PendingRederivation` 数学门禁，不因表示转换绕过。

阶段记录（2026-07-30，020 全 sector/state）：mixed 三顶点 smoke 扩展为 `24/24`。对 `{"11","01","10","00"}` 四个 reachable sectors 的全部合法 state-bit 组合逐一执行公开到内部再回公开的精确往返；`"10"` massive-only child 覆盖四态，`"01"` massless quotient child 覆盖二态，`"00"` simultaneous-contracted child 不保留状态占位。lower-sector 与 simultaneous-contracted sector 的 `ds` 均只返回新公开表示。

阶段记录（2026-07-30，跨包 DE 与 prefactor 修复）：mixed 三顶点 family 使用 `em=massiveFull`、`ez=masslessFull quotient`，全 sector 顺序为 `{"11","01","10","00"}`，master 维数为 15。dSIBP 020 对 15 masters 分别关于 `{k1,k2,k3,sE1,sE2}` 求导，75 个对象全部经 MadStree `MSReduce` 闭合；五个 `15 x 15` 矩阵与 `MSDLogDE` 逐项严格相等，检查 `9/9`。定位过程中修复了 time-only public key 逆转换后从旧 pack 重建 prefactor 的缺陷：massive 已缩并 child `J["01",{0,0},{0}]` 现在从冻结 metadata 取得 $N_s=(4i/\pi)e^{-\pi\operatorname{Im}\mu}sE1^{2\mu}$，故 `ds[...,sE1]` 正确保留 $(2\mu/sE1)J_s$；full 模式逻辑未改。

范围判定（2026-07-30，`DSDE/DSScaleCheck`）：两者只消费经 `DSKiraImport` 验证的 reduction artifact；当前 tree/time-only active-basis Kira closure 明确不支持，time-only DE 的公开路线是 `DSTreeNaiveDE/DSTreeDLogDE`。因此 020 不改 `DSDE/DSScaleCheck` 的 full/Kira 数据合同，也不重跑 reduction/scaling；这不是未迁移的 time-only 公开 `J` 边界。

阶段记录（2026-07-30，MadStree contact event phase）：2401 参考 code 表明 pure-massive 的 `-I T^-1 OmegaTilde0 T` kernel 不得再乘统一负号；massless quotient 的解析 time-chamber 定义积分则要求含 masslessFull 的 event 保留 `(-1)^N0`。v0.5 现使用 `chi_v(E)=phaseSign_v`（`N0=0`）和 `chi_v(E)=(-1)^N0`（`N0>0`），并对 pure-massive contact 合并后的 component 检查 phase sign 唯一。fresh 证据为 core/paper Eq. (3.68) `49/49`、正负 massless 定义积分与 FlintNDE `9/9`、三条共同 theta simultaneous/cycle/chart `22/22`、mixed 跨包 DE `9/9`。

阶段记录（2026-07-30，MadStree 零线 key）：`msSectorKey[{}, contractedIds]` 现显式返回字符串 `""`，表示 root-line 位宽为零的唯一 sector；不把 `Missing` 或空列表送入 `StringJoin`。单顶点零 Hankel family 的 FlintNDE 边界专项 fresh 通过 `9/9`，同时检查初始化无 Wolfram 消息、schema width 为 0、sector key 的值为 `""` 且 `StringQ` 为真；解析值相对差约为 `4.94*10^-45`。

### 当前任务：2026-07-30 time-only key 严格化与 MadStree v0.5 自包含发布

纠正 019/v0.4 收尾阶段对“未迁移 full-loop”与“兼容旧 key”的混淆：本任务仍只改变 time-only 表示，但 time-only producer、metadata 与 consumer 必须只接受 root propagator 顺序的定长 `0/1` 字符串，旧 `"top"`、`"contracted:..."` 和收缩线名拼接 key 必须在 time-only 信任边界直接拒绝。full-loop 保留原 key 是因为不在本任务范围内，不得用一个无 `ibpMode` 的 helper 同时接受两套 key。另从冻结 MadStree v0.4 新建 v0.5，使该版本自带 FlintNDE、可按标准 Wolfram package 形式初始化并从调用脚本目录生成全 sector 约化/DE/主积分产物。MadStree 不新增外动量求导算符；DE 独立检验直接调用 dSIBP 已有算符，再接 MadStree 的表示转换与迭代约化。

- [x] 把 dSIBP 019 的 top/key predicate 改为显式读取 `ibpMode`/schema；time-only 遇到旧 key fail closed，full-loop 继续走其独立 legacy producer/consumer，不建立跨 mode 兼容入口。
- [ ] 重建 dSIBP 019.0 候选，只重跑受 key 严格化影响的 time-only endpoint/contact、四传播子 key、example 05、API/example coverage 与正式路径检查；不重跑 full-loop、Kira、reduction 或 sunrise。
- [ ] 从冻结 v0.4 建立 MadStree v0.5 与更新说明；在版本内部 vendor 一份可直接导入的 FlintNDE，默认 adapter 指向该内部副本，使单独下载 MadStree 即可运行，同时保留集中路径变量供显式覆盖。
- [ ] 把 MadStree v0.5 整理为 dSIBP 类似的标准 Wolfram package 调用：用户先给 topology/单顶点信息，再初始化 context；初始化后可生成全 sector 的迭代约化 metadata、dlog DE、对应完整主积分顺序和可选符号 `a_i` 代入规则，并明确返回实际生成目录。所有文件产物缺省写到调用脚本所在目录的相对 `results/` 或 `results_temp/`，不得写入 package 源码目录。
- [ ] 盘点 dSIBP 已有外动量求导算符的公开输入/输出以及 time-only `J` 到 MadStree `MSIntegral` 的可逆交集，不在 MadStree 内复制求导实现。固定全 sector master 顺序后，由 dSIBP 对对应 `J` masters 求导，把导数积分转换成 MadStree 原生表示并用 `MSReduce` 约回同一 basis，输出 DE 矩阵与残留证书。
- [ ] 新增三顶点 `massiveFull + masslessFull` 跨包数值检验：一路调用 dSIBP 外动量求导算符并接 MadStree 迭代约化，另一路直接调用 MadStree 公式 dlog DE；统一 basis、normalization、`NuConvention`、数值点和导数变量后逐矩阵元比较，并记录矩阵维数、非零差数、最大差与未约回积分。
- [x] 修正 MadStree dlog contact 的顶点/event 符号：pure-massive 局部块使用当前 component 唯一 `phaseSign`，含 masslessFull 的 simultaneous event 使用 `(-1)^N0`；默认 `+1` 恢复 2401 Eq. (3.68) 的保存参考，正负 massless 定义积分、三边 simultaneous 和 dSIBP 跨包 case 同时零残差，混合符号 pure-massive component 直接拒绝。
- [ ] 因 v0.5 改变 package/adapter/输出接口，fresh 重跑 MadStree 全部开发 tests、三个 examples 与 T1--T6 独立验证；开发 tests、examples、版本化 T1--T6 报告和 summary 已完成，尚待手册编译目检、README/版本索引/规划/根文档收尾、缓存清理及 GitHub 发布。

验收标准：time-only 输入任何旧 key 都明确失败，且不存在能在缺少 mode/schema 时把 `"top"` 与全 `1` 位串视作同一身份的公共 helper；MadStree v0.5 从自身目录即可找到内置 FlintNDE，用户脚本只需标准 `Needs`/初始化流程即可在脚本目录获得全 sector masters、迭代 metadata、dlog DE 和可选 `a_i` 代入产物；mixed 三顶点的 dSIBP 求导算符 + MadStree 约化路线与 MadStree 直接 dlog 公式在固定数值点逐项相等且无未约回积分。MadStree 不新增第二套求导算符。未运行的 full-loop/Kira/reduction/sunrise 不得写成通过。

阶段记录（2026-07-30）：019 内已删除无 mode 的 `sectorKeyTopQ[key]` 定义与调用；所有 predicate 消费点显式携带 `ibpMode` 和 root 位宽。只有 `timeOnly` 分支要求定长 `0/1` 字符串，full-loop 仍判定既有 `"top"`/收缩线 key。`parseTopology` 的旧-key 拒绝现会在 `makeTopologyData` 立即转成 fail-closed 初始化结果，不再把 `$Failed` 送入 sector producer。专项 fresh smoke 通过 `9/9`，实际拒绝 `"top"` 与 `"e1_e2_e4"`；正式候选、example 和发布路径尚未重建，仍由下一项验收。

MadStree v0.5 建立记录（2026-07-30）：已从冻结 v0.4 建立新版本，不回写旧版；版本根 `MadStree.m` 支持把 v0.5 目录加入 `$Path` 后直接 `Needs["MadStree`"]`。FlintNDE `v0.1.0.dev0` 源码已内置到 `Vendor/FlintNDE/`，缺省配置相对当前版本目录解析；Python 恢复默认 `__pycache__/`，由根 `.gitignore` 排除。新增 `MSFormulaData/MSWriteFormulaArtifacts`，直接复用既有公式 producer 输出全 sector masters、递推 metadata、完整 dlog DE 和可选 `TimePowerRules` 代入，缺省落到调用脚本目录。该阶段的 `16/16` package/artifact smoke 后续已扩展为 `18/18`，完整 final 验证见下方 fresh 记录。

MadStree v0.5 / dSIBP 020 适配边界（2026-07-30）：020 的公开 time-only schema 已固定为 `J[sectorKey,timeShifts,stateBits]`，因此 v0.5 的 `MSToDSIBPJ/MSFromDSIBPJ/MSFromDSIBPExpression` 将改为与 `MSIntegral[sectorKey,timeShifts,stateBits]` 逐槽直接互换，不再构造或解析 019 的 root-line packs。该改动只作用公开 adapter，不改变 MadStree master order、递推或 dlog 公式。只有两边 context 的 sector/state-slot schema 一致时才允许把对象交给 dSIBP 求导；MadStree 专用 `RedundantH` 若没有对应 dSIBP context，只认证 MadStree 内部往返，不冒充跨包求导能力。

适配与跨包最终执行记录（2026-07-30）：v0.5 adapter 已改为 020 三槽逐项映射，package/artifact 专项 fresh 通过 `18/18`，quotient 与 `RedundantH` 均在各自 MadStree context 内 exact round trip。在固定精确点对 mixed 三顶点 15 masters 的 5 个变量执行 dSIBP 020 `ds` -> MadStree `MSReduce`；75 个导数全部闭合且每个矩阵均为 `15x15`。修复 dSIBP lower-sector prefactor 与 MadStree contact event phase 后，五个矩阵与直接 dlog 公式逐项严格相等，最终检查 `9/9`。

MadStree v0.5 fresh 验证记录（2026-07-30）：全部 11 个开发 tests 已串行通过，核心计数包括 `49/49`、边界 `9/9`、massive full edge `5/5`、massive vertex `7/7`、massless edge `9/9`、mixed three-vertex `7/7`、vertex-family NDE `7/7`、vertex/reduce `18/18`、simultaneous/cycle/chart `22/22`、package/artifact `18/18`、跨包 DE `9/9`；三个 examples 均退出 `0`。新建的 v0.5 T1--T6 不复制旧报告或 summary，fresh 计数依次为 `24/24`、`12/12`、`18/18`、`15/15`、`17/17`、`16/16`，总 wall time 依次为 `31.142 s`、`161.428 s`、`8.250435 s`、`1.516580 s`、`1.858434 s`、`3.113660 s`。T1 的 25/15 边界均由 v0.5 直接生成；T6 由 Wolfram 入口调用内置 FlintNDE 并验证保存点与 fail-closed。并行启动多个 Wolfram/FlintNDE 开发脚本曾有一轮非零 shell 退出且未留单项输出；同四项随后串行均通过，因此正式证据只采用串行结果。

MadStree v0.5 手册与 active 文档记录（2026-07-30）：根 README、子包 README、文档索引、版本索引和开发规划均已指向 v0.5，缺省 FlintNDE 路径写为版本内 `Vendor/FlintNDE`。`tree_formula.tex` 经 XeLaTeX、BibTeX 和两遍复编生成 29 页 PDF；目检封面、package convention 公式页和末页后发现并修复封面遗留的 v0.4 标题/摘要，再编译确认 v0.5 显示正确，无裁切、重叠或未定义引用。渲染 PNG、LaTeX 辅助文件和开发 `results_test/` 已清理，正式 PDF、验证报告与 summary 保留。

019 表示缺口与 020 处理结论（2026-07-30）：019 的公开 time-only 对象仍是 `J[aList,linePacks,{}]`，因此冻结为旧接口；020 已新建中央 public boundary，把所有公开 time-only 输入/输出改为 `J[sectorKey,timeShifts,stateBits]`，内部仍复用旧 producer。`stateBits` 是 sector slot registry 的离散 $n_i$ 态，不强制按顶点分组；massless quotient 整边共享一位。当前跨包 DE 只认证 020 新表示，不再通过 019 line pack adapter。

### 当前任务：2026-07-30 dSIBP 019 / MadStree v0.4 sector 定长位串统一

新建 dSIBP `019` 与 MadStree `v0.4`，仅在两包的 time-only 表示及其 shrink/contact sector 消费链中，把 canonical `sectorKey` 统一为按初始化时 root propagator 顺序排列的定长字符串。每一位对应一条 root propagator：`1` 表示未收缩，`0` 表示已收缩；不能发生 contact shrink 的传播子位始终为 `1`，因此 top sector 是全 `1` 字符串。该 key 是字符串而不是整数，前导零是 sector 身份的一部分。dSIBP 的统一积分仍使用 `J[aList,linePacks,{}]`，root-ordered packs 保留物理状态；MadStree 的 `MSIntegral[sectorKey,...]` 直接把位串作为第一参数。纯动量、Kira/reduction、与 time-only sector 无关的 DE/数值模块不因本任务重跑。

- [x] 盘点 dSIBP 018.1 与 MadStree v0.3 的版本资产、sector-key producer/consumer、examples 和验证入口；确认新版本复制边界且不回写旧版。
- [x] 新建 dSIBP `019_dSIBP`、正式 `019.0` 交付和 MadStree `v0.4`，均写 `UPDATE_NOTES.md`，同步版本索引、loader、README 与手册版本信息。
- [x] 修改两包唯一 sector-key producer，使 sector metadata、contact/shrink DAG、master、关系/DE、边界、serializer 与查询接口共享同一 root propagator 顺序和定长位串；增加 key schema/位序 metadata 与输入门禁。
- [x] 更新两个新版本的 examples、开发测试和版本化独立验证任务；旧版报告/summary 保留其执行时事实，不改写成新接口结果。
- [x] 只重跑 dSIBP 019 与 MadStree v0.4 中涉及 time-only shrink/contact sector key 的 examples、开发检查和正式验证，保存实际版本、输入、耗时与结果；纯动量、Kira/reduction、FlintNDE 及其它不相关流程未重跑。single-massive sunrise 与本改动无关，未重跑，DE/scaling 数值闭环继续标为 pending。
- [x] 编译并目检受影响手册，运行 key 唯一性、单/多收缩编码、不同路径合并、递推/DE/边界闭合、公开 API/example coverage、whitespace 与 Git 范围检查。

验收标准：若 root propagator 顺序为 `{e1,e2,e3,e4}`，收缩 `{e1,e2,e4}` 的 key 必须严格为字符串 `"0010"`，top 必须为 `"1111"`；不同收缩集合不得碰撞，同一集合经不同路径必须合并。dSIBP 019 与 MadStree v0.4 的 active 文档、源码、examples 和新验证结果不得继续把 `"top"` / `"contracted:..."` / `"e2_e4"` 当作当前 canonical key；旧版本必须保持可复现。sunrise 未执行的数值闭环不得写成通过。

完成记录（MadStree v0.4，2026-07-30）：已从 v0.3 的 34 个 tracked 文件建立 v0.4，未复制 `results_temp`、Python cache 或测试生成物。sector producer 现按全部 root `lines` 顺序生成定长字符串，context 保存 `sectorKeySchema`；contact DAG、sector/master、递推、dlog、boundary 与 legacy-J adapter 已统一消费该 key。两条 root line 的实际 sector 顺序为 `{"11","01","10","00"}`。开发检查 fresh 通过 core `49/49`、simultaneous/time-only chart `22/22`、vertex/reduction `18/18`，三个随包 examples 均退出 `0`。受 key 影响的独立验证 T1--T5 fresh 通过 `24/24`、`12/12`、`18/18`、`15/15`、`17/17`，总 wall time 依次为 `27.195 s`、`128.645 s`、`9.246280 s`、`1.555188 s`、`1.881676 s`；T6 为 FlintNDE-only 且不依赖 sector identity，本轮未重跑。T1 的 15/25 target projection 差约为 29 位零，massless relation residual 约为 37 位零；T2 target 相对差为 `9.46e-23`；T3 full/contact 差约为 48 位零。公式手册经 BibTeX 与两遍 XeLaTeX 编译为 28 页，无未定义引用；封面、含 `"0010"` 的第 10 页和末页已渲染目检。TeX/PDF SHA-256 分别为 `4211B8FD127CC49D4684F88F79298FFB4C7EAF68F9F6A72DC36AE49666F6C6B8`、`7E8F4EBEE3B8FB75791C72A8C6210E09E865CA385EA822F7B12622B699CB6FA8`。

完成记录（dSIBP 019.0，2026-07-30）：已从 018 的 25 个 tracked 模块建立 019；只有 `ibpMode -> "timeOnly"` 的 sector producer 和 consumer 改用定长位串，full-loop 继续使用旧 key，故本轮未重跑 Kira、reduction、圈动量 examples 或 sunrise。专项 fresh 通过 massless endpoint/contact `9/9` 与四传播子 key `7/7`，并唯一生成收缩 `{e1,e2,e4}` 的 `"0010"`。API/example coverage 通过 `5/5`，覆盖 39 个公开函数；example 05 在候选与正式路径均退出 `0`，massive dlog 严格等于 naive DE，massless quotient 按既有 `PendingRederivation` 边界 fail closed。正式程序和 56 页手册与候选逐字节相同，SHA-256 分别为 `2E6CDF020FA6B69BADD397759A0B735210D3912C301897DC4ECFF4B63C4C5CFD`、`45B5AA81B2FEB96F30C0777373867362730B8D0472CFC0FBE04D60B70A410E96`；封面、新位串页和末页已目检通过。

### 当前任务：2026-07-30 三程序包 examples 与文档/报告清理

为 MadStree、FlintNDE 各保留或整理 `2--5` 个典型 example，dSIBP 保留现有 6 个公开 example，作为程序包随附配置和用户入口。优先复用已经运行并有结果证据的现有脚本，不重跑、不重写已经通过的案例；只在目录或入口缺失时做最小迁移与说明整合。dSIBP 的 single-massive sunrise 继续只展示 general seeds 与参数微分算符，其纯数值 DE/scaling workflow 单列为未完成验证任务，不得写成已实现或已通过。

- [x] 盘点三个程序包现有 examples、调用入口、运行证据、重复案例和文档引用；冻结 dSIBP 现有 6 个、MadStree 3 个、FlintNDE 3 个典型案例清单。
- [x] 把已运行案例整理到各包统一 examples 入口；已有案例不重跑、不改写物理输入或 expected，只修必要的相对路径和索引说明。
- [x] 在 dSIBP 权威任务书/进度中登记 sunrise 纯数值 DE/scaling rule 为未完成验证，明确前置条件、不得冒充的旧证据和后续验收输出。
- [x] 审计根 README、三个子项目 README/AGENTS/版本索引、手册/规划及交叉链接；就地整合重复或过时内容，优先让根 README 成为三程序包关系与入口的简洁总览。
- [x] 盘点全部 report 的版本、结论、当前替代证据和引用关系；只删除已被当前报告/进度完整取代且不再承担独立证据价值的过时报告及附件，保留当前版本和仍有来源隔离价值的报告。
- [x] 运行路径、链接、example 数量、版本、whitespace 和 Git 删除范围检查；更新完成记录；Git 提交与推送在本轮收尾执行，结果以最终 Git 状态为准。

验收标准：每个程序包从自身 README 可直接找到代表性 example；dSIBP 保留现有 6 个，MadStree/FlintNDE 各为 `2--5` 个；未重跑案例不产生新的通过声明；dSIBP sunrise 数值 DE/scaling 明确保持未完成；根 README 不重复三份子项目手册；删除报告均有当前替代来源，历史但仍提供独立推导/来源隔离的报告不因版本旧而误删。

完成记录（2026-07-30）：dSIBP 的 6 个现有 examples 原样保留；MadStree 新增由已通过 T4/T5 配置整理的单顶点约化与 time-only 圈图两个公开脚本；FlintNDE 新增由 `test_save_points.py` 已执行配置抽出的正则/指数奇点保存脚本。未重跑任何数值案例；新 Python 脚本通过 AST 解析，新 Wolfram 脚本通过注释/字符串感知的分隔符与章节标记静态检查。根 README 已压缩为三包职责、工作流、版本、examples、输出和文档入口；三包 README 同步。README 相对链接检查通过，active docs 的旧目录/旧正式版本残留扫描为空，example 数量为 `6/3/3`，`git diff --check` 通过。

dSIBP 报告归档删除 013--015 已移除代码版本报告、被后续结论取代的 016 中间/未完成报告及附件、一个被更新稿取代的 016 外部报告，以及明确不属于独立报告的两份 018 开发日志；当前保留最新 `2026-07-24-1124-016-外部.md`。018.1 的 source-isolated 独立 benchmark 和 single-massive sunrise 纯数值 DE/scaling 均仍未执行，不作通过声明。

### 当前任务：2026-07-30 三程序包目录统一

把仓库根目录整理为 `package-dSibp/`、`package-MadStree/`、`package-FlintNDE/` 三个并列子项目。dSIBP 代码只保留最新三个整数版本 `016_dSIBP/`、`017_dSIBP/`、`018_dSIBP/`，删除 `010`--`015` 单文件及 `014_dSIBP/`、`015_dSIBP/`；正式 018.1 交付、当前文档、examples、检查和报告统一移入 `package-dSibp/`。FlintNDE 整体改名；MadStree 若缺少子项目级文档入口则补建。三个子项目分别维护就近 `AGENTS.md`，根 README/AGENTS 只保留三者关系和共同规则。

- [x] 盘点 dSIBP 010--018 的文件、Git 状态和全部实时路径引用；冻结只保留 016、017、018 的版本边界。
- [x] 设计并实施三套并列目录；保留正式结果、验证报告和当前 018.1 交付，删除确认无消费者的历史实现。
- [x] 更新全部相对路径、loader、测试、TeX、README、进度表、`.gitignore` 和三个子项目 `AGENTS.md`；MadStree 增加统一 Documentation 入口。
- [x] 从新路径运行 dSIBP massless endpoint `7/7`、MadStree core `47/47`、MadStree boundary/FlintNDE adapter `8/8` 和 FlintNDE 全量 `76/76`；三套当前入口均从新路径加载，dSIBP/FlintNDE 手册分别重编译为 32 页并完成代表页目检。
- [x] 审计迁移后的 Git 删除/重命名范围，提交并推送 GitHub，记录最终 commit 与远端一致性。

迁移状态（2026-07-30）：仓库根目录现以 `package-dSibp/`、`package-MadStree/`、`package-FlintNDE/` 暴露三个程序包。dSIBP 的 016、017、018 已移入 `package-dSibp/versions/`；010--015 已从工作树删除但保留于 Git 历史。历史独立报告保留其执行当时的旧路径文字，不作为当前入口。dSIBP 018.1 用户手册按候选先行门禁两遍 pdfLaTeX 重编译为 32 页，实际版本说明页目检无裁切、重叠或坏字形；候选、文档原件和正式交付的 SHA-256 均为 `07712A905853A33A558CC72116D562AD7F99D62069404DEED409DCB9290E22CD`，候选与渲染临时文件已清理。迁移与功能提交 `b9128a0`（`Reorganize package roots and retain dSIBP 016-018`）已推送到 `origin/main`；提交前远端分歧为 `0/0`，Git 识别 `220` 个 rename、`8` 个 add、`36` 个 modify 和 `42` 个 delete，未提交 cache、TeX 中间文件、`results_test` 或 `results_temp`。

验收标准：根目录只通过三个 `package-*` 子项目暴露程序包；所有公开入口和手册链接均指向新位置；dSIBP 代码版本只保留 016、017、018；每个子项目的代码、Documentation、验证/测试和输出边界由自身 `AGENTS.md` 明确规定；三套当前入口均可从全新进程加载或运行。

### 当前任务：2026-07-30 FlintNDE 本性奇点边界保存

把 FlintNDE 当前已经认证的指数型高阶-pole 局部解接入可复用保存合同。对严格解耦、可继续桥接的 `exponential_power_log`，边界项统一写成 `{phi,a,b,C}`，表示 `exp(phi(z)) z^a (log z)^b C`，其中 `phi` 保存局部变量负幂的 exact Q(i) 系数；对缺少 Stokes connection 的 `formal_exponential_asymptotic` 只允许保存奇点起点。普通点、正则奇点与指数型奇点继续使用同一 `(coordinate,"save")` 标签和即时逐点/最终汇总文件合同。

- [x] 新增 exact `{phi,a,b,C}` 边界对象、公开构造器、输入验证和指数 sector 一致性检查；保留旧 `{a,b,C}` 自动推断指数 sector 的兼容入口。
- [x] 为可桥接的指数型奇点实现起点、终点和中间奇点边界反解与保存；中间奇点只接受显式 `(coordinate,"save")`，超出 continuation-ready/Stokes 能力时在输运前拒绝。
- [x] 增加保存后重新作为边界输入、终点反解、中间奇点即时文件、formal 起点、`phi=0` sector 与 fail-closed 的专项测试；保存点专项 fresh `10/10`，FlintNDE 全量 fresh `76/76`、wall time `8.653 s`。
- [x] 同步 FlintNDE 两份 README、TeX 手册/论文规划、MadStree adapter 说明/tree formula 和根 README；FlintNDE 32 页与 tree formula 28 页均重编译，日志无实际 warning、overfull、underfull、undefined 或 fatal，第 `10,26,27` 页及第 `25,26` 页已渲染目检。

完成记录：公开 `exponential_boundary` 接收 `{phi,a,b,C}`，其中 `phi` 为 exact Q(i) 负幂 Laurent 多项式，`phi=[]` 表示零指数签名；旧 `{a,b,C}` 指数分支输入保持兼容。严格解耦 `exponential_power_log` 的起点、终点和显式中间 `(coordinate,"save")` 均可恢复原积分基边界并重新输入；`formal_exponential_asymptotic` 只保存起点并标记 `continuationReady=false`。FlintNDE TeX/PDF SHA-256 分别为 `D13A0B44D63FF4903BD73F8467F1D24F9CBD9EF4980DAE83680251E5A073901D`、`2632F5BB8D781B43A3BAFAE7DE026A60B1ECA9FAE09BB443C5F2A8F8F859B9CA`；tree formula TeX/PDF 分别为 `A1CADA65AD4CA3C78E2AE1C6CE81E446E14735AE5E414F03A7967701C22EDBF9`、`4EEF585F05D4D70D9715594C0A3628AAEE773E593BC3ED75D01F140FD29D5EA6`。

验收标准：保存文件能完整恢复局部指数 `phi`、power/log 指标与原积分基 `C`，并可直接重建同一物理解；程序不得从单个普通点数值猜测未认证的 Stokes 数据，也不得把 start-only formal 渐近路线冒充中间/终点可继续边界。

### 当前任务：2026-07-30 新版本更新说明与分支规则

从本任务之后新增的 dSIBP、MadStree 与 FlintNDE 版本必须包含独立更新说明，记录相对基线版本的新增功能、修复、接口或 convention 变化、迁移要求、验证状态和已知限制；本规则不追溯 dSIBP 018.1、MadStree v0.3、FlintNDE `0.1.0.dev0` 及更早资产。建议每个新版本从稳定主线新建独立 Git branch 开发，但是否建立、保留或合并 branch 均由用户明确决定，agent 不得因版本完成而自动合并。

- [x] 将规则写入根 `AGENTS.md` 与 `package-MadStree/AGENTS.md`，明确 dSIBP、MadStree、FlintNDE 三种版本结构各自的更新说明路径。
- [x] 同步根 README、MadStree README、FlintNDE 根 README 与 Python package README，说明既有版本豁免、更新说明最低内容及 branch 合并选择权。
- [x] 重写根 README 开头，先概括 dSIBP、MadStree、FlintNDE 的主要功能、技术特征、输入输出、相互关系和两条实际工作流，再进入版本与目录细节；同时明确 FlintNDE 可保存正则奇点 `{a,b,C}` 边界常数，批量入口不写 `save` 文件时由 adapter 自动回退逐分支保存。
- [x] 检查 Markdown、路径和 `git diff --check`；三组件规则与路径扫描通过，本任务未为现有版本补造更新说明，也未创建或合并 branch。
- [x] 审计本地待提交范围和远端分歧，提交本轮应发布资产并推送 GitHub；三程序包目录迁移与本轮功能统一由提交 `b9128a0` 发布到 `origin/main`。

验收标准：规则源与用户说明一致，未来新建版本可从目录结构直接发现更新说明；文档不得暗示 branch 必须合并或 agent 可自行合并。

### 当前任务：2026-07-30 MadStree 通用多 sector Frobenius 边界

纠正把自动无穷远边界限制为单顶点和两顶点单 massive `G++` 的实现边界。先审计 MadStree 当前 topology/sector/contact DAG、component、slot registry、master key 和 master digest 是否已经对任意 tree/time-only graph 通用初始化，并明确不同 subsector 即使具有相同局部 shift/state bits 也不会发生积分键冲突的机制。随后直接消费这些通用 metadata 与用户选定的 strict time-rank chart，为每个 contact-reachable sector 生成与 `MSDLogDE[context]["masters"]` 同序的 Frobenius/leading boundary data，并沿 sector DAG 组装完整边界；不得按单个函数族、单个图或单组参数写专用通过分支。

- [x] 审计并记录 sector 初始化、canonical sector key、component/slot registry、master key/digest 及跨 subsector 防冲突机制。`MSIntegral[sectorKey,shifts,bits]` 以 canonical contracted-line set 的 key 为第一参数；初始化新增同源 `sectorIdentityCertificate`，对 contraction set、sector key、完整 master、global index 和 digest 一次性认证，任一碰撞直接返回 `SectorIdentityCollision`。core fresh 为 `47/47`。
- [x] 在 MadStree v0.3 独立验证任务书总则加入：检视所有公开功能是否适配任意 dS 树图的通用性，禁止为单个函数族、单个图或单个参数点编写专用生产分支以通过检验。
- [x] 推导并实现通用多 sector Frobenius boundary producer，复用现有 sector metadata、time-rank chart、normalization、base time power、master order 与 contact maps；公开入口已删除 `AsymptoticBoundaryFamilyUnsupported` 的 family 分派。pure-massless 检查同时发现 dlog contact 的指数二态相位必须按每个 selected massless line 多乘 `-1`；修正后定义积分 DE 残差 exact 为零，目标值最大相对差约 `6.27e-34`，massive 2401/T3 保持 `18/18`。
- [x] 增加不由生产代码点名的 pure massless、mixed 三顶点和 triangle time-only 多 sector 验证，检查 sector 边界覆盖、master 同序、contact/leading residual、跨 subsector 键与 FlintNDE 输运；九个开发脚本 fresh 为 `128/128`，T1--T6 fresh 为 `24/24,12/12,18/18,15/15,17/17,16/16`，FlintNDE 为 `76/76`。
- [x] 更新 MadStree 手册、README、开发规划、根 README/待办；明确真正仍不支持的数学条件，而不是用图名描述能力边界。相关程序、验证与文档已随提交 `b9128a0` 推送到 `origin/main`。

T1 验收路线纠正（2026-07-30）：不再把每次重做完整 `25 x 25` 符号 intertwining 当作门禁。
冗余四态与 quotient 二态的公式矩阵、contact、低阶约化和 dlog 均先代入任务书冻结的非奇异
有理数值点，再检查 `25 -> 15` 投影结果与直接 15 维结果。两种 context 还必须分别调用自己的
生产 `MSBoundaryData` 与 FlintNDE，得到同一点的完整 25/15 维主积分数值；随后比较
`Pglobal.I25` 与 `I15`，并把完整 `I25` 逐 sector 代入 `F01=-F10`、`F11=F00` 及恢复动量
后的 `qz^2` 关系。manufactured ordinary boundary 与只算 child sector 的定义积分不再替代这项主验收。
RedundantH 的 25 个 Frobenius 初值必须全部由其自身 `MSBoundaryData` 直接生成；不得从 15 维
quotient 边界用 embedding 反推。批量数值优化只允许共享同一个 25 维局部基和输运算子，
不能改变边界来源。fresh T1 已按此合同通过 `24/24`：两套生产边界分别含 `15`、`25` 个直接
生成的 Frobenius 分支，FlintNDE 批量输运列数分别为 `15`、`25`；边界匹配点与目标点的
`Pglobal.I25-I15`、完整 `I25` 的全部 massless 关系及恢复动量后的 `qz^2` 关系均为高精度数值零。

验收标准：生产边界代码只按 context metadata、sector DAG 和 rank chart 分派，不出现测试图 id、固定顶点数、固定 master 数或论文例参数；任意已通过公式/dlog/chart closure 的 tree context 都能生成完整同序边界，或因明确的数学条件结构化 fail closed；同一 shift/state bits 位于不同 sector 时保持为不同 master/integral key；独立验证同时包含源码通用性审计和非点名拓扑运行证据。

### 当前任务：2026-07-30 根目录项目说明同步与 GitHub 发布

在本项目总根目录统一说明三个相互隔离但可衔接的组成：正式 topology-driven `dSIBP`、公式直接计算包 MadStree v0.3，以及由 MadStree 通过相对路径自动调用的 FlintNDE 数值微分方程后端。根文档必须分别写清职责、当前版本/入口、已执行验证、输出目录和真实能力边界；不得把 dSIBP 中禁用的旧公式型 tree 路线与 MadStree 当前通用 multi-sector producer 混为一谈，也不得把 FlintNDE 的数值输运能力写成 dS 边界条件推导。

- [x] 盘点根 README、根 AGENTS、根待办以及 MadStree/FlintNDE 自身 README 和当前磁盘入口，确定三者的真实加载与调用关系。
- [x] 更新根 README、根 AGENTS 和根待办，使 MadStree v0.3、FlintNDE、dSIBP 018.1 的定位、验证状态、fail-closed 边界和文档入口一致。
- [x] 运行 Markdown/路径/版本/`git diff --check` 检查，核对待提交资产与忽略边界；检查远端分歧后提交并推送 GitHub。

验收标准：根目录读者无需进入子目录即可区分三个组件、找到各自入口和手册，并知道哪些数值边界已认证、哪些任意树图能力仍未完成；GitHub 远端包含本轮正式文档、MadStree v0.3、FlintNDE 及当前应发布的非忽略项目资产，推送后本地 HEAD 与远端目标分支一致。

上一轮发布前复验（2026-07-30，提交 `304e4ba` 前）：MadStree 九个脚本 fresh 合计 `115/115`，其中 core `42/42`、单顶点/约化 `18/18`、simultaneous/cycle/chart `20/20`、六个数值/边界脚本合计 `35/35`；FlintNDE 完整 Python unittest fresh 为 `68/68`。该计数只记录上一轮发布状态，当前结果见本文件最上方的通用多 sector 任务。

GitHub 发布记录（2026-07-30）：主发布提交 `304e4ba`（`Publish dSIBP 018.1, MadStree v0.3, and FlintNDE`）已推送到 `origin/main`，包含 dSIBP 018.1、MadStree v0.3、FlintNDE、T1--T6 独立验证和根目录三组件说明。发布前完整 staged whitespace 门禁通过，未提交 cache、`pyc`、`results_temp` 或开发 `results_test`。

### 当前任务：2026-07-30 完成 MadStree v0.3 T1--T6 独立验证与文档归档

T1 先前只冻结了两顶点、单条 massless `G++`、`NuConvention -> "Positive"`、`nu=1/2` 的拓扑与 convention，尚未冻结顶点时间幂、外能量、传播子动量、待约化 shift 和数值目标点，因而还不是可重复的完整函数族。用户现将 T1 改为三顶点链：一条 massive full propagator 与一条 massless full propagator。package 新增初始化时固定的 massless 表示选择：缺省共享二态 `"Quotient"`，或保留两个 h endpoint 的冗余四态 `"RedundantH"`；两种 context 都必须由 package 本身直接生成递推、contact 和 dlog，再由 exact sector-wise embedding/projection 比较。T1 只取单步或相邻层的低阶 time shift；所有 sector normalization、Hankel 常数、massless embedding/projection 与数值尺度必须先写入独立验证任务书，再执行程序。T3 继续求出论文 contact master 到 MadStree normalized child master 的显式 basis map，再执行五维 Frobenius 边界与同一点输运。

- [x] 实现并记录 `masslessRepresentation -> "Quotient"|"RedundantH"`；context 初始化后固定，不允许逐调用切换，缺省保持 quotient 二态兼容。开发检查 `42/42`：mixed sector 维数 `{16,4,4,1}`、完整 `25x25` dlog 和 `nu=1/2` 输入门禁均通过。
- [x] 冻结三顶点 mixed T1 的完整函数族、共同 normalization、四态/二态映射、仅一阶的待约化积分、数值点、路径、阶数和通过条件，并先同步独立验证任务书。
- [x] massive pinch normalization 与直接冗余边界路线修正后 fresh 重跑 T1：三顶点 mixed tree 的 quotient `15` masters 与 redundant-H `25` masters 在递推、contact、dlog、两套独立生产边界和目标输运上通过 `24/24`；25 维边界不是由 15 维 embedding 反推，anchor/target projection 差与完整 `I25` 的 massless `q^2` 关系 residual 在当前精度下均为零。
- [x] T1 exact 检查：master 顺序、massless embedding/projection、递推/约化、contact 和完整 block-triangular dlog intertwining 全部闭合且 residual 为零。
- [x] T1 数值检查：`MSBoundaryData[quotientContext]` 与 `MSBoundaryData[redundantContext]` 分别直接生成完整 `15 x 15` 与 `25 x 25` Frobenius 初值系统，再由 FlintNDE 以 `15`、`25` 列批量输运；自动报告为 `24/24`，目标投影差和含 `qz^2=25/16` 的关系残差均为高精度数值零。
- [x] T3 建立论文五维 basis 到 MadStree master order 的显式可逆映射；修正后顺序与 normalization 完全相同，正反映射均为 `IdentityMatrix[5]`。
- [x] T3 用论文边界系数和 MadStree/FlintNDE 从同一 blow-up 奇点输运到同一普通点，完整五维比较 fresh 通过 `18/18`。
- [x] Boundary/FlintNDE 保存点修订后 fresh 重跑文献验证：T2 单顶点三 massive 仍为 `12/12`，最大目标相对差约 `9.46e-23`；T3 两顶点 `G++` 仍为 `18/18`，完整五维与 contact 分量差在当前精度下均为零。T3 自动报告的秒数字段已改为固定十进制格式，不再泄漏 Wolfram precision mark。
- [x] 更新 T1/T3 报告、机器 summary、任务书、手册和状态；massive pinch normalization 修正后 T1/T3 均已 fresh 重跑并关闭。
- [x] 盘点此前已经完成的其它功能测试，按独立来源与测试对象拆分为 T4--T6；任务书先固定输入、normalization 和通过条件，不得复制开发脚本或把原开发回归计数直接冒充独立验证。
- [x] T4 独立重建核心/单顶点函数族、低阶 `MSReduce` 与 H/h 正反变换检查；fresh 通过 `15/15`，两种 `NuConvention` 的全部矩阵、约化系数和 round trip residual exact 为零。
- [x] T5 独立重建 simultaneous contact、time-only 圈图 canonicalization 与全 sector normal-crossing 证书；fresh 通过 `17/17`，显式 massive pinch normalization `{2,3,5}`、七 sector/九 transition 和六张各 25-divisor chart 均通过。
- [x] T6 独立验证 MadStree--FlintNDE adapter 的普通点/正则奇点能力边界、逐个 `save` 点即时输出、调用目录归属和超能力奇点 fail-closed；fresh 结果 `16/16`，普通保存点对解析值最大相对差约 `1.09e-33`，正则奇点起点保存为 `{a=2,b=0,C={1}}`，二阶 pole 分类为 `non_fuchsian_input_basis` 并在普通输运前 fail closed。
- [x] 把 T1--T6 的状态、修正后的 contact 符号与 massive pinch normalization 同步到 README、`DEVELOPMENT_PLAN.md`、公式手册和 `todolist_temp.md`；手册已重编译并目检，受影响回归与 `git diff --check` 均通过。

验收标准：T1 和 T3 各自具有完全冻结的函数族与数值点；T4--T6 各自有来源隔离的验证程序、轻量正式结果和自动报告；所有报告包含实际 normalization、路径、展开阶数和分项耗时；所有 exact basis/DE residual 为零且数值差受 refinement 控制，才可把对应状态改为 `passed`。

T1 首次正式运行的 exact 部分全部通过：全局映射维数为 `S:25x15`、`P:15x25`，左逆、全部 `M1/M0`、contact、两只一阶约化和完整 dlog intertwining 均为零残差。随后逐 master 重复三维 chamber `NIntegrate` 在外层 `904 s` 上限被终止，未产生数值通过结论。函数族、点、精度和 normalization 不变；验证 oracle 改为每个 sector/chamber 一次向量值积分，让全部 state components 共享同一自适应网格后重跑。该改动只优化独立 oracle，不进入生产 Kernel。

T1 第二次把同 sector masters 合成向量积分后，top-sector 三维无限域 `NIntegrate` 仍在 `904 s` 上限被终止，说明瓶颈是该 chamber 自适应积分本身。验收路线据此收窄但不降低公式覆盖：完整 15/25 维 dlog 改用确定性的 compatible ordinary boundary `br=Sglobal.bq` 做 FlintNDE continuation；真实定义积分只计算 massive 已收缩、massless 仍活跃的 2/4 维 child sector，独立检验 `Pi/2` normalization 和非平凡 `qz^2=25/16`。该修订已先写入独立验证任务书；两次超时均不计为通过。

2026-07-30 T6 续跑记录：普通点保存路线、逐点文件、汇总文件和非法三元组预检均已实际工作；当前 `10/15` 不是生产公式失败，而是独立 probe 把正则奇点启动后的普通段设成同一点到同一点，使拉回 connection 恒为零，FlintNDE 因没有可用于自动半径控制的奇点而按能力边界拒绝。下一步把 probe 改为同一 `A(t)=2/t` Frobenius 启动接一段非平凡、精确可解且路径外有 pole 的普通输运，同时核对 refinement 字段的实际 schema；通过后再重跑 T1--T6 和受影响回归。

T6 完成记录：probe 已改为 `A(t)=2/t` 从 `t=0` 启动到 `t=1`，再以其归一化末值沿 MadStree 一维 Laplace dlog 系统从 `{k0=-8 I,a=1/3}` 输运到 `{k0=3,a=1/3}`。正式阶数由不足以满足 refinement 的 `48/72` 提高为 `72/96`；普通主链/参考链差满足 `1e-22`，三个保存点对解析值最大相对差约 `1.09e-33`。验证脚本自动报告 fresh 为 `16/16`，所有保存点输出都位于验证调用目录的 `results/flintnde_save_points/run-UUID/`，版本源码目录执行前后无新增 JSON/pyc。

受影响开发回归审查：六个历史 FlintNDE 脚本曾把 `MSBoundaryData["values"]`（无穷远级数在有限 anchor 的匹配值）误当成目标点定义积分，并继续传入已删除的 `AccuracyGoal/PrecisionGoal/MaxRecursion`。测试合同现已就地修正：单顶点专用接口与等价 topology 做同序 NDE 等价；零 h-block 对解析 Gamma；massive、pure massless、mixed 三顶点和 triangle context 都检查各自生产边界、leading residual、输运与 master digest；不得恢复有限点直接积分 fallback。

当前 fresh 验收（2026-07-30）：独立验证 T1--T6 依次通过 `24/24`、`12/12`、`18/18`、`15/15`、`17/17`、`16/16`；六个版本化目录均由 `run_validation.wls` 自动生成 `000_` 报告与 `results/summary.wl`。MadStree 九个开发脚本闭合为 `128/128`，其中 core `47/47`、单顶点/约化 `18/18`、simultaneous/cycle/chart `22/22`、六个数值/边界脚本 `41/41`；FlintNDE 当前 Python `unittest` 为 `76/76`、耗时 `8.653 s`。T1 的 25 维边界由 MadStree 直接生成，T6 的正则奇点结果保存为可复用 `{a,b,C}`；FlintNDE 自身另验证指数型 `{phi,a,b,C}` 起点、终点和中间点保存。

文档完成记录：README、版本索引、开发规划、独立任务书、待办与 `tree_formula.tex` 已同步。手册新增完整 mode 的 `nu^2=d^2/4-m_eff^2/H^2`、`(-tau)^(3/2) H_nu`、massless/massive 晚时幂次与 IBP 边界项关系，并说明 blow-up 后 regular-singular 与本性/高阶 pole 的能力边界；无名保存点输入和逐点输出合同现同时覆盖后端已认证的 `{phi,a,b,C}` 指数型边界。XeLaTeX 最终生成 `28` 页 PDF，日志无 warning、overfull、underfull、undefined 或 fatal；最新第 `25,26` 页已渲染目检。当前 TeX/PDF SHA-256 分别为 `A1CADA65AD4CA3C78E2AE1C6CE81E446E14735AE5E414F03A7967701C22EDBF9`、`4EEF585F05D4D70D9715594C0A3628AAEE773E593BC3ED75D01F140FD29D5EA6`。`git diff --check` 通过，PDF 临时渲染目录已清理。

T3 exact paper probe 发现四个必须先修复的生产缺陷。第一，当前 `msContactDLogBlock` 在 2401.00129 Eq. (3.68) 的 transformed log kernel 外又乘了一个负号；top `4x4` 对角块与 2411.03088 Eq. (4.4) 完全相等，但 top-to-sub 列整体反号，使第五 indicial vector 错成 `{0,5/8,-1/3,0,1}`，论文值应为 `{0,-5/8,1/3,0,1}`。第二，两顶点第五分支的物理系数不能由 generic child normalization 猜测，必须直接使用 2411.03088 Eq. (4.11)，并按 context 的 `formulaNu` 实施正/负 prefactor 变换；当前尚未推导不等顶点时间幂的推广，必须 fail closed。第三，实正内部动量的 massive pinch normalization 不能把形式符号 `(-k)^(-2 nu-1)` 直接交给 Mathematica 主支；按 2411 Eq. (4.2) 固定为 `-(4 I/Pi) Exp[Pi Im[formulaNu]] k^(-2 formulaNu-1)`，避免伪复相位。第四，`msSingleVertexLeadingCoefficient` 的多行乘积缺少显式 `*`，导致两顶点前四个系数只保留 `(-I)^(A+1)`；必须恢复 Gamma、动量幂和 Hankel endpoint coefficient。修复后要先做 exact `Omega`、leading vector 与 coefficient 对照，再 fresh 重跑 T1 和五维 T3 输运。

T3 修复后 fresh 运行通过 `18/18`。固定点为 `{k12=-30 I,k34=-6 I,ks=1,nu0=2,nu1=1/5}`，anchor 为 `{k12=-32 I,k34=-8 I}`，blow-up 为 `x=k34/k12,y=1/k34` 且沿 `t:0->1`；随后普通路径 `s:0->1` 无内部奇点。生产和独立论文路线均用阶数 `56/80`、工作精度 `50`；完整 target vector 最大相对差为约 48 位精度的零，contact 分量同样为零。边界生成、生产输运、论文输运与总 wall time 分别为约 `0.086/8.529/8.503/18.717 s`。正式自动报告和 summary 已生成。

### 当前任务：2026-07-29 固化 MadStree 目录规则与验证报告字段

在 `package-MadStree/AGENTS.md` 建立 MadStree 子项目的局部目录合同，集中规定版本目录、独立验证任务书、逐任务验证目录、正式结果、中间文件和调用目录的边界。独立验证报告除目标、方法和结论外，必须简要给出实际数值点、实际输运路径、边界/局部/输运展开阶数及计算耗时；未执行的 pending 项也必须明确写“未执行”及原因，不能省略字段造成已经执行的印象。

- [x] 新建 `package-MadStree/AGENTS.md`，记录当前目录结构、版本冻结规则、验证工作区和输出归属。
- [x] 更新独立验证任务书与顶层 README，使报告字段合同和规则入口一致。
- [x] 核对现有验证报告生成器：T1 已把未执行的点、路径和阶数显式写入 pending 报告；T2 已增加实际 anchor、仿射路径、自适应路径点、边界/输运阶数和分项计时；T3 旧脚本尚无自动报告且加载路径已过时，必须在后续 T3 重写时按本合同实现，当前不得视为合格验证程序。

验收标准：MadStree 目录规则有且只有一个局部 `AGENTS.md` 权威入口；验证报告可仅凭报告本身复核计算点、路径、展开设置和时间成本；pending 报告明确区分计划参数与实际执行参数。

T2 报告模板重跑发现迁移后的 `MSRuntimeDirectory` 路径解析调用了本机 Mathematica 不支持的双参数 `ExpandFileName[path,base]`，使 FlintNDE 未启动并停在 `9/12`。现已改为绝对路径直接单参数展开、相对路径先与调用脚本目录拼接后单参数展开；同时让 transport `Failure` 在验证脚本中保持结构化，不再触发后续 `Lookup::invrl`。边界与独立定义积分部分仍通过，完整 T2 必须修复后重跑，不能沿用本次 `9/12` 作为通过结果。

修复后 T2 已 fresh 重跑并通过 `12/12`，总 wall time 为 `131.785 s`。报告记录目标点 `{k0,k1,k2,k3}= {-12 I,1,2,1}`、anchor `k0=-30 I`、从 anchor 到目标的仿射 `s` 路径及 FlintNDE 实际三点 adaptive path；展开设置为边界级数 `28`、主输运 `72`、reference 输运 `96`。分项 wall time 为边界 `34.994 s`、独立定义积分 oracle `16.231 s`、MadStree/FlintNDE 输运 `39.668 s`，其中后端 primary/reference 为 `0.830/1.335 s`。anchor 级数差为零，定义积分最大相对差约 `2.27e-45`，目标点最大相对差约 `9.46e-23`。自动报告和轻量 `results/summary.wl` 保留；运行 JSON/cache 只在验证调用目录的 `results_temp/`，版本源码目录无 cache。

文档与归档检查：`tree_formula.tex` 已用 XeLaTeX 连续编译两遍得到 26 页 PDF，SHA-256 为 `9B07799A33DEE7F788189178EA036A2E3597A7F5014ACEE2DC8C1169EE425E01`，日志无实际 warning、overfull 或 undefined reference；`git diff --check` 通过。`.gitignore` 继续忽略全部验证 `results_temp/`，但显式保留各验证任务的轻量 `results/summary.wl` 和未被忽略的 `000_...report.md`。

### 当前任务：2026-07-29 MadStree 升版触发规则确认

MadStree 不因任意一次代码、公式或接口更新而自动新建版本目录。是否升版只由用户明确要求触发；用户未要求升版时，小改动和当前开发修订继续写入当前工作版本。收到明确的新开/发布版本指令后，才复制当前工作版本建立新的 `MadStree-vX.Y/`，并把此前版本冻结；冻结版本不得覆盖或删除。

- [x] 核对 `package-MadStree/` 全目录，不存在“任何代码、公式或接口更新都必须新建版本”的残留规则。
- [x] 统一顶层 README、版本索引和 v0.3 版本说明中的升版触发表述。

验收标准：版本号和版本目录不因改动规模或类型自动变化；只有用户明确要求时才升版；已冻结版本继续保持只读并永久保留。

### 当前任务：2026-07-29 MadStree v0.3 版本化目录与独立验证归档

按用户确认后的目录合同整理 `package-MadStree/`：独立验证任务书单列，不复制进验证工作目录；每个验证任务使用一个名称显式包含 `MadStree-v0.3` 的独立文件夹，文件夹内保存验证程序、正式结果以及由程序自动生成的 `000_...report.md`。当前源码、Backend、手册、examples 与开发测试迁入 `versions/MadStree-v0.3/`。版本升级只在用户明确要求新开/发布版本时执行；同一工作版本内的小改动不自动升版。已经冻结的旧版本不得覆盖或删除。

- [x] 建立根目录版本索引、当前版本加载入口、单列独立验证任务书及 T1--T3 三个版本化验证目录。
- [x] 迁移当前 MadStree v0.3 源码和文档，修正 FlintNDE 集中相对路径及全部加载示例。
- [x] 让 MMA 自动调用产生的 JSON、日志和可复用 Python cache 写入调用脚本目录的 `results_temp/`，版本源码目录保持只读。
- [x] 为 T1/T2/T3 分别提供可执行验证程序并自动生成各自 `000_...report.md`；2026-07-30 当前 fresh 结果依次为 `24/24`、`12/12`、`18/18`。
- [x] 更新 MadStree README、开发规划、公式手册和 `.gitignore`；迁移后的加载、FlintNDE、验证报告与路径写入检查均已完成。

验收标准：`versions/MadStree-v0.3/` 可显式加载且自动找到根目录 `000_FlintNDE`；每个验证任务目录名包含版本号且不含任务书副本；验证程序从自己的目录运行并生成 `000_` 报告；T2 的既有 `12/12` 结果在新路径重现，T1/T3 不被误报为通过；只有用户明确要求时才新开后续版本，已冻结旧版本不得原位覆盖。

### 当前任务：2026-07-29 以 2411 无穷远边界替换生产有限点积分并执行 MadStree 测试

纠正此前边界架构：有限 Euclidean 普通点上的闭式定义积分或 `NIntegrate` 只允许放在 `package-MadStree/test/`，作为独立数值 oracle、精度比较和效率比较工具；生产 `Kernel/` 中必须删除该路线，也不得在无穷远边界不支持或失败时静默回退。正式 `MSBoundaryData` 以 2411.03088 的 $k_0\to\infty$ nested blow-up、Frobenius 级数和 sector-leading system 为唯一数学来源，并输出与 `MSDLogDE` 完全同序的数据。

- [x] 从生产 `Kernel/Numerics/Boundary.wl` 删除有限 anchor 定义积分、闭式 chamber 积分和 `NIntegrate`；测试专用定义积分现位于 `test/DirectBoundaryOracle.wl`，不被 Kernel 或用户公开入口加载。
- [x] 实现单 component/单顶点 $k_0\to\infty$ Frobenius 分支、leading coefficients、总次数级数及论文二进制 master order 到 MadStree master order 的精确映射；生产结果携带级数阶数、收敛比、截断差、晚时指数及 `directIntegrationFallbackQ -> False`。
- [x] 两顶点单 massive $G_{++}$ 已实现 bottom-to-top indicial leading system：按 contact 后 normalization 和 theta rank 生成五个规范化 leading vectors、外置物理权重、master digest 与一参数 blow-up chart；完整高阶 power-log 递推不在 MadStree 重写。
- [x] `MSEvaluateTree`/`MSFlintNDETransport` 已接通 FlintNDE 奇点模块：对 $x=x_A t,y=y_A t$ 的 exact $\mathbb Q(\mathrm i)(t)$ 系统逐个调用 `frobenius_boundary`，在 $t=1$ 外乘物理系数求和后继续普通点输运；不支持的 family 和非 exact leading data 均 fail closed，无直接积分 fallback。
- [x] 运行新边界 producer 的 exact/closure 测试及 T1--T3；有限点定义积分只保留为独立 oracle，不作为生产 expected。2026-07-30 当前 fresh 结果为 `24/24`、`12/12`、`18/18`。
- [x] 同步 README、开发计划、tree formula 手册和独立验证任务书，删除把 finite Euclidean anchor 称为生产功能或生产基线的旧表述；手册已重编译并目检。
- [x] 在 tree formula 的递推奇异层章节补充完整 mode 的晚时标度：$\nu^2=d^2/4-m_{\rm eff}^2/H^2$、$u_k\sim(-\tau)^{d/2\pm\nu}$；区分裸 Hankel 发散、完整 mode 端点行为和 $M_1$ 递推奇异层，并限定端点项消失结论的适用范围。

2026-07-29 两顶点实现路线纠正：MadStree 不自行实现二维或一维 Frobenius 高阶递推。对论文 blow-up 选严格一参数曲线 $x=x_A t,\ y=y_A t$，MadStree 只生成拉回后的 exact $\mathbb Q(\mathrm i)(t)$ 系统、五个 indicial 指数、规范化 leading vectors 和外置物理系数；FlintNDE 的 `frobenius_boundary` 负责从 $t=0$ 正则奇点生成 power-log 局部解并输运到 $t=1$。由于 FlintNDE 的 leading vector 必须属于 $\mathbb Q(\mathrm i)$，每个规范化分支分别输运，Gamma 等超越系数只在返回 MadStree 后相乘并求和。此前加入 `Boundary.wl` 的自写 bivariate recurrence 必须删除，不能作为生产或测试路线保留。

奇点适配专项记录：同一两顶点五维系统在 `TransportOrder -> 16`、`ReferenceTransportOrder -> 24` 下实际完成；FlintNDE 把 $t=0$ 分类为 `regular_singular`，五个 exact 分支指数为 $\{9,38/5,31/5,24/5,46/5\}$，规范化 leading vectors 全部通过，随后 $t=1$ anchor 到用户普通点的输运也完成。T3 的论文 contact-vector 符号对照与最终数值 oracle 仍属于测试验收，未因接口完成而标记通过。

回归记录：Python 适配器通过 `py_compile`；生产 `Kernel/` 扫描无 `NIntegrate`，旧 `msBivariateTaylorMatrix/msBivariateFrobeniusBranch/msBivariateCoefficientTable/msBivariateBranchValue` 均已消失。单顶点三 massive 的 T2 在新适配器下重跑仍为 `12/12`，target 相对差 $9.46\times10^{-23}$。本轮没有宣称 T3 通过：论文 Eq. (4.5) 的 contact leading vector 与 MadStree normalized child-master basis 之间仍需写清显式 basis map，慢速二维定义积分在 120 秒内未完成，不能用超时结果充当验收。

验收标准：生产源码中不存在 `NIntegrate` 或定义积分边界 fallback；`MSBoundaryData` 的每个成功结果均可追溯到 2411 Frobenius/sector-leading 数据并与 DE master digest 同序；不支持的积分族明确 fail closed；功能完成与 T1--T3 的执行结果分开记录。

单顶点实现与 T2 记录（2026-07-29）：视觉核对原 PDF Eq. (3.44) 后修正 OCR 漏失的 $(\widetilde A)_{|a-b|\cdot\mathbf 1}/2^{|a-b|\cdot\mathbf 1}$ prefactor；此前误写会在 $|a-b|\ne0$ 时既不满足 DE 也不等于定义积分。三 massive、八 master、显式 `NuConvention -> "Negative"` 的测试通过 `12/12`：leading coefficients exact 相等；生产与独立级数在 anchor 差为零；测试专用定义积分最大相对差 $2.27\times10^{-45}$；FlintNDE 到目标点与 28 阶独立 2411 级数最大相对差 $9.46\times10^{-23}$。测试同时加入所有 Frobenius branch 的 $\operatorname{Re}\widetilde A>0$ 门禁；原先过小的 $\nu_0$ 会令多个 state-1 定义积分在 $\tau\to0$ 发散，已改用满足自然晚时幂次条件的参数，而不是提高数值积分递归深度。

### 当前任务：2026-07-29 登记 MadStree 三项文献与冗余基独立数值验收

本轮只把用户指定的三项测试整理为可执行任务，不在前置接口尚未闭合时提前运行或宣称通过。正式任务单统一放在 `package-MadStree/test/TEST_PLAN.md`，测试产物只能进入各自的 `results_test/`。凡与 2411.03088 比较，初始化时必须显式指定 `NuConvention -> "Negative"` 以匹配该文献的 $h=z^{-\nu}H_\nu$；不得使用 MadStree 缺省的正 prefactor convention。

- [x] 无质量 full edge 的冗余四态 Hankel 公式与 massless quotient 二态公式对照：T1 已改为三顶点 `massiveFull + masslessFull` 链，低阶约化、完整 dlog、两套直接边界、同点输运及带 $q^2$ 的 massless 关系 fresh 通过 `24/24`。
- [x] 复现 2411.03088 Sec. 3.3 的单顶点三 massive、$k_0\to\infty$ 通用解：八 master 测试 `12/12`，同序 leading、独立级数、定义积分 oracle 与 FlintNDE 输运均通过。
- [x] 复现 2411.03088 Sec. 4 的两顶点单条 $G_{++}$ 例：论文 blow-up、五主积分系统、边界系数及完整 top/subsector 同点输运 fresh 通过 `18/18`。

执行门禁：只有冗余四态 Hankel family、显式 quotient/projection 对照、2411 无穷远 Frobenius 边界及论文基到 MadStree master order 的转换全部实现并通过各自 closure/capability gate 后，才运行对应测试；否则保持 pending，不用有限 Euclidean anchor 冒充论文无穷远解。

### 当前任务：2026-07-29 完成 MadStree 通用边界、共同 theta、time-only 圈图与 shifted contact dlog

先以根目录当前 `000_FlintNDE/` 为唯一数值后端实况，重新确认其高阶 pole、Lee--Moser/Fuchsian reduction、不规则奇点与 Stokes 数据能力，不沿用旧版能力说明。随后补齐 MadStree 仍未实现的四条主线：nested blow-up/theta 固化/全 sector normal-crossing 机器证书；显式共同 theta 的 multi-edge simultaneous contact；time-only 圈图初始化与 sector/contact canonicalization；以及 `R^(1)` 落在 child 非零 shift 时自动递推回 child masters 并组装 dlog 非对角块。

- [x] 盘点新版 `000_FlintNDE` 的公开入口、奇点分类、Lee--Moser/Fuchsian reduction、不规则/Stokes 数据结构、已执行测试和 MadStree 适配缺口；当前 66 项 `unittest` 全通过。README/手册的最终同步仍并入本任务最后一项完成。
- [x] 实现严格 time rank 对应的 nested blow-up chart、theta 固化和完整 block-triangular connection 的 normal-crossing 机器证书；证书覆盖 top/subsector letters、normalization divisor、坐标 Jacobian 与 shifted-contact 递推新增分母，`RankOrder -> All` 可检查全部 root strict charts。
- [x] 实现显式共同 theta 的 multi-edge simultaneous contact：事件级 target sector、一次顶点合并、odd-subset contact 系数、共享/massive slots 同步删除、base-power path independence 与 canonical 去重。三平行边产生三个 single 和一个系数 `1/4` 的 triple event；单边收缩后的未选 massless/massive coincident lines 分别使用零奇态/`10->01` quotient。
- [x] 扩展 topology/context 以接受只执行 time 积分的含圈 incidence graph；`MSInitTimeGraph` 不初始化 loop momentum/ISP，sector/contact canonicalization 只按 root vertices、full-line events 和 component partitions 决定。triangle 只产生 7 个 reachable sectors，不生成不可达的全三边收缩 sector。
- [x] 在 top-to-sub dlog producer 中对非零 `R^(1)` child shift 调用公式递推，逐列抽取到全局同序 master basis 的 reduction matrix 后右复合 contact block；保存 residual、remaining shifts、正反向奇异层和 closure 状态，失败时返回 `contactShiftReductionFailed`。
- [x] 收紧 FlintNDE 能力预检：所有非普通起点、内部点和终点都必须实际通过同一个局部 basis dispatcher；正则奇点的位置不在 $\mathbb Q(i)$、exact Frobenius 谱不受支持或内部/终点只有 start-only 解时，`continuation_ready=False`，正式建路直接抛出 `LocalReductionError`。
- [x] 为上述四项增加相互独立的小规模 Wolfram/Python 测试，更新 MadStree 规划、README、tree formula 与原 dSIBP 隔离说明；重跑受影响回归并编译、目检手册。

验收标准：不得再把有限 anchor 的高维直接积分称为最终通用边界；所有自动边界 chart 都携带可复核 normal-crossing 证书；共同-theta 一次事件不能退化为任意单边 contact 的顺序乘积；time-only 圈图不要求 loop momenta/ISP；所有 dlog 非对角块最终只含同序 masters 且 residual 为零；新版 FlintNDE 能力结论必须由当前源码和测试支撑。

最终实现与验收（2026-07-29）：MadStree 九个脚本合计 `115/115`：core `37/37`、单顶点/约化 `18/18`、单顶点 NDE `7/7`、零 Hankel `7/7`、massive external `7/7`、massless full edge `6/6`、massive full edge `6/6`、mixed 三顶点 `7/7`、simultaneous/cycle/chart `20/20`。FlintNDE 使用系统 Python 的 `unittest discover` 通过 `68/68`；新增两项分别确认非 $\mathbb Q(i)$ 正则奇点位置和非 $\mathbb Q(i)$ indicial 谱在 plan 阶段令 `continuation_ready=False`，正式建路抛出 `LocalReductionError`。本机该 Python 未安装 `pytest`，未把缺少 pytest 当作代码失败。

文档已同步到 MadStree/FlintNDE README、开发计划、tree formula、FlintNDE 手册、原 dSIBP 隔离说明和 `package-MadStree/test/TEST_PLAN.md`。tree formula 26 页与 FlintNDE 31 页均完成全页总览；tree 新增重点页 20、21、24 和 FlintNDE 更新后的能力预检/保存点页 9、10 已放大目检，无裁切、重叠、黑块、空白或公式越界。FlintNDE 经 BibTeX 和两遍 XeLaTeX 重编，最终 log 无 warning、underfull、overfull、未定义引用或 fatal；BibTeX 只有四条既有 arXiv/preprint empty-journal metadata warning。`git diff --check` 通过，PDF 渲染和文献抽取临时目录已清理。

最终 SHA-256：`FlintNDE.tex` 为 `383397D27C612D1CF8681EFCFE64BE94B5C5AE68FC32EF7F65174950FB2C5B37`，`FlintNDE.pdf` 为 `C1825ACB54A623AD0B60109D235847895E2D0794263471514E2D0073B158A98B`；`tree_formula.tex` 为 `D6FD403E151BAB7BEBBA5DFD6FC3361EEE00B4D37A887FAC9732085E7BB37524`，`tree_formula.pdf` 为 `7365319682B4BE54FB257DA25539C4483904342B6861970184F37C65C4AF4C77`。新登记的 T1-T3 仍是待实现后执行的独立验收，不计入上述 `115/115`。

### 当前任务：2026-07-29 收紧迭代约化定义域并补推 massless top-to-sub dlog

纠正此前“`MSReduce` 支持任意积分线性组合”的过宽表述：公式递推只在线性张成的 MadStree family 内闭合，即固定初始化 context 的合法 contact-reachable sectors、各 component 的整数时间幂 shift，以及该 sector slot registry 中全部二进制状态；它不生成或约化任意传播子幂、任意新指标或 context 外积分。本轮只先修推导、手册与规划，不据未认证推导改 package 公式代码。

- [x] 沿用 2401.00129 的二进制字串、删位、补位和 Kronecker 顺序，重新陈述 MadStree 原生 `MSIntegral` 与论文积分指标的对应关系及 `MSReduce` 的精确定义域。
- [x] 区分当前有限 Euclidean anchor、2411.03088 的无穷远 Frobenius/sector-DAG leading system，以及 FlintNDE 的 Lee--Moser/不规则 Stokes 局部问题，删除容易混淆当前用户功能的表述。
- [x] 写清迭代约化中 massive/massless 被收缩边的 `R` 项：相同的 parent-to-child 递推结构继续成立，区别只在局部 contact selector、整体系数和 massless `4->2` quotient。
- [x] 推导未收缩 massless full edge 存在时的 top-to-sub dlog 块：从 2401 两端点二进制求和式出发，对 spectator massless edge 使用 quotient embedding/projection；对被收缩 massless edge把两个端点 bit 的求和投影成一个 shared bit 的固定 selector，并明确求和范围、删位/补位和符号。
- [x] 把结论写入 `package-MadStree/Documentation/tree_formula.tex` 与规划/README，重新编译并目检 PDF；若公式尚不足以支持非零 child shift 的通用实现，保持代码 fail closed 并准确说明。

完成结论（2026-07-29）：`MSReduce` 的精确定义域已冻结为固定 context 的 contact-reachable sectors、逐 component 整数 time shifts 与逐 sector 全部二进制 state bits 所生成的合法 `MSIntegral`；只允许这些对象的有限线性组合，不再称“任意积分”。手册沿用 2401 的 MSB-first `IntegerDigits` 顺序，给出 pure-massive 式 (3.68) 的 `Delete/Insert` 分量公式。对 massless contact，raw 中间态求和由 `delta_{r_e,1}` 精确塌缩为在 child 字串中插入 shared bit `1`；package 的 top-to-sub 块为两个端点 log kernel 按 `-2,+2` 加权相加，所有未缩并 massless shared bits 只给 spectator delta。普通单边 contact 的 base-power convention 保证 `R^(1)` 直接落在 child 零 shift master；`contactShiftRequiresReduction` 只保留给未来 generalized/multi-edge contact 的非零 child shift，不表示当前 master basis 约化失败。公式实现未改，只收紧公开 usage 文本。

本轮检查：以本地 2401 PDF 第 18--19 页 Eq. (3.65)、(3.68) 和 `reference/ref_code/codebubble/Omega_tau_generator.m` 交叉核对二进制符号，并严格区分论文逐顶点 $T_{n_v}$ kernel 与 massless quotient 的全 sector $U_s$ kernel；MadStree core 回归 `37/37`。`tree_formula.tex` 经 XeLaTeX、BibTeX、两遍 XeLaTeX 编译为 25 页，最终 log 无实际 warning、未定义引用、underfull、overfull 或 fatal；25 页全量渲染，新增第 10--14、23 页另行放大目检，无裁切、重叠或公式越界。`git diff --check` 通过。TeX/PDF SHA-256 分别为 `EF257535C7B31FD9B0C564AC93EE3A76DDC8C3FA6AA83FFD2A14E534040BCCD4`、`40E04EC924CE79CB80B47AD0AE148EF33205DBF13795C4EFFE3BF18F61513708`。

### 上一已完成任务：2026-07-29 MadStree 递推奇异层补记

- [x] 修正公式手册的 $M_0$ 奇异层说明：不分类讨论全正号或解析延拓，只写共线轴两侧动量模长和相等时，对应带符号 energy letter 为零。保留已确认的 $M_1$ 相等阶数消去与时间幂 regulator 结论，不改程序。

### 已完成任务：2026-07-29 NDE 路径点保存、单顶点函数族入口与直接递推约化补全

本轮同时修改根目录 `000_FlintNDE/` 与 `package-MadStree/`。FlintNDE 增加显式 save-point 输出合同；MadStree 增加不要求图 topology 的单顶点函数族初始化，并确认/补全 fixed-context 合法积分的线性组合约化和 H/h 公开变换。所有运行结果写到调用目录，不把用户结果写进程序包源码目录。

- [x] 盘点 FlintNDE 当前 `NDEs`/路径 API、普通点与奇点边界输入、逐段输运回调和输出布局；冻结路径点 `save` 标签的向后兼容输入格式。用户只写 `(coordinate,"save")`，不为 save point 命名；普通路径列表和 adaptive path 的起终点均接受该格式，输出只按路径顺序编号。既有 `NamedPoint` 名称仅保留给旧奇点路由诊断，不属于 save-point 合同。
- [x] 实现 save-point 即时输出：每算到一个带 `save` 标签的普通点，立即写一个含坐标与结果向量的独立结果文件；奇点按既有奇点边界 `{a,b,C}`/局部展开合同输出，并与坐标组成同一记录；最后再写全部 save records 的汇总文件。缺省输出根为调用者当前工作目录，可由显式参数覆盖，不回落到 FlintNDE package 目录。双链 refinement 只输出 reference 链；中途失败保留已完成逐点文件而不伪造 complete 汇总。
- [x] 为 save-point 合同增加 ordinary、regular-singular start/target 和中断即时写入测试；定向测试 `4/4` 通过。直接 exact regular-singular power-log 可从终点匹配值反解可复用 `{a,b,C}`；Lee--Moser 后原基、不规则/Stokes 边界尚无认证反向 jet，遇到此类奇点 save 时显式拒绝，不把普通点值冒充边界。
- [x] 盘点 2401.00129 与 `reference/ref_code` 的单顶点 family 输入和公式，新增 `MSInitVertexFamily` 专用入口；紧凑输入直接使用 `ki={k0,k1,...}`、`nui={timePower,nu1,...}` 和 Hankel branches，也支持显式 `k0/timePower/hBlocks/exponentialBlocks`。用户不输入 vertices/lines topology；每个纯指数 block 记录为 `1x1`，每个 h block 记录为 `2x2`。
- [x] 让单顶点 context 复用现有 slot registry、`M1/M0/U`、master 顺序、递推、dlog、自动边界与 FlintNDE 接口；等价单顶点 case 的 slot、四个 masters、`M1/M0/M0Diagonal` 和 dlog potential 已逐项相等。新增 `MSEvaluateVertexFamily` 只接受专用 context 并复用同一边界/输运链；半整数一条 h block 的数值检查 `7/7`，两项输运值与目标定义积分在显示 31 位精度内相同。
- [x] 复核每个 building block 的求逆结构：massive endpoint 固定为 `sigma2` 并用 `msPaperT/msPaperTInverse`，外腿指数是 `1x1`，massless shared 固定为 `sigma1` 并用自逆 Hadamard。`M0` 只对共同对角 basis 的 scalar letters 取倒数，`M1` 只逐对角元取倒数；Formula/DE 源码已无一般大矩阵 `Inverse`，静态门禁和数值矩阵回代均通过。
- [x] 补全/验证 `MSReduce`：接受固定 context 内单个合法 `MSIntegral` 及其有限线性组合，使用 memo 沿 shift/contact DAG 约化；`MasterBasis` 可指定 context 全部 masters 的排列。输出 result、master rules、同序 coefficient vector、non-master residual、remaining shifted integrals、memo count 和逐步正/反向 singular layers；不完整或重复 basis fail closed。
- [x] 验证 H/Hankel state 与 h state 的正反变换继续作为 MadStree 公开函数；`MSHTohMatrix`、`MShToHMatrix` 和全 sector `MSConvertBasis` 读取初始化时固定的 `NuConvention`，既有正/负 convention 局部及全局往返检查均通过，积分对象在 base time power 不可唯一恢复时继续 fail closed。

当前定向证据：新增 `test_vertex_family_reduce.wls` 为 `18/18`，`test_flintnde_vertex_family.wls` 为 `7/7`，既有 `check_core.wls` 回归为 `37/37`。单顶点紧凑入口与等价 tree-context 的公式矩阵和 dlog potential 完全相同；含正、负 shift 的线性组合约化到四个零 shift masters，用户反序 basis 的 coefficient vector 精确反序，正向奇异层为 energy letters，反向奇异层为 `M1` 对角本征值。
- [x] 更新 `package-MadStree/DEVELOPMENT_PLAN.md`、README、公式手册和 FlintNDE 文档；保存点文档只使用 `(coordinate,"save")`，不要求或接受用户点名。补齐即时逐点文件/最终汇总、奇点 `{a,b,C}` 输出、单顶点紧凑/显式输入、局部张量逆、`MSReduce` 返回合同和 H/h 固定 convention。
- [x] 运行受影响 Python/Wolfram 测试，重新编译并目检 FlintNDE 与 tree formula PDF。

本轮最终验收（2026-07-29）：设置相对 `PYTHONPATH=000_FlintNDE/code/package` 后，FlintNDE 全套测试通过 `66/66`（含 save-point `4/4`）。MadStree 八个脚本合计通过 `95/95`：core `37/37`、单顶点/约化 `18/18`、单顶点 NDE `7/7`、零 Hankel `7/7`、massive external `7/7`、massless full edge `6/6`、massive full edge `6/6`、mixed 三顶点 `7/7`。Formula/DE 源码的一般 `Inverse[` 计数为零；scoped `git diff --check` 通过。FlintNDE 手册经 XeLaTeX/BibTeX 编译为 31 页，tree formula 经 XeLaTeX/BibTeX 编译为 22 页；最终 LaTeX logs 无 warning、未定义引用、underfull、overfull 或 fatal，53 页全部渲染总览，新增保存合同页及单顶点/局部逆/约化页另行放大目检，无裁切、重叠、黑块或空白页。FlintNDE BibTeX 仍报告四条既有 arXiv/preprint 条目的 empty-journal metadata warning，不影响引用解析。`FlintNDE.tex/pdf` 的 SHA-256 分别为 `682B0F806AF271B8392E27275F635B2EF589EE33E734A6D7E6761EBF211C247C`、`67A81DC77C8D7668FF2684A271603B1026334009E64D982D5FC5ECEC2599E704`；`tree_formula.tex/pdf` 分别为 `30C456D391665E4976870B179A5542BEDC79454745A33E3066209E03C9E2E912`、`A55034FE8DA053543B5010E2E2168B5792AD09810DD2F9430F66DA1BCF392CB3`。PDF 渲染临时目录已清理。

### 当前任务：2026-07-29 接入 FlintNDE 并实现 MadStree 自动边界到任意普通点

在项目根目录使用用户新加入的 `000_FlintNDE/` 作为数值微分方程后端。MadStree 负责从 topology、主积分顺序、公式型 dlog DE 和边界渐近生成完整边界向量与安全路径；FlintNDE 只负责局部解和数值输运。两者通过 MadStree 内集中定义的根目录相对路径连接，不在模块或函数体中散布绝对路径；以后 `000_FlintNDE` 改目录时只修改一个路径变量。

- [x] 盘点 `000_FlintNDE` 的公开 Python 入口、矩阵/变量/边界/路径输入格式、普通点与奇点路由、精度和输出合同；以 MadStree-owned 薄适配器调用 `RationalMatrixSystem`、`build_adaptive_path` 和 `transport_path_refined`，不向 FlintNDE 本体加入 dS 逻辑。
- [x] 在 `package-MadStree` 中集中定义项目根目录与 `000_FlintNDE/code/package` 的相对路径变量，并提供查询、覆盖和存在性检查；缺省路径不依赖当前工作目录。
- [x] 定义 MadStree 到 FlintNDE 的中立序列化合同 `madstree_flintnde_transport_v1`：master digest、列向量方向、exact $\mathbb Q(i)(s)$ connection、边界向量、精度、refinement 和 provenance 均显式保存。
- [x] 实现自动有限 Euclidean 边界 producer `MSBoundaryData`：按严格 $K_v$ rank 选择普通 anchor，逐 sector 使用自身 component time power、normalization、branch 与 master basis；零 active-line sector 使用 Gamma 闭式，非负整数幂的 massless/正 prefactor $\nu=1/2$ 指数型 tree 使用 time-chamber 增量变量闭式，其余当前支持的 tree family 使用阻尼 chamber 定义积分。输出与 `MSDLogDE[context]["masters"]` 同序。
- [ ] 把现有 2411.03088 单顶点 Frobenius 与两顶点单 massive $G_{++}$ leading-system producer 推广到任意多 sector tree/time-only graph；生产有限普通 anchor 定义积分路线已经删除，不得恢复为 fallback。
- [x] 实现 preferred/target 普通点与路径 producer：用户按原符号给齐所有顶点能量和传播子模长；内部转换到 $K_v=i\varsigma_vk_{0v}$ 并选择首选 rank。沿 anchor-to-target 仿射路径生成 exact $\mathbb Q(i)(s)$ connection，由 FlintNDE 检查两端普通性、分析内部奇点并构造 adaptive path。
- [x] 增加端到端公开入口 `MSEvaluateTree`，由受支持且 dlog 已认证的 tree context 返回目标普通点的同序主积分数值、refinement、实际路径、边界数据、master digest 和 FlintNDE provenance；不满足 exact path、普通点或 dlog 门禁时 fail closed。
- [x] 用单顶点 0-fold、单 massive external、单 massive full edge、单 massless full edge和 mixed 三顶点树分层验收，并以解析值或目标点定义积分做固定非奇异精确点比较。
- [x] 复现 2411.03088 的无穷远边界例子：单顶点三 massive 为 T2 `12/12`，两顶点单 massive $G_{++}$ 为 T3 `18/18`；两者不替代上一项通用多 sector producer。
- [x] 把接口、数据流、公式、失败状态和数值示例整合进 `package-MadStree/DEVELOPMENT_PLAN.md`、公式手册、README 和可执行 example；运行 MadStree、FlintNDE 受影响测试及 PDF 检查。

当前执行证据：单顶点零 Hankel family 的 Gamma 边界到解析目标值、单 massive external vertex、单 massless full edge及其 contact subsector、单 massive full edge、mixed 三顶点四 sector/十五 master 已分别通过端到端检查；core 公式检查为 `37/37`。自动边界计算同时暴露并修正了 package $(-\tau)^A$ convention 下 top-to-sub contact potential 的整体符号：raising row 为 $M_0J(A+1)=M_1J(A)-R$，所以旧实现相对定义积分残差恰差两倍 contact 项，翻号后只剩有限差分误差。2411 文献无穷远例子仍未认证；现有证据覆盖 representative tree families，不等于所有 topology/参数区域的全面认证。

2026-07-29 性能门禁记录：single massive full edge 首次端到端定义积分在 180 秒内未完成，未得到数值结论。为避免已知半整数 Hankel 仍走通用 `NIntegrate`，边界 producer 增加严格限定于缺省正 prefactor、$\nu=1/2$ 的 $z^{1/2}H_{1/2}^{(1,2)}$ 及其一阶导指数闭式，并把对应 massive endpoint 化为 chamber Laplace rate shift；一般 $\nu$ 保持原数值路线。优化后 single massive full edge 在约 4 秒内通过 `6/6`，五个 master 的 FlintNDE 输运值与目标点定义积分在显示的 31 位精度内逐项相同；mixed 三顶点在约 10 秒内通过 `7/7`，15 个 master 的最大相对差在 25 位显示精度内为零。

2026-07-29 本轮验收：FlintNDE 本地 package 在设置相对 `PYTHONPATH=000_FlintNDE/code/package` 后通过 `62/62`；未设置该路径的首次调用只有六个 import errors，不是测试逻辑失败。MadStree core `37/37`；自动边界/输运分别为单顶点零 Hankel `7/7`、massive external `7/7`、massless full edge `6/6`、massive full edge `6/6`、mixed 三顶点 `7/7`；最小 example 完整运行退出码为 0。`tree_formula.tex` 经 BibTeX 和两遍 XeLaTeX 编译为 21 页，最终 log 无 warning，全部页面已渲染目检，无裁切、重叠、黑块或公式越界。scoped `git diff --check` 通过。

实现边界：`000_FlintNDE` 作为独立后端读取，dS 专用 boundary/topology 逻辑只进入 MadStree 适配层。边界处的形式级数不能直接冒充普通点数值；只有边界常数、分支、normalization 和从 chart 到有限 anchor 的局部求值全部确定后，才调用普通点输运。所谓“任意 dS 树图”以当前 MadStree 能初始化且公式型 DE 通过 closure/normalization 认证的 family 为准；未认证 common-theta gauge、time-only 圈图初始化和不规则 Stokes connection 继续 fail closed。

### 上一进行中任务：2026-07-27 重整 MadStree building-block 公式与边界坐标

为 `package-MadStree` 增加任意树积分、并预留只含 time 积分的圈图 family 的边界条件 producer，使后续可以把同序主积分边界向量交给用户将提供的数值微分方程模块，并由一个边界点演化到任意非奇异点。实现前先定位并完整阅读用户所指的“多变量超几何函数与 dS IBP DE”参考文献，冻结其积分表示、边界极限、解析延拓区域、分支与 $i\epsilon$ convention。

本轮先纠正并冻结指数/Wick 约定。对 $\tau_v=-t_v<0$ 和顶点指数 $\exp(i\epsilon_v k_{0v}\tau_v)$，内部统一定义 $k_{0v}=-i\epsilon_v K_v$、$K_v>0$，从而指数恒为 $\exp(-K_vt_v)$。因此 $\epsilon_v=+1$ 时用户参考记号是 $k_{0v}\to-i\,pik_{0v}$，$\epsilon_v=-1$ 时是 $k_{0v}\to+i\,mik_{0v}$；内部实现不依赖 `pik0/mik0` 这些外部名字，而在初始化时生成稳定的顶点能量坐标、符号和正向阻尼变量。此前只写 $K_0=ik_0>0$ 的段落必须全部按这个统一式重写。

- [x] 在本地 `reference/`、项目 `.bib` 和 PDF 中确认目标文献的题名、作者、arXiv/DOI 及完整正文是否存在。
- [ ] 从目标文献提取可推广到任意 tree topology 的边界极限、所需降阶/subsector 数据和主积分归一化；与 MadStree 当前 $(-\tau)^A$、$\nu=|\nu|$、sector/master 顺序逐项对齐。
- [ ] 规划并实现边界条件 producer；输出必须与 `MSDLogDE[context]["masters"]` 同序，携带适用域、分支、奇异面和未确定常数，不猜测物理边界。
- [ ] 建立解析小图、文献例子和后续数值 DE 模块的交叉检查。
- [x] 以 building-block registry 重写公式层：每个不带 theta 的指数为一维 slot，每个 massive $h$ endpoint 为二维 slot，每条未缩并 massless full propagator 由端点四态 quotient 成一条共享二维 slot；初始化保存每个顶点关联的 slot 编号与端点 incidence。
- [x] 从 registry 直接组装每个 $\tau_v$ 的 $M_{1;v},M_{0;v}$、正反迭代矩阵与 sector contact source，证明 massless shared slot 不妨碍单个顶点公式，只使不同顶点的矩阵在同一 tensor factor 上作用，因而不能解释为彼此独立的顶点空间。
- [x] 从同一 registry 直接组装 sector diagonal 与 top-to-sub dlog DE；逐项说明哪些待求逆矩阵只含每个 slot 的同一 Pauli 方向，何时可用局部 $2\times2$ 对角化的 Kronecker 积避免大矩阵求逆，何时因不对易 Pauli 方向而必须拒绝这一捷径。
- [x] 对 $M_1$ 和 $M_0$ 分别给出 lowering/raising 的 general-index 本征值，完整列出正向、反向迭代的奇异层；区分真正的矩阵不可逆、端点发散、energy letter 奇点和仅由所选 shift 方向触发的零分母。
- [x] 在公式手册加入 $\epsilon_v=\pm1$ 顶点的指数、Wick 坐标、SK/顶点符号和 contact incidence 区别；用户输入继续使用其自己定义的全部传播子模长和顶点能量，初始化后才转换为内部 signed-energy 与正阻尼坐标。
- [ ] 首选所有顶点纯指数能量同时沿有序 blow-up rank 趋于阻尼方向无穷；没有该一维指数的顶点允许引入辅助 $k_{0i}$，但必须先确认从边界演化到 $k_{0i}=0$ 不穿过 DE 奇点、不会令公式递推的 $M_0$ 逆或其它约化矩阵产生不可处理的奇异层。
- [ ] 对含 $G^{++}/G^{--}$ 的多顶点边界，先选相连顶点 $\tau_i$ 的渐近 rank 偏序，再反推 $k_{0i}$ 的无穷 rank 并执行 blow-up；验证每条 theta 在各 sector 中固定为 $0/1$，contact subsector 合并端点后继承同一 root-rank，且 blow-up 后边界局部没有剩余退化多变量奇点。
- [x] 把 rank 规则加强为所有 $\tau_i$ 的严格总序，并从 top-to-sub dlog 公式证明：对任意图（包括只计算 time 积分的圈图），所有含辅助 $k_{0i}$ 的 affine-linear denominator 在嵌套 blow-up 后都是 coordinate monomial 乘非零 unit；contact DAG/partition 只改变 coefficient support，不产生新的非线性或无主导项分母。
- [ ] 边界 producer 最终只接受用户给出的一个非奇异普通点；程序自动生成无穷边界数据和到该点的不穿奇点路径。到达该普通点后，才把同序数值向量交给用户后续提供的数值 DE 模块。
- [ ] 允许用户指定 preferred 起点。先把该点转换到内部正阻尼坐标 $K_v$；在所有与 theta 相容的严格 time-rank charts 中，优先选择 $K_v$ 实部大小顺序最接近该点的 chart。由于大 $K_v$ 对应小 $|\tau_v|$，能量降序必须对应 time 尺度升序；该选择只优化首段路径和可能遇到的 letters 数，不得绕过普通点证书或保证全局最少奇点。

本轮边界：已从“只确认文献存在”推进到边界结构推导和实现规划；本轮不写边界 producer，先冻结 blow-up、theta/subsector、普通点和递推奇异层合同。

文献确认记录（2026-07-27）：目标文献已完整保存在 `reference/ref_paper/2411.03088_Multivariate hypergeometric solutions of cosmological (dS) correlators by d log-form differential equations.pdf`，为 Jiaqi Chen、Bo Feng、Yi-Xiao Tao，arXiv:2411.03088v2 [hep-th]，JHEP 03 (2025) 075，DOI `10.1007/JHEP03(2025)075`，本地 PDF 34 页、389887 bytes。摘要明确声明给出任意 tree-level cosmological correlator 齐次解、任意单顶点 family 的多变量超几何解及 DE 边界条件；目录包含 $k_0\to\infty$、$k_n\to\infty$、解析延拓、任意 vertex family 和多顶点 boundary conditions。正文进一步讨论退化多变量极点的 blow-up 以及 tree-level 边界系数因子化，正是本任务所需文献。项目现有 `000_note/02_ntoe_boundary_condition_amflow/` 也已引用并初步讨论该文献，下一轮应先审计该旧 note 的 convention 与结论，不从零重复抄写。

边界结构确认记录（2026-07-27）：2411.03088 Sec. 4.1 明确指出两顶点同时沿阻尼能量无穷边界时，逆变量中出现 $t_1,t_2,t_1+t_2$ 三个 divisor 穿过二维原点；文献用 $x=k_{34}/k_{12}$、$y=1/k_{34}$ blow-up 后得到非退化边界。推广到正/负顶点时先定义 $K_v=i\epsilon_vk_{0v}>0$，再把严格 root 全序写成 $x_j=K_{\sigma(j+1)}/K_{\sigma(j)}$、$x_V=1/K_{\sigma(V)}$。每个 sector component 的 letter 都成为 coordinate monomial 乘边界非零 unit；contact subsector 使用同一 root rank，合并 component 的阻尼能量为成员之和、rank 为成员最大 rank。normal-crossing 检查必须覆盖完整 block-triangular DE 和所有 contact-reachable sectors。

辅助能量取零确认记录（2026-07-27）：每个辅助纯指数是 $1\times1$ block，generic kinematics 下不增加主积分数且只平移顶点 signed energy $p_{0v}$ 和 $M_0$，不改变 $M_1$。但零点不是自动普通点：单 full edge 的 top letters 在辅助能量为零时仍可为 $p_{0u}\pm q$、$p_{0v}\pm q$，缩边 sector 却可含必为零的 $p_{0u}+p_{0v}$。因此全辅助能量同时跑到零通常使完整 top/subsector 系统落在奇异面；只有逐步移除且每一步所有 sector letters 均非零时，才可用普通点 DE 到达零。raising 在 $\kappa=0$ 时因 $M_0^{-1}$ 失效；不可避免的零点需在 generic 参数先约化后取受控极限，或由用户基于已输出 DE 另行处理。

$M_1/M_0$ 双向专项复核记录（2026-07-27）：2401.00129 Eq. (3.39)、(3.48)、(3.50) 给出逐 state 本征值。$M_1^{-1}$ 只进入 lowering，故 $\lambda_1=0$ 是离散 shift resonance；$\widetilde M_0^{-1}$ 只进入 raising，故 $\kappa=0$ 是运动学 energy-letter 奇异面。$M_1=0$ 在 raising 中只作 numerator，$M_0=0$ 在 lowering 中也只作 numerator，都不产生该方向的 pole；二者同时为零时原 IBP row 只剩 constraint，两个方向都不能跨越，但仅凭 rank loss仍不能断言新增 master。此前把所有 $M_1=0$ 一概等同于 $\tau^{-1}$ 端点发散过强；一般 mixed/Hankel state 是否有对数端点必须另查具体渐近。纯 massless 顶点是明确例外：没有 massive endpoint 项时 $M_{1;v}=(\nu_{0,v}+n_v)I$，故完整 $M_{1;v}=0$ 恰在 $\nu_{0,v}+n_v=0$，同时其 $\Gamma(\nu_{0,v}+n_v)$ 端点积分发散；应先作解析正规化并在 generic regulator 下约化，正规化系统不存在这一物理递推失效。边界 producer 不为未正规化的离散 resonance 增加 layer；recurrence 返回方向、shift、state 和原始退化 row。

其它参数奇点的范围记录（2026-07-27）：完全不含内部 $K_v$ 的奇点不是本任务的“待解决障碍”。边界 producer 只证明并检查所有 $K_v\to\infty$ 的共同简并已被严格 time-rank blow-up 消除；自动路径只接受普通点并绕开已有 DE letters 的奇异面。package 不计算这些奇点上的发散值，也不替用户选择局部解析；用户若主动前往奇点，直接使用已输出 DE 自行 blow-up。

文档验收记录（2026-07-27）：上述结论已整合进 `package-MadStree/DEVELOPMENT_PLAN.md` 的 Phase F 和 `Documentation/tree_formula.tex`。手册现从 building-block registry 统一给出一维指数、massive h endpoint、massless shared edge 的 $M_1/M_0$ 原子，单顶点正反递推、top-to-sub dlog、共同对角化条件、正负顶点阻尼坐标、preferred rank、双向奇异层和任意 time-only 图的 blow-up 证明；massless quotient 基函数另记为 $\mathsf m_{e,0},\mathsf m_{e,1}$，不再与递推矩阵 $M_0,M_1$ 撞符号。BibTeX 与两遍 `pdflatex` 编译完成，最终日志除 `ctexart` 的 UTF8 提示外无 warning、未定义引用、underfull、overfull 或 fatal；20 页 PDF 的第 8--20 页已逐页渲染检查，无截断、重叠、黑块、乱码或错误省略号。MadStree 现有核心检查保持 `37/37`。最终 TeX 为 59531 bytes、SHA-256 `ABDD0A1E3D970E3674B82B6EC4877952B26F5E7F9988F819A255A1D828E55233`；PDF 为 1079989 bytes、SHA-256 `119B3794BC100CA84AAAECF5B7A8EEDFB20CB31BF0EB4A2B4F6E38765799B62C`。本轮没有实现或启用边界 producer、preferred-point 坐标转换或 time-only 圈图初始化。

### 上一已完成任务：2026-07-27 新建公式型树图程序包 `package-MadStree`

在根目录新建独立程序包 `package-MadStree/`（Mad dS tree），目标是不生成一般 loop/momentum IBP 系统，而直接使用 tree formula 对任意 massive/massless 混合 dS 树图构造主积分、迭代约化矩阵、block-triangular dlog DE，并为后续边界条件与数值求值提供统一对象。原 `000_note/03_tree_formula/` 已整体迁入 `package-MadStree/Documentation/` 作为自包含公式手册；具体用户指南在公开接口稳定后补齐。

状态边界：原 `dSIBP`` package 内基于 tree formula 的 `repIterative`、公式型 `DSTreeDLogDE` 及其直接 tree-family producer 从本任务起暂记为禁用，不作为当前正式能力，也不继续在 018 内开发或认证。旧源码暂不删除、不改写运行时行为，只保留为历史参考；新公式能力只在 `package-MadStree/` 中实现。依赖 loop time-IBP 的 naive tree 对照路线仅可作为后续独立验证 oracle，不作为新包主算法。

- [x] 盘点参考 `codebubble`、018 tree/time-only/sector 模块与 H/h 正反变换，区分可复用 convention、可借鉴组织方式和不得复制的 loop/ISP/Kira 初始化负担。
- [x] 建立 `package-MadStree/` 的模块化 Wolfram package、README、开发计划、`Kernel/`、`Documentation/`、`Examples/` 与 `test/results_test/` 边界；迁入并改写 tree formula 手册的程序包介绍。
- [x] 定义原生 time-only 积分表示、拓扑输入和最小初始化信息：有序顶点、full/cross/external lines、端点与 SK 类型、$\nu=|\nu|$、能量、时间零点、contact-reachable sector、共享 massless slot 和 normalization。
- [x] 移植并收缩原 package 的输入 convention 与 time-only、H-mode、h-mode 正反变换，使其只依赖新包的原生 tree 对象。
- [x] 按 tree note 公式实现局部原子矩阵、全图 Kronecker 嵌入、massless $4\to2$ quotient、sector 递推矩阵、contact 注入与 block-triangular dlog potential；不得通过生成大量 IBP 方程再线性求解得到结果。
- [x] 实现迭代约化、dlog connection 查询和后续数值求值所需的稳定公开数据结构；本轮数值模块至少固定接口与未实现边界，不伪造边界条件。
- [x] 用单顶点高函数族、纯 massive 两顶点、单 massless full edge 与 mixed 三顶点小树做精确检查；与论文/参考 code/旧 package 的适用子集比较，并记录未认证项。
- [x] 统一函数记号：正文、接口说明与测试只写普通 $h$；正/负只属于 $z^{\pm\nu}$ prefactor convention，不给 $h$ 添加正负或论文专用下标，测试哑变量也统一使用输入参数名 `nu`。
- [x] 冻结 convention 所有权：`NuConvention` 只在 `MSInitTree` 初始化时选择，context 建立后所有 sector 公式、递推、dlog 与全局 H/h 换基只读取该值，不提供逐调用改写入口。

验收标准：新包可以仅由 tree topology 初始化，不要求 loop momenta、ISP、scalar-product closure、general IBP seeds、Kira 或 reduction metadata；任意函数族阶数由 slot 数参数化；所有 master/basis/sector 顺序确定；massless full edge 在两个顶点共享同一个二维 slot；迭代和 dlog 直接由公式组装；H/h 局部及全 sector 状态向量正反变换可逆，legacy time-only adapter 只开放可唯一解析的 massive 子集；任何尚缺边界条件的数值功能明确返回未完成状态。原 018 tree 公式接口在进度文档中保持禁用，直到新包独立验证并另行决定迁移或删除。

规划完成记录（2026-07-27）：新增 `package-MadStree/DEVELOPMENT_PLAN.md`，冻结原生 `MSIntegral[sectorKey,aShifts,stateBits]`、sector/slot/master 顺序、`(-tau)^A` 递推符号和 top-to-sub 的幂次基准。论文 PDF 复核确认 top 对 subsector 的 dlog authority 是 2401.00129 Eq. (3.65)、(3.68)；`reference/ref_code/codebubble/Omega_tau_generator.m` 确有对应的 `buildTopToSubsectorBlock`、`OmegaTopToR1/R2` 和同序 `MIstau`，但其注释把公式误标为 Eq. (3.27)，新实现不沿用该误标。规划明确 raw contact 的 child shift 为 `a_u+a_v-1`，因此 `R^(0)` 落在 `-1`、DE 使用的 `R^(1)` 落在 child 零 shift master；共同 theta 多边 event 若不能满足 sector base-power path independence 则 fail closed。

首阶段实现完成记录（2026-07-27）：`package-MadStree/` 已形成 8 个模块、README、最小 massless example 和自包含公式手册。`NuConvention` 只在 `MSInitTree` 选择；Hankel order 始终输入为 $
u=|
u|$，缺省 $h=z^
u H_
u$，2401 负 prefactor选项为 $h=z^{-
u}H_
u$，之后 recurrence、dlog 与局部/全 sector H/h 换基固定读取 context，公开换基接口没有逐调用 convention option。fresh-kernel 核心检查通过 `37/37`：包括确定性 master digest、单 massless edge 的共享二态和 $-2/+2$ contact、pure massive Eq. (3.68) 精确 $4\times1$ 对照、mixed 三顶点 sector/DE、正负 convention 的局部与全 sector H/h round trip、无 context/覆盖 convention 的拒绝、legacy massive time-only round trip、massless adapter fail-closed、递推闭合及数值边界门禁。手册最终为 14 页，TeX 35054 bytes、SHA-256 `4CCC4644ED9663826C20B9595BD21BEF064B889BDAE82FFFBAFFD9FF51FD752E`；PDF 243571 bytes、SHA-256 `45A31557C50A081BD47E80D1268C3D6E225C38C5200E882473DD9DF642AC2BFA`；最终日志无 warning，14 页已全量渲染目检。显式共同 theta 多边 event、任意 mixed normalization 的 constant-residue/flatness、积分对象级 H/Hankel family 换基和物理 boundary constants 仍明确 fail closed 或未认证。

### 上一已完成任务：2026-07-27 以全图张量和重写 massless tree formula

从头重写现已迁入 `package-MadStree/Documentation/` 的公式手册：不再从“纯指数 0-fold 顶点”类比含 theta 的 massless propagator，而先把 2401.00129 的纯 massive 顶点公式统一解释为一个 1 维指数因子与若干 2 维 h 因子的 Kronecker-sum time generator。对含 theta 的 massless full edge，先保留两个端点 h 二态形成的 4 维空间，再用 massless 的 $00/11$、$10/01$ 关系构造显式 embedding/projection，把它约为一条边共享的 2 维因子；该因子同时进入两个顶点的 time-IBP，因而破坏按顶点独立的 tensor factorization，但保留全图 tensor-product 组装。余项以缩边 sector 的固定注入矩阵加入。

- [x] 从局部函数系统出发推导纯 massive 的 $M_{1,v}$、$M_{0,v}$，明确 $ik_{0,v}$ 是 1 维指数矩阵与其余单位矩阵的张量积。
- [x] 给出 massless full edge 的 4 态到共享 2 态的常数 embedding/projection，并逐项算出两个端点算子在同一 2 维因子上的相反作用。
- [x] 在全图空间上写出所有顶点 regular IBP 的统一 Kronecker 和、共同对角化、迭代矩阵和 dlog DE；不通过 sampled IBP 或 reduction 反推公式。
- [x] 把 theta/contact remaining term 写成从 lower sector 到 parent sector 的张量注入矩阵，并明确使用升幂关系中的 $R^{(1)}$ 构造 DE 非对角块。
- [x] 用 $z=-k\tau$ 的无量纲 h 方程解释 $h_{zz}+h=0$ 与 $\partial_\tau^2h=-k^2h$ 的等价性；区分内部 h-state、物理导数系数和实数化后的符号。
- [x] 清理旧 note 中把 massless full edge 与无 theta 指数因子混同、过早引入 orientation 求和或使用不一致 $F_{ab}$ normalization 的内容；重编译并逐页检查 PDF。

验收标准：全文从 1 维/2 维局部函数系统和全图 tensor space 开始；纯 massive 公式作为同一构造的特例出现；每条 massless full edge 从两个端点的 4 态严格约为一个共享 2 态，两个顶点矩阵使用同一 tensor slot；physical $k^2$ 只由 $\partial_\tau=-k\partial_z$ 恢复，不能污染无量纲 h EOM；迭代与 dlog 矩阵可由 topology 直接组装，contact 以 lower-sector block 保留；本轮不修改 package 源码或解除 `PendingRederivation`。

完成结论（2026-07-27）：`package-MadStree/Documentation/tree_formula.tex` 已按全图 Kronecker-sum 逻辑从头重写。纯 massive 的一维指数 slot 与二维 h slots 统一进入同一 tensor space；每条 `masslessFull` 先以两个端点四态出现，再约为整条边共享的二态，该 slot 同时进入两个端点的 time-IBP。固定 sector 的迭代矩阵、energy letters、dlog 对角块和 lower-sector contact 注入矩阵均可由 topology 直接组装。正文严格区分三层表示：论文 h-state 有 $F_{11}=+F_{00}$；package 抽出端点相位后的 quotient 有 $F^J_{11}=-F^J_{00}$，二者由 $D_\sigma=\operatorname{diag}(1,i\sigma)$ 连接；裸物理时间导数恢复 $\mathcal F_{11}=k^2\mathcal F_{00}$。package quotient 的端点 regular 系数为 $+i\sigma q/-i\sigma q$，contact 为 $-2/+2$；无量纲 EOM 始终是 $h_{zz}+h=0$，$-k^2$ 只来自两次 $\partial_\tau=-k\partial_z$。该阶段的旧 hash 已由上方 MadStree 首阶段完成记录取代；原 `000_code/018_dSIBP/` 的公式型 tree 接口仍保持禁用。

### 上一已完成任务：2026-07-26 Hankel convention 辨析（当前已由 MadStree 正阶输入 convention 收口）

这一阶段曾用负 Hankel order 复用 2401 公式；后续用户 convention 已明确替代该写法。MadStree 现在始终输入 $
u=|
u|$ 并使用正阶 $H_
u$，只保留一个函数名 $h(
u,0;z)=z^{s_
u
u}H_
u(z)$。缺省 $s_
u=+1$；2401 对应 $s_
u=-1$；全部公式由统一替换 $
u\to-
u$ 联系，不能只改 Hankel 下标。

- [x] 核对正负阶 Hankel 的 branch phase，确认无需为 $h$ 增设另一函数名。
- [x] 将程序输入收口为 $\nu=|\nu|$ 与正阶 $H_\nu$，把正负仅作为 prefactor convention。
- [x] 对缺省 $\nu=1/2$ 验证 $z^{1/2}H_{1/2}^{(1,2)}\propto e^{\pm iz}$ 和 $1-2\nu=0$。
- [x] 在 MadStree 实现中把 convention 冻结在初始化 context，并对正负选项分别做 H/h round trip。

验收标准：全文只写一个 $h$；Hankel order 固定为正的 $\nu=|\nu|$；缺省正 prefactor在 $\nu=1/2$ 直接得到指数核；2401 负 prefactor由同一初始化选项复现；所有 EOM、Wronskian、递推与 dlog 使用同一 context convention。

完成结论（按 2026-07-27 当前 convention 更新）：旧的负阶参数路线不再作为现行 package 约定。现行输入、手册和测试统一使用 $
u=|
u|$、正阶 $H_
u$ 和单一函数 $h$；缺省正 prefactor使 $
u=1/2$ 恰为指数核，负 prefactor只用于复现 2401。完整实现与最新手册验收数据见当前任务的首阶段完成记录。

### 上一已完成任务：2026-07-26 massless tree formula 与公式型 dlog DE 推导

在现已迁入 `package-MadStree/Documentation/` 的 ``tree formula'' 专项 note 中，以第一篇 dS IBP 文献 2401.00129 的 tree 迭代约化与 dlog DE 结构为起点，把 massless 传播子先写成半整数阶 Hankel building block 或等价指数核乘时间/动量幂，推导 quotient 后的递推关系、contact/shrink 非齐次项及其对 DE 的影响。允许先保留由 massless 额外关系诱导的有限求和，再判断求和能否闭式消去；必须特别分析一个顶点的 time IBP 如何把作用转移为另一端点的离散指标/导数状态，以及这种转移是否破坏文献 massive-only 递推的逐线因子化。

- [x] 从 2401.00129 提取 massive tree 递推、终止序和 dlog DE 的原始公式与适用假设，并核对项目现行 `J`、三槽 massless quotient、端点相位和 contact convention。
- [x] 分别尝试半整数阶 Hankel 表示与指数核表示，明确哪一个“指数乘特定幂次”的 building block 才对 time derivative 闭合。
- [x] 给出保留 massless 额外关系求和的阶段性 tree recursion/DE 公式，判断求和在何种图结构下可直接计算，以及一般情形为何可能破坏逐线因子化。
- [x] 形成 package 可采用的通用树图参考算法、优势/劣势、尚未证明项和后续验收路线；本轮不修改 `000_code/018_dSIBP/` 或正式交付。

验收标准：note 中所有文献公式都标明来源方程或章节；massless 原始物理导数、无量纲 quotient 与 `b`/fixed 模长因子严格分层；递推终止条件、contact lower-sector 项、有限求和指标域和 dlog 字母来源写清；能直接闭式化与只能保留算符/求和形式的结论明确区分；不得把推测写成已认证 package 能力。

完成结论（按 2026-07-27 当前 convention 更新）：`package-MadStree/Documentation/tree_formula.tex` 及同名 PDF 已形成自包含推导。采用 $(-\tau)^A$、有序端点、共享 quotient 和 $-2/+2$ contact convention 后，缺省 $h(1/2,0;z)=z^{1/2}H_{1/2}^{(1,2)}(z)\propto e^{\pm iz}$。每条未缩并 `masslessFull` edge 只贡献一个共享 quotient 二态；regular 顶点算子可由逐边 Hadamard 变换同时对角化，递推分母和 dlog letters 是带共享 orientation 的线性 energy sums。contact 是严格降 sector 的非齐次映射，完整约化为按 contact DAG 自动终止的有限 path sum。mixed/common-theta、一般 normalization 的 constant-residue 化、同 basis naive 对照和 exact flatness 仍未认证。

### 上一已完成任务：2026-07-26 唯一积分顺序与 `userMI` 公开 basis

先修正 backend 排序合同：`DSLinear` 返回或用户显式重排后的 `integralList` 是唯一积分顺序，`DSKiraPlan`、active-basis preparation 和 serializer 不得再次按 reference-style key 暗中排序。随后实现正式 `userMI` basis 功能，接受用户按顺序给出的单个 `J` 或齐次线性组合，保存 `J`、`userMI[i]`、backend ID/变量之间的可逆映射、秩与闭包状态；massive bubble example 的 reference master 选择必须改为调用该 package 功能，不在 example 中自写 basis adapter。

- [x] 审计 `DSLinear -> DSKiraPlan -> DSKiraExport -> DSKiraImport -> DSDE` 的积分顺序与 active basis 数据流，冻结唯一顺序所有权和兼容边界。
- [x] 实现唯一 `integralList` 顺序合同，删除 plan/export 的二次暗排序，同时保留用户显式重排入口。
- [x] 实现公开 `userMI` basis 构造与查询接口，支持有序 `J`/齐次线性组合、可逆映射、秩/闭包诊断和 backend manifest 持久化。
- [x] 把 pure massive bubble 的 reference 19-master 选择迁移到 package `userMI` 功能，删除 example 内重复实现。
- [x] 每完成一项同步更新 tech note 对应用户步骤，并在候选单文件重跑积分顺序 `8/8`、bubble `userMI` `16/16`。
- [x] 按兼容修订号 `018.1` 统一运行时版本、候选程序、用户手册、coverage manifest 与正式交付文件名；完成候选 PDF 视觉检查和正式路径复验后清理候选产物。

验收标准：同一 `linearData["integralList"]` 在 plan/export/import 全链字节顺序不变；显式用户重排只发生一次；合法 19 维 `userMI` basis 可双向恢复且 derivative closure/targets 使用同一顺序；massive bubble example 不定义私有 basis 变换函数；package 仍只生成/序列化关系，不运行 reduction。

顺序合同完成记录（2026-07-26）：新增公开 `DSReorderIntegrals[linearData,order]`，由用户在 backend 边界前显式改变 `integralList`、积分 ID 与线性方程 ID；结果保存顺序 authority 和 digest。`DSKiraPlan` 不再执行 reference-style 隐式排序，`preferredIntegrals` 仅记录/筛选已存在对象；公开 export 与底层 serializer 均移除 `KiraIntegralOrder` 选项并只读取输入 `linearData`。轻量两积分 smoke 通过 `8/8`，覆盖默认顺序、preferred 不重排、显式重排、方程 reindex、plan 继承、公开 API 和旧 option 移除。

`userMI` 功能完成记录（2026-07-26）：新增公开坐标 token `userMI[i]` 与 `DSUserMI[linearData,expressions,spec]`。输入可为有序单个 `J` 或齐次线性组合；package 在现有 `integralList` support 上构造精确系数矩阵，检查全体/active 行秩，选定 pivot `J`，保存 `userMI -> J` 与“pivot `J` -> userMI + 未替换 spectator `J`”双向规则、round-trip residual、mapping/order digest，再复用既有 active-basis 导数闭包与 backend ID。import 后公开 `masterTokens` 返回 `userMI[i]`，同时以 `backendMasterTokens` 保留 Kira token。bubble 真实积分表轻量 smoke 通过 `16/16`：21 个候选秩 21、前 19 个 active 秩 19、support 数 37、双向 residual 全零、formal closure/plan 同序；massive bubble 成品 example 已删除私有 basis adapter，只把 21 个 reference 候选交给 package `DSUserMI`。

发布完成记录（2026-07-26）：兼容修订统一发布为 `018.1`。正式单文件 `package_018.1.wl` 为 818532 bytes、SHA-256 `3F5F07778B3C6C0C3ED9260EBEB3A5A48FEAA3D9FE66EFF97F9AC59C50B46DAA`；正式用户手册 `package_018.1.pdf` 为 32 页、956474 bytes、SHA-256 `3C2910450A8D64F343C98A4992A6AC594B8931D244890368C2D024BAC797D613`。候选顺序检查 `8/8`、bubble `userMI` `16/16`、39 个公开函数的 API/example coverage `8/8` 均通过；正式路径 loader `3/3`、顺序 `8/8`、bubble `userMI` `16/16` 通过。手册 userMI/Kira 和跨页 API 表已渲染检查，无裁切、重叠或缺字，最终日志无 overfull 或未定义引用。本轮没有运行 Kira/reduction；旧 reduction 结果不因 manifest 变更自动获得重新认证。

### 上一已完成任务：2026-07-26 全项目文档与临时文件整理

统一当前权威文档、独立任务书、用户手册、example 说明和临时 todolist 的任务边界，删除或收缩已经取消、重复、过时或与现行程序不一致的叙述。`03_single_massive_sunrise/` 固定只生成 continuous indices 保持 general 的 IBP seeds 与 `{ss11,kE}` general 参数微分算符；手推与 package 对照均不得进入连续撒点、`DSGenerateIBP`、`DSLinear`、Kira、master、DE 或 scaling。清理文件前按用途、Git 状态和活动进程确认边界，只删除可重跑临时产物或明确废弃文件，保留用户未提交源码修改、权威文档、正式交付、报告和三个长期 examples。

- [x] 审计全部 Markdown/TeX/example manifest 中的 sunrise、当前版本、任务状态和目录边界，形成冲突清单。
- [x] 就地统一 plan/design/tech note、独立任务书、用户手册、README、example README/manifest；不在 `AGENTS.md` 重复技术推导。
- [x] 整理本文件和 `todolist_temp.md`：取消过时的 sunrise full-flow 待办，把旧过程明确划入历史归档，并把当前 backlog 收缩为发布、独立检验和三个通用设计项。
- [x] 盘点并清理明确临时、可重跑或已取消路线的文件；三个长期 examples、正式交付、报告、参考材料和用户未提交源码修改均保留。
- [x] 运行全仓残留扫描、文档/TeX 基本检查和 `git diff --check`，记录未处理项与保留理由。

验收标准：所有当前态文档明确 sunrise 只到 general seeds/operators；不存在把 sunrise 撒点、`linearData`、Kira、DE 或 scaling 写成当前/后续任务的未取消表述；临时文件清理有可复核清单；不覆盖或误删用户现有修改。

完成结论：plan/design/tech note、用户手册源、common-theta 专项清单、研究进度与根 todolist 已统一到 018 当前状态；旧 468/468、80/80 和 014/016/017 路线只标为历史证据。已删除空的根 `results_temp/`、被忽略且无引用的 `.qoder/` 与 `tmp/`、手册源目录下误留的空 `000_code/test/` 与 `check-smoke/results_test/` 树，以及本轮候选 TeX 编译产物；根 `check/`、`000_code/check/`、`000_code/test/results_test/` 按项目规则保留为空，`.agents/` 作为工具边界保留。两份 TeX 源在候选目录以 `pdflatex -halt-on-error` 编译通过且第二轮无未定义引用；未覆盖正式 PDF。全仓当前态残留扫描和 `git diff --check` 通过。

### 上一已完成任务：2026-07-26 项目体积盘点与过时产物清理

统计当前工作树清理前后的总字节数，并按目录、文件大小、Git tracked/ignored 状态和活动进程核对占用来源。只删除已取消路线、可重跑 checkpoint/cache/log、`results_temp/results_test` 运行产物及明确过时的候选交付；保留源码、权威文档、正式报告、当前 package/PDF、三个长期 examples 及用户未提交修改。清理完成后记录逐目标删除大小、总释放量、剩余主要占用和 `git diff --check`。

- [x] 记录清理前项目总大小、文件数及最大目录/文件：含 `.git` 共 80935283 bytes、6332 个文件；其中 `.git` 52409012 bytes，实际工作树 28526271 bytes。
- [x] 核对拟删除对象的用途、Git 状态和活动进程，形成精确删除清单：旧 `.qoder/repowiki` 缓存、早于当前 package/family 的 bubble `kira/userSystem` 与 `kira/result`、两套 TeX 编译中间文件；均为 ignored/untracked，且无 Wolfram/Kira/Fermat/Qoder 进程占用。
- [x] 删除确认过时或可重跑的产物：`.qoder/repowiki`、bubble `kira/userSystem`、bubble `kira/result` 及两套 TeX 中间文件，共 149 个文件、7937340 bytes。长期 bubble 的 active basis、dlog basis、初始化快照、export manifest、jobs/list、前后处理脚本，以及三个 examples 和正式 package/PDF 均保留。
- [x] 清理后含 `.git` 共 72998837 bytes、6183 个文件；实际工作树 20589825 bytes，净释放 7936446 bytes（与删除字节差来自本轮进度记录增长）。`git diff --check` 通过，未新增删除型 tracked diff。`.git` 仍占 52409012 bytes，其中大量 loose unreachable objects 可能承担历史未提交内容恢复；本轮不执行不可逆 `git prune --expire=now`。

### 上一已完成任务：2026-07-26 single-massive sunrise 收缩为 seeds 与微分算符

用户终止全无质量 sunrise 的 complete `linearData` 重新生成。长期 `03_single_massive_sunrise/` 恢复并固定为单 massive、双 massless 的两圈 family，但任务边界收缩为两类 general 符号对象：未对连续指标撒点的完整 IBP seed identities，以及由声明参量到基础动量不变量的 general 参数微分算符。独立手推也只推这两类对象；package 对照只调用初始化、`DSSeeds/DSAllSeeds` 和微分算符 metadata/公开入口，不进入 `DSGenerateIBP`、`DSLinear`、Kira、master 选择、DE 或 scaling。

- [x] 停止全无质量 complete producer；旧 single-massive Kira 工作树、1795 apparent masters、3000-target reduction、导数缺口及未闭合 scaling 结果继续作废。
- [x] 清理本轮 `results_temp/massless_sunrise_regeneration/`、全部 sunrise reduction/linearData/DE/scaling 中间产物及对应 WSL 工作区；保留 Kira 安装目录和三个长期 examples。宿主共删除约 3.81 GB。此前 Codex 沙箱用户下的 `wsl -l -q` 空结果不能代表真实 Windows 用户没有发行版；复核后确认原 `Ubuntu-22.04` 注册仍在，只删除了其中旧 sunrise/Kira 工作区。
- [x] 重写独立任务书中 single-massive sunrise 的 Phase 1/Phase 2 边界：手推和 package 检验都只覆盖 general seeds 与 general 参数微分算符，不撒点、不序列化、不约化。
- [x] 收缩 `03_single_massive_sunrise/main.wl`、README、coverage manifest、plan/design/tech note、用户手册与项目概况；保留 topology、ISP、zero point、parity、symmetry 和参数 convention，使手推对象可直接与 package 对接。
- [x] 运行轻量 fresh-kernel 检查：formal `package_018.wl` 得到 855 个 general templates、5 个 sector、2 个 time generators、6 个 momentum generators，微分变量为 `{ss11,kE}`，两个 general `ds` witness 均成功；未生成 sampled/linear/Kira/DE/scaling 对象。

发布边界：正式冻结单文件仍是现有 `package_018.wl`，其旧 massless 四态 source 枚举给出 855 个 templates；当前模块主线的 `00/10` quotient representatives 给出 275 个 templates，并通过纯符号 general-seed smoke。凌晨遗留候选早于当前模块改动且缺少同版本候选 PDF，已作为 stale candidate 删除。本轮不以两个 sunrise 轻量检查替代完整候选回归去覆盖正式 package；后续正式发布必须重新构建同源 candidate package/manual，并按发布门禁复验。

## 2. 历史记录归档

以下内容按日期保留为实现、错误修正和验收的历史证据。旧标题中的“当前任务”、旧“下一步”和未勾选项只表示当时状态，不构成当前 backlog；当前未完成工作只认本文件第 1 节。根目录旧 `todolist_temp.md` 已在单最新版清理中删除，不再作为并行规则源。其中所有 sunrise full-flow 叙述均已取消，不得据此恢复撒点、`linearData`、Kira、master、DE 或 scaling。

### 历史记录：2026-07-26 已终止的 sunrise 全数值 scaling 路线

该路线于 2026-07-26 按用户最新边界终止。下列计数只说明当时实际执行过什么，不再形成待办；相关 Windows/WSL 中间产物已清理，single-massive sunrise 现在只保留 general seeds 与 general 参数微分算符。

用户明确要求直接删除不满足纯数值 producer 合同的 911 MB checkpoint，不再保留为错误路线证据。本轮只删除 `results_temp/sunrise_scaling_validation/linear_data_isp03_a_m1_3_b_m1_3_checkpoint.wl`；删除前核对绝对路径、大小、SHA-256、Git 状态与活动进程。随后使用 `sunriseNumericPoint0` 从头生成 `a,b∈[-1,3]`、两个 numerator ISP `∈[0,3]` 的 complete system，要求 `seedResidualCoefficientVariables==={}`、`sampledCoefficientVariables==={}`，并确认逐关系去掉全部 `J` 后 coefficient `Variables==={}`。只有新 checkpoint 可进入 3000-target Kira 初探、实际 master basis、同一点导数闭包、`DSKiraImport/DSDE` 与 scaling relation；旧 workspace、旧 master 数和旧 reduction 不得复用。若实际 masters 的一阶导数超出当前包络，先报告精确缺口，不自行扩大范围。

- [x] 删除旧 911 MB checkpoint 并记录删除前身份；目标为 ignored 的 `results_temp/sunrise_scaling_validation/linear_data_isp03_a_m1_3_b_m1_3_checkpoint.wl`，删除前 911097530 bytes、SHA-256 `D837DA61DEF3F44031740E1686642D71D94A8F076FAE3D135180696654237121`，无 Wolfram/Kira 进程占用；删除后 `Test-Path=False`。
- [x] 重跑全数值 complete producer，验收范围、parity、symmetry、去重、完整性及 coefficient residual 全部闭合。275 个 templates 的 `seedResidualCoefficientVariables={}`；854378 个 candidates 经 odd/odd 接受 216856 个，`sampledCoefficientVariables={}`；`DSLinear` 去重为 115421 条关系、94275 个积分，删除 101435 条重复，`completeSystemQ=True`，ISP 严格 `[0,3]`、负 ISP 为 0。generation 634.42 秒、linearize 152.53 秒；新 source digest `057723fe9ec5084efbdd04d92b04c0c65257e59960cafbcdabdf676f160f1cb8`。新 checkpoint 915399234 bytes、SHA-256 `BF811248896CB206356EF9C4E6FE2FB20AB8F22BEC0D8B64EC4CDC8968642FF4`。
- [x] 从新 checkpoint 选择 3000 个有界 targets，在新的 WSL 工作区运行 Kira 2.3。115420 个非零输入方程中 36420 个为零、79000 个独立；递归选中 51799 条关系，得到 1795 个当前有限系统 masters，3000 targets 的 unreduced 为 0。Kira 总时间 1927.8 秒，外层 wall time 32:07.96，峰值 RSS 2405544 KiB；master IDs 位于 1--3000，最大编号间隔 61，无远端断层。只回收 `kira_list.m` 与 `masters`，SHA-256 分别为 `5A9EDD12786E509B85961489388FDE6268298ACA0753A76281B1E64607681EDF`、`DDD83154A620750624C6F4229AF5FCF3F0E8EE4762976EB3E60BA5E9A7A8F5AA`。
- 已取消：不再对 1795-master basis 构造导数闭包，也不再执行 import、DE 或 scaling。终止前已知的 7559 个映射缺口仅保留为历史失败边界。
- 已完成收口：相关脚本、进度和技术文档已改为 general seeds/operators 边界。


### 历史记录：2026-07-25 已终止的 single-massive sunrise scaling 验证

本节保留旧路线的实现与错误边界证据，不得据此恢复 sunrise 撒点、`linearData`、Kira、DE 或 scaling 任务。所有未完成步骤均于 2026-07-26 取消。

续跑登记（2026-07-26）：从用户指定的 `ISP∈[0,1]`、odd/odd parity、已定向 symmetry 和 `{ss11,kE}` 两标度重新开始，不读取两轮负向扩张 checkpoint 作为 producer 输入。先冻结 family normalization 与完整 target envelope，再生成 complete `linearData`；pre-reduction 只取不超过约 1000 个低复杂度 targets，formal reduction 只取实际 masters 及其一阶导数闭包。验收仍要求 Kira unreduced 为 0、`DSDE` 无残留 `J`/source 且 normalized basis 的符号 Euler residual 全零；任一阶段失败即记录首个失败对象并停在该边界，不以单个数值点替代符号结论。

术语确认（2026-07-26）：ISP 本身只指不可约标量积的 numerator，`ispN=n>0` 在被积函数中乘入该坐标的 `n` 次幂，`ispN=0` 表示不含该 ISP。package 对用户显式负整数不作输入阻断，但负值对应同一坐标槽的额外 denominator，物理上不再称为 ISP；当前 sunrise 的 `ispN∈[0,2]` 全部属于 numerator。

3000-target 续跑边界（2026-07-26）：按用户指定，下一轮把两个 numerator ISP 的 target range 先统一抬到 `[0,3]`，随后用 3000 个低复杂度 targets 重做 pre-reduction。ISP 不参加 parity：它对应不可约标量积的二次齐次 numerator 幂，不能据此对 `ispN` 本身附加奇偶筛选；sunrise 的 odd/odd 约束只作用于两组 line-power/endpoint-state 组合。重跑 producer 前必须从实际 sealed seed metadata 确认两圈一外方向的 6 个 momentum generators 全齐：四个 `q_m·∂_{q_l}` 与两个 `kL·∂_{q_l}`。

Momentum-generator/parity 专项（2026-07-26）：当前 855 个 sealed seeds 的 top sector 实际含 6 个 momentum generator 标签，逐项为 `{∂q1·q1,∂q1·q2,∂q2·q1,∂q2·q2,∂q1·kL,∂q2·kL}`，与 `L(L+K)=2(2+1)=6` 的理论集合完全相同。两条 requested parity vectors 的最后两个 ISP 槽均为 0，`ispParityFreeQ=True`；5 个可达 sector 的 affine odd/odd constraints 全部启用并保持。检查不撒连续指标点、不生成 linearData、不运行 Kira。

范围重置（2026-07-26）：按用户最新指定，single-massive sunrise 的完整 producer target envelope 改为两个顶点幂 `a∈[-1,3]`、三条 line power `b∈[-1,3]`、两个 numerator ISP `ispN∈[0,3]`。旧的 `a∈[0,1]、b∈[-2,2]、ispN∈[0,3]` checkpoint 与其 3000-target pre-reduction 只保留为上一轮边界证据，不再作为本轮 producer、master basis、DE 或 scaling 输入。`seedRanges` 只保存构造 general templates 所需的最小离散起点，不等同于最终 target envelope；完整撒点域继续由 `DSGenerateIBP` 从目标包络逐生成元反推，并在生成元作用前应用逐 sector affine odd/odd parity。两个 ISP 槽的 parity 系数严格为 0，不对 `ispN` 做奇偶筛选。massive line 固定使用 `h` mode。

纯数值 seed 更正（2026-07-26）：sunrise 当前演示以降低约化复杂度为目标，IBP seed/撒点关系必须在 producer 层使用同一个固定非奇异精确有理点把 `dim`、zero-point、mass/normalization 参数以及 `kE/ss11` 全部数值化，不得把 `kE/ss11` 留到 Kira serializer 才代入。验收分两层：`DSSeeds[...,ApplyNumericRules->True]` 的 `seedResidualCoefficientVariables` 必须为空（general `a/b/ispN` 只作为连续 seed indices 单列，不算物理残量）；`DSGenerateIBP` 代入连续 seed 点后，收集关系中全部不同的 `J[...]`、提取各自系数与常数项并精确重构原关系，这些系数的 `Variables` 必须为空，且汇总字段 `sampledCoefficientVariables==={}`。现有 911 MB checkpoint 的该字段为 `{kE,ss11}`，因此只证明范围/parity/去重，不满足纯数值 producer 合同，不再进入本轮 Kira、DE 或 scaling。

纯数值 seed 专项完成（2026-07-26）：`family_conventions.wl` 统一定义固定点 `{dim,alpha,betaM,beta0,nuM,etaNu,kE,ss11}={37/11,5/7,7/13,11/17,19/23,29/31,31/19,37/23}`，正式 sunrise example、complete producer 与 smoke 共用该规则。模块入口 fresh-kernel 得到 275 个 quotient templates；`seedResidualCoefficientVariables={}`。对 96 个 top qIBP templates 只取一个 `a/b` 为零、ISP 输出保持非负的小点，得到 24 条非空数值关系；`sampledCoefficientVariables={}`。独立系数审计对每条关系收集全部不同 `J`，逐个提取 `Coefficient` 与常数项，24/24 精确重构原关系，系数 `Variables` 合集为空。20 模块重建的候选单文件为 809346 bytes；正式 sunrise example 显式加载候选后得到 275 templates、1 条限定单点关系，两个 residual 字段均为空，ISP target 为 `[0,0]` 与 `[0,2]`。正式交付单文件尚未覆盖，需与更新后的候选手册一起完成发布检查后晋升。

Massless endpoint seed canonicalization（2026-07-26）：无质量传播子的端点态 `11` 与 `00`、`10` 与 `01` 由代数关系连接，不是 IBP 关系；`n_i∈{0,1}` 的遍历属于 `DSSeeds` 的离散 seed 枚举层，而不是 `DSGenerateIBP` 的连续指标撒点层。package 必须在 seed producer 内按 line orientation 与现行 massless convention 选定稳定代表，只生成每个代数等价类的一种端点态，同时保留被消去状态到代表态的完整精确系数。特别是 `11 -> 00` 在独立外动量情形可能含参数系数，不能依赖关系生成后的表达式相等或 `DSLinear` 去重来消除。当前正在运行的旧四态 sunrise producer 已终止且不写 checkpoint；实现后先做小范围专项 smoke，确认 seed 数、代表态、参数系数、time/momentum generator 覆盖与下游关系等价，再按 `a∈[-1,3]、b∈[-1,3]、ispN∈[0,3]` 重跑。

Massless endpoint seed canonicalization 完成（2026-07-26）：018 保留公开三槽 `{b,n1,n2}`/`{"F",n1,n2}`，但 `DSSeeds` 对每条 masslessFull source line 只生成 `n2ToZero` 的 `00/10` 两个代表；massive 仍生成四态，cross/shrunk 不生成离散态。raw 四态数、代表态数、删除数和 canonical direction 写入 `seedTemplateSummary -> discreteStateAudits`；完整性证书按代表态数验收，不把 quotient 已删除状态误报为缺失。原始物理 `{11}=+q^2{00}` 的两个模长因子仍由既有分层承载：cycle line 每次导数作 `b->b-1`，fixed/独立外动量 line 每次显式乘结构化模长参数；内部无量纲 quotient 保持 `F11=-F00`，不得脱离该系数链单独解释。`check_massless_seed_quotient_representatives.wls` fresh-kernel 通过：sunrise 总模板 `855->275`、top `512->128`，top raw/代表/删除为 `64/16/48`，source 仅 `00/10` 且 canonical 方程无 `n2=1`；6 个 momentum generators、odd/odd 与 ISP parity-free 保持。fixed massless timeOnly source 仅 `00/10`，并精确得到 `du=+i sE1`、`dv=-i sE1`、`du^2=-sE1^2`、`du dv=+sE1^2`。

最终范围 complete producer（2026-07-26）：使用新的 275 个 quotient-representative templates，source target envelope 固定为两个 `a∈[-1,3]`、三条 `b∈[-1,3]`、两个 numerator ISP `∈[0,3]`。`DSGenerateIBP` 枚举 854378 个候选点，逐 sector odd/odd 接受 216856 个；ordered symmetry、同一精确有理规则与数学方程 canonical 后，`DSLinear` 去重为 115421 条关系、94275 个积分，删除 101435 条重复。5 个 sector 的 derived seed 域均非空，`completeSystemQ=True`，ISP 严格 `[0,3]` 且负 ISP 为 0，残留 coefficient variables 仅 `{kE,ss11}`。积分表总体 `a∈[-2,6]` 来自 lower-sector compact 顶点把两个 source `a` 相加，不改变 source envelope 的两条 `[-1,3]`；line power 仍严格 `[-1,3]`。source digest `869c2e1037b57a0abc32bc1be9de9e30751e5f5feca84f160aa1cbf0c12b3da5`；producer 461.26 秒、linearize 107.95 秒；checkpoint 911097530 bytes，SHA-256 `D837DA61DEF3F44031740E1686642D71D94A8F076FAE3D135180696654237121`。下一步只取 3000 个低复杂度 targets 做 Kira 初探。

ISP `[0,3]` complete producer（2026-07-26）：保持 `a∈[0,1]`、三条 line power `b∈[-2,2]` 不变，两个 numerator ISP 取 `[0,3]`。373956 个候选点经 odd/odd 接受 89728 个，`DSLinear` 去重为 22753 条关系、16129 个积分，删除 66975 条重复；855 个 templates 全部非空，ISP 指数严格在 `[0,3]`、负 ISP 为 0，`completeSystemQ=True`。source digest 为 `797938d8c48136f38470f26bd5db8d28b76008e3ddfa29617301dedec40c250b`；producer 194.94 秒、linearize 29.83 秒，checkpoint 约 164 MB。下一步按用户指定只取前 3000 个低复杂度 targets。

3000-target pre-reduction 导出（2026-07-26）：从上述同源稳定顺序取前 3000 个 targets，在唯一精确有理点导出 22752 条非零 Kira 方程，用时 65.09 秒。serializer 返回 `ready`、`pureRationalBackendQ=True`，backend coefficient variables 为空，文本无 `I`、`dsii`、`Complex` 或 numeric dummy；下一步运行一次 Kira 初探并记录实际 masters 与 unreduced。

3000-target pre-reduction 结果（2026-07-26）：Kira 2.3 使用 10 个 Fermat instances，约 24.1 秒完成；22752 条输入中 12509 条为零、10243 条独立，3000 targets 的 unreduced 为 0，返回 2363 个 masters。生成元专项已确认 6 个 `q·q/q·kL` 全齐且 ISP 不参与 parity，因此高 master 数暂归因于有限 `b∈[-2,2]` envelope 的边界。直接把 2363 basis 附加到完整 linearData 的 closure probe 代价过高；下一步按 8 个 chunk 直接调用与 `DSDE` 同源的 sector-aware `ds`，再用同源积分映射统计缺失 targets。

非负 ISP 首轮边界（2026-07-26）：固定 `a∈[0,1]`、三条 line power `b∈[-2,2]`、两个 ISP `∈[0,1]` 后，producer 枚举 70452 个候选点，odd/odd 接受 17184 个，`DSLinear` 去重为 3841 条关系、3292 个积分，删除 13343 条重复；全部积分的 ISP 指数严格在 `[0,1]`。但完整 template set 中存在反推 seed 域为空的 generator，artifact 为 `subsetQ=True`、`completeSystemQ=False`，故本批不得进入 Kira。下一步只把两个 ISP 上界抬到 `2` 以容纳完整 shift span；不降低 ISP 下界，也不扩大 `a/b`。

非负 ISP complete producer（2026-07-26）：保持 `a∈[0,1]`、`b∈[-2,2]` 不变，仅把两个 ISP 上界抬到 `2` 后，855 个 sealed templates 全部得到非空 seed 域；189303 个候选点经 odd/odd 接受 45682 个，`DSLinear` 去重为 11183 条关系、8788 个积分，删除 34499 条重复。全部 ISP 指数严格在 `[0,2]`，`completeSystemQ=True`，source digest 为 `11f6aba745d70ab45aa44621622287e5424d646e7d09616c0bd274b6a7426ef7`；producer 97.97 秒、linearize 14.64 秒。下一步只从稳定排序中取前 1000 个低复杂度积分作 pre-reduction targets。

Pre-reduction 导出（2026-07-26）：从上述同源 checkpoint 的稳定积分顺序取前 1000 个 targets，在唯一精确有理点导出 11182 条非零 Kira 方程，用时 30.63 秒。serializer 返回 `ready`、`pureRationalBackendQ=True`，backend coefficient variables 为空，文本审计确认没有 `I`、`dsii`、`Complex` 或 numeric dummy；下一步只运行该 workspace 读取实际 master 列表。

Pre-reduction 结果（2026-07-26）：Kira 2.3 使用 10 个 Fermat instances，约 8.5 秒完成；11182 条输入中 6004 条为零、5178 条独立，递归选择 1842 条关系，1000 targets 的 unreduced 为 0，返回 973 个 masters。该高 master 数表明有限 `b∈[-2,2]` envelope 仍保留大量边界 masters，不直接作为 formal basis 结论；下一步只构造这 973 个实际 masters 的 `{ss11,kE}` 一阶导数闭包并统计缺失范围，再决定是否只扩 `b` 或 ISP 上界。

- [x] 修正 ISP 指数合同：ISP 的定义零点固定为 `0`；负指数在物理上对应额外 denominator，但 package 不阻断用户显式选择的负 `ispN` 范围。package 自动从 target envelope 反推 seed 域时不得把用户下界继续向负方向扩，只能保持下界或取更高幂；`n=0` 求导必须先精确为零。
- [x] 作废并隔离两轮未经用户指定而向负方向扩张的 sunrise 数据：首轮 `ISP∈[-1,1]` 的 11183 条关系、8502 个积分、853 masters 及导数闭包诊断，以及扩张轮 `ISP∈[-2,2]` 的 421859 条关系、165857 个积分和 3.146 GB checkpoint，均不得继续作为当前 numerator-only sunrise 的 Kira、DE 或 scaling 验收证据。保留文件只作为错误路线证据；这不构成 package 对用户显式负范围的阻断。
- 已取消：不再重启任何 sunrise 连续指标 producer 或 Kira 初探。
- [x] 撤销未配置 `parityConstraints` 的 sunrise producer/checkpoint：从 855 个 general seed templates 的全部 time/momentum shift 在 GF(2) 上确定两个独立 parity generator，固定为 `odd, odd`，并验证 root 约束经 contact affine map 传播到每个可达 sector；旧无 parity 关系及其 target 统计不再进入 Kira。
- [x] 固定 sunrise 对称性 canonicalization 的方向：用户提供的顶点交换与 massless-line/ISP 交换规则必须先按等价类选定唯一 canonical representative，只允许非 canonical 对象单向替换到该代表，禁止同时出现 `A->B` 与 `B->A`。未来可在 package 中提供一个低优先级 helper，按缺省复杂度排序把用户给出的未定向等价关系自动定向；该 helper 不属于本轮 scaling 验收或 018 发布门禁。
- 已收缩完成：`03_single_massive_sunrise` 保留 `{ss11,kE}`、单 massive/双 massless、顶点交换和 massless line/ISP 同步交换 convention；不再定义该 example 的 Euler scaling 验收。
- [x] 错误路线留档但作废：曾用 `b∈[-4,4]`、ISP `∈[-2,2]` 构造 6349279 个候选点、1554446 个 parity 接受点，`DSLinear` 去重为 421859 条关系并删除 1132587 条重复，覆盖 165857 个积分；checkpoint 为 3146107241 bytes，SHA-256 `08C028D2F1EBFF042C5621C6E59A07AB916303B9E8A1F3FF8D2682B1ADD40962`。该广义负幂 family 并非程序非法，但负向扩张不是用户为当前 sunrise example 指定的范围，故不得继续作为该例的验收输入。
- 已取消：不再为 sunrise 导出 Kira 输入或运行外部 reduction；旧 `results_test/sunrise_scaling_validation/` 已删除。
- 已取消：不再为 sunrise 调用 `DSKiraImport`、`DSDE` 或 `DSScaleCheck`。
- [x] 补齐通用 loop-topology scaling degree：`ScalingRelation->"LoopTopology"` 按 root 圈数、目标 sector 活动顶点、`a/a0`、`b/b0` 或 `bS/bS0`、ISP 的二次齐次次数以及完整 sector prefactor `N_s` 逐 master 计算；非齐次 `N_s`/master 组合失败，bubble 专用 reference 路线保持不变。fresh-kernel sunrise 轻量检查 `7/7`，top 次数为 `2 dim-2 alpha-betaM-2 beta0-2`，单个 ISP 幂增加 1 时次数精确增加 2，5 个可达 sector 均得到非失败次数；fixed-line `N_s=(u+v)^(2 nu1)` 检查扩为 `11/11`，自动 normalized-J 次数精确为 `-2`。
- 已完成收口：sunrise example/README、plan/design/tech note、用户手册、TODO 与本进度统一为 general seeds/operators。

验收标准：只有 complete producer、外部 reduction 的 0 unreduced、`DSDE` 无残留 `J`/source，以及 normalized basis 的 Euler scaling residual符号全零同时成立，才可写“sunrise scaling relation 已验证”。

阶段记录（2026-07-25）：固定点 producer 已得到 855 个 templates、189303 条关系；`a∈{0,1}` 与 `a∈{-1,1}` 两个 complete checkpoint 分别含 35027 与 75550 个积分。首次从 checkpoint 续跑 formal export 时出现 17 个 derivative targets、缺失 7 个；同一 `check-smoke` 分别加载模块入口与未修改的正式 `package_018.wl` 均通过 `5/5`，公开 `ds` 和 active-basis wrapper 都给出同一组 16 个 canonical targets。根因是 consumer 只读回包含用户 symmetry 规则表达式的 checkpoint，却没有重新加载定义 `sunriseCanonical` 等 helper 的 `family_conventions.wl`，因此顶点交换规则不能求值。下一步先让 checkpoint consumer 恢复同源 family convention，再判断剩余 target-envelope 边界；当前没有 package 代码缺陷结论，Kira 尚未运行。

单积分 closure 诊断记录（2026-07-25）：恢复 `family_conventions.wl` 后，较小的 `a∈{0,1}` checkpoint 对一个人为候选积分得到 1 个 active token、16 个 derivative targets、总 targets 17、缺失 0，并可导出 150556 条方程。这 17 个 targets 只来自“单个候选积分加其 `{ss11,kE}` 导数闭包”，不是 sunrise master 数，也不是 formal active basis；相关脚本与结果不得用于完整 DE/scaling 验收。正确顺序是先让 complete 方程系统配合不超过约 1000 个低复杂度 targets 做 Kira 初探，从 Kira 实际 master 列表建立完整有序 basis，再导出该 basis 及全部一阶导数闭包。首次单积分导出还显示 backend variables `{ike,dsc2,dsii}`、`pureRationalBackendQ=False`、backend text audit failed，因此没有启动 reduction。

Kira 实数化修复登记（2026-07-25）：任何 family 进入 Kira 前，实数化都必须从 topology/line 的 phase dependency metadata 结构性识别每个 massless propagator 动量原子 `k`，定义单个实 backend 变量 `ik` 并执行 `k -> -I ik`，不得按符号名猜测；这一替换本身就是 Kira-only 实数 convention。现有 exporter 只对无变量全数值系统运行 Gaussian phase gauge，含实符号变量时会把残余 `I` 写成 `dsii` 并仍可返回 `ready`，这是通用 serializer 缺陷。修复后所有剩余整体虚相位都由可逆逐积分 phase gauge 消除，任何 `I/dsii/Complex` 残留都在写 `ibp.kira` 前拒绝。初次 Kira 探测不得 target 全积分表，按预估 master 规模设有界候选（无更具体依据时不超过约 1000）；formal 阶段的 targets 必须在实际 master basis 确定后取“全部 masters 加其一阶导数闭包”，不得沿用单积分诊断的 17 个 targets。实现与检查直接参考已经完成的 massive bubble 路线，不重新构造另一套 convention。当前模块入口的最小 serializer smoke 已通过 `16/16`：同时覆盖 `P0 -> -I ip0`、数值截面、普通导数 Jacobian、Euler 不变、含实符号参数的 phase gauge、纯数值输出、实虚混合负例以及最终文本无 `I/Complex/dsii`；这只确认实数化模块边界，尚不替代 sunrise formal export/Kira 闭环。

Parity 纠正登记（2026-07-25）：当前 sunrise full-flow case 的 `parityConstraints` 为空，故虽然 `DSGenerateIBP` 在撒点前调用了 parity prefilter，实际 189303 个候选点全部通过；逐点 symmetry 已执行，但规范化后的重复 IBP 也未删除。这批 checkpoint 及其 17/1000-target 规划全部作废。新路线必须先由 general seed shift 证明两个独立 GF(2) generator，固定 `odd, odd`；每个 sector 使用由 root constraint 经 compiled contact map 推导的 affine offset，不能把 top 点域直接复用到 lower sector。逐点完成 symmetry、数值代入和规范化后再 `DeleteDuplicates`，并分别记录候选点数、parity 接受点数、去重前/后关系数。

Symmetry 定向登记（2026-07-25）：参考代码采用“先给等价类排序，再向选定代表单向替换”的规则。当前 sunrise 也必须遵守这一边界；package 只消费用户已经排序、已经定向的 `symmetryRules`，不对双向规则做反复替换。自动把未排序等价关系按缺省积分复杂度与稳定字典序定向，可作为后续低优先级易用性优化单独设计，但不得阻塞 odd/odd producer、Kira、DE 或 scaling relation 验收。

数值撒点修复记录（2026-07-25）：`DSSeeds[...,ApplyNumericRules->True]` 现在在 general template 密封前逐线性项代入精确规则并约分，同时把仍出现的系数对象拆成合法的 continuous seed indices 与其它 residual variables；不再把尚未撒点的 `a/b/bS/ispN` 误报成参数残留。`DSGenerateIBP` 固定按“parity 预筛、连续指标取点、用户已定向的 symmetry、同一精确数值规则再次代入化简、残留变量审计”处理每条关系，`DSLinear` 再按数学方程去重并返回去重前、后和删除数。单模板定向测试得到 `candidate=4`、`parityAccepted=1`、撒点后 residual 为空、symmetry 幂等、线性方程 `1 -> 1`；下一步从新 odd/odd producer 生成完整数据。

Odd/odd producer 与 Kira 初探记录（2026-07-25）：完整 producer 从 855 个 general templates 枚举 189303 个候选点，odd/odd 与各 sector affine parity offset 筛选后接受 45682 个点；ordered symmetry、精确数值化和方程 canonical 后，`DSLinear` 从 45682 条 source equations 去重为 11183 条方程，删除 34499 条重复，覆盖 8502 个积分，source digest 为 `79d12c7cc382ad81ce0c3e23a53aacc7c1be55b1408bebb01c0ec4970eff22a2`。pre-reduction serializer 写出 11182 条非零纯有理 Kira 方程，取排序后的前 1000 个低复杂度积分为有界 targets；文本无 `I/dsii/Complex` 或数值 dummy。Kira 2.3 在 `FERMATPATH=/home/jiaqichen/Softwares/ferl6/fer64`、10 并行、triangular/back-substitution 开启且 FireFly 关闭时，用时约 8.6 秒，选择 1509 条方程，返回 853 个实际 masters，1000 targets 的 unreduced 为 0。这里的 853 只定义下一步 formal active basis；尚未构造其全部 `{ss11,kE}` 一阶导数闭包，不能据此宣称 DE 或 scaling 已通过。

首次全 master 导数闭包诊断（2026-07-25）：同源 `repkira2J.m` 已把 853 个互不重复的 master IDs 全部恢复为有序 `J`，无未映射对象。对这 853 个 `J` 构造 `{ss11,kE}` 一阶导数时，当前 8502-integral 包络精确缺少 3794 个 canonical targets；缺失对象的连续指标范围为 `a∈{0,1}`、所有 line power 合计范围 `b∈[-4,4]`、ISP `∈[-2,2]`，endpoint 状态仍严格为 `{0,1}`。下一轮只把原 producer 的 `b∈[-2,2]`、ISP `∈[-1,1]` 定向扩至这两个实测范围，`a`、parity、symmetry 和离散态不变；扩张后必须重新做有界 pre-reduction 并读取新的 master 列表，旧 853 不直接作为 formal basis。

ISP 边界纠正（2026-07-25）：上一段闭包诊断及其 853-master 输入来自维护者自行选择的 `ISP∈[-1,1]`，扩张 checkpoint 又继续降到 `ISP∈[-2,2]`；这一负向扩张不属于用户为当前 numerator-only sunrise example 指定的范围，因此只能保留为错误路线历史，不能再指导该例的包络扩张。已停止仍在加载扩张 checkpoint 的 Wolfram 进程；下一轮必须从 `ISP∈[0,1]` 重新生成，若需要更高幂只扩上界。其它用户显式请求负范围时 package 仍照常生成。

ISP package 边界二次纠正（2026-07-25）：上一版把“ISP 零点为 0、负幂在物理上是额外 denominator”错误实现成了用户输入门禁。用户现明确 package 不得阻断显式负范围；需撤销 `DSInit`、`DSGenerateIBP`、生成后 `J` 与公开 shape 层的负值拒绝。保留的实现约束只有：metadata 中 ISP 定义零点固定为 0；自动反推 seed 域不得低于用户给定 target 下界；指数 0 的 ISP 求导先精确返回 0。此前 `13/13` 只证明了错误门禁按实现工作，不能作为最终验收结论。

ISP package 最终实现记录（2026-07-25）：所有负值拒绝已经撤销；Association 输入即使携带其它 `zeroPoint` 也统一规范为 `0`，但 `ispData` range、全局 seed range、显式 `DSGenerateIBP` target 和负 `J` 均照常接受。`dsDerivedSeedRangeAudit` 对 ISP 使用 `Max[target 下界, shift 逆像下界]`，因此不会为了覆盖升幂项擅自向更负方向取 seed；用户若显式给负 target 下界则完整保留。`n=0` 的 momentum/external ISP 导数仍先返回 0。更正后的 `check_isp_numerator_contract.wls` fresh-kernel 通过 `14/14`：显式负 target 生成的 ISP 值为 `{-1,0,0,1}`，正/负用户下界都保持，负 `J` 可由 `rep2Integrand` 原样还原。同轮参数微分通过 `11/11`、loop-topology scaling degree 通过 `7/7`；sunrise 的两个 odd parity generator 在全部 5 个可达 sector 保持，855 个 general templates 的 seed coefficient residual 为空。源码静态审计没有负 ISP 拒绝消息或 shape gate；未运行 Kira 或 reduction。

### 当前任务：2026-07-25 固化三个典型 example 并修正 tadpole 圈数边界

- [x] 审计当前 root topology 圈数算法对普通边、平行边、自环和多连通分量的处理，明确“两顶点单边”和“一顶点 tadpole 自环”的区别；圈数使用 `L=E-V+C`，候选 topology smoke `7/7`：普通两顶点单边为 0 圈、自环 tadpole 为 1 圈、三平行边 sunrise 为 2 圈。
- [x] 长期保留三个典型成品 example：pure massive bubble、mix bubble+tree、single-massive sunrise。前者保留 dlog basis 与既有 reference 对照；后两者按限定物理配置整理为正式 examples，未扩张为全 family 检验。
- [x] mix bubble+tree 固定为一条 massive cycle propagator，其余 cycle/bridge propagators massless，用于覆盖 `kE/kL`、无圈参量定义、massless 新 convention，以及 cycle 与 bridge propagator contact/shrink 的复合数据流。
- [x] single-massive sunrise 作为唯一 sunrise example，固定两顶点、三条平行传播子、仅一条 massive line，并包含 ISP；当前只覆盖多边图圈数、ISP 闭合、mixed massive/massless general seeds 与参数微分算符。
- [x] sunrise 保留圈外动量 `kL`，并把两个顶点的外腿能量统一为同一个 `kE`，对应 general 微分变量 `{ss11,kE}`；通过公开 `symmetryRules` 实现顶点交换，以及两条同类 massless 平行传播子连同对应 ISP 指标的同步交换。canonical witness 精确得到 `J[{0,1},{{0,1,0},{1,0,1},{2,1,0}},{1,3}]`，未照搬 bubble 的 parity 或 R1/R2 规则。
- [x] 把三个案例及选择理由整合进 plan/design；用户手册只给使用入口、输入和流程，不写项目为何选择这三个案例。已同步 example README、coverage manifest、项目 README/规则和 TODO；候选 API/source coverage `37/37`，`git diff --check` 通过。
- [x] 修复本轮 example 预检暴露的两个 018 边界：共同-theta contact 的原子系数精确为零时先删除该选择，避免 `0 J[...]` 进入 contact normalization 后污染 `dtau`；不存在可重定义动力学坐标时，`DSInit`/`DSParameterNotation` 输出缺省行为说明而不是拼接 `None`。未新增运行时门禁；候选 mix 复验确认 `n=0` contact 不含 `$Failed`。
- [x] sunrise 只调用 `DSSeeds/DSAllSeeds` 构造可达 sector 的 general 模板，并读取 `{ss11,kE}` operator metadata/公开 `ds` witness；不再选择单点模板，不调用 `DSMetaSeedRange/DSGenerateIBP/DSLinear`。mix 候选仍独立确认 bridge `n=1` 收缩、`n=0` 零 contact、canonical momentum pack 与 `ds` 系数项。
- [x] 明确 massive bubble 的既有 `361/361` reference 对照已在 package 关系上应用顶点交换、两条平行传播子交换、`R2 -> R1` canonical 与固定 parity；sunrise 只复用公开 symmetry 模块的数据流，不把 bubble 特有的 parity/sector 规则误写成 sunrise 对称性。

验收标准：自环 tadpole 的图论圈数为 1，普通两顶点单边为 0，三条平行边 sunrise 为 2；三个长期 example 都有明确入口、限定配置和 manifest 记录，且不把 smoke 或旧运行结果冒充正式 example 闭环。

正式状态：候选与正式 `package_018.wl` 均为 794,809 bytes、SHA-256 `E137F7259B429D2F181E1A8BA101C36A42CEFE2145975F1900A24FDFE8B3EA06`；候选、交付与权威 `package_018.pdf` 均为 32 页、947,312 bytes、SHA-256 `FCF95B34B7F0ADCE037E5CB3029F239A78DA8AA50793F21AEEDD8A488BBC0EBA`。受影响页面渲染无裁切、重叠或缺字；候选及正式路径 sunrise、mix、topology `7/7`、massless endpoint/contact `7/7` 均通过，API/source coverage `37/37`。本轮未运行 Kira 或 reduction。

### 当前任务：2026-07-25 清理过度扩张 check 并按功能重组

- [x] 盘点项目内现有 check 脚本、目录、Git 状态和活动进程；逐项区分 general 种子、参量微分算符、scaling relation、小范围功能验证、大面积手推撒点、全流程扩张、过时版本和重复 wrapper。清理前根目录 `check/` 含两套旧 full-audit、共约 38 MB；`check-smoke/` 含 016/017、31 个 018 脚本及约 81 MB 运行产物；无 Wolfram/Kira/Fermat 活动进程。
- [x] 删除根目录 `check/` 的全部旧 expected、全 family 手推、mutation、Kira/reduction 和运行结果；删除 `check-smoke/` 的全部 016/017、expanded-envelope、full-workflow、reference producer/adapter、Kira post、coverage/release gate、失败副本和其它过时脚本/产物。正式 package、examples、reference 资产和独立报告未删除。
- [x] 在根目录 `check-smoke/` 建立五个明确功能子目录，只保留 general IBP seeds、参量微分/sector prefactor、单点 scaling、massless endpoint/contact 和 Kira energy convention；脚本与临时输出分离，不把 smoke 结果作为主线或独立 expected。
- [x] 同步 `AGENTS.md`、`check-smoke/README.md`、独立 README/任务书、研究进度与 TODO，记录保留/删除边界。静态检查确认根目录 `check/` 条目数为 0，`check-smoke/` 只含 README 和五个功能脚本，保留脚本不调用 `DSGenerateIBP/DSLinear/DSKiraExport/DSKiraImport/DSDE`，旧名称与重型路线无残留；WLS 原始括号计数、Markdown fence 和 `git diff --check` 均通过。本轮未运行 Wolfram、Kira 或 reduction。
- [x] 长期保留 `independent-benchmark/package/examples/04_pure_massive_bubble_closed_loop/` 作为完整成品 example：`main.wl` 覆盖 package 初始化、seed、关系、`linearData`、package `DSUserMI` 和 Kira export，外部 Kira 后由 `post_kira_check.wl` 完成 19-master import、DE 与 scaling；`reference_user_mi_basis.wl` 只提供 reference 候选数据，不自建 basis adapter。该完整路线不属于可清理 smoke。

验收标准：根目录 `check/` 为空并留给下一轮独立会话从头执行；`check-smoke/` 不再保留大面积手推撒点或过时 check，每个子目录只验证一个明确功能；完整 massive bubble 到 scaling relation 固定保留为正式 example；所有删除目标均已确认用途和 Git 状态，用户无关改动不受影响。

### 当前任务：2026-07-25 收缩独立验证边界并撤销误执行

- [x] 明确完整关系经 reduction 构造 DE 后的精确 scaling relation 是 package 流程的闭环验收；IBP、basis/normalization、DE 任一错误都会进入 scaling residual。Phase 1 不执行该流程；Phase 2 必须在任务书点名的限定 family/branch/parity 内基于正式 package 完整执行。
- [x] 用户确认按限定项目收缩任务书：Phase 1 只手推未代入 seed 点的 general IBP seed identities 与参量微分算符；Phase 2 才按点名 family/branch/parity 运行 package 全流程。必要 convention 必须在首次使用处完整写清，避免手推完成后再做未声明 adapter。
- [x] 重写第 10、12--18 节：第 16 节只保留双 loop-Gram、fixed/dependent binding 与同源 exact/under/over 代表；第 17 节保留结构合同和 fixed bubble+tree，删除原第 17.5 节全-family 扩张，收缩 `N_s/contact/parity` 到点名 family；第 18 节只作两阶段总结，不新增任务。
- [x] 在任务书末尾新增第 19 节“验证功能覆盖索引”，逐项记录功能、convention、定义章节、验证章节、函数族、固定 branch/parity、Phase 1/2 覆盖和排除边界。
- [x] 同步 `independent-benchmark/independent-benchmark.md` 与 README 的两阶段边界：Phase 1 由新 subagent 在任务书固定范围内推导未代入任何 seed 点的 general symbolic IBP seed identities 与参量微分算符；Phase 2 才加载正式 package 比较并执行点名流程。
- [x] 明确 Phase 1 禁止撒点、`DSGenerateIBP`、`DSLinear`、Kira/reduction、DE matrix、scaling 数值点和 expanded-envelope；Phase 2 的运行结果不得反向成为 Phase 1 expected。
- [x] 明确 scaling relation属于 Phase 2 基于 package、外部 reduction 与 `DSDE` 的闭环验收，不属于 Phase 1 手推对象，也不改变 package 只生成和序列化关系的边界。
- [x] 停止本轮误启动的独立 post 与全部 dSIBP Wolfram 子进程；本轮 `codex-independent-benchmark/` 的推导、撒点、reduction 与 post 结果全部作废，不进入正式报告、发布门禁或 Git 提交。
- [x] 清理误执行产生的独立工作区：删除前确认解析路径位于项目内、8,650 个文件均未被 Git 跟踪，且没有本项目 Wolfram/Kira 进程；`codex-independent-benchmark/` 已删除，未把其中 expected/results 纳入交付。

验收标准：Phase 1 不生成数万关系或运行后端，只冻结限定情景下未撒点的 general IBP seed identities 与参量微分算符；Phase 2 先做通用对象单向比较，再按任务书点名的 family/branch/parity 走完 package 生成、序列化、外部 reduction、回读、DE 和 scaling 验收。

### 当前任务：2026-07-25 018 候选构建、正式验收与 GitHub 发布

- [x] 就地校正本文件、`todolist_temp.md`、018 维护报告和 README 中已经过时的未完成状态；expanded-envelope、reference README/19-master 资产、E018-15/18/19 和 018 massive tree/provenance 不重复执行。
- [x] 在 `000_code/test/results_test/` 从当前 `000_code/018_dSIBP/` 构建同版本候选单文件和用户手册。前序候选曾为 794,167 bytes、SHA-256 `CDA2302295F961274488CFC1109B9041476A18782E17AB683F56E86D27BAF7BE`；本轮 example/contact 边界修复后的最终候选更新为 794,809 bytes、SHA-256 `E137F7259B429D2F181E1A8BA101C36A42CEFE2145975F1900A24FDFE8B3EA06`。最终 PDF 为 32 页、947,312 bytes、SHA-256 `FCF95B34B7F0ADCE037E5CB3029F239A78DA8AA50793F21AEEDD8A488BBC0EBA`；manifest token 仍为 018。
- [x] 只加载候选路径完成受影响回归：production/load `19/19`，结构化 `N_s/contact/ds/rep2Integrand` `16/16`，massless `11/11`，meta `16/16`，empty range `10/10`，tree standard/full `13/13`，tree provenance `15/15`，bubble+tree `12/12`，example `5/5`、API `37/37`、manual `6/6`，canonical equivalence `18908/18908`、差值 0；Kira energy `19/19`、phase round-trip `16/16`、retained input `14091/82252/2987/1/0`，expanded retained reduction post `20/20`，单固定点三套 DE 各 `361/361`、差值 0。017 contact/parity 的 29 个仍有效断言通过，唯一旧断言要求显式 `bS0` override 不改变 tree formula，与 018 normalized-`J` 合同冲突，已排除；旧 baseline reduction manifest 缺少当前 artifact identity，不能导入，已由 expanded retained reduction 取代。PDF 32 页及第 1、10、26、32 页渲染通过。未重新生成 reference IBP，未运行 reference Kira。
- [x] 候选全部通过后，以同一候选字节覆盖 `independent-benchmark/package/` 并删除旧 016 单文件/PDF。前序候选曾完成 example/API `5/5`、`37/37`、manual `6/6`、massless `11/11`、expanded retained post `20/20` 与三套 DE 各 `361/361`；本轮最终字节只对受影响范围 fresh 复验 sunrise、mix、topology `7/7`、massless endpoint/contact `7/7` 和 API/source coverage `37/37`，未重跑 Kira/reduction。正式程序 SHA-256 为 `E137F7259B429D2F181E1A8BA101C36A42CEFE2145975F1900A24FDFE8B3EA06`，交付/权威 PDF 均为 `FCF95B34B7F0ADCE037E5CB3029F239A78DA8AA50793F21AEEDD8A488BBC0EBA`。
- [ ] 后续按收缩后的 `independent-benchmark/` 任务书，在新的空 source-isolated 工作区完成 018 正式独立验收并把报告归档到 `000-report/`；维护侧 `check-smoke/` 不得作为独立 expected 或结论来源。本轮只修改任务书和目录边界，不执行该验收。
- [x] 2026-07-25 误执行说明：此前在 `codex-independent-benchmark/` 生成的所谓 Phase 1 producer、endpoint 递推、全-family structural ledger、freeze inventory/hash 和 post 结果均超出用户授权及当前收缩任务书，全部作废；不得进入正式报告、发布门禁或后续 expected，也不得断点续跑。
- [x] 清理已确认 ignored/untracked 且无活动进程占用的候选和运行产物；清理前 `000_code/test/results_test/` 含 26 个可重建文件，清理后为空。fresh fetch 确认 `HEAD...origin/main=0/0`，正式资产已由提交 `2937faf` 推送到 `origin/main`。

验收标准：正式目录只保留同版本 `package_018.wl/pdf` 和成品 examples；候选与正式程序/PDF 逐字节一致；所有发布检查明确加载各自目标路径；数值交叉检查只使用既有固定点 `{ks,ip0,P0}={43/17,29/13,-29 I/13}`；不把维护 smoke 冒充 source-isolated 独立证明；Git 提交不包含 ignored `results_test`、reference 重新生成物或其它可再生产物。

### 当前任务：2026-07-25 按独立 report 精简 018 运行时门禁

- [x] 以 `000-report/2026-07-24-2244-018-内部.md` 的分类为准，逐项对照 018 当前实现，形成“生产硬边界 / 删除或改为状态消费 / 仅 `AuditLevel -> "full"` 执行”的明确清单；不再把同源 producer/consumer 的重复自证误当成正确性。
- [x] 修改 018 默认 `standard` 路径：保留用户输入合法性、数学未推导边界、递推终止、外部 artifact 身份与 reduction/DE closure；模板内容 hash、source digest/coverage、tree solve/public representation/dlog residual 只在 `full` 重算，standard 消费 producer 状态；删除重复内部 `linearDigest`；Kira completion marker 降为诊断 warning。
- [x] 把“不在 package 运行时乱加验证门禁；数值比对只使用一个固定数值点”的约束归纳到 `AGENTS.md`，不扩写技术公式，也不把 `check-smoke`、独立 benchmark、coverage 或发布哈希搬进用户计算。
- [x] 只运行受影响的结构性回归和既有单一固定点矩阵比对；生产合同 `19/19`，tree standard/full `13/13`，massless/非法边界 `11/11`。唯一固定点 `{ks,ip0,P0}={43/17,29/13,-29 I/13}` 上，`P0/ip0/ks` 三种表示各 `361/361` 精确相等、差值 0；没有新增数值点、reference IBP 生成或 reference Kira。

验收标准：默认 `standard` 路径不再对 sealed 同源数据做 report 已判定为开发证书的全量复核；`full` 仍可显式执行深审计；非法用户输入、未推导 massless quotient、tree 非下降/cycle、Kira artifact 身份/target/master/map/系数域和 `DSDE` residual closure 仍 fail closed；受影响输出与精简前严格一致，并在一个固定点完成现有 reference 矩阵比对。

完成结论：018 模块化主线已按 report 精简运行时门禁。standard 路线不再逐模板/全集重算 hash，不再在 `DSLinear` 重算 source digest 或生成重复 `linearDigest`，不再默认扫描 tree 公开表示和 dlog residual；Kira completion marker 降为 warning，并已在临时移走 `kira.log` 后使用同一固定点结构闭合结果验证可正常导入，随后原日志已恢复。full audit 仍能拒绝模板篡改并通过完整 source/coverage、tree solve/public/dlog 证书。保留的硬边界仅覆盖用户输入、未推导 massless quotient、递推终止、外部 artifact identity/映射/targets/masters/系数域及 DE residual closure。本轮未构建或晋升 018 单文件/PDF。

### 当前任务：2026-07-25 报告门禁、massless 修复与 018 独立交付

- [x] 逐条复核 `000-report/` 最新报告中的门禁发现与 massless 问题。生产门禁 F1--F5 已落为 sealed artifact、complete/subset/raw、prepared Kira payload、内容 digest 和 DSInit I/O 解耦合同；F6 的 producer parity 与 tree solve residual 改为 standard/full 两级。massless fixed-line contact 与 lower-sector `ds` 已 fresh 复验；公式型 massless `timeOnly` quotient 仍按重新推导边界 fail closed。
- [x] 修复仍存在的正确性门禁与可闭合 massless 数据流。`check_018_production_gate_contract.wls` 通过 `17/17`：sealed complete、sealed subset、raw、模板篡改、formal/pre-reduction、export identity、manifest 篡改和 init 写失败均符合合同。`check_018_massless_report_regression.wls` 通过 `11/11`：atomic `++` contact 精确为 `-2/sE1^beta1`，三槽状态保留；pure massless bubble top `{E1,E2,ss11}` 为 `3/3`、两个 lower sector 合计 `6/6`，非法 shape 仍返回 `$Failed`，formula dlog 仍返回 `masslessQuotientFormulaNotCertified`。
- [x] 更新 `independent-benchmark/` 任务书、README、reference 说明和全部过时 016/017 口径；提供 massive bubble 可直接加载的同序 19-master 选择、映射、DE/scaling probe 与来源哈希，不重新生成 reference IBP/Kira。
- [x] 完成 massive-bubble expanded-envelope 晋升门禁：只在 package 侧把 top 目标包络扩为 `a[v1],a[v2] in [-2,5]`、`b[1],b[2] in [-3,6]`。fresh prepare 得到 197232 条生成关系、7850 个 linear 积分、48699 个 Kira 方程和 7871 个导出积分；当时 formal plan 直接接受旧 `active_basis_19.wl`，本轮已用 package `DSUserMI` 与 `reference_user_mi_basis.wl` 数据取代该私有 adapter。原 target 合同为 19 个 active token + 196 个 derivative `J` = 215，baseline/expanded 同为 215，旧 derivative target 缺失 0。Kira 2.3 wall 41.47 s、峰值 62464 KiB，masters 仍严格为 IDs `1..19`、215 targets、unreduced 0；post `20/20`，import/closure/scaling 全通过，physical `P0`、backend `ip0`、physical `ks` 三套各 `361/361`，差值 0。全程未重新生成 reference IBP，未运行 reference Kira；这些计数是迁移前历史结果，新 `userMI` manifest 仍需外部 Kira fresh 结果才能重新认证完整闭环。
- [x] 从 018 模块构建候选单文件 package 和同版本用户手册；候选路径的 API/example/coverage、massless、Kira import/DE/scaling/reference 及 PDF 渲染均已完成，正式同字节覆盖与覆盖后复验转由本文件顶部发布任务继续执行。
- [x] 为原始 massive-bubble reference code/result 写独立 README，明确结果源、`P_pkg=-P_ref`、`P0=-I ip0`、原始 dlog basis 的显式 `ks`、允许变换与禁止 fresh reference reduction。
- [ ] 完成 Git 工作树/远端分歧检查，提交本轮正式资产并推送当前分支；ignored `results_test` 与其它可再生产物不提交。

本轮生产合同的接口边界：`DSSeeds` 每条模板保存独立 `templateHash`；`DSGenerateIBP` 返回 `artifactContract={sealedQ,sourceDigest,completeSystemQ,subsetQ,auditLevel,capabilities}`。`DSLinear` 对 sealed producer 的 `standard` 路线只消费 source digest 字段和 producer 摘要，不重算关系 hash，也不重复 symmetry/coverage；`full` 才重算 source digest 与 coverage。sealed subset 与 raw 可线性化但 `completeSystemQ=False`，formal `DSKiraPlan` 和缺省直接 `DSKiraExport` 拒绝。formal plan 保存并实际消费 `preparedLinearData`，certificate 必须与该 payload 相同。export manifest 的 `artifactIdentity` 固定 equations/map/targets/rules/active payload 及实际导出文件 SHA-256；import 不再以 packageVersion 字符串或 completion log 措辞代替内容身份与结构闭合。

### 当前任务：2026-07-25 018 massless 三槽端点关系与动量因子专项复核

- [x] 从原始 kernel 导数关系重新区分临时双端点标签 `{10}/{01}`、`{11}/{00}` 与 018 normalized quotient `F01/F10`、`F11/F00`。确认 `{11}=+q^2{00}` 是微分恒等式，不是普通 symmetry；`F11=-F00` 只有与逐次动量因子抽取合用才成立。
- [x] 对 cycle massless line 与 fixed/独立外动量 massless line执行当前 018 fresh-kernel 原子 probe，12/12 精确通过：cycle 每次 `b->b-1`，得到同端点 `-J[b-2]`、交叉端点 `+J[b-2]`；fixed 每次显式乘 `sE1`，在 sector prefactor `sE1^-beta1` 上得到 `-sE1^2 J` 与 `+sE1^2 J`。
- [x] 源码、动态输出、版本边界和未完成路径已补入 `000-report/2026-07-24-2244-018-内部.md` 第 11 节。另从正式 `package_016.wl` fresh 验证其 cycle massless 仍为二槽 `{b,n}`、fixed 为单槽 `{n}` 且无 `"F"`；故上述功能仅在 018 开发主线实现，尚未正式交付。massless 公式型 tree 仍为 `PendingRederivation`。未确认新的 018 物理缺陷，未伪增错误编号；临时 probe 已清理，018 程序未修改。

### 当前任务：2026-07-24 018 生产门禁必要性独立审计

- [x] 盘点项目概况、plan/design/tech note、用户手册、正式 016 单文件交付与最新 018 模块化程序的真实全流程，建立公开入口到 producer、`linearData`、serializer、DE/scaling consumer 的调用图。
- [x] 对 018 每一类 fail-closed gate、重复 shape/capability/context/provenance/coverage 检查和运行期自检逐项追踪触发位置、调用频率、失败后果与下游依赖；不得把消息、开发 smoke 或发布检查误记为生产门禁。
- [x] 按“仅用户输入阶段保留”“内部不变量仅在边界断言”“降级为 warning/status”“移出生产路径改为开发/发布 check”“直接删除”给出客观分类，并逐项拷问其必要性；不得为了减少检查而删除防止错误数学对象进入关系生成的输入契约。
- [x] 报告已归档为 `000-report/2026-07-24-2244-018-内部.md`，记录当前提交与脏工作树快照、已读文档、静态证据、未执行的重型流程以及建议的精简顺序；本轮未修改 018 实现，也未把尚未复现的性能判断写成确定结论。

### 当前任务：2026-07-24 018 未完成工作闭环与工程交付

017 开发检查点已经以提交 `8032ce7` 推送到 `origin/main`；该提交记录统一三槽 sector、结构化 prefactor、parity 和反向 seed 包络等阶段成果，但不视为正式发布。开发主线随后切换为 `000_code/018_dSIBP/`，017 及更早目录保持只读；018 现已按本文件顶部任务同字节晋升为正式交付。

- [x] P0（E018-18，2026-07-25）：已从只读既有 `DEP0.m`、`DEks.m`、`MIdlogNote.m` 及 provenance 闭合 reference/scaling 差异。19 个 master 定义逐项相同；只实施 source-defined homogeneity、显式 `ks` 和 `P_pkg=-P_ref` 变量变换，未从差矩阵反解或固化 post-hoc adapter。
- [x] E018-18 用户指定测试假设：reference `G/R1` 与 package normalized `J_s=N_s I_s` 的整体定义按逐项相同测试；最终 master 比例为 `{1,...,1}`，三套 DE 各 `361/361`，无需任何额外常数、符号或动力学 normalization。
- [x] E018-18 验收：package/reference basis 同序、同归一化，scaling 恒等式和三个变量表示的 `19 x 19` DE 已在同一固定点逐项 `361/361`；package 侧 expanded envelope fresh reduction 保持 19 masters、215 targets、`unreduced=0`，post `20/20`。reference 侧只复用哈希一致的既有解析结果。
- [x] E018-18 代码/文档收尾：scoped `git diff --check`、018 fresh-kernel 模块加载、技术说明两遍 pdfLaTeX 和第 35--36 页渲染检查均已通过，错误与修复已追加到 `000-report/2026-07-24-2037-018-内部.md`。
- [ ] 清理 `check-smoke/results_test/018_tech_note_compile/` 及同轮已确认 ignored/untracked 的可再生运行目录；清理前重新确认路径、Git 状态和活动进程。
- [x] E018-18 reference 原始 dlog basis 与独立 benchmark 补充：以 `MIdlogNote` 为原始物理 basis，第 15--18 项各含一个显式 `ks`；stored matrix 先做已由 provenance `361/361` 确认的 homogeneity lift，再用 `T=DiagonalMatrix[{1^14,ks^4,1}]` 恢复显式因子，`ks` 方程包含 `D[T,ks].Inverse[T]`。最终直接使用 `P_ref=-P_pkg=29 I/13` 与 `d/dP_pkg=-d/dP_ref`，19 个 master 定义比例逐项均为 1，不使用 basis adapter；此前 74 个差值全部消失。
- [x] P0（E018-19，2026-07-25）：把 reference 的 Kira 能量 convention 提升为 018 缺省 backend convention。实现从初始化 phase-energy dependency data 构造 `k -> -I ik`，不按符号名猜角色；复合/复用能量按结构拆分和聚合，非相位空间坐标不变。Kira 数值规则只给 `ik` 代入精确实有理数，manifest 同步保存物理截面 `k -> -I r`、双向映射、来源角色、普通导数 Jacobian `D_k=I D_ik` 和 Euler 不变性；公开物理 API convention 未改变。export/import 已按 energy map、数值规则、Gaussian fallback 的可逆顺序接通。
- [x] E018-19 验收：结构与负例 smoke `19/19`、Gaussian round-trip `16/16`、`N_s/kEpower` `16/16` 和 fresh package load 均通过。受影响 package bubble 已 fresh export/reduce/import：`14091` 个方程块、`2795` 条独立关系、19 masters、215 targets、0 unreduced；Kira 文本纯有理且无 `I/dsii/ccc`，energy map 后 Gaussian gauge 为 `notRequired`；物理 scaling matrix/source residual 均为零，求值点 `{ks,ip0,P0}={43/17,29/13,-29 I/13}`。reference 未重新生成或约化，而是复制并逐字哈希核验既有五份解析结果；copy/post 门禁全部通过，随后与 package 的 physical `P0`、backend `ip0`、physical `ks` 三套 `19x19` 矩阵分别 `361/361` 精确相等，差值均为 0。此前启动的 reference fresh prepare 已中止并隔离，不计入结果。
- [x] E018-21 文档/PDF 收尾：reference 只复用已有解析结果的纠正已写入 plan/design/tech note、独立任务书和错误报告。51 页技术说明候选经 Poppler 渲染检查第 35--36 页无裁切、重叠、黑块或乱码，随后以同一字节覆盖 `000_note/dS_IBP_package_tech_note.pdf`；候选与正式路径 SHA-256 均为 `BEEE84BE65D054C95863C33E5DCB68CD1D200398E7DBF0C161202AA8CFC0145D`。不得重新生成 reference IBP、不得运行 reference Kira、不得从差矩阵反解 adapter；018 package/用户手册仍不构建、不晋升。

- [x] 按最后修改时间与当前 diff 盘点项目概况、`todolist_temp.md`、plan/design/tech note、正式 016 用户手册、018 修复日志与最新模块化程序全流程；说明每份最新文档实际更新了什么，并核对正式交付/开发主线边界、当前 P0 正确性阻塞、后续依赖顺序和不得越过的发布门禁。盘点确认 22:19--22:20 的最新文档批次撤回 post-hoc basis adapter，把 E018-15 的统一 normalized-`J` 数据流列为首要代码阻塞；`todolist_temp.md` 另记录的逐积分 `I` phase gauge `14091/82252/2987/1/0` 证据尚未同步到本文件或 018 错误日志，不能只凭临时清单计作权威完成。当前 tech note TeX 晚于 48 页 PDF，用户手册源码/PDF仍为 017，正式交付仍为 016，三者必须在 P0 修复和 fresh 闭环后由同一 018 候选统一重建。
- [x] 从 017 字节复制建立 018 基线，只更新 loader、版本 token、构建器和 018 专属模块名，不回写 017。
- [x] 先闭合 massive-only tree 公共路线：`DSTreeNaiveIBP` 的公开三参数 `J` reduction 可逆还原为 Private sector-tagged vertex basis，`DSTreeNaiveDE` 无 shape/residual，并与同序同归一化 `DSTreeDLogDE` 逐项精确相等；fresh smoke 为 `12/12`，三个同序 `5 x 5` 矩阵逐项差值均为零。
- [x] 让单积分 `DSTreeSeeds` 和批量 `DSSeeds` 共用 direct pure-time 生成路线；逐条保存并核验 `generationRoute`、`sectorKey`、generator、source integral、context hash 和公开三参数 `J` 表示。fresh smoke 为 `15/15`，9 个 general templates 的批量/单积分结果逐条相等。
- [x] 审计 018 全源码的数量、规模和最大步数限制；未发现 `MaxSeedRuleCount`、`MaxDiscreteRuleCount`、`MaxEquationCount`、`MaxShrinkSectorCount`、`TimeConstrained`、`MemoryConstrained` 或任意最大递推步数。现存缺省范围不截断用户显式范围，tree 迭代只保留严格下降与循环检测。
- [x] P0：优化 `DSGenerateIBP` 的 sector topology/canonical 与公开坐标投影热路径。一次调用预建 sector topology cache，general template 只投影一次，canonical 后仅在重新出现内部原子时再投影；sector shape gate、EOM/symmetry/parity 顺序不变。massless `[-2,2]` fresh workflow 保持 `13232` 条方程、`2726` 个积分和零内部原子泄漏，generate 由旧基线 `108.0 s` 降到 `47.28 s`（约降低 `56.2%`）。
- [x] 修复 `DSGenerateIBP` 的 raw-expression 路线：补齐无 provenance 时的 parity fallback，并让 batch 使用当前 context 的完整 sector metadata；raw 输入保持 `unsealed`，不因只给部分模板而绕过 `DSLinear` 完整 coverage gate。bubble+tree 代表性 top/e3 检查为 `12/12`。
- [x] 把超时的 monolithic `check_017_full_workflow.wls` 拆成初始化/seed、撒点/canonical、linearData/参数闭合和 derivative/DE 的 staged runner；每段分别保存数量、耗时和首个失败。massless 路线已完整通过；bubble+tree 全量 `[-2,2]` 在五分钟内只完成 init/redefine/seeds，未计作通过，另以不冒充完整系统的代表性 smoke 验证本次跨 sector 优化。
- [x] 完成全部受影响的 sector/parity/meta-range/tree/parameter-closure 回归；`DSGenerateIBP` 按反推 seed 点域后、代入生成元前筛 parity，且不把 bad-parity 积分替换为零。017 contact/parity 中与 018 合同仍相容的 29 项全部通过；要求显式 `bS0` override 不改变 tree formula 的旧断言已被 normalized-`J` 数据流取代，不作为 018 门禁。
- [x] 更新成品 examples、`coverage_manifest.wl`、用户手册、tech note 和独立 benchmark 任务书；018 候选 package/PDF 已生成并完成候选路径复验，正式晋升按本文件顶部发布任务继续执行。
- [x] pure massive bubble 已完成 package 侧 fresh Kira export/import、DE、scaling 和只读 reference 对照；package 本身只生成和序列化关系，外部 reduction 由维护流程单独运行。
- [x] P0：完成 bubble+tree 全量 `DSGenerateIBP` 优化与闭合。general template 先做一次 target-sector-aware canonical；仅当用户 symmetry 依赖具体数值或 tadpole odd-ISP 需要整数幂次时才逐撒点重跑。新旧路径在 `[-1,1]` 的 `18908` 条关系逐条 `SameQ`，差异 `0`；新路径 `14.11 s`，兼容旧路径 `67.55 s`。完整 `[-2,2]` 生成 `397592` 条关系、`65252` 个积分，23 个 seed group 全部非空，generate `126.24 s`、linearData `346.51 s`，parity/参数闭合与零内部原子泄漏全部通过；未增加数量门禁或缩小用户包络。
- [x] P0：在 pure massive bubble 上先冻结解析 master 一阶导数与 215 个最小 targets，再把 `dim`、`nu`、zero-point/normalization、`s11` 与 backend `ip0` 统一代入确定性精确有理点。实际 Kira 文本不保留符号参数；物理 DE/scaling 只宣称截面 `{s11,P0}={1849/289,-29 I/13}`，不冒充全符号 DE。
- [x] P0：已按 Kira 实际读取文本确认纯有理系数域，无 `I/dsii/ccc` 或 backend coefficient variable；expanded envelope fresh reduction 证明 active backend masters 仍严格为 IDs `1..19`、原 215 targets 全部包含、`unreduced=0`，不再把 preferred master 本身误作充分性证明。
- [x] P0：重新核对 pure massive bubble 的外部变量约定。reference top sector 先保留 `P1`、`P2`，随后取 `P1=P2=-I k0`；018 family 用共同变量 `P0` 表示这一相等值，并保持 `P0` 与空间模长 `ks=ss11` 独立。因此初始化即取 `P1=P2=P0` 是正确的，不需要改成三个独立变量。`361/361` 表示两套 `19 x 19` DE 矩阵逐元相等，不是 361 条 scaling relation；Euler/scaling 只有一个矩阵恒等式 `P0 A_P0+ks A_ks=diag(degrees)`。此前从差矩阵反解 normalization adapter 的路线已撤回；最终 19 个 master 定义比例均为 1，并通过 `P_pkg=-P_ref` 的变量/导数方向及原始显式 `ks` 恢复得到三套 `361/361`。
- [x] P0 持续规则：`000-report/2026-07-24-2037-018-内部.md` 同时作为 018 的错误报告与修复日志。018 开发、smoke、外部工具和回归过程中遇到的每个失败或真实缺陷，必须先立即登记原始现象、证据和归因状态；修复后只能在同一条目追加根因、修复方案、复验命令/计数和剩余边界，不得覆盖原始错误证据，也不得只在临时日志或会话回复中说明。该规则已经对 E018-01 至 E018-12 生效，并继续约束 018 后续全部工作。
- [x] P0：`E018-11` 的“补齐跨版本 basis adapter”任务已经撤销。历史 014 的 `361/361` 仍只作为旧版本证据；018 不从差矩阵反解或固化任何 adapter，reference 比较直接使用 `J_s=N_s I_s` 的同一 convention。
- [x] P0：撤销“先构造额外 normalized common basis 再比较”的路线。当前需要修的是 package 自身对 `J_s` 隐含 prefactor 的实现，而不是给 master 另乘系数。
- [x] P0：撤回上述 basis-adapter 方案。由差矩阵反解出的对角因子只能暴露 convention 不一致，不能证明需要给 master 换 normalization；不得把 adapter 写入 reference metadata、package 或正式比较脚本。
- [x] P0：修复 `E018-15` 的统一 normalized-`J` 数据流。每个 contact-reachable sector 的 `sectorPrefactorData` 结构化保存由传播子收缩自动累积的完整 `N_s`；`J_s=N_s I_s` 直接隐含该 prefactor，任何模块都不得在 `J_s` 外重复相乘。
- [x] P0：`N_s` 的无圈模长部分不缓存为已经乘开的表达式。初始化按 `independentExternalMomenta` 的声明顺序冻结稳定 kE 编号，sector 幂次保存为 `kEpower[be1,be2,...]`；`kEParameterExpressions` 独立保存当前复合表达式。`DSRedefineParameters` 只更新映射，不改稳定编号或幂向量。
- [x] P0：所有跨 sector contact 先产生裸系数 `c_raw`，再统一转换为 `c_raw N_source/N_target J_target`。massive Wronskian normalization 由 lower-sector `J` 吸收，simultaneous contact 的每条收缩线只累计一次。
- [x] P0：公开 `ds[J_s,x]` 同时包含裸积分核导数与 `D[Log[N_s],x] J_s`；fixed-line 裸核不再重复微分已归入 `N_s` 的 zero-point 幂。`ds[c(x)J_s,x]` 保留显式系数导数，`DSDE` 复用该实现。
- [x] P0：`rep2Integrand[J_s]` 先构造裸 sector integrand，再把 metadata 重建的完整 `N_s` 乘回；缺省与复合参数重定义均已检查。
- [x] P0：`check-smoke/check_018_structural_sector_prefactor.wls` 已通过 `16/16`。单 contact 为 `1/sE1` 与 `1/(u+v)`，三线 simultaneous contact 为 `1/(4 sE1 sE2 sE3)`；`DSParameterNotation` 可取回稳定映射，`ds` 对数导数、显式系数乘积法则及 `rep2Integrand` 直接物化差异均精确为零。最终正确性仍由 fresh reduction 后的完整 scaling relation 检查，不新增用户门禁。
- [x] P0（E018-16/E018-17）：Gaussian phase gauge 已接入 Kira exporter/importer。实际导出方程的完整 ID 图按 `J_i=I^p_i K_i` 求解逐积分二元相位和逐方程公共相位；manifest 保存全部 ID 相位、连通分量、约束与可逆 convention，importer 在 ID 映射前恢复物理 `J` reduction。合成 round-trip 及冲突/混合轴负例为 `16/16`；reference prepare 为 `57160` 条生成关系、`14091` 个 Kira 方程块、`2987` 个含 active-basis ID、`82252` 个相位约束、1 个连通分量、冲突 0，前端/backend 变量均为空，实际 `ibp.kira` 无 `dsii/ccc` 且无 dummy。Kira 2.3 的零变量 FireFly 会报 `No variables?`，纯有理路线最终改用官方 `run_triangular+run_back_substitution`；fresh reduction 得到 `2795` 条独立关系、19 masters、215 targets、0 unreduced，Kira 内部耗时 `12.86 s`、峰值 `62464 KiB`。fresh import/DE 闭合，但 scaling 与 reference 仍复现 E018-09 的 `P0 90/361`、`ks 94/361` 差异，不能计作整体闭环通过。
- [x] P0：E018-15 后的 package fresh export/reduce/import/DE/scaling、expanded-envelope 稳定性与同一 `J` basis reference 三套 `361/361` 已完成；禁止 post-hoc adapter。
- [ ] 018 package/用户手册候选重建、全部受影响回归、正式路径复验和 source-isolated 独立验收按本文件顶部发布任务继续执行。
- [x] P1：用户自选 preferred basis 已实现。按用户给定顺序接受单个 `J` 或 `J` 的齐次线性组合，并以 `userMI[i]` 作为稳定公开 token；生成并保存 `J`、`userMI[i]` 与 Kira ID 的双向映射及可逆性/秩审计。RT variable 对接留给未来实际 RT serializer，不伪造当前不存在的 backend 映射。
- [x] P1：backend 排序合同已统一。`DSLinear` 或用户显式 `DSReorderIntegrals` 得到的 `integralList` 是唯一顺序来源；Kira serializer 逐项消费该列表，不再按 reference-style key 暗中重排。未来 RT serializer 必须复用同一合同。
- [ ] P2（低优先级）：含 massless tree line 的 quotient-basis 迭代约化和公式型 dlog DE 继续返回 `PendingRederivation`，待 massive 路线和 018 工程闭环完成后再独立推导。

018 P0 阶段验收记录（2026-07-24）：早期候选单文件 SHA-256 `8ECCD0B66171362E1671F83B9B68BAF0BEF13B047400B1FAD425FBB362AEC830` 已被后续性能候选取代，最终候选哈希须在 Kira/回归完成后重算；两遍 pdfLaTeX 后的 48 页技术笔记 PDF SHA-256 为 `72142E397280F18516E3D9CD4E46EC31E86800632FE7A4A539BD94B7D4BBB886`。此前 massive tree、seed provenance、meta-range、parameter closure、contact/parity 与 massless workflow 的结构检查结果仍保留为相应模块的历史记录，但它们不会检查 `J_s` 隐含 prefactor 的导数，不能证明当前 IBP/DE 正确。E018-15 修复并通过完整 scaling relation 前，旧 Kira/DE/scaling 结果和候选哈希全部不得用于晋升；018 仍只是候选开发版。

### 当前任务：2026-07-24 017 统一三槽、sector parity 与正式交付

017 的表示、幂次、奇偶性和 subsector 求导设计已经冻结并进入正式实施。016 及更早目录保持只读；当前主线源为 `000_code/017_dSIBP/`。大版本内的兼容修订沿用同一模块目录，并把交付版本依次标为 `017.1`、`017.2`；发生新的表示或物理 convention 变更时才新开整数版本目录。
- [x] 进入 017 正式实施前复核项目 `000-report/`、平行独立审计目录和内部其它会话的最新正式报告；当前平行报告原字节已归档为 `000-report/2026-07-24-1124-016-外部.md`，SHA-256 `502672D32CEE8A159BD744CD45766C875DDA3ACE08FF908BC06B8EC26EF4DCCF`。
- [x] 从实际 massless contact relation、指标 slot map 与 top/child zero point 独立推导 parity：massless cycle 由 `n1+n2=1, bS=b` 使 child affine offset 翻转 1；massive h/H 由 `n1+n2=1, bS=b+1` 保持 parity。fixed/non-loop line 不进入 parity generator；simultaneous contact 按被 shrink 的 massless cycle 线数模 2 累加。
- [x] 以当前 016 工作树为基线创建独立 `000_code/017_dSIBP/`，实现统一三槽/单槽 shrink schema、可逆 sector identity、结构化 `sectorPrefactorData`、sector-aware `ds/DSDE`、统一 relation/parity 层和 time-only 转换；016 目录及正式 016 交付保持不动。
- [x] 在 `check-smoke/` 增加可复用的 017 定向轻量检查，覆盖 massless/h/H top-to-contact parity、同形 subsector 消歧、fixed prefactor 乘积法则、compact lower `ds`、参数重定义、pure-time 转换、公开 tree 表示审计和明确未认证的 massless formula dlog capability。
- [x] 正式交付前扫描权威文档与 examples 生成物的旧 convention；发现并修正用户手册、plan/design/tech note 中残留的 massless 单 `n`/二槽和空 fixed pack 说明。旧 examples `init/` 只视为待 017 重跑覆盖的生成物，不作为 017 证据。
- [ ] 修复公开 `DSTreeNaiveIBP` 结果回灌 `DSTreeNaiveDE` 时的 representation 边界：公开三参数 `J` reduction 必须先单向还原为 Private sector-tagged vertex basis，再调用已冻结的 massive-only 公式内核；验收要求无 `Part::partd`、全部变量闭合，并与同 basis 的 dlog DE 逐项一致。
- [ ] 统一单积分 `DSTreeSeeds[vertex,int,context]` 与批量 `DSSeeds` 的 direct pure-time provenance：两者必须共用 `dsDirectTreeSeedRecord`，不得让前者继续走 loop 投影兼容路；公开输出仍仅含三参数 `J`。
- [ ] 修正 `DSGenerateIBP` 的范围语义：用户给的是全部 reachable sectors 共用的最终关系指标包络 `[L,U]`。对同一输入组、同一指标的全部整数 shift `Delta`，反推 seed 区间必须是交集 `[L-Min[Delta],U-Max[Delta]]`，从而缩小撒点域，并使作用 shift 后的结果外包络刚好为用户给定范围；不得误用会扩大点域的并集 `[L-Max[Delta],U-Min[Delta]]`，也不得要求用户手工准备逐生成元 route。reference bubble 先按 top 的共同外包范围罩住 top/lower，只有 target/DE closure 报告明确缺口时才扩张相应边界，不预先照抄旧脚本随手放大的 lower `[-4,8]`。
- [ ] `DSGenerateIBP` 必须按“反推 seed 候选点 -> 在生成元作用前按 sector parity 筛点 -> 把数值点代入 general seed -> EOM/symmetry/canonical -> parity certificate”的顺序工作；不先生成全量关系后删除，不把 bad-parity 积分置零，也不设置方程数门禁。验收记录共同包络、逐模板反推范围、未筛候选点数、parity 后实际点数、方程数与 closure 缺口。
- [x] `DSGenerateIBP` 在实际枚举前必须逐输入组双语打印反推结果，格式为 `编号 i（来源）：{指标,min,max},... / Group i (source): ...`。编号只对应用户输入的 seed 分组，不把自定义分组误称为 IBP 算符。若某组出现 `min>max`，另行 warning 并明确列出编号、来源与空区间；warning 必须解释目标包络窄于 shift 跨度，因而不存在能使全部移位后积分留在包络内的 seed 点，该组 IBP 撒点为空，最终关系集合缺组而不能作为完整约化系统。返回数据保存同一 `emptySeedRangeGroups`，完整 reduction capability 为 false。全局消息开关可关闭这些 info/warning，但不得改变返回状态。
- [ ] 把 shift 提取独立成公开、可覆盖初始化的 `DSMetaSeedRange[seeds,{indices...}]`（兼容别名 `metaSeedRange`）：先把每个积分的全部参数 `Flatten` 成统一指标数据，再以 `Variables` 发现实际连续指标，不依赖 `J` 的三槽/单槽位置约定。每个输入组分别保存 shift 上下界及相对目标包络的 offset；使用 `DSSeeds` 推荐的 `seedGroups` 时自然对应各 `{sectorKey,ibpClass,generator}`，传入 flat seeds 时则按用户选择合成总组。用户声明相对实际变量有多余或遗漏时给双语 warning、返回 `extraIndices/missingIndices`，但仍按实际完整变量集建立 metadata；同一会话再次调用覆盖旧状态。`DSGenerateIBP` 校验 seed hash 后复用该 metadata，无缓存时允许自动初始化。
- [ ] seed-range 分组由输入层次显式控制，不由 metadata 强行重排：`DSMetaSeedRange` 收到 flat `{seed1,seed2,...}` 时把整体作为一组；收到含二级或更深列表的输入时，以最外层每个元素为一组，并把该元素内部任意深度先 `Flatten` 后分别统计。调用时双语输出该规则、实际分组数与每组来源；同一 IBP 算符内不同 `n_i` 状态是否合组由用户提供的层次决定。`DSSeeds` 必须同时返回 flat `allSeeds`、逐 `{sectorKey,ibpClass,generator}` 的嵌套 `seedGroups` 和同序 `seedGroupMetadata`，公开 `DSSeedGroups`/`DSSeedGroupMetadata` 供用户直接选择，并保证 `Flatten[seedGroups,Infinity]` 与 `allSeeds` 的密封模板有序集合相同。
  - 2026-07-24 阶段验证：候选单文件已实现默认分组初始化、用户覆盖初始化和正确交集公式；`check_017_meta_seed_range.wls` 为 `15/15`，覆盖 flat 一组、逐算符三组、三级嵌套组内 Flatten、逐离散模板九组、少报/多报 warning 后继续、覆盖旧状态、parity 预筛和最终指标数值化。`check_017_reference_bubble_seed_offsets.wls` 为 `5/5`，88 条模板按 10 个 sector/生成元组保存，top 共同关系包络 `a in [-1,4]`、`b in [-2,5]` 自动反推出四组更窄 seed 范围。完整 core/examples 回归和正式交付尚未完成。
  - 2026-07-24 编号/空组验收：`check_017_empty_seed_range_warning.wls` 使用人为拆分的两组 custom seeds，而不是缺省生成元分组；结果 `10/10`。公开输出逐组打印 `编号 1/2` 与 `Group 1/2`、来源及 `{index,min,max}`，不再称为 IBP 算符。目标包络 `{0,0}` 使两组出现 `min>max`，warning 完整解释不存在可保持全部 shifted integrals 位于目标包络内的 seed 点，因此两组 IBP 撒点为空、最终关系集合不完整；返回 `emptySeedRangeGroups={1,2}`、`equationCount=0`、`completeTimeIBPQ=False`、`completeCanonicalQ=False`。候选回归另通过 metadata `15/15`、reference bubble offsets `5/5`、sector `21/21`（2176 关系、parity 失败 0）、contact/parity `30/30` 和 tree public boundary 退出码 0。`check_017_full_workflow.wls` 的全量 `DSLinear` 两次运行超过 4 分钟，本轮未取得完成结果，不计为通过；完整 core/examples 回归和正式交付仍未完成。
  - 同轮候选身份：`000_code/test/results_test/package_017.wl` SHA-256 `6F5849F6FE735962CF7C55F211EC223BE10B9255D1AFC0A11073CC29740FCD1B`；候选用户手册 `package_017.pdf` SHA-256 `98946594257FA2480B2C7A7A0A9E795FD94D9B5232FAF42305B721EA177010AF`。技术 note PDF SHA-256 `543EEE15FBA9F3E273305CB842647AA5D49F7F5E2D7979BECBD821D8BADA4685`；新增页已渲染抽查，无裁切、重叠或乱码，渲染临时文件和本轮孤儿 Wolfram smoke 进程均已清理。候选尚未覆盖 `independent-benchmark/package/`。
- [ ] 完成模块级 smoke 后更新 plan/design/tech note、正式用户手册、examples coverage 和独立 benchmark 任务书；严格按候选目录生成、哈希、候选路径复验、正式路径覆盖、正式路径复验的发布流程交付 017。
- [x] 同步 shrink/subsector zero-point 文档合同：列明 h/H/massless 缺省 compiled `(s,z)`、进入 child 指标与 zero point 的实际映射，证明 h/H 缺省 contact 不改变 subsector parity；用户手册明确建议一般不要修改 `shrinkBShift`/`shrinkZeroPointShift`，若确需修改必须同步重建 child zero point、coefficient 与 parity metadata。
- [x] 补齐 massless shrink 的吸收与 parity 边界：缺省 `(s,z)=(0,0)`，cycle line 的 `a/a0/bS/bS0` 无 shift，fixed line 的 `B_t-B_s=0`、`N_s/N_t=1`，不新增模长幂吸收；但 contact 支持 `n1+n2=1` 且 `bS=b`，所以 cycle child 的 parity offset 相对 parent 翻转 1。pure massless 不自动开放 parity；已有认证 root parity 的 mixed h/H family 必须传递该 affine offset。
- [x] 在所有相关权威文档中明确：`linePacks` 始终按 root `lineOrder` 保留全部传播子槽位，full/shrunk pack pattern 是 shrink set 的隐式、可逆编码；`aList` 不参与 sector 身份判定，外部 `sectorKey` 必须由该 pattern 派生并交叉核验，不能形成第二套互不一致的编号。
- [x] 冻结 fixed/non-loop line 的收缩 normalization convention：吸收量只由 source/target sector 已选 zero point 的差决定，不设置额外整数吸收开关。016 缺省传播 `B_t-B_s=z`，因此 compiled 总 shift `(s,z)` 中只把 `z` 放入 target prefactor，关系系数保留 `r^-s`；额外归一化由用户后续的 master/basis 选择处理。
- [x] 在 017 逐线保存 source/target zero point、compiled integer/zero-point shift、zero-point override 来源和由一般差公式得到的剩余 coefficient exponent；这些数据进入 `sectorPrefactorData`，并通过统一 materializer 的 `N_s/N_t` 改变跨 sector 约化与 DE 系数。没有增加 `fixedShrinkConvention`、`fullContactPower` 或与 zero point 并行的吸收状态。
- [x] 重新编译 44 页 tech note 并目视检查末三页：一般 normalization 公式、h/H/massless 缺省表和“无额外吸收开关”表述一致；最终日志无未定义引用或 fatal error，页面渲染临时文件已清理。
- [x] 报告归因收口：旧 atomic massless `-2/sE1^beta1` 结论把 normalized lower `J` 已拥有的 sector prefactor又外乘一次，不进入 017；compact lower `ds` root-shape gate 是真实缺陷；强制 massless 二槽/vertex-pack 的要求已被统一三槽合同取代，但 massless quotient 上的公式型递推/dlog 仍明确保持未认证。
- [x] 017 已补齐任意 contact-reachable sector 的 seed 生成；target sector 继承 root loop space，并使用自己的 compact vertex map、pack state、zero point、prefactor 和 parity 生成完整关系。三条 fixed line 的专项检查覆盖 `top/e1/e2/e3/e1_e2_e3` 全部可达 sector 及各自全部 active time generator。

- [x] 复核 016 的 pack 实现：cycle 收缩线保留 `{bS}`，fixed/non-loop 收缩线为 `{}`；这是 016 历史 schema，不延续到 017。实测 `{,n1,n2}` 会被 Mathematica 当作 `{Null,n1,n2}` 并发出 `Syntax::com`，而 `_` 是 `Blank[]` pattern，二者都不能作数据占位。017 使用短字符串 `"F"` 表示 fixed power slot。
- [x] 复核 atomic massless contact 的当前 normalization：016 的 fixed lower integral 已通过 sector zero point 含相应模长幂，因此报告对 016 提出的额外 `sE1^-beta1` 很可能重复计数。017 仍把 fixed-line 因子视为 sector-normalized `J` 的 prefactor，只把它从 line pack 移入结构化 sector metadata；不能因 pack 中没有 `b` 就再次外乘同一幂次。实施前仍需展开 massless、H、direct h、H-to-h 四类 top/lower integrand，冻结逐项物理幂次表后裁决。
- [x] 确认 lower/contact `ds` 的真实问题：公开 shape gate 在 sector 解析前按 root `aList` 长度检查，导致合法 compact subsector 被拒绝；017 改为逐积分先唯一解析 sector，再按 sector topology 验证和求导。
- [x] 决定所有物理路线统一使用三顶层槽 `J[aList,linePacks,ispList]`；每条 root full line 的 pack 固定为三槽，shrunk pack 可退为单槽但对应 root line 位置不删除。pure time-only 仍保留空 `ispList`，不再公开生成并行的 `J[vertexPacks]` 表示。
- [x] 冻结 017 line-pack schema：cycle full massive/massless 均为 `{b,n1,n2}`，fixed full 均为 `{"F",n1,n2}`；收缩后允许退为单槽，cycle shrink 为 `{bS}`，fixed shrink 为 `{"F"}`。massless full 不再退化为单个 `{n}`，两个端点态由内建 massless 额外关系在统一 relation 层约化。
- [x] 决定 fixed/non-loop line 不保存 `b/bS`。其动量幂属于各 sector 的 `J` normalization，并以结构化 `sectorPrefactorData` 保存，不能成为 Kira 积分编号或 master 排序轴。不同 sector 由原始 line 位置、full/shrunk pack shape、`"F"` sentinel、compact vertex map 和显式 `sectorKey` 唯一解析。
- [x] 复核 016 的 `aList/a0` 合并：target sector 先由被 shrink line 的端点并查集得到代表类；每个 child `a` 等于该类 parent compact `a` 之和减该 contact event 内全部整数 `aShift`，每个 child `a0` 等于类内原顶点 `a0` 之和减 target sector 全部相关 zero-point shift。主 simultaneous 路线对平行多线只合并顶点一次并累加 shift；旧单线 helper 对已 coincident 端点存在 `2 a` 分支，017 不沿用。
- [x] 017 只保留一个 sector vertex-power merge：`aChild[r]=Total[aParent[parent classes in r]]-Total[selected aShift in r]`，`a0Child[r]=Total[root a0 in r]-Total[target zShift in r]`；active representative 按 root vertex 顺序稳定排列，已同一代表类的端点不会再次产生 contact 或重复顶点幂。
- [x] 在 family 初始化时为每个 sector 生成结构化 `sectorPrefactorData`，并由参数重定义同步更新当前输出坐标。
- [x] 提供唯一 prefactor materializer；normalized `J_s=N_s I_s` 的 `ds/DSDE` 含 `(D[N_s,x]/N_s)J_s`，跨 sector source 项由同一 normalization 数据得到系数。
- [x] fixed-line top/contact 幂次按 `zeroPointOnly` 冻结并验证：h/H 缺省 residual integer power 为 1，显式合法 `bS0` override 会同步改变 prefactor 导数和 contact normalization。
- [x] 建立统一 non-IBP relation 层：massless 内建关系与 EOM/canonical、tadpole symmetry、用户 symmetry 和 parity metadata 共存；tadpole 与用户 symmetry 继续求并集。
- [x] 为 h/H 建立 top-sector parity generator，只引用 cycle line 与允许的 ISP 整数槽，fixed/non-loop line 完全排除。
- [x] contact 通过 GF(2) affine slot map传递 parity：massless cycle 翻转，massive h/H 保持，用户整数重基按模 2 平移；不可判定非整数重基拒绝显式 parity 初始化。
- [x] parity 只在 h/H compiled function system 上开放；非 h/H family 的显式 parity 请求红字失败，普通未筛选 IBP 保持可用。
- [x] 奇偶筛选直接作用于 `DSGenerateIBP` 撒点域，不先生成全量关系后删除，也不把不合 parity 的积分替换为 0。
- [x] 已增加轻量 parity certificate；massive two-line 专项验证 352/352，失败时原样报告关系而不删项。
- [x] H parity 已按裸 Hankel EOM 验证 `b+n mod 2`，没有使用错误的普通偶/奇函数论证。
- [x] H 缺省 compiled `{bShift,zShift}={1,0}` 与 contact `n1+n2=1` 共同保持 child parity；h 的 `{1,2nu}` 也保持整数指标 parity。
- [x] pure time-only 的公开积分、seed、relation、zero point、prefactor 和微分算符统一使用三参数 `J`。massive-only 公式内核作为 Private vertex-basis 适配器继续认证；含 massless line 时返回中英双语 `PendingRederivation`。
- [x] 已完成 schema/metadata、sector-aware `ds`、relation/parity、time-only 公开转换和定向 smoke；正在完成 tech note、用户手册、examples、独立 benchmark 与 017 候选交付。

报告与源码遗漏复核结论（2026-07-24）：项目内最新正式内部报告和两份外部报告均已逐项复核。旧报告的 atomic massless 额外 `sE` 幂会重复计算 normalized lower-sector prefactor，不作为 package 缺陷；compact lower `ds`、reachable-sector generator 缺失是旧版真实缺陷，017 已修；massless quotient 的公式型迭代/dlog 仍按计划 fail closed。外部报告的 UTF-8 与 `StringRiffle` 结论针对旧字节或已修路径。源码续审另发现旧公开 `J[vertexPacks]` overload、`DSGenerateIBP/DSLinear` 旧 representation 分支以及 `repIterative` 诊断字段泄漏，现已删除或统一公开化；Private 公式内核保留。当前四组模块 smoke 为 `4/4`、`21/21`、`14/14`、`30/30`。

设计验收标准：同一物理积分的 fixed-line 模长幂只进入该 sector 的结构化 `J` prefactor，不因幂次不同产生多个 master token；参数重定义后 prefactor 仍能按 line/key/parameter/power 列表调取；不同 sector 的 normalization 导数及其比值不遗漏；合法 compact subsector可唯一解析；h/H EOM、缺省 shrink、contact 和用户 integer zero-point 重基均保持声明的 parity；seed 数量在生成前按 parity 域缩减；任何模块都不靠 `J->0` 掩盖奇偶性不闭合；massless 闭式递推/dlog 的未推导边界被明确隔离。

017 最终源码 smoke 记录（2026-07-24）：最终清理旧公开 overload 后重新运行 `check_017_load.wls`、`check_017_sector_model.wls`、`check_017_full_workflow.wls`、`check_017_contact_parity_contracts.wls`，分别通过 `4/4`、`21/21`、`14/14`、`30/30`。先前写入本文件的 016 手册/PDF 哈希只属于旧交付，不能作为 017 文档完成证据；017 技术笔记、用户手册、examples、独立任务书和候选交付仍须按本节未完成项实际生成并复验。

### 当前任务：2026-07-24 平行独立审计最新报告续审与真实问题收口

在已经完成全 package 资源门禁清理的 016 正式交付基础上，重新读取平行目录 `F:\Agent-projects-nut\dSibp-independent-benchmark` 的最新磁盘快照。报告发现必须按当前正式 package、公开调用合同和可重复触发条件逐项归因；只有当前交付可复现且会改变结果、状态或合法工作流的问题才进入修复。审计方错误输入、未完成实验、旧交付结论或未写入正式报告的中间文件不得冒充 package 缺陷。

- [x] 记录平行目录中正式报告、补充报告和相关证据文件的时间、大小与哈希，确认真正的最新结论文件。
- [x] 逐项对照当前 016 源码、正式单文件、手册和公开接口，区分真实 package 问题、审计方问题与尚缺证据的覆盖缺口。
- [x] 对每个疑似真实问题建立最小只读复现；只有复现成立后才修改当前模块化 016，不回写 010--015。
- [x] 如需修复，先在候选路径运行受影响 smoke 与必要回归，记录哈希后再同字节覆盖正式 package/手册，并复验正式路径；本轮没有新增真实问题，不需要重建候选或覆盖正式字节。
- [x] 清理 `000_code/test/results_test/`、`check-smoke/results_test/` 和本轮可重跑临时产物；保留报告、独立任务书、正式 package/手册、examples 与 reference bundle。
- [x] 更新本节结论、真实问题清单、检查计数、正式交付哈希和仍未由外部审计完成的边界。

验收标准：最新正式报告的每项结论均有当前版本证据归因；真实 package 问题全部修复并在候选及正式路径复现通过；审计方错误不通过放宽正确性门禁来迁就；临时产物清理后不影响正式交付、报告和 reference bundle。

完成结论（2026-07-24）：平行目录真正的最新正式报告为 `report/2026-07-24-package016-独立验证.md`，最后修改时间 03:00:13，9488 字节，SHA-256 `CB8A053B183668DB3FE00941F44E8E44019324D02A61AF9F0AF69C0690CAAD66`；原字节已归档为 `000-report/2026-07-24-0300-016-外部.md`。该报告自述并实际加载的是旧交付 `99724F7F417F2B887BAF52238D2EB880D75C089706E634C16C8A15A666AACFD6`（659207 字节、14130 行），不是当前正式 `63269CA619048C691B74C4E4F134462413428E7B27C36304B9DD6771120BB784`（645219 字节）。报告新增的 Kira/DE/scaling 结果属于旧交付证据；其中 UTF-8 直接加载与 `timeOnly` 空参数提示是此前已确认且已修复的问题，没有发现当前正式字节上的新真实 package 问题。报告把旧 `MaxSeedRuleCount/MaxEquationCount` 说成应由审计方调大，仍与用户合同冲突；把“全部 10 族 ds 通过”写入总结，但正文只给 pure massive bubble 一族的 `ds` 乘积法则，且遗留表明确全族逐变量、H/h、tree、根号坐标和参数闭合未完成，因此不能视为当前 016 全量验收。

当前正式路径复验：无编码选项的 `Get[package_016.wl]` 经 authority smoke 实际加载成功，正式文件含 UTF-8 BOM；完整生成 `13/13`（1815 equations，两个 top generator 各 225 条）和公开初始化/角色 smoke `13/13` 均通过。正式 package/PDF/reference bundle 哈希保持不变。本轮未修改 016 源码或手册；`000_code/test/results_test/` 与 `check-smoke/results_test/` 已清空，未跟踪的 `codex-independent-benchmark/full-audit-c38c4152/` 可重跑工作区已删除，正式报告与 reference bundle 保留。

### 当前任务：2026-07-24 全 package 门禁审计与遗留代码清理

用户给出的 `seedRanges` 与 `generatorSeedRanges` 唯一决定连续 seed 集合；所有适用离散 `n_i=0,1` 状态恒为完整枚举。正式 package 不得自行估算、抽样、截断，或因内部连续规则数、离散状态数、方程数超过任意常数而拒绝生成。本轮审计全部公开与私有门禁，删除 package 自设的数量、规模、迭代次数和 quick-check 硬阻断；只保留能防止数学错误、输入歧义、来源污染或不完整结果继续下游的正确性门禁，并清理所有失去调用者的消息、helper、usage、preset 字段与文档残留。

- [x] 枚举全部 `Max*`、`limit`、`tooMany`、`notReady`、`capability`、sample/preset 与迭代停止条件，逐项分类为错误资源门禁或必须保留的正确性门禁。
- [x] 删除 loop、time、pure-time、`DSGenerateIBP`、shrink-sector 和初始化路线的全部 package 自设数量门禁，生成集合只由用户输入决定。
- [x] 删除公开 `DSSeeds` 路径的隐式 sample 行为：任何 preset 与 `Automatic` 都完整枚举用户连续范围、全部生成元及全部 `n_i=0,1` 状态。
- [x] 审查其它模块的规模/迭代门禁；可由确定算法自然终止的路径删除任意次数上限，真正不收敛返回可诊断的数学失败而不是固定小常数。
- [x] tree 迭代递推以到 endpoint 的严格下降距离和 canonical 状态循环检测作为数学终止证书；删除任务书中“超过最大步数”要求，任意有限 endpoint 距离均不得被 package 自设次数阻断。
- [x] 删除公开 usage/options、内部允许键、失效消息/helper、手册及独立任务书中的旧数量门禁、抽样和最大步数说明；保留的 seed preset 只提供连续范围缺省值。
- [x] 修复 `timeOnly` 无动力学变量时参数重定义指南的空列表字符串错误、`Missing` 提案泄漏和欠完备 binding 的 `Indeterminate` 展示，并保持中英双语。
- [x] 修复 Windows 标准 `Needs["dSIBP`"]` 与直接 `Get[package_016.wl]` 的 UTF-8 兼容性：loader 采用 ASCII 并显式指定 UTF-8，冻结单文件真正写入 UTF-8 BOM。
- [x] 复核平行独立审计目录的最新正式报告及其后续运行时间线；只修报告用当前交付可复现的真实 package 问题，审计方错误输入、过时数量门禁结论和尚未写回报告的运行草稿不作为源码缺陷。
- [x] 做源码调用图与残留扫描，删除本次改动暴露的无调用兼容壳、重复计数、占位英文和过时工程分支，不改历史 010--015。
- [x] 增加轻量回归：preset 只提供缺省连续范围但不改变完整离散枚举，并覆盖超过旧连续/方程阈值的确定性输入。
- [x] 从只读 reference code/result 重建 `independent-benchmark/reference-results/pure_massive_bubble/` 轻量对照 bundle；记录来源哈希、19 维 master 顺序、convention adapter、固定有理 probe、两套 `19x19` DE 与 scaling 对角结果，未复制 55 MB/42 MB 原始大表。
- [x] 更新独立任务书和 README，明确 reference bundle 只在 source-isolated 手推与 fresh Kira 完成后的最终对照阶段读取；其它 family 不得从该 bundle 构造 expected。
- [x] 从当前模块源码构建候选单文件和手册，候选通过后同字节覆盖正式交付；正式路径复验并清理运行产物。

验收标准：任何有限的用户 seed 范围都完整展开；不存在 package 自设数量上限、预计数量确认或隐藏抽样；tree 递推只以严格数学进度或循环/奇异性诊断终止；标准 package 加载不依赖用户手工指定字符编码；输入不合法、拓扑不闭合、动量/ISP/参数欠完备、来源或哈希不一致、外部 reduction 未完成、DE 含未约对象等正确性门禁仍保持 fail closed，并在交付记录中列出保留理由。

完成结论（2026-07-24）：016 已删除 `MaxSeedRuleCount/MaxDiscreteRuleCount/MaxEquationCount`、shrink-sector 数量/深度、tree/reduction 固定迭代次数、`UseSampleOnly/DiscreteMode/sampleOnly/sampleDiscreteRules` 及相应死代码。连续范围只由用户输入和仅提供缺省范围的 preset 决定，全部适用 `n_i=0,1` 恒完整枚举；tree 三条递推改用 `{剩余可 contact theta 线数, endpoint 距离}` 字典序下降和重复 canonical 状态检测。候选/正式 smoke 通过完整范围 `13/13`（1815 equations，两个 top generators 各 225 条，均超过旧阈值）、模板 `14/14`、massive/EOM `9/9`、tree `14/14`、Kira plan `14/14`、DE 变量保护 `10/10`、Gate R5 `22/22`、authority `13/13`、example/API `4/4 + 33/33` 和手册 `5/5`。成品 examples 01、05、06 均退出 0；所有运行提示无通用英文占位句。

本轮门禁清理完成时采用的平行报告快照最后修改于 01:39；该文件随后在 03:00 更新，续审结论见上一节。两个快照中的 `timeOnly` 空提示与 Windows UTF-8 都是真实旧版 package 问题，均已修；sunrise 圈数、massive Association 键、atomic 动量角色是审计方调用错误；把旧 `Max*` 视为可配置即合理也是审计结论错误。03:00 报告新增旧交付的 Kira/DE/scaling 闭环，但仍未完成 tree、全族 `ds`、参数闭合等任务，且没有验证当前门禁清理后的正式 hash，不能标为当前 016 全量验收。

massive bubble 轻量 bundle 的四项提取门禁全部为 True：维数、精确 Gaussian 有理数、scaling `361/361` 和 source diagonal 一致。正式程序 SHA-256 为 `63269CA619048C691B74C4E4F134462413428E7B27C36304B9DD6771120BB784`；29 页权威/交付手册均为 `B7F49EBFCAE7D89D298B46B64125C7AD34AA11A1291D45B35CC200617DA1C316`；reference bundle 为 `51A141F8086A8904D4670AE9B9D3C90A7866156534648C33E94450F4E3E599AC`。PDF 全 29 页已渲染，重点页无裁切、重叠、黑块或乱码。本轮没有运行 package 边界外的 Kira reduction。

### 当前任务：2026-07-23 更新交付后的全量审计续跑

用户已再次更新 package、独立任务书和正式用户手册，并要求 package 相关检查全部从当前交付重跑；手推 expected 只有在任务书 convention、family descriptor、selector 或物理合同发生变化时才从受影响边界重推，无变化部分可在重新核对来源与哈希后复用既有 source-isolated 冻结字节。本轮不得复用任何旧 package actual、Kira/reduction、DE、消息/API 或功能测试结论。

数值口径修正（2026-07-24）：用户明确本轮只要求纯数值约化/对照。关系、导数、DE 与 scaling 的 package actual 和独立 expected 使用同一组确定性、非奇异有理规则后，以精确算术比较；不使用浮点容差，也不再把大范围符号 `Together`、符号矩阵化简或大规模符号 `DSKinematics` 作为验收前提。身份、shape、sector/generator 覆盖、消息、capability、hash、非法输入拒绝和参数闭合等结构门禁仍按结构检查，不得因改用数值系数而删除。此前启动的 Section 16/17 `N=100` 符号 kinematics 压力测试已终止且未产出结果，并从本轮审查范围删除，不以静态源码扫描替代；后续只重跑有物理或接口意义的数值/结构 checker。

轻量正确性边界（2026-07-24）：用户进一步要求所有门禁只服务正确性，不做过度检验，并在可数值化时一律数值化。因此本轮删除全部 notation 位宽、压力/性能、消息颜色与措辞、重复 API surface 和重复 example 门禁；每种物理关系、约化链和会导致错误结果下游传播的 fail-closed 条件只保留最小代表 case。未运行项在报告中记为“按用户范围排除”，不得写成通过。

续审记录（2026-07-24）：此前审查会话因连接问题中断，本轮从 `codex-independent-benchmark/full-audit-c38c4152/` 的 Phase 2 断点恢复。恢复时先重新核对任务书、正式 `package_016.wl`、正式 `package_016.pdf` 与 coverage manifest 的磁盘哈希及工作树状态；只有 convention/descriptor/selector 未变化且 reuse manifest 复验通过的手推证据可以继续使用。所有 package actual、消息/API/examples、serializer/importer、Kira/reduction、DE、scaling 与 reference comparison 均从当前正式路径重新运行，不沿用中断会话的运行结论。

续审身份漂移（2026-07-24 03:00）：恢复后任务书由冻结的 `C38C4152...E9E28` 更新为 `9012B04D...A493`。逐行差分只有三项合同变化：tree 递推取消最大步数、`DSSeeds` 取消离散抽样、Phase 2 新增 `reference_probe.wl` 最终对照；family topology、物理 convention、branch/sector/seed/variable selector 均未变化，因此对应手推关系和矩阵可在 manifest 复验后继续复用，旧“超过最大步数应拒绝”负例不得复用。正式 package/PDF 随后也在审查期间再次写入，所有基于旧 `99724F7F...AACFD6` / `AF0E75D0...FC148` 的 package actual 均作废，待交付身份稳定后从零重跑。

- [ ] 记录当前 Git、工作树、唯一版本化 package/PDF、任务书及 coverage manifest 身份，并确认审计期间无漂移。
- [ ] 对当前任务书与上一轮冻结任务书做结构化差分；逐项判断 convention、family、branch/sector/seed/variable selector 和 reference 来源边界是否变化，重推全部受影响 expected。
- [ ] 在 `codex-independent-benchmark/` 下建立本轮正式内部审计工作区；复验可复用手推 manifest 和全部 mutation/完整性门禁，不读取 `check-smoke/`。
- [ ] 对当前 package 全量重跑单向 relation/derivative、H、tree、coordinate、graph/routing、消息/capability、API/examples、serializer/importer、payload closure 和源码审阅。
- [ ] 把 package/expected 对照统一改为双方同点的确定性有理数值 exact check；删除审查脚本中的大范围符号化简和无物理意义的 `N=100` kinematics 压力测试。
- [ ] 删除非正确性门禁和重复覆盖；每项保留最小代表 case，并把范围排除项与真实通过项分开报告。
- [x] 将当前轻量 exact 数值审查的已确认缺陷、通过项和未决边界写入同一份 `000-report/2026-07-23-1508-016-内部.md`；报告未用未统一 normalization 的 mixed massive 差值判定 package。
- [x] 在同一报告登记 massless `timeOnly` 表示与额外关系的 quotient 缺口：当前三槽保留不等于完成按顶点/时间打包；旧 `repIterative`/dlog 公式必须连同 massless 额外关系重新约化后再构造。已记录两种实现顺序和共同的轻量 exact 验收，最终表示方案待用户决定；按用户要求不增加独立微分闭合门禁。
- [ ] 按当前任务书规定的 fresh reduction 矩阵运行外部 Kira、import、DE、scaling 和 reference 对照；不同输入使用互不复用的工作区和 input hash，package 本身不得启动 reduction。
- [ ] 更新同一份 `000-report/2026-07-23-1508-016-内部.md`，给出逐组 total/equal/nonzero/failed/first failure、矩阵维数、Kira 统计、最终哈希和全部未执行边界。

验收标准：手推复用必须有当前任务书差分和冻结 manifest 证据；任何 convention 或 selector 变化都从受影响推导边界重建；所有 package actual 与 reduction 均来自当前交付的 fresh run；最终报告不把 package 自检、旧 actual 或局部通过替代 source-isolated 全量结论。

### 当前任务：2026-07-23 完整手册与独立 benchmark 交付同步

本轮对 016 的正式用户文档和独立验证交付做一次全公开接口覆盖审计。以冻结单文件的 `DSPublicAPI[]`、各函数 `::usage`/`Options`、成品 examples 与当前实现为共同事实源，补齐用户手册中所有新增功能的用途、输入输出、缺省选项、失败门禁和最小调用流程；独立任务书只写验证目标与公开调用，不依赖私有 helper。完成后重新构建候选单文件和手册，执行源码调用覆盖、运行时 example、PDF 文本/视觉检查与正式路径复验，再用同一候选字节更新 `independent-benchmark/package/`。

- [x] 建立 `DSPublicAPI[]`、`::usage`、手册正文/汇总表、coverage manifest 与 example 源码的逐函数覆盖矩阵。
- [x] 补齐 `DSAllSeeds`、`DSGenerateIBP/generateIBP`、`DSKiraPlan`、Kira symbolic/postDerivative 合同、精确零模板、双语消息与 tree/timeOnly 共用撒点路线的用户说明和可复制调用例。
- [x] 核对既有初始化、参数重定义、原子算子、loop/tree workflow、Kira import/export、DE/scaling 与 introspection 函数均在手册正文和附录汇总表中有明确入口。
- [x] 更新 `independent-benchmark/independent-benchmark.md`、`README.md`、交付 package/PDF 和 examples 说明，保证任务指引不要求调用私有参数或运行 package 边界外的 reduction。
- [x] 在候选路径完成 package/API/example/PDF 检查，记录哈希后同字节晋升；正式路径复验并清理全部候选与渲染产物。

本轮验收（2026-07-23）：冻结单文件程序仍为 SHA-256 `99724F7F417F2B887BAF52238D2EB880D75C089706E634C16C8A15A666AACFD6`；更新后的 29 页用户手册为 SHA-256 `AF0E75D05DA2539F425EC3C549BFCC59A0D4BE1233DD2F3E32967D8A5A2FC148`。候选路径通过手册正文/附录/私有接口残留门禁 `5/5`、example manifest `4/4` 与 public API `33/33`、模板撒点 `14/14`、massive/EOM/精确零模板 `9/9`、direct pure-time/tree `10/10`、Kira 两阶段 planning/postDerivative `14/14`；受影响的 mixed-bubble 成品例在 6 秒内生成 50 个模板/50 条方程并确认统一与逐指标结果相同。PDF 对新增 seed/Kira 页和公开函数附录做了 Poppler 渲染目视检查，无裁切、重叠、坏字形或孤立尾页。候选字节已覆盖正式手册原件及 `independent-benchmark/package/` 的 package/manual，正式路径重复通过手册/API/example、mixed-bubble 与 Kira planning 门禁；package 未运行 reduction。

### 当前任务：2026-07-23 模板化撒点接口与双语运行反馈

本轮首要任务是用直接、可审计的模板展开替代未验证的 `DSSeedPlan` 最大 Cartesian box 优化草稿。`DSSeeds` 必须额外返回已经扁平化的统一模板集合 `allSeeds`：各 sector、各 time/momentum generator 以及每个离散 `n_i=0,1` 状态均已纳入，EOM 与可判定的 canonical/symmetry 已在模板阶段执行；连续指标 `a_i,b_i,bS_i` 与 ISP 指标仍保持符号。公开撒点接口为 `DSGenerateIBP`，并保留用户写法 `generateIBP` 作为同义入口。

- [x] 实现 `DSGenerateIBP[seeds,{indexMin,indexMax}]`：先 `Flatten[seeds,Infinity]` 到一维模板列表，再让全部真实连续指标取同一整数闭区间。
- [x] 实现精细形式 `DSGenerateIBP[seeds,{index,indexMin,indexMax},...]`：给定指标必须真实出现，且对模板中的全部连续指标 exact cover；未知、缺失、重复、非整数或反向区间一律拒绝并返回结构化诊断。
- [x] `n_i` 永远不得出现在连续撒点范围中。模板构造先完整遍历每个 sector 的全部 `n_i=0,1` 状态，再执行 EOM/canonical；精确零模板保留为可审计状态记录，符号 `n_i` 仍由撒点入口拒绝。
- [x] 输出保存扁平模板、变量顺序、范围规则、展开后的 canonical equations、计数、输入哈希与离散/EOM 证书；`DSLinear` 可直接消费该结果，不要求用户自行拼接旧 seed batch 私有字段。
- [x] 所有面向用户的 info/progress/warning/error 文本统一中英双语，每句先中文、后英文；fatal error 不受全局消息开关影响，warning/error 在 notebook 中保持红字。
- [x] 轻量 smoke 覆盖嵌套 `Table` 自动磨平、统一范围、精细 exact cover、unknown/missing/duplicate/`n_i` 门禁、`n_i=0,1` 完整性、EOM 先行、合法零模板、pure-time/tree 以及与现有小范围 `DSSeeds -> DSLinear` 路线的逐项交叉。

验收标准：新撒点模块不调用整数优化器；统一与精细两种接口在同一范围上得到完全相同的方程集合；所有失败均返回 `status -> "failed"`、明确 reason 和问题指标列表；生成结果无符号 `n_i`、无 forbidden `n`，并能进入 `DSLinear`。在此门禁通过前，不继续五族 Kira 正式运行，也不晋升候选程序或手册。

候选验收（2026-07-23）：冻结单文件程序 SHA-256 `99724F7F417F2B887BAF52238D2EB880D75C089706E634C16C8A15A666AACFD6`，29 页手册 SHA-256 `812332FDC48F986FCC55B840CE555BCAD37D863CF4ED1BBAC483FDECDC7A73CC`。候选路径检查依次通过撒点门禁 `14/14`、massive/EOM/精确零模板 `9/9`、direct pure-time/tree `10/10`、Kira planning `14/14`、DE 变量数值化门禁 `10/10`、example manifest `4/4` 与 public API `33/33`；mixed bubble 成品流程生成 50 个模板/50 条方程，统一与逐指标结果严格相同。所有这些运行均强制加载候选单文件和候选手册，未运行 Kira reduction。

晋升结论（2026-07-23）：候选程序与手册已按同一字节分别覆盖 `independent-benchmark/package/package_016.wl`、`independent-benchmark/package/package_016.pdf`，手册原件 PDF 同步更新；正式路径串行复验重复通过上述全部计数，所有公开输出均未出现通用英文占位句。并发启动七个 Wolfram kernel 时曾有一个树图进程以 Windows `0xC0000005` 退出；清空残留进程后，正式路径串行树图检查稳定通过 `10/10`，因此该事件记为宿主并发进程异常，不是 Wolfram 断言或 package 结果失败。

### 当前任务：2026-07-23 五个 Kira 数值闭环、反向撒点与最小 DE targets

替代下方旧的“两个 fresh Kira”计划。full-loop bubble+tree 与 tree dlog 没有经验证的 projection/basis map，不得直接比较；tree dlog 只与同序同 normalization 的 tree naive DE 比较。圈图的综合正确性改用数值 reduction 后的 Euler/scaling residual：master 表示和一阶求导先保持解析，形成完整 Euler 组合与所需待约积分集合后，才在同一非奇异有理点数值化系数并运行 Kira。

- [ ] 从当前 benchmark family 中固定 5 个规模可控的圈图闭环，包含 `pure_massive_bubble_reference` 和 mix bubble+tree；每个 family 只固定一个 parity channel，且 5 个 channel 不全取 even。
- [ ] reference bubble 用 package 完成同点数值 reduction/scaling，并与允许的 reference `_de`/已有结果在完全对齐 convention、basis、normalization 和数值点后比较；需要的对比资产复制到 `independent-benchmark/` 下新建的只读 reference 目录，目录内 README 说明每个文件的来源与用途。
- [ ] 其余 4 个 family 不伪造已有 DE 对照；通过同一 master basis 下的数值 Euler matrix/source residual 为零验证 IBP+一阶求导+reduction+DE 组合。
- [x] 参考 reference `_ibp` 脚本的 seed 范围和积分排序，在 016 中新增可公开调用的模块化组件：由 `DSSeeds` 给出已完成离散态/EOM 的扁平模板，用户再通过 `DSGenerateIBP` 的统一或逐指标范围显式撒点；`DSKiraPlan` 与 `DSKiraExport` 按 reference 惯例生成积分顺序、targets 和 Kira 基础信息。
- [ ] 反推的 seed 范围必须覆盖对候选 master 一阶求导产生的全部积分；Kira targets 只包含构造 DE 所需的最小待约集合。若 master 未知，允许先做一次轻量预约化选 master，再重建最小 targets 的正式约化；两次 manifest 和输出必须隔离。
- [x] 公开 API、options、错误门禁、进度信息和成品 example 写入手册与 coverage manifest；独立任务书只负责调用这些模块并审计输出，不要求审阅方重写撒点或 Kira 信息生成器。
- [x] 撒点/Kira planning 模块按候选先行门禁重建 package/手册，运行新增 smoke 和受影响回归后同字节晋升。
- [ ] 五个 family 的真实 Kira reduction/scaling 闭环仍须完成；本次模块晋升不冒充该外部计算验收。

验收标准：5 个 family 的名称、branch、parity channel、active basis 和有理点全部冻结；每个 family 只运行一个 parity channel 的真实 Kira；解析一阶导数先于数值代入；seed 覆盖 derivative closure 且 formal targets 为最小集；5 套 scaling residual 都为零，reference bubble 还与已有结果同点一致。

本轮真实 Kira 的 family/channel 冻结如下；`parity` 按每条 cycle line 的 `b+Sum[n]`（shrunk line 为 `bS`）奇偶性依 `lineOrder` 书写，fixed/bridge line 不另造 parity 槽。各 channel 必须作为输入 `symmetryRules` 的确定规则进入 package，而不是在导出后筛结果。

| family | branch | parity channel | reduction oracle |
| --- | --- | --- | --- |
| `pure_massive_bubble_reference` | `--` | `{even,even}` | 数值 scaling；另与 reference `_de` 同点比较 |
| `pure_massless_bubble` | `--` | `{odd,even}` | 数值 scaling residual |
| `mixed_bubble` | `-+` | `{odd,even}` | 数值 scaling residual |
| `mixed_triangle` | `+-+` | `{even,odd,even}` | 数值 scaling residual |
| `mixed_bubble_tree` | `+++` | cycle `{odd,even}`；bridge 无 parity 槽 | 数值 scaling residual |

五族都先在解析层冻结有序 candidate masters、各动力学变量的一阶导数、Euler 组合和 derivative target closure；之后才允许在同一非奇异有理点把全部 reduction 系数数值化。这里得到的是该点上的精确 scaling 验证，不冒充符号 DE 矩阵。tree `DSTreeNaiveDE`/`DSTreeDLogDE` 仍只在纯树图的同序 master basis 中比较，与上述五个 full-loop Kira 系统无直接矩阵比较。

### 当前任务：2026-07-23 Kira 数值边界、scaling identity 与 mix bubble+tree

收紧独立审阅的外部 reduction 合同：Kira 只数值化约化所需的非微分系数参数，所有 DE 变量及其函数依赖必须保持符号；fresh Kira 只选 reference massive bubble 和 bubble+tree 两个代表 family，避免把全 family 数值 reduction 误写成验收要求。同时从 reference `_de` 路线、当前 `DSScaleCheck` 和一般齐次性推导准确的 scaling relation，再写入任务书。

- [x] 明确 Kira 前可数值化集合、禁止数值化集合、generic rational probe、奇异面/秩退化门禁、manifest 与 fresh-result 时间门禁；记录 equations、independent relations、masters、targets、unreduced 和耗时。
- [x] 推导并写明同序 master basis 下的 Euler/scaling matrix identity，包括 basis scaling degree、DE 矩阵组合、非齐次 source 和换基/degree conjugation；与 `002 bubble_de.m` 和当前 `DSScaleCheck` 实现逐项核对。
- [x] 把数值 Kira 闭环限定为 `pure_massive_bubble_reference` 与第 17.4 节 bubble+tree；2026-07-25 收缩后，前者完成 package export/import/DE/scaling 与 reference 路线比较，后者只完成 package full-loop Kira/DE/scaling，不再要求 Phase 1 独立推 DE matrix。tree naive/direct-dlog 只在同序 tree master basis 内比较；没有经验证的 loop-to-tree 投影时禁止直接比较。
- [x] 将 bubble+tree 改为一条 massive h cycle、另一条 massless cycle、massless bridge；同步 sign-dependent packs/contact、zero-point、参数闭合、Kira/DE 合同、成品 example 和用户手册，避免仍按三条 massive 线验收。
- [ ] 先在候选路径重建程序/手册并运行受影响 smoke；仅在候选通过且记录哈希后覆盖正式独立交付，正式路径复验后清理候选与运行产物。

验收标准：任何 Kira numeric rule 都不包含 DE variables；导入后 `DSDE` 仍可对全部声明变量求导；scaling residual 按推导的矩阵恒等式逐项为零；只运行两个明确代表 family；mix bubble+tree 的 cycle/fixed packs、contact DAG、zero-point、seed/linearData 和 DE 均与新质量配置一致。

### 当前任务：2026-07-23 最新审阅报告归因与 authority 修复

逐项复核 `000-report/2026-07-23-1508-016-内部.md` 的四个阻断及 reviewer mutation 说明，分别回到当前任务书、图论/routing 和 reference 输入依赖判断责任归属。报告本轮未进入 Phase 2，因此不得把 authority 阻断改写成 package 运行失败，也不得为修任务书无依据重建 016 程序或手册。

- [x] 把第 9.5 节 `mixed_triangle` 明确修为由现有三条 routing 唯一确定的一圈 family：`loopMomenta={q}`、`L=1,K=2`、三个 momentum generators，无 ISP。
- [x] 把第 17.3 节 `vertex_energy_signs` 自定义坐标改为完整覆盖 `{k1,k2}` 三维 Gram basis，并逐 energy case 覆盖其 `kE` 模长；over/under 均相对该完整 exact 规则定义。
- [x] 为第 17.4 节 bubble+tree 固定 `vertexSignCases=<|"+++"->{+1,+1,+1},"++-"->{+1,+1,-1}|>`，两分支执行同一完整验收矩阵。
- [x] 扩充第 13.2 节只读 reference source 许可到 `001` 直接读取的四份 Omega/MI 源数据；仍禁止把旧 `kira_bubble` reduction/result 当作 Phase 1 expected，并明确 active 17、辅助 20/21 可由何处重建。
- [x] 运行任务书静态一致性、引用残留、Markdown fence 和 `git diff --check`；新任务书 SHA-256 为 `CCF390AB9D39643B4B6023C1F8C8ED5111567FB85EFE84E237495B885EEB6073`。旧 `2E6F0C57...` 审计结论保留为旧 authority 的真实证据，新快照必须从 Phase 1 重新审计。

归因标准：B1--B4 若由任务书内部矛盾或许可来源不足触发，归为维护方 authority 问题；审阅方保持 `MISSING/BLOCKED`、未读取 package、未把阻断写成 package `FAIL` 属正确处理。reviewer 自有 comparator 在 mismatch 时仍退出 0 属审阅工具问题，虽然本轮 JSON gate 已检出 mutation，不进入 package 或任务书修复。

完成结论（2026-07-23）：B1--B4 全部确认是维护方任务书/来源许可问题，均已修正；本轮报告没有 package actual，因此不存在由该报告证明的 016 源码缺陷。任务书 authority 静态检查 `8/8`，公开接口维护 smoke `12/12`；`mixed_triangle` 初始化为一圈，`vertex_energy_signs` exact context 的变量顺序为 `{r11,r12,r22,e1,e2}`。当时任务书中的三 massive-line bubble+tree 快照已被本文顶部的 mix bubble+tree 新合同取代，不再作为当前验收输入。运行日志已清理，只保留可复用 smoke 脚本。审阅方的 comparator 非 fail-fast 是其工具改进项；B4 中把显式候选系数说成必须依赖旧 Kira 产物不够准确，真正缺的是未获许可的四份 Omega/MI source，但其阻断结论仍正确。

### 当前任务：2026-07-23 GitHub 仓库改名后引用同步

- [x] 验证 `git@github.com:J7C/dSIBP-development.git` 可访问且 `main=921dd7b`，本地 `origin` 已更新。
- [x] 全仓扫描改名前的仓库名及旧 GitHub URL；README 和当前开发状态已更新，历史审计报告中的当时证据保持原样。
- [x] 引用更新提交 `e8a96e6` 已推送到新远端，`HEAD...origin/main=0/0`。

验收标准：当前使用说明和开发状态只引用 `dSIBP-development`；正式 Mathematica package/context 仍为 `dSIBP``；仓库保持 private 的状态由 GitHub 权限接口或用户设置确认，不凭 SSH 可访问性猜测。

完成结论（2026-07-23）：GitHub 新 SSH 地址、远端 `main` 和本地 `origin` 均已验证；README 已采用开发仓库名 `dSIBP-development` 并明确正式 package/context 仍为 `dSIBP``。排除 `.git`、独立审计工作区和历史归档报告后，当前项目文档中的旧仓库名/URL 命中数为 0。

### 当前任务：2026-07-23 更新交付后的全量独立审计重启

用户已更新 package、正式手册与独立任务书。本轮以当前磁盘快照为新的唯一审计对象，从新的来源隔离工作区重新执行第 2--17 节；旧 `check/full-audit-932331ab/`、旧冻结 expected、旧 package 对照和旧 reduction 产物仅保留为历史证据，不进入新一轮 expected 生成、计数或结论。

本轮重启时 Git `HEAD=921dd7b44c2c6d01cc81293c02efb126bc9f738d`；完成 Phase 1 交叉审阅时为 `668c8d2a2730ec023d63e7fa969abfc214b2c832`。并发提交只更新仓库改名状态，任务书、package 与手册的下列 SHA-256 身份始终未漂移。

- [x] 动态解析唯一当前交付版本为 `016`；记录任务书 SHA-256 `2E6F0C578A51199C1590E091050465E5194343590784D4146BEB93A6A1FAAB77`、程序 SHA-256 `6EA99CF51BA1AB6916E8FFB99266F1C9596036E5B87147A40C1D0E251D34A67A`、手册 SHA-256 `59E6BC3072AA251E1B15840882053C152CCE60B9A1BCA6BB84C42C64E1862DCD`。
- [x] 复核任务书第 9.9 节当前 authority：标准两圈 sunrise 使用 `loopMomenta={q1,q2}`、`loopExternalMomenta={k}`、两个 ISP 与六个 momentum generators；此前中间快照的 `{q}`/双外向量冲突不再存在。
- [x] 旧 `2E6F0C57...` 快照第 9.5 节存在 authority 冲突：`mixed_triangle` 声明 `loopMomenta={q1,q2}`，但三条 line routing、`dqq/dqk` generator 和正文只使用未声明的单一 `q`。一圈解释有 652 tuples，按声明的 `L=2,K=2` 合同应有 1247，精确缺 595；审阅方未从 package 猜修。
- [x] 旧快照第 17.3 节 `vertex_energy_signs` modern-field 变体使用未声明的 `k` 和“唯一 loop Gram”；第 9.10 节实际为 `{k1,k2}` 的三维 Gram basis，相关 exact/over/under、自定义 `ds`、seed/linearData 和参数闭合被正确阻断。
- [x] 旧快照第 17.4 节 bubble+tree 未给出具体 `vertexSignCases`。不同混合 sign strings 改变 full/cross line、sector DAG、seed 和 DE 维数，审阅方正确地没有代选。
- [x] 旧快照第 13.2 节许可的两份 reference 脚本不自包含 active 17 与辅助 20/21 的候选表达式；`001` 直接依赖的四份 Omega/MI source 不在 Phase 1 许可来源内，相关项被正确阻断。
- [x] 在 `check/full-audit-2e6f0c57/` 建立来源隔离说明、工作计划、身份记录、参与者所有权和 163 项五级覆盖矩阵。
- [x] 对九个 authority-valid family 完成双路线手推：relations `2028/2028`、general derivatives `206/206` exact，非零差值、重复、缺失、意外项均为 0；H、symmetry/parity 专项通过。
- [x] 对第 14--17 节可执行子集完成 73 条 flat tree relation、73 条 projection、3 个 iterative case、4 个 invalid case、naive/dlog 矩阵和坐标/路由 expected；fresh verifier `31/31`。
- [x] 独立 reviewer 完成 relation coefficient、缺失 artifact group、later relation/group/source-hash 三类 mutation；完整 family 证据链两次均为 113 files，combined SHA-256 `0C92E8387CF7989AC6B2A691D42E6A9F4F42C442186D4B23416CFC878351E63F`，逐文件差异 0。
- [x] Phase 1 最终矩阵为 `PASS 65 / FAIL 0 / MISSING 68 / BLOCKED 30`，完整冻结授权未取得；九-family 与 later 可执行子集只授权为 byte-stable Phase 1 evidence，不能开放 Phase 2。
- [x] Phase 2 package/手册内容读取、全量单向比较、API/examples、消息/capability、serializer/importer、payload hygiene、两套 fresh Kira、DE/scaling/reference actual 均未执行；只由协调者记录了交付身份哈希。
- [x] 同一份正式内部报告已更新为本轮证据、阻断项和未运行边界。

验收标准：新一轮目录不读取 `check/full-audit-932331ab/`、`check-smoke/`、旧 expected、旧报告或 package 自检结果来生成 expected；第 2--17 节全部有新快照证据；Phase 1 冻结先于任何 package/手册读取；两套 reduction 的 input hash、manifest 和结果目录互异且均为 fresh；任何失败或未执行项不得被局部通过覆盖。

该轮结论（2026-07-23，适用于旧任务书 `2E6F0C57...`）：**BLOCKED / INCOMPLETE**。四类 authority/reference 缺口使完整 Phase 1 不可冻结；按任务书门禁没有加载 package/手册内容，也没有运行 package、Kira 或 DE。已完成的九-family 和 later 子集证据保留，但不得表述为当前交付通过全量审计。当前修订后的任务书身份与后续验收入口见本文件顶部“最新审阅报告归因与 authority 修复”。

### 当前任务：2026-07-23 GitHub 开发仓库改名与版本发布

- [x] 改名前已核对 `main`、原 `origin` 与 `HEAD...origin/main=0/0`；SSH fetch 正常。
- [x] 审计当前非忽略工作树：发布源码、文档、任务书、examples、正式 package/手册、归档报告和 `check-smoke`；根目录 17 MB 独立审计工作区 `check/` 已明确忽略。
- [x] 发布前门禁通过 Gate R5/sunrise `22/22`、example coverage `4/4`、public API `29/29` 和手册哈希一致；提交 `6c231e7` 已推送到当前 `origin/main`。归档报告原件保留三处 Markdown 强制换行尾空格。
- [x] 用户已把 GitHub 私有仓库重命名为 `dSIBP-development`；本地 `origin` 已同步为 `git@github.com:J7C/dSIBP-development.git`，`HEAD...origin/main=0/0`。

验收标准：GitHub 仓库保持 private；新仓库名与正式 package `dSIBP` 明确区分；远端没有遗漏本版非忽略项目资产，也不包含临时候选或运行产物；最终工作树与远端状态明确记录。

完成结论（2026-07-23）：开发版本提交 `6c231e7` 及状态记录提交 `921dd7b` 已推送；GitHub 仓库现名为 `dSIBP-development`，本地 `origin` 已更新并验证 `main` 一致。根目录独立审计工作区 `check/` 未上传；正式 Mathematica package/context 继续使用 `dSIBP``。

### 当前任务：2026-07-23 独立验证交付同步复核

- [x] 从当前 `000_code/016_dSIBP/` 模块源码构建临时候选，比较其与 `independent-benchmark/package/package_016.wl` 的字节和 SHA-256：均为 599608 bytes、`6EA99CF51BA1AB6916E8FFB99266F1C9596036E5B87147A40C1D0E251D34A67A`，逐字节相同。
- [x] 强制加载候选运行 sunrise/Gate R5 smoke，结果 `22/22`；确认正式单文件不是落后副本。
- [x] 比较正式手册原件与独立验证目录 PDF：两者 SHA-256 均为 `59E6BC3072AA251E1B15840882053C152CCE60B9A1BCA6BB84C42C64E1862DCD`。
- [x] 候选已清理。package 与手册原件均已同步到独立验证目录；本轮无需重新覆盖正式交付。

完成结论（2026-07-23）：`independent-benchmark/package/package_016.wl` 是当前 14 个模块重新构建所得单文件的逐字节相同副本，候选加载 smoke 为 `22/22`；`package_016.pdf` 与正式手册原件逐字节相同。sunrise 修正只改变独立任务书与维护 smoke，不改变 package API、实现或用户手册内容，因此没有制造仅更新时间不同的新副本。

### 当前任务：2026-07-23 两圈 sunrise authority 纠正

上一轮把“三条平行边”解释成两个独立外部向量分别注入三条边的广义两圈 family，并据此写成四个 ISP、八个 momentum generators。用户现明确目标是标准 sunrise 图；此前 9.9 的四 ISP/八生成元结论暂停有效，必须按单一外部动量流重新计算标量积空间和分母秩。

- [x] 对标准两圈 sunrise 的 loop/external 标量积空间、三分母秩和缺失方向做独立线性代数计算：空间维数 5、分母秩 3、最小 ISP 数 2。
- [x] 按计算结果修订任务书 9.9 和定向 smoke；不得把广义双外向量 family 继续称作 sunrise。
- [x] 用模块入口和正式单文件验证图圈数、routing、最小 ISP closure、五个标量积方向、精确反解和六个 momentum generators；通用核心无需修改，未重建或覆盖交付。
- [x] 更新完成结论与哈希；本轮未生成候选或 smoke 运行产物，无需清理。

验收标准：第 9.9 节 topology 与标准 sunrise 的单一外部动量流一致；ISP 数量由 `L(L+1)/2+LK` 个 loop-dependent scalar products 减去三分母的独立秩得到；package 初始化、ISP closure、seed coverage 与任务书 generator 数完全一致。

完成结论（2026-07-23）：任务书 9.9 已改为标准两圈 sunrise：`{q1,q2,k-q1-q2}`、`loopExternalMomenta={k}`、两个 ISP `{q1.k,q2.k}` 和六个 momentum generators。定向 smoke 同时核对 `graphLoopCount=2`、`nK=1`、五个标量积方向、`spCount=5`、`ispCount=2`、坐标唯一求解及 `q1.q2=(D3-k^2-D1-D2+2 rho1+2 rho2)/2`，模块入口和正式单文件均为 `22/22`。016 通用核心原已支持该结构，故 package/手册未重建；正式哈希保持程序 `6EA99CF51BA1AB6916E8FFB99266F1C9596036E5B87147A40C1D0E251D34A67A`、PDF `59E6BC3072AA251E1B15840882053C152CCE60B9A1BCA6BB84C42C64E1862DCD`。

### 当前任务：2026-07-23 Gate R5 新发现修复

根据 `000-report/2026-07-23-1508-016-内部.md` 的 fresh-kernel 证据，修复当前 016 交付中的复合独立外动量编号、atomic massless `timeOnly` seed 能力和失败 capability 一致性，并纠正独立任务书第 9.9 节三平行边 family 的圈数/routing/ISP authority。公开状态枚举的函数调用、选项、返回结构和示例属于用户手册与成品 example；独立任务书只规定必须经公开文档化接口完成的验收范围。

- [x] 修复 `{p1+p2,p2}` 等复合 `independentExternalMomenta` 的顶层位置匹配，使不同声明稳定得到不同 `sEi`。
- [x] 让公开 `DSSeeds` 在 `timeOnly` 的未 shrink `masslessFull` sector 中枚举全部适用 `n=0/1` 状态并生成 contact-reachable sector，不依赖私有调用参数。
- [x] 统一 `DSInit` 失败 Association 与嵌套 capability；任何最终初始化失败均不得留下可误用的 `initialization/time/momentum` 能力。
- [x] 已撤回错误的双外向量、四 ISP、八 momentum-generator 输入；标准 sunrise 的最终修订与复验由上一小节记录。
- [x] 更新用户手册、公开 API/example coverage、模块源码与冻结单文件；完成候选路径和正式路径 smoke，记录哈希并清理运行产物。

验收标准：`energyCaseC` 缺省变量严格为 `{sE1,sE2}` 且 `DSInit` 成功；atomic massless `++` 的 `DSSeeds` 覆盖 `{top,e1}` 与全部适用 `n=0/1` 状态；构造一个最终失败 context 时所有公开下游 capability 均为 False；9.9 标准 sunrise 通过 graph/routing/ISP 初始化并生成规定的 `2(2+1)=6` 个 momentum generators。

历史完成结论更正（2026-07-23）：其中前三项程序修复仍有效；原 9.9 的双外向量、四 ISP、八 momentum-generator 结论是 topology 误解，不能作为 sunrise 验收证据。标准 sunrise 改为单一 `k`、两个 ISP 和六个 momentum generators 后另行复验；既有 package/手册哈希仅表示上一轮正式字节，不表示这项错误输入已通过。

### 当前续作：2026-07-23 Gate R3 来源隔离、只读复核与参数闭合修复

Gate R2 的物理全等计数已通过，但 R3 审阅发现 `family_repair_current/inputs_family_repair_current.wl` 仍读取 sibling `family_current/` descriptor，且 `self_check_family_repair_current.wls` 会覆盖 `results/summary_family_repair_current.wl`；交叉 closure 随后又发现两条路线共同把 `vertex_energy_signs` 的原始 `k1/p1/p2` 模长泄入 payload。此前仅依据双路线全等给出的 PASS 因共同盲点失效，本轮同时修复来源隔离、冻结工作流、production canonical/parity 与最终坐标闭合。

- [x] 仅依据当前任务书第 9--11 节，在 repair 目录自包含重建十个 family descriptor 与 reference symmetry 必要 helper。
- [x] raw 路线只计算 expected payload；由独立一次性 freeze 脚本负责落盘，self-check 只读冻结输入并只 `Print/Exit`。
- [x] 删除 self-check 对 `results/summary_family_repair_current.wl` 的写入；如需保留 summary，由独立 freeze 脚本显式生成。
- [x] 把 reference R2-to-R1、整数 orbit canonical、R1 endpoint 与 parity 接入两条 production relation/derivative 链，并用独立算法全等复核。
- [x] 在 `raCase`/`sbConfigured` 分别把每个 energy case 的独立模长映射到 `sEi`、loop Gram 自模长映射到 `ssii`；从 descriptor 推导原始方向符号并加入零残留拒绝门禁。
- [x] 重新冻结 expected，运行 fresh-kernel 只读自检，并确认 `2983/2983`、`261/261`、artifact `49/49` 及全部 residual 门禁仍通过。
- [x] 扫描所有执行脚本的加载路径，确认不存在 sibling `family_current`、package、`000_code` 或 `check-smoke` 读取，并更新文档与 SHA-256 manifest。

验收标准：fresh reviewer 运行 `self_check_family_repair_current.wls` 前后所有冻结文件哈希不变；两条路线只共享本目录从任务书转录的 descriptor；任何 sibling expected/helper、package 或维护 smoke 资产均不进入加载图。

完成结论：最终 fresh self-check 为 relations `2983/2983`、derivatives `261/261`、artifacts `49/49`，逐式非零、重复、缺失、意外、placeholder、internal、private、package-context 与 source-direction residual 均为 0。交叉首残留 `energyCaseB/++/top/dtau[v1]` 的 `-I J Sqrt[sp[k1,k1]]` 已映射为 `-I J ss11`；case A/C 的独立方向按列表顺序映射为 `sE1,sE2`。修后 case A/C 的物理系数集合为 `{alpha1,alpha2,beta1,beta2,dim,sE1,sE2,ss11,ss12,ss22}`，case B 为其不含 `sE2` 的子集，`k1/p1/p2` 残留均为空。

### 当前续作：2026-07-23 Gate R2 双独立物理路线修复

在 `check/full-audit-932331ab/family_repair_current/` 内继续修复 Gate R2。冻结 raw-kernel/product-rule expected，显式 `J` index-shift 路线只共享 family descriptors，不共享 relation、coefficient、sector、state、momentum 或 derivative helper；不得读取 package、`000_code/`、旧 expected/report、`check-smoke/`、Git 或 memory，也不得修改 `family_current/`。

- [x] 修复显式路线语法并在 fresh kernel 生成八个 full-loop family 的全部 relation/derivative payload。
- [x] 按 evaluated tuple key 检查总数、唯一数、重复、缺失、意外项与首个非零差值；不得通过排序或后处理隐藏差异。
- [x] 对 relation、derivative 和 executable artifact 做全 payload exact residual scan，覆盖 symmetry/parity/reference canonical、完整外部 ISP 导数与真实 `HToh` 变换。
- [x] 仅在 `2983/2983` relations、`261/261` derivatives、全部 artifact exact、重复/缺失/意外/placeholder/internal/private/package 调用均为零时把 Gate R2 标为 PASS。
- [x] 更新来源隔离、覆盖、推导、状态与结果摘要，并记录所有交付文件的 fresh-kernel SHA-256。

验收标准：两条物理路线独立构造并逐 tuple 严格相等；relation key 和 derivative key 均为已求值、唯一的结构化状态；完整 payload 不残留生成 helper 或 package 私有符号；任一未执行项、计数不足或非零差值都必须保留 FAIL/MISSING，不得升级为 PASS。

完成结论：fresh kernel 顺序复验通过。raw-kernel/product-rule 与显式 `J` index-shift 路线分别得到 2983 个唯一 relation tuple 和 261 个唯一 derivative tuple，逐式非零差值均为 0；49 个 HToh/reference/per-family artifact 叶组全等。重复、缺失、意外 tuple 及 placeholder、route-internal、private、package-context 残留均为 0，八个 full-loop family 无缺失，Gate R2 状态为 PASS。该结论不包含 package 对照或 reduction。

### 当前任务：2026-07-23 最新任务书全量独立 check 执行

按用户最新指令，以当前 `independent-benchmark/independent-benchmark.md` 为唯一 Phase 1 权威输入，立即执行第 2--17 节全量 source-isolated check。审计基线为 Git `HEAD=70c190a1606bdd1a4641b2fabfbf8ea3ff05a5ec`，任务书 SHA-256 为 `932331AB2A2724B92BA9A70F2A5D02D55D455B07B65CF130FB3675BF7009EA5C`。当前工作树含维护者未提交的任务书、package、examples 和文档修改，全部保留并按最终磁盘快照复核，不回滚或覆盖。

任务书已修正核心 convention：第 9 节范围为八个 graph-valid loop family 与两个 atomic `timeOnly` family，生产性输入统一使用 `loopExternalMomenta`、`independentExternalMomenta`、缺省 `ssij/sEi`、cycle/fixed line pack 和统一 Head `J`；Phase 2 动态解析 `package/` 中唯一最新版本交付。此前 `check/contributors/` 基于旧任务书生成的推导、expected、自检和覆盖矩阵全部标记为 stale，不得参与本轮冻结、计数、package 对照或报告结论；本轮新产物只写入 `check/full-audit-932331ab/`。

- [x] 在 `check/full-audit-932331ab/` 建立独立来源说明、工作计划、任务书快照哈希、参与者所有权和五级覆盖矩阵；旧 `check/contributors/` 只保留失效标记，不作为输入。
- [ ] Phase 1 第 2--13 节曾完成并在 Gate R4 冻结，但 Gate R5 发现第 9.9 节 family authority 本身不满足 graph-loop 门禁，相关 accepted 状态已撤销，必须在任务书修正后重新独立生成。
- [ ] Phase 1 第 14--17 节产物字节保持冻结，但因全局 authorization 撤销，不再作为可开放完整 Phase 2 的 accepted evidence。
- [ ] oracle-only 自检与 Gate R4 审阅曾给出授权；Gate R5 以五个 fresh-kernel probe 复现前置失败后正式撤销该授权，accepted bytes/digest 只保留为历史证据。
- [ ] Phase 2 已动态解析当前最新 package/手册并进入 runtime gate；因 Gate R5 阻断而停止，未运行完整单向逐式比较、工程负例、两套 fresh reduction 或 DE。
- [ ] 按 family/branch/sector/generator/state/variable 报告 total/equal/nonzero-difference/failed/first failure、矩阵维数、参数差集、Kira 统计和哈希；正式报告归档到 `000-report/YYYY-MM-DD-HHmm-{currentVersion}-内部.md`。

验收标准：新任务书第 2--17 节逐项有当前证据；Phase 1 冻结先于 package 读取且 Phase 2 后哈希不变；旧 convention/旧 expected/旧报告不进入数据流；两次 reduction 使用不同 input hash、manifest 和结果目录并均满足 0 unreduced；任何未执行或证据不足项明确降级，不能用结构自检或 package 自检冒充 source-isolated strict-zero 证明。

Phase 1 冻结记录（2026-07-23）：Gate R4 逐项矩阵为 `PASS 130 / FAIL 0 / MISSING 33 / INDETERMINATE 0`，其中 33 项均为明确留给 Phase 2 的 runtime obligation。family relations 为 `2983/2983`、general derivatives 为 `261/261`、HToh/reference artifacts 为 `49/49`，逐式非零差值、重复、缺失、意外项、placeholder、内部原子和 source-direction residual 均为 0；pure-time/tree scoped relations 为 `987/987`，两条路线各覆盖 5815 个 variable tuple；共同-theta term/DAG 分别为 `18720/18720` exact，contact/permutation 为 `44560/86800`，O007--O010 failure 均为 0。31 个 accepted 文件以 ordinal `path|SHA256` 冻结，任务书 SHA-256 为 `932331AB2A2724B92BA9A70F2A5D02D55D455B07B65CF130FB3675BF7009EA5C`，冻结 manifest SHA-256 与 accepted digest 均为 `E0FE0E5649A227CBA024A519C543ED65615A2CAA9ABE2CC7D028F59B0C978676`；Freeze 与紧随其后的 Verify 均通过，至此才开放 package 读取。

Gate R5 撤销记录（2026-07-23）：进入 Phase 2 后动态解析出的唯一交付为 `package_016.wl/pdf`，fresh kernel 运行时版本为 `016`，程序/PDF SHA-256 分别为 `1B56520F3174EDE70AC2FAE30E18AD056A50AD47038B63E74C981D7F1FB4D3DC` 与 `55B13233C78F0083DCE0DAB0E2D38EC984FE2AA1596AF01823C728BD5A13BE4C`。独立 reviewer 的 `5/5` fresh probe 复现三个阻断：(1) 第 9.9 节三条平行内线给出图圈数 `3-2+1=2`，却固定 `loopMomenta={q}`，两分支均被正确的 routing gate 拒绝，故任务书 authority 与 Phase 1 graph-valid 前提失效；(2) 第 9.10 节 energyCaseC 的两个独立模长在当前 package 中同时映射为 `sE1^2`，坐标 rank `4/5`、缺 `sE2`，两分支初始化失败；(3) `atomic_massless_line/++` 初始化成功但公开 `DSSeeds` 返回 `unsupportedTreeState/masslessFullNeedsTreeState`，且公开 Options 无 `TreeState` 参数。Gate R5 因此把 Phase 1 authorization 标为 `REVOKED`、Phase 2 标为 `STOPPED`；保留的 31 文件 digest 复算仍为 `E0FE0E...`，但只证明字节未变，不再表示验收通过。完整 `2983/261/49` package 对照、API/importer 全负例、两套 fresh Kira、DE/scaling/reference comparison 均未执行；Kira/reduction/DE 运行数为 0，两个预备 reduction 目录保持空。正式报告归档到 `000-report/2026-07-23-1508-016-内部.md`。

Gate R5 后续作边界（2026-07-23）：按用户指令继续所有不依赖三个已确认阻断的检查。9.9 两分支、energyCaseC 两分支和 atomic massless public seed 路线保持失败/缺失，不通过修改 expected、手写替代规则或私有参数绕过；其余可初始化 family 的 relation/general `ds`/H 路线、tree/custom、API/examples、消息/capability、serializer/importer 和 payload hygiene 继续执行并分别报告实际覆盖分母。pure massive bubble 的默认与 exact custom fresh Kira、import、DE、scaling/reference 路线允许作为隔离诊断闭环运行，必须使用不同 context/input hash/目录和本轮新结果；其通过不能恢复 Phase 1 authorization，也不能把总报告升级为 PASS。所有新增结果回写同一份 `2026-07-23-1508-016-内部.md`，明确区分 blocked、not applicable、diagnostic pass 与正式全量通过。

### 当前任务：2026-07-23 独立任务书 convention 与最新交付一致性审计

本轮只审计并修订 `independent-benchmark/independent-benchmark.md`、目录说明和独立交付副本，不执行独立推导、package 物理对照或 reduction。任务书第 2--15 节的用户输入、公开坐标和 package 调用必须与当前主线 convention 对齐：统一使用 `loopExternalMomenta`、`independentExternalMomenta`、`ssij/sEi`、cycle/fixed line pack 和统一 Head `J`；旧 `externalMomenta/externalLegMomenta/externalInvariantRules/sij` 只允许出现在明确标注的兼容性测试或 reference-code adapter 中。Phase 2 一律动态解析 `package/` 中唯一提供的当前程序/手册，不在执行指令中硬编码版本号。

- [x] 逐段盘点第 2--17 节的旧输入字段、旧平方坐标、旧 line pack、旧 Head 与硬编码 package 调用，区分错误的当前 convention 和合法的历史适配。
- [x] 把全部当前审计输入、手推变量和 package 验证入口改为最新版 convention；保留的旧记号均显式标记为 compatibility/reference-only。
- [x] 统一 Phase 2 为动态解析唯一交付程序和同 token 手册，记录运行时版本与哈希，但不调用或引用旧 package；成品 examples 共用无固定版本加载器。
- [x] 校验 `independent-benchmark/package/` 中程序与手册分别等同于当前模块源码构建结果和权威 PDF；二者已完全一致，无需重复覆盖。
- [x] 扫描残留、检查 Markdown fence、Wolfram 语法与 `git diff --check`，记录具体修复和交付哈希。

完成结论：任务书第 2--15 节原先确有 `externalMomenta/externalInvariantRules/s11`、全线 `{b,...}` pack 及单 bridge 伪作圈图的旧 convention。现已统一为显式 `loopExternalMomenta/independentExternalMomenta`、缺省 `ssij/sEi` 根号坐标、cycle/fixed pack 和统一 `J`；两个 atomic family 改为合法 `timeOnly` fixed-line oracle，第 9 节其余八个 family 使用 graph-valid full-loop 输入。旧字段只保留在 compatibility probe，`G/R1/R2` 只保留在 reference adapter。成品 examples 不再加载仓库内版本目录，而是动态加载交付目录唯一程序并核对同 token 手册。fresh example 还发现并修正了 package 用户 warning 中残留的 `externalInvariantRules/default sij` 文案；模块重建程序与晋升后的交付程序 SHA-256 均为 `1B56520F3174EDE70AC2FAE30E18AD056A50AD47038B63E74C981D7F1FB4D3DC`。权威与交付 PDF 均为 `55B13233C78F0083DCE0DAB0E2D38EC984FE2AA1596AF01823C728BD5A13BE4C`。

验收标准：任务书中的生产性输入和 Phase 2 调用不再依赖旧字段、旧默认平方坐标或任何写死版本；所有保留的旧 convention 都有明确兼容/参考边界；交付目录只有一对最新版本化程序/手册且与权威成品哈希一致。

### 当前任务：2026-07-23 自定义变量 DE 与完备性任务书扩展

本轮只扩展 `independent-benchmark/independent-benchmark.md`，不执行独立审计。新增一个同时含两个 `kL` 与至少两个 `kE` 的固定自定义坐标例，使用 `k11=|kL2|`、`k22=|kL1|`、`k12=|kL1+kL2|`，明确它与缺省 `ss12=Sqrt[sp[kL1,kL2]]` 不同；并要求 bubble+tree、该双 `kL` 例和另一个 energy/ISP 例分别测试 exact/overcomplete/undercomplete、自定义 DE 算符生成及 seed/DE 最终参数闭合。

- [x] 在任务书固定双 `kL`/双 `kE` topology、缺省坐标、自定义规则、Jacobian 秩和逐变量 DE 算符检查。
- [x] 为 bubble+tree、双 `kL` mixed triangle 变体和 `vertex_energy_signs` 变体分别加入 exact/overcomplete/undercomplete 自定义变量矩阵。
- [x] 规定过完备红色 Warning、返回冗余/null-space/约束并关闭有歧义的 `ds/DSDE`；欠完备红色 Error、返回 missing/null-space 并拒绝 context 及下游入口。
- [x] 对全部成品 examples 和 benchmark families 检查 `DSSeeds/DSLinear` 及适用 DE 的计算 payload 只含最终缺省或自定义参数，不残留旧坐标和内部变量。
- [x] 更新完成检查表、进度/TODO，并通过 `git diff --check`。

完成结论：任务书已将双 `kL`/双 `kE` mixed-triangle、bubble+tree 和 `vertex_energy_signs` 固定为三组 exact/overcomplete/undercomplete 自定义变量矩阵，并把缺省/自定义最终参数闭合扩展到全部 benchmark families 与成品 examples。本轮只修改任务书和项目记录，未执行独立审计。

验收标准：任务书能独立判断 exact 自定义参数化的每个用户变量是否获得正确 DE 算符；任何 over/undercomplete 情形都不能静默生成歧义导数；三个固定例的消息严重级别、红字显示、capability 和下游行为均有明确 expected；默认与 exact 自定义结果的参数差集都为空。

### 历史作废方案：2026-07-23 第 2--17 节全量 source-isolated 独立审计

> 2026-07-25 已由本文件顶部收缩任务取代。下列未勾选项仅保留历史，不再执行，也不属于当前 todolist；当前边界以独立任务书第 1、15--19 节为准。

本项由独立执行者负责，不属于当前维护 agent 的执行范围。独立执行者在仓库根目录 `check/` 中从头执行修订后的 `independent-benchmark/independent-benchmark.md` 全部第 2--17 节，不恢复、读取或复用旧 expected、旧 check、旧运行结果或旧报告证据。第一阶段只读任务书和其中允许的公开来源，完整手推第 9 节八个 graph-valid loop family、两个 atomic `timeOnly` family、第 14 节两个 pure-time/tree family、全面物理工程闭环、根号坐标层及全部图论/fixed-line/参数闭合任务；全部 expected 与推导冻结并记录 SHA-256 后，第二阶段才按任务书动态解析并加载当前提供的最新版本 package/正式手册，做全量单向对照和规定的两次 fresh reduction 闭环。

- [x] 消除任务书增量与版本措辞歧义：总则、14--17 节、报告门禁与完成清单统一要求每轮从头完整执行第 2--17 节，只在全量冻结后动态解析当前最新 package，不加载或引用任何旧 package 与旧报告作为本轮证据。
- [x] 在根目录 `check/` 建立来源隔离说明、全量工作计划和多 agent 文件所有权边界；`check-smoke/` 不作为本轮输入。
- [ ] 独立生成并自检第 2--13 节八个 full-loop family 与两个 atomic `timeOnly` family 的全 branch/sector/适用 generator/state、ISP、h/H、EOM/symmetry/zero-point 和 general derivative expected。
- [ ] 独立生成并自检第 14--17 节 pure-time/tree、Kira/DE/scaling 输入、根号/自定义坐标、graph/routing、exact/over/under、bubble+tree 六变量与全 family 参数闭合 expected。
- [ ] Phase 1 全量自检通过后冻结 derivation、expected、输入和 SHA-256；独立交叉审阅覆盖与公式，冻结文件在 Phase 2 前后不得变化。
- [ ] Phase 2 动态解析并显式加载当前最新 package，执行单向逐项比较、功能/负例/API/example/backend-neutral payload 测试，并运行默认与 exact 自定义坐标两套隔离的 fresh reduction 闭环。
- [ ] 报告每组 total/equal/nonzero-difference/failed/first failure、五级覆盖计数、矩阵维数、参数差集、Kira 统计和哈希；正式结论按 `currentVersion` 归档到 `000-report/YYYY-MM-DD-HHmm-{currentVersion}-内部.md`。

验收标准：第 2--17 节没有任何因“历史增量”“旧版本已冻结”或“已有报告”而跳过的项目；Phase 1 先于 package 读取且 frozen hash 前后相同；两个 reduction 使用不同 input hash、manifest 和结果目录并均满足 0 unreduced；所有未通过或未执行项明确报告，不能用 package 自检替代 source-isolated 证据。

### 当前任务：2026-07-23 独立任务书完整性与轻量 smoke 目录边界

本轮只确认 `independent-benchmark/independent-benchmark.md` 是否完整收录此前约定的独立审计任务，不执行独立推导、package 对照或 reduction。项目根目录新增 `check-smoke/`，作为维护 agent 日常小范围、轻量、可随时清理的 check/test 唯一目录；独立审计不得读取、复制、写入或使用该目录中的任何脚本、结果或结论。

- [x] 逐项核对任务书对 family/branch/seed、h/H/T、共同 theta、pure-time/tree、Kira/DE/scaling、自定义参数、general 指标闭合、EOM/symmetry/zero-point 和源码审阅的覆盖。
- [x] 建立根目录 `check-smoke/` 及目录说明，规定轻量脚本、临时输出和清理边界。
- [x] 在 AGENTS、独立任务书和独立 README 中明确 `check-smoke/` 与 source-isolated 审计工作区完全隔离。
- [x] 确认 `check-smoke/` 无运行结果，独立任务书仍在，执行 `git diff --check`。

完成结论：任务书第 2--18 节已覆盖全部既定审计任务，新增章节范围索引只用于快速定位，不改变验收内容。本 agent 不执行独立审计；后续日常轻量检查统一进入根目录 `check-smoke/`，独立执行者明确禁止消费该目录。

验收标准：任务书包含全部既定审计任务且不要求本 agent 执行；`check-smoke/` 只保存目录说明或轻量 smoke 脚本，运行产物只进其 `results_test/` 并在任务结束后清理；内部独立审计使用单独的新建工作区，外部独立审计使用项目外工作区，二者均禁止消费 `check-smoke/`。

### 当前任务：2026-07-23 独立审计工作区重置

按用户要求删除全部现存手推、expected、package 验证脚本、独立工作树和运行结果，不按轮次或版本保留；根目录 `check/` 与正式验证目录 `000_code/check/` 仅保留为空目录，供新的独立执行者从头审计。历史归档报告及附件全部保留，仅删除本次新建且未跟踪的 `2026-07-23-0917-016-内部.md`。当前 016 package 源码、任务书、正式交付、用户手册、成品 examples，以及 Kira 信息文件生成器、serializer/import 接口和当前示例信息文件不属于审计产物，保持不变。

- [x] 删除根目录 `check/` 的全部手推、expected、脚本、哈希和结果，并重建为空目录。
- [x] 删除 `codex-independent-benchmark/` 和本次新建的 `0917` 报告；恢复并保留全部历史归档报告及附件。
- [x] 删除 `000_code/check/` 中 010--016 的全部验证脚本、旧 hand-derived cross-check 和 reference 内容，并保留空目录。
- [x] 删除 README 中已经失效的验证计数和脚本运行入口。
- [x] 复核两个 check 目录为空、独立审计工作树与 `0917` 新报告不存在，历史归档报告完整，并通过 `git diff --check`。

验收标准：根目录 `check/` 与 `000_code/check/` 均存在且条目数为零；不存在 `codex-independent-benchmark/` 或 `0917` 新报告；历史归档报告及附件保持完整；Git 状态只记录审计工作资产的删除，不改动当前 016 package 源码、任务书、正式交付、用户手册或成品 examples。

### 当前任务：2026-07-23 016 候选测试版先行发布门禁

本轮不再把模块源码直接覆盖到正式 `package_016.wl/pdf`。先在 `000_code/test/results_test/` 生成候选单文件与候选手册，所有 016 正式专项和根目录 phase 2 都必须显式加载候选单文件；候选全部通过后，才允许以候选字节覆盖 `independent-benchmark/package/` 中的现行正式版本，并对正式路径做覆盖后复验。

- [x] 构建脚本支持可选候选输出路径，缺省正式输出合同保持兼容；016 检查支持显式 package 路径，避免误加载模块源码或已有正式包。
- [x] 在 `000_code/test/results_test/` 生成 `package_016_candidate.wl/pdf`，记录覆盖前正式哈希与候选哈希。
- [x] 只加载候选单文件运行 single-file、bubble+tree、topology、direct tree DE、pure-time theta、example/API coverage 与根目录 phase 2；任何失败均不得覆盖正式版本。
- [x] 候选全部通过后用同一候选文件覆盖正式 `package_016.wl/pdf`，确认正式与候选 SHA-256 完全相同，并重跑 single-file、bubble+tree 与 phase 2。
- [x] 清理候选及临时运行产物，更新进度、TODO 和报告附录，执行 `git diff --check`。

验收标准：候选阶段的每个测试输出必须能证明实际加载路径为 `000_code/test/results_test/package_016_candidate.wl`；覆盖前后哈希明确记录；正式覆盖仅发生在候选全绿之后；package 仍只生成和序列化关系，不运行 reduction。

完成记录：候选程序与覆盖前正式程序均为 `E720FCA8974A707588A96501FAA580BD0CD9109477F021836DDF1389ABFF359D`；候选 PDF 为 `55B13233C78F0083DCE0DAB0E2D38EC984FE2AA1596AF01823C728BD5A13BE4C`。候选 fresh-kernel 依次通过 single-file `10/10`、bubble+tree `36/36`、topology `49/49`、direct tree DE `22/22`、pure-time theta `14/14`、example/API `7/7` 与 `29/29`、phase 2 `402/402`（非零差值 `0`）；每项输出均指向候选路径。候选手册 27 页，封面、第 14 页和末页渲染无裁切、重叠、黑块或乱码。晋升后正式程序、交付 PDF 与权威 PDF 分别与候选哈希相同；正式路径复验 single-file `10/10`、bubble+tree `36/36`、phase 2 `402/402`。一次并行启动三个 Wolfram kernel 得到进程级退出码 `-1073741819`，串行复验全部通过，未计作断言失败。未运行 reduction。

### 当前任务：2026-07-23 016 独立 benchmark 任务书完整性与实现映射审计

在已发布 016 与现有 source-isolated 报告之上，逐项回收此前提出但可能只停留在计划、任务书或 package 自检层的要求。审计必须分别记录：任务书是否明确要求、016 是否存在公开或可审计实现、是否有正式回归、是否已有冻结手推 oracle 的独立证据；四者不得互相替代。

- [x] 通读独立任务书、README、016 公共 API/模块和正式 check，建立 requirement-to-evidence 映射表。
- [x] 补齐任务书中遗漏的固定 branch/parity/seed 范围、系数乘积法则、general 指标参数闭合、EOM/symmetry 条件性、全 `tau/q` zero-point、pure-time 迭代交叉检验及 tree naive/dlog DE 同 basis 检验。
- [x] 核对 Kira 基础输入、外部 reduction 全文件回读、DE 生成和 scaling relation 检验是否被正确描述为闭环任务，并保持 package 不运行 reduction 的边界。
- [x] 对每项功能确认 016 实现状态；若任务书点名而实现缺失，则补 016 模块、公开门禁和正式 check，不以文档声明代替功能。
- [x] 运行与改动风险相称的正式检查和 Markdown/whitespace 审计；更新本节结论，明确仍须由新一轮独立执行者完成的项目。
- [x] 复核 Git 分歧，提交并推送本轮任务书、实现和文档更新。

验收标准：任务书可由未读主线 expected 的执行者独立完成，所有固定 family/branch/seed/zero-point 与结果比较范围无歧义；实现映射对“已实现、仅有正式自检、已有独立证据、仍未实现”作出可复核区分；package 只生成/序列化关系，不在内部启动 Kira/Fermat reduction。

本轮实现映射结论（2026-07-23）：

| 要求 | 016 实现 | 正式自检 | source-isolated 证据 | 当前结论 |
| --- | --- | --- | --- | --- |
| `ds` 系数乘积法则与用户变量链式法则 | 已实现；fixed-line 加权 `J` 的吸收泄漏已修复 | bubble+tree 36/36；六变量逐个 general-index `ds` | 014/015 与 016 增量报告有对应子集；本轮根目录报告不是严格 blind | 功能已实现；custom loop/tree DE 四层闭环尚未独立执行 |
| 双动量列表、cycle/fixed、exact/over/under 与 capability | 已实现；公开 loop `DSSeeds/DSLinear` 已投影到用户坐标 | topology 49/49、bubble+tree 36/36 | 016 报告覆盖既有增量 family；本轮附录记录新修复 | 功能已实现；新固定 `l1,k1,k2` family 的全 branch/sector 独立覆盖仍待 17.4--17.5 |
| general 指标 EOM/symmetry 条件性 | 已实现条件门禁：EOM 只匹配可判定整数状态，自动 tadpole 与用户规则取并集 | 既有 EOM/tadpole/symmetry 专项分散覆盖 | 旧 family 有独立子集 | 缺少第 17.5 节要求的全 family、符号指标源码与运行证据 |
| 全物理 zero-point 与 simultaneous contact 乘积 | 已实现于 loop、fixed coefficient、tree projection/iteration/dlog 数据流 | pure-time theta 14/14 及 014/016 专项 | 013/014 tree 与三平行 massive h 已独立通过 | 缺少第 17.5 节统一的全 h-family 参数并集报告 |
| tree naive IBP+DE 与迭代/dlog 同 basis | 已实现 | direct tree DE 22/22、pure-time theta 14/14 | 014 tree 独立报告及 016 两/三顶点符号/有理 probe | 已实现且已有独立证据 |
| Kira export/import、DE、scaling 闭环 | 已实现，package 本身不启动 reduction | serializer/import 负例与 workflow 已通过 | 014 fresh Kira 2.3：19 masters、0 unreduced、DE/scaling/reference 比较通过 | 016 继承该能力；本轮未重跑外部 Kira |
| 全 family 参数闭合差集与废弃代码/效率/模块化审阅 | 不需要新增公共 API，现有 metadata/`coefficientVariables` 可支持审计 | 尚无覆盖新范围的统一正式 check | 无 | 已写入第 17.5 节，必须由新一轮 source-isolated benchmark 执行 |

后续根目录审计实际发现并确认两项公共边界缺陷：general-index fixed-line `ds` 私有 helper 泄漏，以及公开 custom `DSSeeds/DSLinear` 残留内部 `kk`。二者已修复并重建单文件/PDF；fresh kernel 通过 bubble+tree 36/36、direct tree DE 22/22、pure-time theta 14/14、topology 49/49、single-file 10/10 和 phase 2 402/402。新增 17.4--17.5 在形成新的冻结 expected、单向比较和报告前，仍不得宣布 source-isolated 四层闭环完成。

### 当前任务：2026-07-23 016 独立 benchmark 的 bubble+tree 与参数闭合扩展

在现有 016 冻结交付之上扩展独立任务书，不先改写已冻结手推 expected。固定新 family：bubble 的两条 cycle line 动量为 `l1`、`l1+k1+k2`，bubble 的外接动量为 `k1+k2`；同一 bubble 顶点经一条无圈 bridge propagator（动量 `k1+k2`）连接三点顶点，三点顶点再连接两个无圈外腿 `k1`、`k2`。用户显式声明 loop external 与 independent external 两个列表，独立审计 exact/overcomplete/undercomplete，并验证缺省/自定义根号坐标、general 指标 IBP/DE 参数闭合和消息合同。

2026-07-23 约定澄清：传播子 `momentum` 中的加减法始终是矢量线性组合；例如 `k1+k2` 对应的模长严格为 `Sqrt[sp[k1+k2,k1+k2]]`，绝不解释成 `Sqrt[sp[k1,k1]]+Sqrt[sp[k2,k2]]`。后者只有在用户把它作为独立标量显式写入 `vertexEnergies` 时才表示“模长之和”。`vertexEnergies` 与传播子三动量、Hankel 指标 `nu` 三者互相独立，不从 topology 动量自动推断。

- [x] 逐字固定上述 topology 输入，运行当前 package，记录初始化后的内部 loop routing、`loopExternalMomenta`、`independentExternalMomenta`、缺省参数规则、dependent bindings、capabilities 与全部用户提示。
- [x] 批判性核对角色定义：loop external 只审计线性张成完备性；缺省参数包含其完整 Gram，自无圈实际出现模长集合中按二次型秩排除从属项，且不生成 independent external 之间未出现的额外交叉点积。
- [x] 若当前提示未完整说明 `DSRedefineParameters[context,rules]` 的左端格式、右端自定义参数格式、过/欠完备后果及示例，则修改 016 package、手册和相关权威文档，并重建交付。
- [x] 把 bubble+tree 作为用户手册第一个完整工作流例子：先以独立 `E1,E2,E3` 走完初始化、变量提示、seed 与微分算符，再说明外腿能量合并必须由用户显式建模，程序不从传播子矢量和自动猜相位能量。
- [x] 在该例后比较两种绑定方案：保留两条外腿并以可逆参数规则令 `sE1 -> E0-sE2`、`sE2 -> sE2`；或从 topology 起只保留一条总能量外腿并直接使用 `E0`。明确第二种改变 topology，不与第一种视为同一个 family 的纯坐标重命名。
- [x] 更新 `independent-benchmark/independent-benchmark.md`：执行方必须先独立选择并手推角色、缺省参数、time/momentum IBP、微分算符、EOM/symmetry/canonical 与 zero-point 产物，冻结 expected 后才允许加载 package。
- [x] 加入 loop/independent momentum 列表分别 exact/over/under 的错误输入任务；过完备须红色 warning 后按 capability 合同继续，欠完备须红色 error、给出 missing/null-space 证据并阻断所有读取初始化状态的后续入口。
- [x] 加入自定义坐标 exact/over/under 与用户变量 DE 检查；最终微分算符必须覆盖全部可用用户变量，并严格对用户所选变量求导。
- [ ] 对全部 benchmark family 的所有 seed 与 general 指标 `ai/bi/...` 微分结果做参数闭合审计：提取 `Variables` 后剔除 `J` 指标槽符号，区分合法参数、裸内部坐标和因输入欠完备导致的残留；同时核对定义参数集合与实际出现参数集合。
- [ ] 明确并验证 general 指标下 EOM/symmetry 的条件性：只在模式可判定时作用，不以符号大小猜 canonical 顺序；h family 的 `tau/q` 默认均含显式 zero-point，并在手推 IBP/DE 中保留其乘积。
- [ ] 手推冻结完成后执行 package 单向比较、消息/颜色/内容审计与源码审阅；报告废弃代码、可改进点、效率热点和可复用模块候选。
- [x] 覆盖 `independent-benchmark/package/` 中的 `package_016.wl`、`package_016.pdf` 与必要 examples，复核目录边界、哈希、PDF 和受影响回归。

本轮 package-facing 完成记录（2026-07-23，修复后）：bubble+tree 缺省变量固定为 `{ss11,sE1,sE2}`，完整导数变量为 `{ss11,sE1,sE2,E1,E2,E3}`；两种 `E0` 建模方案均由用户显式给出，程序不从矢量和猜测模长和。正式合同先修复 `externalLegSquaredCoordinate[i]` 输出，再由根目录报告确认 fixed-line 加权 `J` 的私有 helper 泄漏；当前合同进一步覆盖六变量逐个 general-index `ds` 及 custom `DSSeeds/DSLinear` 载荷，最终为 36/36。六个 016 正式检查为 bubble+tree 36/36、direct tree DE 22/22、pure-time theta 14/14、topology audit 49/49、single-file 10/10、example/API coverage 7/7 与 29/29；根目录 phase 2 为 402/402。27 页用户手册已重新编译并渲染检查封面、新 `kL/kE` 术语页和末页，无裁切、重叠或坏字形。最终 `package_016.wl` SHA256 为 `E720FCA8974A707588A96501FAA580BD0CD9109477F021836DDF1389ABFF359D`；候选先行晋升后的权威 PDF 与交付 PDF SHA256 均为 `55B13233C78F0083DCE0DAB0E2D38EC984FE2AA1596AF01823C728BD5A13BE4C`。临时测试与 PDF 渲染目录已清理。上面尚未勾选的全 family/branch/sector、custom loop Kira/DE、custom tree 双路线与 source-isolated 单向比较必须在下一轮独立 benchmark 中执行，本轮正式自检不替代该验收。

### 当前任务：2026-07-22 016 显式动量角色、图论门禁与桥边指标重建

此前未完成的“由统一外动量原子表自动猜角色”016 草稿作废；015 保持冻结，016 从 015 重新建立。用户必须分别给出 `loopExternalMomenta` 与 `independentExternalMomenta`：前者定义圈积分完整 Gram 坐标，后者只定义实际无圈动量模长。兼容输入 `externalMomenta`/`externalLegMomenta` 只作旧字段别名，不再参与自动角色推断。符号名称没有语义，`alice`、`bob` 与 `k1` 等价。

- [x] 用无向多重图的第一 Betti 数 `L=E-V+C` 计算结构圈数；自环计一圈、平行边分别计数、`extLegs` 不计入内部边。以逐边删边的连通分量变化识别 bridge/cycle line，并修正自环 incidence 列。
- [x] 新增 `ibpMode -> "full"|"timeOnly"`。树图缺省 `timeOnly`；含圈图缺省 `full`，也允许用户选纯时间模式。`full` 要求用户圈动量数严格等于结构圈数、line routing 的圈系数矩阵满秩且位于图 cycle space；`timeOnly` 不以圈动量缺失阻断初始化。
- [x] 计算 shift 不变量 loop 外动量需求：若 `Q=A q+r`，先选首组满秩参考行 `A_R`，定义 `q'=A_R q+r_R`，再用 `r'=r-A A_R^{-1}r_R` 消除任意圈动量平移。不得直接把 `q->0` 后全部 `r` 的张成空间当答案；例如 `{q+k1,q+k1+k2}` 只要求方向 `k2`。ISP 中与圈动量耦合的额外外向量也必须加入需求空间。
- [x] 分别审计两个用户列表。loop 外动量按线性向量空间比较；独立外动量按“完整 loop Gram + 实际出现的无圈模长平方”所在二次型行空间比较，因此 `{kE1,kE2,kE1+kE2}` 可对应三个独立模长而不自动引入额外交叉点积。恰完备通过；过完备红色 warning 后继续并记录冗余/额外方向；欠完备红色 error、返回 missing/null-space 证据并拒绝注册可用 context。
- [x] 所有下游入口读取 context capability gate。至少保存 `initializationUsableQ`、`timeIBPUsableQ`、`momentumIBPUsableQ`、`derivativeUsableQ` 与失败审计；不得只在 `DSInit` 打印一次后让 `DSSeeds`、`ds`、`DSDE` 或 serializer 绕过。
- [x] 重建 loop `J[aList,linePacks,ispList]` 的逐线 pack schema：cycle line 保留 `{b,n...}`，bridge/non-cycle line 只保留 `{n...}`；masslessCross bridge 可为空 pack，shrunk bridge 也不保留 `bS`。sector metadata 必须承担 pack 类型判定，不能继续只靠第一槽或 pack 长度猜 sector。
- [x] 抽象统一的 line-power 操作。对 cycle line，动量幂变化移动 `b/bS`；对 bridge line，固定 `b0/bS0` 与所有 EOM、时间导数、模长导数、Wronskian/contact 引入的幂变化进入显式能量系数。`shiftLineB` 的全部调用点、integrand 展开、zero-point、canonical、排序、projection 和 serializer 都必须逐项适配。
- [x] momentum IBP 只对 cycle-line `xi` 与 ISP 建立复合算子：先求 `xi` 对圈动量的导数，再用完备 `d/dq_l.q_m`、`d/dq_l.k_j` 作用；bridge line 不得生成 `z_e`、`b` shift 或 momentum building-block 导数。time IBP 仍遍历顶点连接的全部传播子，并对 bridge line 产生正确 n shift 与显式系数。
- [x] `timeOnly` 缺省使用 pure-time/tree `J[vertexPacks]` 表示，包括用户对圈图只生成 time IBP 的情形；不得先伪造一套不完备 loop momentum 表示再删除 momentum generators。现有 loop-to-tree contact selector、迭代约化和 dlog DE 要与 direct pure-time 路线保持同序交叉验证。
- [x] 初始化输出 notation：`ssij -> Sqrt[sp[k_i,k_j]]` 遍历用户给定 loop 外动量完整 Gram；`sEi -> Sqrt[sp[p_i,p_i]]` 只遍历用户给定独立外动量。编号按列表总数位宽补零。实现规范入口 `DSRedefineParameters[context,rules]`；无 context 短签名只更新已注册 context，返回新审计并把规则用于 seed、`ds`、`DSDE` 与 metadata。拼写错误且会暗示裸全局赋值的 `redefineParamater` 不作为正式 API。
- [x] 新增“单圈 bubble + bridge propagator + 三点顶点 + 两个外腿”用户 example 和正式 bench，覆盖 exact/overcomplete/undercomplete、圈数不足、纯时间豁免、bridge pack、time IBP、momentum IBP 不触碰 bridge、参数重定义和错误输入。
- [x] 为每版维护公开用户函数清单和 example 覆盖 manifest；正式检查比较 package public API manifest 与全部成品 examples 的覆盖并报告缺项。当前覆盖为 examples 7/7、公开函数 29/29。
- [x] 清理 plan/design/tech/manual 当前合同中残留的旧字段 `externalMomenta`，统一为 `loopExternalMomenta`；015 历史 benchmark 与明确标注的兼容别名保持原样。两份 PDF 已重新编译并目视检查受影响页。

批判性边界：只用 `E-V+C` 能得到圈数，不能证明用户动量路由正确；还必须检查 incidence-cycle 条件和圈系数矩阵秩。只比较外动量向量秩也不能判断无圈模长完备性，因为线性相关向量可给出独立平方变量。过完备坐标可用于 seed 的符号生成，但会破坏唯一逆变换与独立 `ds`；相关能力必须单独关闭。欠完备不是“多一个约束后仍正常”，它会让 IBP 标量积闭合或物理系数求导缺方向，因此 016 按本轮要求阻断初始化。

验收标准：015 文件与交付哈希逐字节不变；所有 bridge line 在任何 sector 的 `J` 中都不含 `b/bS`，且其固定/衍生动量幂只出现在显式系数；full momentum IBP 的 generator、`xi` 和 propagator derivative 只引用 cycle lines；time IBP 在带 bridge 的顶点手推等式逐项相等；两个用户动量列表的 exact/over/under 三态、结构圈数、路由秩和 capability gate 均有确定性正负测试。

续做记录（2026-07-22）：已确认 015 核心 SHA256 仍为 `DA0869BB2BBCB521D0CACA60FADB958E87CB9FC82FA60E0ABE62A3083E6D233A`。016 已实现第一版多重图、bridge/cycle、affine loop-shift 与两类声明审计；底层 smoke 为 13/14。当前唯一失败是旧解析器把 `independentExternalMomenta` 同时并入线性向量基，导致合法的 `{p1,p2,p1+p2}` 被重复抽取后误报非线性。先把“传播子向量原子分解”和“独立模长二次型坐标”彻底分层，再开始 bridge pack 改造；不得以放宽 `LinearSolve` 或忽略 residual 的方式绕过。

续做记录（2026-07-22，当前轮）：传播子向量分解基与独立模长坐标已分层，bubble+bridge 底层 smoke 现为 30/30；任意符号名、换号/换序、圈数不足、bridge 错带圈动量、非法 cycle support、自环、平行边及 `timeOnly` 圈动量豁免均已有确定性探针。bridge pack 第一阶段和 topology-aware line-power helper 已进入 016，`ds` 已可对 bridge 模长及显式系数求导，capability 已接入 context、`DSSeeds`、公开 IBP、`ds`、`DSDE`。但旧核心仍存在直接读取 `pack[[1]]`、固定三槽、`shiftLineB`、shrink、tree back-projection 与 serializer 的 topology-blind 路径；direct `timeOnly` 也尚未建立。因此 016 当前仍是开发版本，不能发布。下一步先完成全仓槽位审计与 bridge shrink/zero-point 手推，再建立 `J[vertexPacks]` 的 direct pure-time 路线；旧 loop-to-tree 路线只保留为交叉验证。

续做记录（2026-07-22，pure-time theta 门禁）：direct tree DE 与拓扑/参数审计已分别达到 22/22、41/41；`DSSeeds -> DSLinear -> DSKiraExport` 的 sector-tagged pure-time 路线已建立。新增共同-theta 专项暴露出一处真实残留：`timeOnly` 圈图仍按结构 cycle 给 line pack 分配 `b/bS`，且独立模长需求仍只收 bridge。修复标准是 `timeOnly` 下所有 active line 均为 `fixedCoefficient`、全部 active line momentum 均进入无圈模长审计，同时保留结构 cycle metadata 仅供拓扑诊断；专项达到 10/10 前 016 不进入文档发布与独立 benchmark 阶段。

续做记录（2026-07-22，发布收口）：上述 pure-time 残留及 contact sector 错误重算圈数的问题均已修复。root topology 现在唯一决定 loop space、cycle/bridge 与 line-power schema；contact sector 只继承并修改线状态。正式专项当前为 topology audit 43/43、direct tree DE 22/22、pure-time theta 10/10、example/API coverage 7/7 且 29/29。剩余发布门禁是统一手册、技术笔记与独立 benchmark 合同，构建 `package_016.wl`/PDF，执行独立手推比较及最终冻结哈希、回归和清理；完成前 016 仍不宣布为已发布稳定版。

续做记录（2026-07-22，独立检验修补）：独立 Phase 2 首轮已把 mixed-coordinate `ds` 的失败归因为 checker 把完整 `DSInit` context 传入只接受 parsed topology 的三参数原子接口；合法的 `ds[expr,var,context["topology"]]` 已确认同时包含系数导数且不残留 `kk`。另发现一处真实缺陷：raw `repIterative[expr,end,context]` 的 source-aware 分支仍反投影到旧三槽 loop time seed，导致用户显式 `treeEnergy -> k12` 时公式递推错误使用 fixed-line notation `sE1`。修复必须复用 016 direct pure-time seed，不得以令 `k12=sE1` 的替换掩盖；两顶点 `++` 与三顶点 mixed chain 均须和直接 seed 的符号解及确定性有理 probe 严格一致。原子接口同时扩展为接受 parsed topology 或完整 context，降低用户误用风险。

完成结论（2026-07-23）：016 已按上述通盘设计完成并冻结。独立检验实际发现并闭环五类 package 缺陷：过完备 loop 声明误入 ISP fatal gate、fixed h 的 `n=1` 径向导数残留 `shiftVertexA`、mixed parameter 重定义未关闭歧义 derivative/inverse、raw `repIterative` 丢失显式 `treeEnergy`，以及 `rep2innerform` 只看局部 coordinate audit 而绕过 topology `inverseKinematicsUsableQ=False`。最后一项修复后 topology 专项由 48/48 增为 49/49；direct tree DE 22/22、pure-time theta/iteration 14/14、example/API coverage 7/7 与 29/29、单文件 10/10。受影响继承回归为 014 generator ranges 14/14、active basis 20/20、Kira import 负例 24/24、初始化 19/19、parallel contact 14/14、tree naive DE 21/21、sector-tagged 20/20、workflow 40/40，以及 015 坐标 54/54、初始化 22/22、workflow 44/44；历史 `014/015_single_file_package_check.wl` 依赖按当前交付规则已删除的旧 package 文件，不作为 016 门禁。六个成品 example 均退出码 0；04 重新生成当前 `init/` 与 Kira 基础输入，状态为 `awaitingExternalKira`、export `ready`、坏系数字符串 0，未启动 reduction。

最终 source-isolated 内部检验 Phase 1 为 55/55，Phase 2 为 175/175，非零差值 0，`firstFailure=None`；冻结 expected 前后哈希均为 `994234D6E700945AD72A06F18D866DFD2FFC6A14A67E39FB37A1C1BB24FC892E`。正式报告归档为 `000-report/2026-07-23-0140-016-内部.md`。最终 `package_016.wl` SHA-256 为 `25D83525524E0B05F4110E95F768B1F4F33DA76C2500E90E6C41B63C50B2EE53`，交付手册为 `EE081B064CE4A41EB1163AB6C392257A8778A98CFC8F14A08CB535969A31E9F8`，冻结 015 core 仍为 `DA0869BB2BBCB521D0CACA60FADB958E87CB9FC82FA60E0ABE62A3083E6D233A`。手册 24 页、技术笔记 41 页均完成最终渲染检查；未运行 Kira/Fermat reduction。

发布复核（2026-07-23）：将 17 个 Mathematica 生成的 init/manifest/结果文件统一清除行尾空白并转为 LF 后，`git diff --check` 通过。随后 fresh kernel 复跑单文件检查 10/10、topology audit 49/49、tree example 05 和参数坐标 example 06，四项均退出码 0；05 的 naive/direct dlog DE 一致，06 的 exact/overcomplete/undercomplete 及 capability gate 均符合合同。复跑后再次执行同一生成文件格式化并复核四个冻结哈希不变。`git fetch origin main --prune` 后本地 `HEAD` 与 `origin/main` ahead/behind 为 0/0，发布提交从共同基线 `8161147` 建立。

Git 发布记录（2026-07-23）：016 核心发布提交为 `f0a82f6`（`release dS IBP 016 explicit momentum graph`），共 106 个文件、49664 行新增、3368 行删除。加入索引后，`git diff --cached --check` 只报告 6 个刻意保持字节原貌的路径：两份 frozen expected、冻结 `package_016.wl`、上游论文 `dS_IBP.tex/JHEP.bst` 与 PDF 文本抽取附件；白名单外错误数为 0。四个冻结哈希在提交前再次通过，故未为消除格式提示而改写独立证据、上游来源或已冻结交付。

### 当前任务：2026-07-22 015 根号动力学坐标 `ssij`/实际无圈模长 `sEe`

2026-07-22 补充约定：原先把 `externalLegMomenta` 自动展开为完整 `sEij` Gram 坐标的方案作废。凡传播子/顶点 building block 的动量表达式不含圈动量，均按输入中实际出现的模长组合识别为外腿动力学原子；`kE1`、`kE2`、`|kE1+kE2|` 若分别出现，缺省是三个独立模长，不主动生成 `sp[kE1,kE2]`。只有能够由圈积分独立外动量的完备 Gram 坐标及已选外腿变量完全表示的组合才消去。初始化必须先给出缺省变量规则，再允许用户以非阻塞的两阶段接口修改并重新初始化；不完备输入拒绝并报告零空间/缺失方向，过完备输入 warning 后允许继续，但必须明确反向变换不可用及需在 family 定义层补充约束。

- [x] 冻结 014 模块目录、兼容入口、单文件 package 与交付手册；新实现只进入 015，并在项目规则中登记版本和默认变量约定。
- [x] 审计 014 中 loop 独立外动量、圈外外腿参数、顶点能量、`sij`、`treeEnergy`、`ds`、`DSDE` 与 serializer 的内外表示，记录旧 convention 及迁移边界。
- [x] 将 015 的 loop-external 缺省坐标改为 `ssij = Sqrt[sp[ki,kj]]`；其它无圈动量只为 topology 中实际出现的模长组合生成 `sE1,sE2,...`，不生成外腿交叉点积。
- [x] 保留 `sij` 原子导数实现，通过链式法则构造用户坐标导数；完整表达式求导同时包含显式系数，实际无圈线另包含 line-local 径向导数，显式旧 `sij` 坐标保持兼容。
- [x] 更新 015 模块、单文件构建、公开 usage/options、初始化 metadata、examples、plan/design/tech note、用户手册、README 和独立 benchmark；不得改写 014。
- [x] 新增正式及独立 bench：同一 family、同一 master 顺序和 normalization 下，比较根号变量链式 DE 与旧 `sij` DE 的 Jacobian 变换，并覆盖 loop 外动量、圈外 external-leg 参数、混合变量、系数乘积分及 scaling relation。
- [x] 从 topology 的实际无圈动量 line/building-block/相位表达式识别外腿模长原子；实现 `DSKinematics` 缺省提案、`KinematicRules` 重选、左端与参数 Jacobian 双秩审计、零空间/约束、可逆性 metadata 与可重复初始化。
- [x] 把实际无圈模长的“出现集合”与“独立坐标基”分开：以完整 loop Gram 为既有基，按首次出现顺序只保留增加秩的无圈模长平方；其余组合保存为 dependent binding，并通过所选坐标表达式参与相位、无圈 line 径向导数和显式系数求导。
- [x] 收紧重选门禁：初始化信息分别显示缺省规则与当前规则；欠完备报告按基础坐标排序的零空间方向，过完备允许 IBP 但禁止冗余坐标 `ds` 及不可逆的 `rep2innerform`；加入 mixed-coordinate Jacobian 与负面回归。
- [x] 构建 015 单文件和手册，运行受影响回归、独立检查、PDF 目视验收、哈希与 `git diff --check`，归档 015 内部报告。

验收标准：015 默认 `ds[expr,ssij]` 与 loop 部分的 `DSDE` 必须严格等于从 014 `sij` 原子导数经 Jacobian 链式变换所得结果；`ds[expr,sEe]` 必须同时覆盖绑定无圈线的分母/building-block 径向导数、顶点相位和显式系数。不得复制或重写 loop 原子导数规则。初始化信息必须明确区分 loop external momenta 与实际无圈模长；014 文件与哈希保持不变。

完成结论（2026-07-22 18:42）：015 已把完整 loop Gram、实际出现无圈模长与其首次出现独立基分开。固定例 `{kE,2 kE,k+kE,k-kE}` 得到 4 个出现项、2 个独立 `sEe` 和 2 条从属 binding；`ds` 通过 Jacobian 同时作用于 loop 原子导数、无圈 line、顶点相位与显式系数，未复制 014 平方坐标原子层。正式专项 54/54、初始化 22/22、workflow 44/44、单文件 10/10、独立坐标 32/32、015 合同 54/54；example 06 已生成含 `dependentMagnitudeBindings` 的同目录初始化文件。用户手册 22 页、技术笔记 39 页均两遍编译并目视通过。清理源码行尾空格并重建后的 015 单文件 SHA-256 为 `BDD2B549D20F5446833C03E784580E483297CFF481A007E80051DD5618036C65`，手册为 `4A9E709B5C55DCC91B66C709B88142D948302BD72B0403ECC9A79A986E4E439F`；014 三个冻结哈希复核不变。正式报告为 `000-report/2026-07-22-1830-015-内部.md`。

### 当前任务：2026-07-22 014 最新打包的单向 clean 回归

- [x] 按最新要求重做第 15.3 闭环：生成 IBP、Kira reduction 与 DE 时保留本次全部可求导动力学参数 `{s11,P0}`，`nu/epsilon/dim` 等不可求导参数在 seed 生成前代入固定的 100 以内有理数；最终严格等式门禁再令 `ks=43/17`、`P0=29/13`（从而 `s11=1849/289`），先检查全部分母非零，再做无浮点容差的精确比较。两组取值均进入任务书、manifest/summary、结果和报告。
- [x] DE 的独立第二路线严格参考与 `001 bubble_ibp_sym.m` 同目录的 `002 bubble_de.m`：区分 `MIdlogKira=MIdlogNote/.repvar/.reppara2N` 和恢复物理变量后的 basis，按 `dk0/dks -> id -> symmetry -> Kira reduction -> DEP0O/DEksO` 顺序生成 reference-style DE；统一 basis、顺序和 normalization 后只与 `DSDE[{s11,P0}]` 逐项相减并检查 scaling。bubble reference 当前不是完整 dlog DE，不做 primitive、letter 或 pole 门禁；直接 dlog 双路线只属于 tree。
- [x] 修正 reference/package 能量适配器：reference `Vpm=0` 的 time-IBP 相位项给出 `P_ref=-I k0`，package `--` 分支对应 `P_pkg=-P_ref=+I k0`；因此 reference basis 中 `{P1,P2}` 必须映射为 `{-P0,-P0}`。active 17 及辅助 20/21 的符号已对齐，并从空 Kira 工作区重跑受影响 package 闭环。
- [x] 逐条复核最新版 benchmark 第 14.3、15.1、15.2 与第 16 节，确认原 `tree 22/22` 只覆盖 seed 计数、直接 dlog 和 API 门禁，尚未覆盖 `DSTreeSeeds` 路线与直接 `A-/A+` 路线的 general/终点约化交叉。
- [x] 在操作前后核对 `FROZEN_STAGE1.sha256` 与 `FROZEN_GENERAL_DS.sha256`，全部手推 `derivation/expected` 保持只读输入边界；最终分别为 7/7、3/3。
- [x] 只运行 014 的确定性单文件 build，生成最新 `independent-benchmark/package/package_014.wl`；未运行 expected 生成或手推改写脚本。
- [x] 删除 `codex-independent-benchmark/check/results/` 中全部 package-facing actual/result，保留 check 脚本、手推目录和 frozen manifest。
- [x] 用最新打包文件重跑 full-family、ISP、H、tree、general-`ds`、contract 和 engineering 七组检查。
- [x] 补回 tree 的 package-side 两路线交叉：直接公式 `repIterative`/`DSTreeDLogDE` 与 `DSTreeSeeds` 产生的 IBP-seed 撒点约化结果逐项相等；冻结 tree expected 未修改，当前为 57/57。
- [x] 在补充探针中确认 `repIterative0` 经公开 `repIterative` 调用后仍为 `{}`，归因于 `setTreeFamilyContext` 没有调用点；先归档到 `000-report/2026-07-22-1242-014-内部.md`，再修 package 并 clean 重跑。
- [x] 检查后再次核对 frozen hashes、package hash、结果计数、Wolfram 结构和 `git diff --check`。

验收标准：两份 frozen manifest 前后分别保持 7/7、3/3；不得修改任何 `derivation.md` 或 frozen `expected.wl`；所有差值只作为 package actual 与 frozen expected 的单向比较处理，失败时不得据 package 输出反改手推；七组 package-facing 结果必须来自清空后的同一最新 `package_014.wl`。

完成记录（2026-07-22 15:35）：package 哈希为 `C743C77A644A94B549E1BD2D4A5F952949EEA079F01658D8508E22FC48576BBF`。七组 clean 结果依次为 full-family 24/24（3018 relations）、ISP 366/366、H 178/178+178/178+16/16、tree 57/57、general-`ds` 16/16+3/3、contract 49/49、engineering 19/19；单文件 10/10，active-basis derivative targets 196/196。fresh Kira 得到 33581 equations、6555 independent、19 masters、1814 targets、0 unreduced；package 后处理 22/22。reference-style degree-lift 与最终 `{ks->43/17,P0->29/13}` 精确 probe 均为 `P0 0/361、ks 0/361`。正式报告为 `000-report/2026-07-22-1530-014-内部.md`。

### 当前任务：2026-07-22 014 文档合同同步

- [x] 将用户手册的稳定版本、加载入口、当前限制和 014 API 状态同步到当前交付。
- [x] 将 `DSSeeds` 的缺省语义写成 `DiscreteMode -> Automatic`，明确由 topology preset 解析且 `quickCheck -> "sample"`。
- [x] 将独立 benchmark 的 ISP 输入统一为 package 的 `{"name","expr","range"}` schema，移除正式 adapter convention。
- [x] 更新 package contract/ISP 检查与验证矩阵，重跑至合同和物理对照均通过。
- [x] 重新编译用户手册，覆盖 `package_014.pdf`，渲染并目视检查受影响页面。

验收标准：手册不再把 012/013/014 的历史阶段写成当前状态；任务书与 package 对 ISP 和 `DSSeeds` 的公开约定一致；contract 47/47、ISP 366/366，PDF 编译无错误且受影响页面无裁切、重叠或乱码。

完成结论（2026-07-22）：README、plan、design note、技术笔记、用户手册、independent benchmark/README 和验证矩阵均已同步到 014 当前合同。`DSSeeds` 缺省固定为 `DiscreteMode -> Automatic`，由 preset 解析为 `quickCheck -> "sample"`、`fullDiscrete`/`bounded -> "all"`；ISP 输入统一为 `name/expr/range`，package-facing check 不再设置字段 adapter。重跑 contract 为 47/47，ISP 为 366/366 相等且非零差值 0，结果记录 `ispInputSchema -> {name,expr,range}`。用户手册和技术笔记均编译成功；20 页用户手册的封面、快速开始、ISP、`DSSeeds`、当前限制和 API 表已渲染目视通过，权威 PDF 与 `package_014.pdf` 的 SHA-256 同为 `138F3A0CB12E12869B54EDA0CDC99CD32BE2ED92E489724E6DA6A1521981D775`，程序交付哈希仍为 `668D23D1FF92EB7C42A1778151A4153F2267D0351DC3A98433CA5BA1ABE0A4DD`。历史报告 `000-report/2026-07-22-1122-014-内部.md` 保留修正前发现，不改写为修正后结果；未运行 Kira、Fermat 或 reduction。

### 当前任务：2026-07-22 014 新版独立 benchmark 的 ISP 覆盖与新增手推

- [x] 保留已冻结的独立手推与 expected；删除并作废此前全部 package 自我验证、package-to-expected 对照脚本、运行结果及旧 014 报告证据，再依据新版任务书和 014 手册从零重写验证层。
- [x] 逐条读取最新版 `independent-benchmark/independent-benchmark.md`、`independent-benchmark/README.md` 和 `independent-benchmark/package/package_014.pdf`，建立“任务书要求—现有独立手推—缺口”清单。
- [x] 对所有要求 ISP 的 family 独立验证标量积闭合性，补齐 ISP 定义、反解公式、seed 点和由手推生成的 expected；不得从 package 输出或旧 expected 反推。
- [x] 完成新版任务书新增的全部手推对象，保存可审计的中间公式、一般指标关系和固定 seed 关系。
- [x] 完成十个 benchmark family 的 24 组固定 sign/energy package 覆盖，核对 sector、生成元、canonical seed、ISP 与非空方程。
- [x] 从 clean results 状态重跑 package contract/engineering 自检及 ISP/H/tree/general-`ds` 单向比较；逐项报告相等数、非零差值数和残留对象。
- [x] 复核 frozen hashes 与 Mathematica 文件结构门禁，更新验证矩阵和新的 014 内部报告；本轮不运行 Kira、Fermat 或 reduction。

本轮验收标准：任务书要求的每个 ISP case 均有显式闭合公式和非退化 seed 覆盖；每个新增手推任务均有不依赖 package/旧 expected 的推导产物；所有 package-facing 验证均为本轮在清空旧验证层后依据新版任务书和正式手册重建；固定 seed 与一般指标两层对照全部通过，或对未通过项留下可复现的逐项差值和责任判定。

完成结论（2026-07-22 11:22）：旧 package 自检、旧 package 对照、旧结果和旧 014 报告已删除，独立手推及 frozen expected 保留。新验证得到 ISP 366/366 相等（非零差值 0）、H-to-h/direct-h 178/178、bare-H 178/178、compiled 16/16、tree 22/22、general-`ds` 独立 16/16、engineering 19/19、十个 family 24/24 runs 与 3018 条方程。contract 为 46/47：手册写 `DSSeeds` 缺省 `DiscreteMode="all"`，package 实际为 `Automatic` 且缺省 quickCheck preset 落到 `"sample"`。另确认手册 012/014 状态文字过期、任务书与 package 的 ISP schema 不同；三者均已在 `000-report/2026-07-22-1122-014-内部.md` 归因。两份 frozen manifest 为 7/7、3/3，源文件结构门禁 11/11；未运行 Kira/Fermat/reduction。

### 当前任务：2026-07-21 013 纯时间 IBP、树图迭代与 dlog DE

阶段 A：先冻结 013 的物理范围和新增 benchmark，再写代码。

- [x] 核对 2401.00129 的 vertex-family 迭代/dlog 公式、2604.14549 的 loop DE/scaling 公式和 bubble reference code。
- [x] 把此前“一版完成工程化与树图”的计划拆成两版：013 只完成纯 time-IBP/tree 物理模块；014 再做标准 package、交互成熟化和 Kira 到 DE 的工程闭环。
- [x] 删除 007--009 主文件及其失效专用 checks，更新现行版本 selector；010--012 保持只读。
- [x] 固定 tree `J[vertexPacks]`、binary master order、loop `dtau` 到 tree seed 映射、general-index `repIterative0`、`repIterative[expr_,end_:Automatic]` 和 dlog DE/master 同序输出。
- [x] 更新 independent benchmark 任务书，只新增纯 time-IBP 内容，不重复此前已完成的 loop momentum、`ds`、theta 等手推检查。

阶段 B：实现、独立检验、报告和修正 013。

- [x] 以 012 为只读基线建立 `000_code/013_dS_ibp_general.wl`，新增 tree/time 模块；允许 loop topology 作输入，但 benchmark 和 tree 路线只调用 `dtau`，不调用 `dqq/dqk`。
- [x] 建立两个固定新增 family：两顶点同号 massive 连接；三顶点 `{+,+,-}` massive chain。各顶点的 massless 外腿能量使用与内部传播子能量独立的符号。
- [x] 对每个新增 family 手推全 pure-time seed、contact source、tree 投影和迭代关系；`013_tree_time_ibp_check.wl` 逐项对照通过。
- [x] 交叉验证 iterative reduction 与 time-IBP seed：general 单步符号比较、两级全约化和确定性有理 master probe 均通过。
- [x] 核对 loop 到 pure-time/tree 的零点投影：目标 sector 的 `a0` 写入 tree `nu0`，被 tree 指标删除的 `b0/bS0` 按完整物理幂次差进入显式能量系数；h massive contact 显式得到完整 `(-k)^(-2 nu-1)`。
- [x] 以 013 加载方式重跑 012 的 14 个 hand-derived cross-check，确认新增 tree 模块不改变既有 time/momentum IBP、`ds`、symmetry、theta 与 ISP 结果。
- [x] 冻结 `package_013.wl/pdf`，删除交付目录中的 012 旧副本，更新三个交付 examples 的加载入口并全部运行通过。
- [x] 运行新增专项，不重复旧 expected；按来源隔离规则在新独立会话完成 benchmark，报告归档到 `000-report/{时间}-013-内部.md`，根据报告修正并重跑至无误。

013 验收标准：tree 1-fold/2-fold/多顶点 master 顺序与 2401.00129 Eq. (3.33) 一致；单步迭代与 Eq. (3.37)/(3.47)/(3.50) 一致；dlog 与 Eq. (3.54)/(3.55) 一致。两顶点同号 case 的 contact source 与 loop time-IBP 一致；三顶点 mixed chain 中 `G++` 只在同号边产生 source，`G+-` 边严格无 `WT`/theta source。`repIterative` 终点长度和步数门禁生效；符号与确定性数值交叉检查均为零。

013 完成结论：正式专项在 strict letter order 下通过 25/25；14 个继承 hand-derived check 全部通过；三个交付 examples 通过 3/3。首次冻结版本的 dlog 物理矩阵、letter 集合和 master 顺序均正确，但 5 个 vertex block 的 letters 序列化把 cut 放在能量之前；独立 frozen expected 将其识别后，013 改为按 `vertexOrder` 逐顶点输出 `{massiveLeg energies, binary cuts}` 并稳定去重，未修改 expected。最终程序 SHA-256 为 `ACD23FA50F2DD0D5736E6145190520C78367F8443397E408D9141DA3F744FE9A`，手册 SHA-256 为 `EFE656E2D2B71E3D9883F9BF4D77D77B8B7F1D6A0E566DCD67F526B9E97783C1`。内部报告 `000-report/2026-07-21-2353-013-内部.md` 给出阶段 1 19/19、full actual 173/173、iterative/API 9/9，结论为允许进入 014。冻结 expected/check/result 已归档到同名附件目录；临时工作区、runner、`_aitest` 与 `tmp/` 已清理。013 仍有已文档化的裸 tree `J` 同 shape sector 歧义限制，014 必须由内部 sector-tagged `treeLinearData` 解决。

### 当前任务：014 模块化 package、交互成熟化与闭环示例

- [x] 建立标准 `dSIBP`` context 和模块目录；example 只 `Needs["dSIBP`"]`，不直接加载实现文件。
- [x] 实现详细输入、独立缺省 option 单元、显式初始化以及与 example 同目录的 `init/` metadata。
- [x] 实现统一消息层：`DSMessagesOn[]`/`DSMessagesOff[]` 控制 info/progress/warning；fatal error 永不静默。warning/error 在 notebook 中用醒目橙/红样式并同时走 Mathematica `Message`。
- [x] 对 sector 初始化、微分算符生成、seed/linear/Kira export/import 和 DE 构造提供阶段信息。Notebook 使用 `Monitor`/`ProgressIndicator` 或 `PrintTemporary[Dynamic[...]]` 在同一显示单元更新 `n/m`；无 FrontEnd 时只打印阶段开始、固定比例里程碑和结束，禁止每处理一项新增一行。
- [x] 将 loop time-IBP 投影到 pure-time/tree 时，以完整物理幂次而非裸整数指标做相对归一化：目标 sector 的 `a0` 进入 tree `nu0`，tree 表示删除的 `b0/bS0` 进入显式能量系数；sector-tagged `treeLinearData`、serializer、Kira 映射和 DE 构造不得丢失该系数。每个 tagged term 必须可审计目标/参考的整数幂、零点幂、完整物理幂及其差值；以非零 `a0/b0/bS0`、h contact 的完整 `(-k)^(-2 nu-1)` 及三条 massive 传播子 simultaneous contact 的显式系数乘积建立专项回归。
- [x] 实现 pure massive bubble 固定 parity subsystem 的 Kira 输入、完整结果取回、`ds`、DE 和 Eq. (51)/(64) scaling 闭环；package 不运行 reduction。
- [x] examples 覆盖 benchmark 现有 family；所有用户函数有 usage、Options 和手册附录汇总表。
- [x] 014 完成后按独立来源从头重做全面手推、package 对照、报告和修正，不沿用 013 报告冒充 014 验收。

014 验收标准：交互与 headless 两种模式无刷屏；全局关闭提醒后仍保留 fatal error；错误返回结构化状态且 notebook 中红字可见。标准 package、相对路径、初始化 metadata、固定 parity Kira fixture、DE/scaling、013 tree/time 功能和所有受影响旧回归全部通过；loop-to-tree 后每项的整数指标、零点和显式系数重组为原完整物理幂次，多 contact 项按各线贡献乘积组合且不使用 `PowerExpand`；独立报告归档到 `000-report/{时间}-014-内部.md`。

014 阶段记录（2026-07-21）：`treeLinearData` 已加入 sector tag、逐来源 `contributions` 和 `physicalPowerAudits`。审计字段分别保存 target/reference 的整数指标、零点、完整物理幂及其差值，并重建实际 projection coefficient。新增两圈三条平行 massive h 线的 triple-contact 专项，验证逐线显式指数 `{-1-2 nu1,-1-2 nu2,-1-2 nu3}` 相乘，merged tree `nu0=gamma1+gamma2-2 nu1-2 nu2-2 nu3`；014 workflow smoke 为 40/40，package/init 为 19/19。该阶段当时只完成本子门禁，tree serializer/多 sector 迭代、真实 Kira fixture 与全面独立验收在后续阶段完成。

014 续作登记（2026-07-22）：先把 sector-tagged tree term 从审计层推进到实际约化层，要求同 shape lower sectors 仍由 `sectorKey` 唯一分派，并按 sector-local endpoint 迭代；随后生成全部 contact-reachable sector 的有序 master list、`masterSectorOffsets` 和同序 dlog block。专项必须同时覆盖单 h contact 与三条 massive h simultaneous contact，检查零点显式系数在迭代前后不变。完成后再补 Kira importer 的 completion marker、input hash、双向映射、target 完整性和 RHS 非 master 残留负面门禁。本阶段不运行 reduction，也不以 synthetic fixture 冒充真实 pure massive bubble Kira 验收。

014 续作阶段记录（2026-07-22）：内部 `dsTreeToken[sectorKey,J[...]]` 已进入实际迭代层；sector-local topology 负责验证 compact 指标并生成 lower-sector `dtau`，root topology 负责最终 shrink sector 与物理幂投影，因而同 shape 的 `e1/e2` 不再靠裸 `J` 猜测。三顶点两条 massive h chain 专项通过 17/17，验证 `top -> e1/e2 -> e1_e2`。`DSTreeDLogDE[context]` 已按 `{top,e1,e2,e1_e2}` 聚合 `16+4+4+1=25` 个同序 masters、offset 和 `25 x 25` sector 对角 dlog 块；lower-sector tagged source equations 已附带，但尚未写入非对角矩阵块，因此当前状态仍明确为 `sectorDiagonalPlusTaggedSources`，不得称为完整 block-triangular DE。下一步先完成 Kira importer 的一个正例与五个定向负例，再组装并专项验证非对角 source blocks；真实 fixed-parity pure massive bubble Kira/DE/scaling 仍是独立未完成门禁。

014 Kira import 门禁记录（2026-07-22）：新增最小三积分正式 exporter 夹具及隔离 case，正例与 completion marker、input hash、非互逆 maps、同步重复 maps、target 不完整、RHS 已识别非 master 残留负例共通过 15/15。`dsKiraInverseMapQ` 现除正反规则一致外，还要求积分和 ID 两侧均无重复；RHS 负例使用 `2 -> 3, 3 -> 1`，确认 `allReductionIDsRecognized=True` 而仅 `rhsContainsOnlyMasters` 失败。测试不运行 Kira/reduction，产物只写入 `000_code/test/results_test/014_kira_import_negative/`。下一子任务为把 tagged lower-sector sources 写入全局 dlog 非对角块。

014 tree dlog 非对角块记录（2026-07-22）：已按 2401.00129 Eq. (3.66)--(3.68) 从每个 sector/vertex 的 loop `dtau` 自动生成 `R^(1)` contact rows；实现明确以 `a=1` seed 构造 `f^(1)` source，不误用 `R^(0)` 的 lower `a=-1`。source 经完整物理幂投影和必要的 sector-tagged 迭代后，按 target sector 的同序 master 抽矩阵。每个 lower sector 自动选择共同 normalization，所有其它入边比值必须与能量变量无关；全局对角块加入 `Log[normalization] I`，非对角块为 `-I Embed[T^-1 Omega0 T].contactMap` 的顶点和。单 h workflow 通过 40/40；三顶点两 massive h chain 通过 20/20，验证 `{top->e1,top->e2,e1->e1_e2,e2->e1_e2}` 非零、全部反向/sibling 块为零、25 维全矩阵 `dlogResidual=0`，且 zero-point/Wronskian normalization 与 tagged master 同步。该阶段当时尚未运行真实 Kira/scaling 和全面独立验收，二者已在后续完成。

014 三条平行 massive h 的 `R^(1)` 专项（2026-07-22）：已通过 14/14。测试不构造 64 维完整 top-sector dlog 矩阵，而直接检查 loop `dtau` 生成并投影后的指定 binary master `R^(1)` contact row。`++` case 的 source sectors 严格为 `{e1,e2,e3,e1_e2_e3}`，不存在偶数子集；直接 triple contact 的三条整数幂 `{1,1,1}`、zero-point 幂 `{2nu1,2nu2,2nu3}` 和显式能量幂 `{-1-2nu1,-1-2nu2,-1-2nu3}` 逐线对应，目标 merged `nu0=gamma1+gamma2-2nu1-2nu2-2nu3`，且该系数在 sector-tagged 迭代后仍保留。固定 `+-` case 由两个顶点统一把三条传播子编译为 `massiveCross`，sector DAG 只有 top，所有 `R^(1)` lower-source row 为空，确认不会把共同-theta规则误用到 `G^{+-}`。

014 真实 Kira/DE/scaling 闭环（2026-07-22）：[完成] 先只读核对 pure massive bubble reference 的固定 sign、even parity、symmetry 与变量 convention，生成一个受控小系统的正式 Kira 输入；Kira 作为 package 外部步骤运行，package 本身仍只负责 serializer/importer。取回真实 completion log、master list 和完整 reduction 后，必须由 `DSKiraImport -> DSDE -> DSScaleCheck` 依同一 master 顺序通过，且 Eq. (51)/(64) 的 scaling residual 严格为零。synthetic importer fixture 只保留负面门禁用途，不得计入该闭环验收。

014 active-basis/target-closure 续作登记（2026-07-22）：真实 Kira 2.3 已通过 parser、FireFly 与 kira2math；小系统为 80 条方程、108 个积分、63 个 Kira masters，扩大统一 seed box 后为 6944 条方程、2337 个积分、1742 条独立关系和 595 个 Kira masters，且 `2337-1742=595`。两次 `DSKiraImport` 均通过，但直接对全部 Kira boundary masters 求导时 `DSDE` 不闭合；扩大有限 box 只会把新边界对象继续列为 masters，因此禁止再以扩大统一 seed 范围作为修复路线。下一步参考 bubble code 的 19 个 dlog 线性组合，新增 backend-neutral active basis：用户提供有序线性组合及稳定 token，exporter 把 `token-basisExpression=0` 加入线性系统并把 token 排在积分顺序最前，只导出 active basis、其导数像及闭包所需 targets；manifest 固化 basis 顺序、表达式、token/ID 映射和 target closure。importer 必须验证这些关系和 IDs 均完整，返回 active masters；`DSDE` 只对 active basis 求导，并同时保留显式系数求导、约化后 residual `J` 门禁和同序矩阵。验收要求固定 19 维顺序、`DSDE[{s11,P1,P2}]` 全部无 residual `J`、权重 `{2,1,1}` 的 `DSScaleCheck` 通过且 Eq. (51)/(64) residual 严格为零；Kira 原始 masters 仅作为 backend 诊断数据保留，不得再自动充当 DE basis。

014 真实 Kira serializer 修正登记（2026-07-22）：[完成] 首次外部运行已真实触发 Kira 2.3 `pyred::parser_error: ill-formed integral*coefficient`，原因是 serializer 把 `kk[1,1]`、`P1/P2` 与 Mathematica 虚数单位 `I` 原样写入 Fermat 系数域。当前子阶段必须把所有原始系数原子确定性映射为小写后端变量，并在 manifest 保存可逆映射；importer 恢复原始变量后再执行同源性、变量集合和 reduction 完整性检查。正式门禁包括：synthetic fixture 覆盖含参量复系数且输出无方括号/反引号/大写 token；重新生成 example 后由真实 Kira 完成 reduction；依据真实输出目录与日志文本校准 importer，最后继续完成 DE/scaling。旧无映射 fixture 只保留兼容性，不得弱化新映射门禁。

014 generator-specific seed 续作登记（2026-07-22）：[完成] active basis、21 个前置关系/前 19 个 active IDs、真实 Kira 2.3 serializer/importer 路线和显式系数求导已完成专项；当前统一 `a/b` box 的 6944 条方程仍缺少 253 个 active-basis 导数目标中的 69 个，且继续扩大统一 box 只会制造更多 boundary masters。下一步给 seed 配置增加按 `sectorKey` 与 generator label 指定连续指标范围的可选覆盖层，旧统一范围保持兼容；`makeTimeIBPSeedBatch`/`makeMomentumIBPSeedBatch` 必须逐生成元构造规则并在 metadata 记录变量、值域和规则数。pure massive bubble 按 `001 bubble_ibp_sym.m` 的 top 四个生成元及 R1 三个生成元精确范围配置，不能使用外包矩形。验收要求 69 个缺失目标全部进入 linear system，Kira list 只包含 19 个 active IDs 与导数闭包 targets，第 20/21 个辅助关系不得成为 master；example 仅在本次 export 为 `ready` 且结果文件时间有效时才允许 import，避免误读旧 reduction。

014 真实 Kira 后处理续作登记（2026-07-22）：generator-specific ranges 已实现并通过 14/14 专项；未覆盖变量继续使用旧 `seedRanges`，每个 generator 的变量、值列表、覆盖范围、规则数和来源均进入 metadata。pure massive bubble 使用 package `P0=+I k0`，reference basis 映射为 `P1=P2=-P0=-I k0`；只在该等能量约束下启用顶点交换 symmetry，独立 `P1/P2` family 禁止复用。经 parity/symmetry canonical 后 active basis 的导数闭包为 196 个积分，manifest 和可逆映射均无缺失。真实 Kira 2.3 已完成 33581 条方程、6555 条独立关系和 19 个 masters；masters 恰为 active IDs `1..19`，辅助 IDs `20,21` 未被选为 masters，1814 个选定目标全部约化且无 unreduced integrals。

014 真实 Kira/DE/scaling 闭环完成记录（2026-07-22）：`DSKiraImport -> DSDE[{s11,P0}] -> DSScaleCheck[{2,1}]` 已按同一 19 维 active master 顺序通过 19/19，`post_kira_summary.wl` 的状态为 `passed`。首次后处理为 17/19；根因是 reduction 系数仍含内部 `kk[1,1]`，而 Euler 算符只对外部变量 `s11` 求导。`BuildDE.wl` 现于 master 分解前把 reduction 系数中的内部 `kk/ISP` 统一外部化到 family 变量；固定点令 `kk[1,1]=s11` 后旧矩阵残差立即为零，正式符号重算后 scaling status 与 matrix residual 均通过。完整 Kira 数据仍由外部步骤产生，package 只负责 serializer、importer、DE 和检查，不运行 reduction。

014 最终收尾登记（2026-07-22）：本轮只处理用户手册最终编译与视觉检查、交互/API/examples 缺口复核、`package_014` 冻结、014 全面独立 benchmark 与报告、临时产物清理和最终受影响回归。独立验收开始前必须先更新 `independent-benchmark.md` 为 014 自包含任务书并冻结交付哈希；不得读取主线 expected 或旧报告生成新 expected，也不得以真实 Kira 闭环本身代替全面手推检验。

014 候选交付冻结记录（2026-07-22）：模块化主线通过确定性 build 脚本拼接为自包含 `package_014.wl`，不含对 `Kernel/*.wl` 的运行时 loader 依赖；全新 kernel 的单文件门禁通过 10/10。程序 SHA-256 为 `668D23D1FF92EB7C42A1778151A4153F2267D0351DC3A98433CA5BA1ABE0A4DD`。用户手册修正三处长行排版后重新编译为 20 页，权威 PDF 与 `package_014.pdf` 的 SHA-256 均为 `457E77E7DF26153BEBEB88DFF842600712DE27ACA0556A84E679E4F39E23068E`；后半第 14--20 页重新渲染，确认无裁切、重叠、黑块或乱码。三个 013 轻量 examples 首次切换 014 后暴露仍调用私有 `parseTopology/makeBaseIntegral` 的交付缺陷，现均改为公开 `DSInit -> DSSeeds -> DSLinear` 路线并通过；tree example 通过，closed-loop example 用时 431.2 秒生成 `ready` Kira workspace，未运行 reduction并正确返回 `awaitingExternalKira`。运行生成的 `init/kira` 已从交付目录清理，旧 `package_013.wl/pdf` 已删除。全面 014 独立会话已从空 `codex-independent-benchmark/` 启动，当前候选在其报告前不得标记正式完成。

014 全面独立验收完成记录（2026-07-22）：内部报告当时归档为 `000-report/2026-07-22-0543-014-内部.md`，同名附件目录保存 frozen 推导、五个正式 phase2 check、PDF 截图、fresh phase3 package/example、Kira manifest/log/master/完整 reduction 表和哈希清单。正式断言合计 296/296，其中物理与 general-index 检查 252/252、工程门禁 25/25、fresh `DSKiraImport -> DSDE -> DSScaleCheck` 19/19；真实 Kira 2.3 得到 33581 条方程、6555 条独立关系、19 个 active masters、1814 个 selected targets和 0 个 unreduced integral。该旧报告及附件已按更新任务书要求删除并作废，不再作为当前独立验证资产；当前报告是 `000-report/2026-07-22-1122-014-内部.md`，保留重新手推、重建检查和文档合同不符的历史证据。

014 `AGENTS.md` 版本同步完成记录（2026-07-22）：已将程序与目录边界从 013 pure time-IBP 阶段同步到通过全面独立验收的 014 模块化主线，明确标准 `Needs["dSIBP`"]` 入口、014 单文件兼容入口以及 010--013 的只读基线/历史边界。定向扫描确认 `AGENTS.md`、README、总体 plan 与当前进度对主线版本的表述一致，`git diff --check` 通过；本任务未改 package、物理公式、benchmark 或交付哈希。

### 当前任务：2026-07-22 tree naive IBP 微分方程与公式型 dlogDE 交叉验证

- [x] 审计现有 `DSTreeSeeds`、`repIterative`、`DSTreeDLogDE`、tree sector-tagged 表示和 loop-to-tree 导数映射，明确哪些部件可直接复用、哪些缺少 naive tree IBP/DE 入口。
- [x] 在 014 模块化主线新增 tree naive IBP 约化与微分方程接口：从 loop time-IBP 投影后的 tree relations 出发，对指定外部变量求导，约化到用户给定且同序的 tree master basis；不得另建积分 Head，也不得绕过 sector tag、零点或显式系数。
- [x] 选择与公式型 `repIterative`/`DSTreeDLogDE` 完全相同的主积分顺序和 normalization，逐变量比较 naive-IBP DE 与直接 dlogDE 的矩阵、source、letters 重构和 residual；至少覆盖两顶点同号 massive family，并以 mixed branch 验证无 theta/contact 的边不会被误用。
- [x] 增加正式 014 专项和独立 benchmark 要求：独立手推/冻结后分别调用 naive IBP+DE 与公式型迭代+dlogDE，两条路线只共享输入 convention 和指定 master list，不共享 expected；报告矩阵维数、非零差值、首个失败和 normalization 审计。
- [x] 更新单文件 package、用户手册、plan/design/tech note、README、bench 交付与哈希；运行受影响回归和新的独立检验，报告归档到 `000-report/`。

验收标准：同一 tree family、同一 sector-tagged master basis 和同一 normalization 下，naive tree IBP 约化得到的每个 DE 矩阵与公式直接生成的 dlog connection 严格相同；两条路线都必须显式包含动力学量系数求导，且求导后没有 residual tree `J`、sector token 或越界指标。mixed `G^{+-}` 路线不得产生 `WT`/theta/contact source。package 仍只生成、序列化和读取关系，不自行运行 reduction。

完成结论（2026-07-22 12:59）：014 新增公开接口 `DSTreeNaiveIBP` 与 `DSTreeNaiveDE`。naive 路线只使用 loop `dtau` 投影、tree 外能量导数和有限线性方程求解，不调用 `repIterative` 或公式型 dlog 矩阵；normalized masters 的 `D[N_s] J_s` 已显式计入。正式专项通过 21/21，单文件门禁 10/10，公开合同 49/49；增量独立 tree 检查通过 57/57，其中 general seed recurrence 10/10、终点约化 8/8、seed-derived DE 8/8。追加回归为 package/init 19/19、sector-tagged tree 20/20、原 tree 公式门禁 25/25、workflow 40/40、package engineering 19/19；更新后的两顶点示例成功执行并显示 naive time-IBP 5/5、逐变量导数 5/5 和 DE 3/3 进度。两顶点 `++` 与 `+-` 均在同序同 normalization masters 上逐变量严格相等，全部差值、source、残留 `J`/sector token 为零，mixed contact/`WT` 门禁为空。单文件程序 SHA-256 为 `B02ED7C1D7E32189EFDAC86D31FC42D26C005031254FEF04145864F5D0B1B1E6`；21 页权威手册与交付副本 SHA-256 均为 `AB8881D2F6EC0414AB312C75B7ABC39FF9661FFE086CCD2B4E1A0F74FF3CC36E`。本轮未运行 Kira/Fermat/reduction。

### 当前任务：2026-07-21 更新 GitHub 仓库

- [x] 审计当前工作区变更与未跟踪文件，排除测试输出、临时产物和不应上传的生成文件。
- [x] 运行提交前的 Git diff、空白与仓库状态检查，确认本地分支与远端没有未处理的分歧。
- [x] 提交本轮项目变更并推送到 `origin/main`，复核远端分支指向新提交。

验收标准：提交范围只包含应长期留存的程序、文档、验证脚本及轻量结果；`git diff --check` 通过；提交成功推送到 `origin/main`；本地 `main` 与 `origin/main` 一致且工作区干净。

完成结论：`.qoder/` 与 `codex-independent-benchmark/todolist_temp.md` 已加入 Git 忽略；本地 `main` 与 fetch 后的 `origin/main` 在发布前为 0/0 分歧，`git diff --check` 通过。正式 `package_012.wl/pdf` 与权威源文件 SHA-256 一致。独立 benchmark 的 5 个专项检查及其余 7 个 family 对照全部通过，总计 2435/2435 relations、101/101 derivatives，另有 reference-only 80/80 derivatives、6/6 symmetry、4/4 parity；未运行 Kira/Fermat reduction。

### 当前任务：2026-07-21 独立 benchmark 交付物与检验报告目录整理

- [x] 核对当前主线程序与手册的权威版本、文件名和哈希，将最新版本化程序包及手册覆盖到 `independent-benchmark/package/`，删除该目录中的旧版本或无版本名副本。
- [x] 精简根目录 `AGENTS.md`：与 plan/design/tech note 重合的技术细节改为文档指引，只保留必须直接约束 agent 行为的项目规则和正确性门禁。
- [x] 更新独立检验目录规范，区分本项目文件夹内由本 agent 新开会话执行的内部检验，以及项目外文件夹中由外部 agent 执行的外部检验。
- [x] 建立项目内统一报告备份目录和命名规则 `{时间}-{版本}-{内部/外部}`；内部检验报告直接落盘，外部报告查阅后复制回项目备份。
- [x] 清理此前报告及与报告 battle 相关的文件、目录和引用，复查不误删正式程序、手册、独立 expected、验证脚本或开发文档。

验收标准：`independent-benchmark/package/` 中正式程序和手册分别与当前权威源文件哈希一致，且只保留最新版本化名称；`AGENTS.md` 不再复述可由项目文档承载的大段公式或约定；内部/外部检验位置、报告回收方式和 `{时间}-{版本}-{内部/外部}` 命名均明确；历史报告与 battle 内容清零；最终目录盘点、哈希和 Git diff 检查通过。

完成结论：当前权威版本为 012；`package_012.wl` 已由 `000_code/012_dS_ibp_general.wl` 强制覆盖，SHA-256 均为 `90F9A2F33DF356AECD75E2517F616C3234FD456649A5562172754773454C978E`；`package_012.pdf` 已由正式用户手册覆盖，SHA-256 均为 `24DD960C464E14E2A0A30CA94F5DCFEB69CCBD3673412387787CBD7873F68638`。交付目录根部只含两份版本化文件，examples 保持不变。`AGENTS.md` 已压缩为文档索引、目录边界、内部/外部检验和正确性门禁；`000-report/README.md` 与 independent README 统一规定 `YYYY-MM-DD-HHmm-<version>-内部/外部.md`。项目内旧不符合报告、两份 review/battle 笔记、内部工作区旧结果总结及包含它们的全目录审计快照均已清理，项目外 `dSibp-independent-benchmark/000_report_v011/` 也已删除；正式 expected、`FROZEN_EXPECTED.sha256`、check 与技术 TODO 保留。旧路径扫描为空，文档链接存在，重命名后的 common-theta 专项测试通过 30/30，`git diff --check` 通过；本轮未改程序逻辑，未重跑全量 Mathematica 回归。

### 当前任务：2026-07-21 为 `vertex_energy_signs` 补 ISP 并完成剩余独立验证

- [x] 在任务书把 `rho1=sp[ell,k]` 正式加入 `vertex_energy_signs` 的 `ispData`，说明由 `(ell-k)^2` 与 `rho1` 反解 `ell^2`、`ell.k`。
- [x] 更新独立 family/derivation/expected，使 ISP 指数至少覆盖 0/1，并生成两个 loop momentum generators 与 `s11` general-index 总导数。
- [x] 更新 package closure/check topology 使用同一显式 ISP 输入；对三种 energy case、两个 sign 分支逐项比较 relations 与 derivatives。
- [x] 更新全量计数、冻结哈希与当前结论；差异归因区分 oracle、convention 与 package 实现责任。

验收标准：六个 energy/sign case 的 scalar-product system 均完整闭合，不再返回 `Missing[IncompleteISP]`；expected 必须先由任务书公式生成，package actual 只用于第二阶段对照。不得运行 Kira/Fermat reduction。

完成结论：任务书已加入 `rho1=sp[ell,k]` 与 `ispSeedPoints={{0},{1}}`，由 `D1=(ell-k)^2` 得到 `ell.k=rho1`、`ell^2=D1+2rho1-s11`。六个 energy/sign topology 均满足结构 ISP 数 1、提供数 1、坐标数完整、scalar rules `computed`、显式反解 residual `{0,0}`。独立 expected 重生为 42 条 time、48 条 momentum 和 24 条 general derivatives；package 对照为 90/90 relations、24/24 derivatives。十个 family 总计 2435/2435 relations、101/101 derivatives；reference-only 80/80、symmetry 6/6、parity 4/4，H 三路保持通过。post-audit v8 聚合哈希为 `BC2E47D190D32899B8787262B587860DF68E956527CD2B88618DC894735DC0CA`。

### 当前任务：2026-07-21 补全 `sij/Dij` convention 并重做独立对照

- [x] 在 `independent-benchmark/independent-benchmark.md` 明确定义对称坐标 `sij=sp[k_i,k_j]=sji`、有序方向算符 `Dij=k_i.\[PartialD]_{k_j}`，并固定 raw 外不变量反解 basis 为 `{Dij | i<=j}`。
- [x] 对所有曾因 external-vector basis 未对上的手推 derivative expected 先按新 convention 重生，不使用 package actual 反向改写公式。
- [x] 重新运行受影响 family 的独立 directional seed、decomposition residual、general-index `ds` strict-zero 对照，并确认其它可闭合 family 没有 convention 漂移。
- [x] 更新覆盖摘要、冻结哈希和当前结论；不把缺 ISP 的 `vertex_energy_signs` 冒充为可由 convention 修复。

验收标准：任务书必须同时解释为什么坐标叫 `sij` 而算符叫 `Dij`，并说明 `Orderless` 只 canonical 标量积、不令 `Dij=Dji`。受影响 expected 必须由独立 oracle 依新任务书重生；对齐后 package 对照严格为零，历史差异仍保留为可复现证据。

完成结论：任务书现明确 `sij` 是对称标量积坐标、`Dij` 是有序方向导数，并固定 raw basis `{Dij|i<=j}`；oracle 已用显式 `i<=j` 构造重生 expected，所有外不变量记录带 `upperTriangularDijBasis` tag。两个 `K=2` family 重新对照为 `mixed_triangle 660/660 + 27/27`、`parallel_massless_bundle_guard 61/61 + 18/18`；当前 basis 与 package 为 3/3 相等（非零差值 0），历史 `{D11,D12,D21}` 为 0/3 相等（非零差值 3）。该阶段的 v7 哈希已由后续补 ISP 的 v8 快照取代。

### 当前任务：2026-07-21 `Orderless` 与多外动量导数结论复核

- [x] 核对 `package_012.wl` 中 `sp`、`kk` 及相关符号的实际 Attributes，确认用户此前要求的 `Orderless` 是否已经实现并实际参与 canonical。
- [x] 区分 `sp[k_i,k_j]` 的参数交换对称性、外部不变量坐标的对称性与方向导数算符 `D_ij=k_i.\[PartialD]_{k_j}` 的有序性，重新判断旧 oracle 的 basis 假设是否成立。
- [x] 用现有 `D_ij` 诊断脚本或最小符号例子复核 `D12/D21` 及 package 默认 basis，并更新检查摘要和本节完成状态。

验收标准：结论必须直接引用当前交付程序中的属性定义和 operator 构造代码；不得把 `Orderless` 自动等同于 `D12=D21`，也不得在未核实 package 实际行为前继续声称是用户 notation 问题或任务书缺失。

完成结论：用户此前要求的实现确实存在：`sp/qq/kk` 均设置 `Orderless`，外不变量只保留 `i<=j` 坐标，package 的 external-vector operator basis 另显式默认 `"upperTriangular"`。旧 0/36 的直接原因是独立 oracle 曾用 `{D11,D12,D21}`，不是 package 内部漏处理对称性；`Orderless` 只令 `s12=s21`，不会令有序算符 `D12=k1.\[PartialD]k2` 与 `D21=k2.\[PartialD]k1` 相同。诊断脚本已纠正，当前/历史两套 basis residual 均为零，当前 oracle/default 相同，default 与历史 raw 导数 3/3 不同。post-audit v6 聚合哈希为 `AD543BC98430554F280A4460E672127CAC1246C6BF7C06239A98C6CA6EF49E9B`。

### 当前任务：2026-07-21 更新任务书下的独立全量续验

- [x] 依据更新后的 `independent-benchmark/independent-benchmark.md` 重新核对固定 sign 分支、h/H/T 路线、family 定义和验收计数，旧冻结结果只作历史证据。
- [x] 对每项不符合完成归因：区分手推 oracle 错误、package 需要更新、任务书/输入定义不足以唯一判定；未完成独立复核前不得把差异直接归为 package 缺陷。
- [x] 按新版固定分支完成十个 family 的全量独立手推与 package check；`vertex_energy_signs` 已补 ISP 并完整闭合。
- [x] 完成两个指定 H family 的 `direct-h`、`bare-H(T=I)`、`H-to-h` 三路 IBP、`ds`、`AT` 与 `WT/shrinkTerms` 验收，重冻 expected 与最终哈希。

验收标准：差异归因必须由独立公式/结构检查支持，不能因 oracle 曾经出错而误报 package，也不能用 package actual 反向改写 expected。最终按新版任务书完成全部固定分支、可达 sector、全部适用生成元和 general-index 总导数检查。

当前结论：十个 family 共完成 2435/2435 条 seed 和 101/101 条 general derivative package 对照；reference-only 为 80/80 derivative、6/6 symmetry、4/4 parity。atomic massive 三路为 78/78；pure massive reference 三路为 456/456 seed、12/12 `ds`，H-to-h 对 direct-h 的 relation、derivative、`AT/WT/shrinkTerms` 全为零。已确认问题均不要求更新 `package_012.wl`：oracle 指标/canonical 错误已修正，多外动量 raw basis 已固定，`vertex_energy_signs` 已由任务书显式补入 ISP 并通过 90/90 relations、24/24 derivatives。post-audit v8 聚合哈希为 `BC2E47D190D32899B8787262B587860DF68E956527CD2B88618DC894735DC0CA`。

### 当前任务：2026-07-21 独立 benchmark 固定分支与 h/H 基变换验收

- [x] 将每个函数族的 sign 覆盖收缩为两个直接选定的冻结分支：一个全 `+` 或全 `-` 分支，再加一个 `+/-` 混合分支；两个分支均覆盖全部 contact-reachable sector 与全部 IBP seeds，不使用运行时随机。
- [ ] 所有含 massive line 的函数族都对两个冻结分支完成直接 h 的独立手推与 package 脚本验证；H 基底只选两个含 massive line 的函数族完成同等级手推与脚本验证。
- [ ] 对两个 H family 分别验证裸 H `T=IdentityMatrix[2]` 与从 H 通过非平凡 `T` 变到 h 的 package 路线，并将 H 经 `T` 得到的结果与直接 h 结果逐项比较。
- [x] 同步正式 `independent-benchmark` 要求、family 固定分支表和目录 README，删除任务书中已被新口径替代的全 sign/mode 声明。
- [ ] 按新口径重冻手推 expected、更新 cross-check 与结果文档；项目内部更广的历史回归可保留，但不得计入独立 benchmark 完成数。
- [ ] 运行专项与全量回归，记录冻结分支、seed 计数、h/H/T 比较计数及首个非零差值；清理临时产物并更新 GitHub。

验收标准：每个函数族恰有一个纯同号和一个混合分支，且选择已直接写死；分支收缩不得抽掉所选分支中的任何 IBP seed。所有含 massive line 的 family 均覆盖直接 h；H 恰选两个 massive family。两个 H family 的 `H(T=1)`、`H(T->h)` 均有独立 expected/package actual 对照，且 `H(T->h)` 与直接 h 在同一 `J` convention、sector、symmetry/parity 和指标基底下严格相等。

### 历史作废方案：2026-07-21 Codex 独立 benchmark 全量补完

> 2026-07-25 已由“general seeds/operators + 两套点名 Phase 2 full flow”边界取代。下列未勾选项不再是当前任务，尤其不再生成逐积分 `expectedDerivatives` 或全量回归。

- [ ] 按第 9.0 节新冻结的 `--/-+` 两分支重新冻结 `atomic_massive_line`：直接 h、裸 H `T=I`、H 经 `T_Htoh` 变到 h；旧 104/104 作为历史过覆盖保留，但不再作为当前正式计数。
- [ ] 建立不读取旧 expected/helper 的通用独立原子规则层，覆盖 massless full/cross、massive 直接 h、两个指定 family 的裸 H/`T_Htoh`、common-theta odd-subset contact、coincident canonical、time/q-IBP、EOM、ISP 与标量积吸收。
- [ ] 补完 `atomic_massless_line`、`pure_massless_bubble`、`mixed_bubble`、`mixed_triangle`、`mixed_sunrise`、`pure_massive_bubble_reference`、`two_loop_isp_toy`、`parallel_massless_bundle_guard`、`vertex_energy_signs` 九个 family 的 `README.md`、`family.wl`、`derivation.md` 与扁平 `expected.wl`。
- [ ] 对十个 family 各自冻结的一个纯同号与一个混合分支、规定的 h/H/T 路线、contact-reachable sector 和初始化独立外部变量生成 general-index `expectedDerivatives`；每条覆盖显式系数导数、两个积分的指标导数和纯系数项。
- [ ] 完成 reference bubble 的 `G/R1/R2 -> J`、`Vpm=0 -> --`、zero-point、`ks^2=s11`、symmetry、R2-to-R1 与 parity 对齐。
- [ ] 冻结全部手推文件哈希后，学习/调用正式 `package_012.wl`，逐 family 比较 seed relations 与 `ds`，记录计数、首个差值、forbidden `n`、内部 `kk` 和拒绝门禁。
- [ ] 运行全量专项检查、结构/来源隔离检查、哈希与 Git hygiene，并回填最终覆盖表和结论。

验收标准：任务书第 9 节十个 family 与第 13 节总导数全部完成；每个 family 只使用冻结表中的两个 sign 分支，每个分支只枚举从原始 kernel 实际可达的 sector，并覆盖全部 active time、全部 `L(L+K)` momentum 与全部剩余 `n=0/1`；两圈 ISP 在零/非零指标点均闭合；massive `n=2` 即时 EOM；common-theta 多线 contact、simultaneous shift、coincident canonical、reference symmetry/parity 均有独立推导和 package 零差值证据。两个 H family 还必须通过 `H(T=I)`、`H(T->h)` 与直接 h 的三路检查。本目录不得读取旧 hand-derived expected/helper 生成新 expected，也不得把 package actual 反向写成 expected。

### 当前任务：2026-07-21 Codex 独立手推与 package 交叉检查

- [x] 定位并复读独立 benchmark 规范、项目原有 hand-derived expected 与现有 package cross-check 入口，记录三者边界。
- [x] 新建 Codex 专用手推/check 目录；手推输入、推导、expected、package actual check 与运行结果分层保存，不覆盖现有 `hand-derived-v2` 或正式 benchmark 交付物。
- [x] 只依据独立 benchmark 给出的定义和 convention 选取 `atomic_massive_line` 并完成手推，不调用主线 package 或原有 hand-derived helper 生成 expected。
- [x] 学习 `package_012.wl` 的公开接口，用独立 check 脚本生成 actual，与新手推 expected 做 canonical 后逐项比较。
- [x] 运行专项检查并记录通过数、失败差值、未覆盖边界和是否发现 package 缺陷。

验收标准：新 expected 的推导来源可人工审查，且不读取 `000_code/check/hand-derived-v2/` 的 expected/helper；check 单向读取新 expected 和正式 `independent-benchmark/package/package_012.wl`；expected 与 actual 的比较覆盖所选对象的全部约定分支，差值 canonical 后严格为零，运行产物与正式源码/交付目录隔离。

完成结论：已新建 `codex-independent-benchmark/`，把来源隔离、family、h/H EOM 与 Wronskian 推导、104 条 frozen expected、独立数值公式检查、package actual check 和结果说明分层保存。`atomic_massive_line` 覆盖 h/H、`++/--/+-/-+`、top/e1、两端点全部 `n=0/1`、两个 time 与一个 momentum 生成元；独立 Hankel EOM/Wronskian 数值残差为 0，expected 结构门禁通过。正式 `package_012.wl` 对照在 check 层实施唯一命名映射 `dim->d` 后为 104/104，topology 全部有效且无 forbidden massive `n`；未发现 package 缺陷。最严格的盲测边界未完全满足：定位阶段曾看到 package 的公开 API 名称，但未读取旧 expected/helper 或 package 公式生成 expected；该暴露已在 `SOURCE_ISOLATION.md` 明示。本轮只完成一个完整高信息量 family，不宣称已完成任务书其余九个 family 或第 13 节全函数族总导数。

### 当前任务：2026-07-21 全函数族总导数 benchmark 与 reference bubble 对齐

- [x] 将 reference bubble 的 `G/R1`、`ks/k0`、zero-point、sector、symmetry 与 parity convention 显式映射到统一 `J` topology。
- [x] 用 package 的 `symmetry` 模块装入 reference 的交换对称性和奇偶筛选，并对齐后比较 reference `dk0/dks` 与 package `ds` 的全部适用检查。
- [x] 为其余现有 hand-derived 函数族独立手推 general-index 单积分导数，再比较 `ds` 作用于带动力学量系数的积分线性组合；显式系数和积分指标均保持符号化。
- [x] 将 reference bubble 对齐规则、全函数族总导数覆盖、乘积法则和独立来源门禁写入正式 `independent-benchmark/independent-benchmark.md` 完成检查表。
- [x] 更新 README、plan/design/tech/manual、交叉检查清单与进度记录，同步版本化 package/manual，运行专项和全量回归。

验收标准：reference 路线不得只比较乘积法则外壳，必须在相同变量、指标原点、sector、symmetry 和 parity 下逐项比较；其它函数族至少对每个可求导外部变量检查 `ds[c_1(s)J_1+c_2(s)J_2,s]`，其中指标为 general 符号且 expected 不调用主线导数 helper；所有差值 canonical 后严格为零，非法变量与非线性门禁保持通过。

完成结论：新增 `reference_bubble_derivative`，将 reference `Vpm=0` 对齐到 `--`，以 `G/R1/R2 -> J`、`a0=2nu`、`b0=-2nu`、shrink 后 `a0R=2nu/bS0=0` 和 `s11=ks^2` 固定 convention；reference 的 vertex/line exchange、`R2->R1`、R1 endpoint canonical 与 parity selection 全部通过 case `symmetryRules` 进入 package `symmetry`。reference 实际求导 basis 在 symmetry 后只保留 top/R1，全部 0/1 端点态、general 连续指标、`dk0/dks` 单积分和带参量系数组合通过 80/80，symmetry/parity/zero-point/变量原子检查均为 True。独立手推 helper 另从 `Dij=k_i.d/dk_j` 构造传播子、building block、ISP 和相位导数，对 10 个物理 family 的全部 sign/mode、contact-reachable sector 和初始化变量完成 468/468 general-index 总导数比较。正式 independent benchmark 已新增第 13 节和完成检查项。13 个根目录 012 测试与 14 个 hand-derived cross-check 全部通过；两份 PDF 编译及受影响页目视检查通过，正式手册与 `package_012.pdf` 哈希一致，主线 012 与 `package_012.wl` 哈希仍一致；未运行 Kira/Fermat reduction。

### 当前任务：2026-07-21 临时/过时结果清理与 GitHub 发布

- [x] 只读盘点 `tmp/`、`results_test/`、LaTeX 辅助文件、Kira 日志、正式 package 交付物与未跟踪本地工具目录。
- [x] 删除可再生的临时/测试输出和过时日志，不删除正式源码、检查脚本、expected、PDF 或版本化 package。
- [x] 重新运行受影响专项回归、交付物哈希、Git hygiene 和远端分叉检查。
- [x] 提交本轮 012 tadpole/总导数实现、文档、benchmark 与清理结果，并推送 GitHub `main`。

验收标准：根目录不再保留 `tmp/`；两个 `results_test/`、LaTeX 辅助文件和旧 `kira.log` 均清理；`.qoder/` 不删除也不提交；正式 `package_012.wl/pdf` 与权威源文件哈希一致；推送后本地 `HEAD` 与 `origin/main` 一致。

完成结论：已清除 `tmp/`、`000_code/check/results_test/`、`000_code/test/results_test/`、两份 PDF 的 LaTeX 辅助文件和旧 `kira.log`；测试重新生成的 Kira smoke 输出也已二次清理。13 个根目录 012 测试和 12 个 hand-derived cross-check 全部通过，Kira export smoke 为 11/11；`git diff --check` 通过，012/package 与 manual/package PDF 哈希分别一致，011 哈希未变。012 tadpole/总导数实现、文档和 benchmark 已由提交 `1cd010c` 推送到 GitHub `main`；`.qoder/` 保持本地未跟踪且未进入发布。

### 当前任务：2026-07-21 GitHub VPN/SSH skill 去冗余

- [x] 登记用户纠正：删除针对单次操作失误添加的两条冗余禁止性表述。
- [x] 将 `exec` 故障说明缩短为直接可执行规则：若 `$env:SHELL` 指向 PowerShell，则在当前命令环境中清除该变量，再运行 SSH/Git。
- [x] 复读并执行等价结构校验，确认 skill 保留端口、代理类型和 SSH 安全约定。

验收标准：skill 只保留直接可执行的 `exec` 故障处理，不扩展为多层环境审计或额外行为禁令；明确说明 OpenSSH 内部的 `exec` 不是通过 SSH 配置关闭，而是通过避免让 PowerShell充当其命令 shell 来解决。

完成结论：两条冗余禁令及三层 `SHELL` 检查已删除。skill 现只说明：OpenSSH 内部会为 `ProxyCommand` 加 `exec`，无法也无需在 SSH 配置中关闭；若当前 `$env:SHELL` 指向 PowerShell，则先从当前进程移除该变量，再运行 `ssh -G`、`ssh -T` 和 Git。最终文件 76 行，等价结构校验通过。

### 当前任务：2026-07-21 GitHub VPN/SSH skill 失败经验固化

- [x] 登记任务边界：复盘本次 `22308` 正常但 Codex 进程 `SHELL=PowerShell` 导致 OpenSSH `exec ProxyCommand` 失败，以及误用直连验证的错误链条。
- [x] 更新 `github-vpn-ssh/SKILL.md`，加入可复用诊断顺序和进程级 shell 兼容处理。
- [x] 执行与 `quick_validate.py` 源码等价的 frontmatter、命名、描述长度、允许字段和行数校验并复读最终内容；官方脚本因本地 Python 缺少 `PyYAML` 未能直接启动，不将依赖缺失误报为 skill 内容失败。

验收标准：skill 必须明确区分 SOCKS5 端口问题与执行 shell 无法解析 `exec`；只在当前 `$env:SHELL` 指向 PowerShell 时清除该进程变量，不修改 host key、私钥或 `StrictHostKeyChecking`。

完成结论：最新版 `github-vpn-ssh` skill 已新增 PowerShell/OpenSSH `exec` 故障分流：确认端口后，若 `$env:SHELL` 指向 PowerShell，则清除当前进程变量并重试 SSH/Git。原有端口选择、host key、私钥和 `StrictHostKeyChecking` 约定保持不变；等价结构校验通过。

### 当前任务：2026-07-21 GitHub VPN/SSH 发布通路修复

- [x] 按 `github-vpn-ssh` skill 登记边界：比特加速器使用 `127.0.0.1:22308` SOCKS5；不修改 SSH host key、私钥、`StrictHostKeyChecking` 或 GitHub 主机字段。
- [x] 核对 `ss-local.exe` 监听、SOCKS5 GitHub HTTP 响应和 `ProxyCommand` 执行 shell：`22308` 正常，GitHub 返回 HTTP 200；故障来自 Codex 进程的 `SHELL=PowerShell` 无法解析 OpenSSH 前置的 `exec`，不是代理端口或协议错误。
- [x] 恢复启用现有 `ProxyCommand`；仅对 Codex 发起的 SSH/Git 命令移除进程级 `SHELL=PowerShell`，验证 `ssh -G` 明确展开 `127.0.0.1:22308`、`StrictHostKeyChecking ask` 保持不变，`ssh -T` 成功认证且普通 `git fetch` 经该配置完成。

验收标准：`22308` 由当前 VPN 的 `ss-local.exe` 监听；`curl --socks5-hostname` 获取 GitHub 有效响应；SSH 认证经该代理成功；现有 `ProxyCommand` 内容不被改写，仅在 VPN 开关需要时注释或取消注释。

完成结论：VPN 的 `22308` 端口和 SOCKS5 协议均正常，无需改端口；实际故障是 Codex 进程的 PowerShell `SHELL` 覆盖无法执行代理命令前置的 `exec`。SSH 配置中的 `ProxyCommand` 现已启用；移除该进程级覆盖后，`ssh -G`、GitHub SSH 认证和 Git fetch 均确认走 `127.0.0.1:22308`，当前本地 `main` 比 `origin/main` 领先一个发布提交。仓库内外均未留下兼容 helper；系统级 OpenSSH shell、host key、私钥和其它 SSH 配置内容均未修改。

### 当前任务：2026-07-21 主线 012 GitHub 快照发布

- [x] 登记发布范围：提交当前 012 common-theta 正确性修正、文档、测试、独立 benchmark 与版本化 package 交付物；排除本地工具目录和临时渲染文件。
- [x] 发布前核对工作树、远端分支、交付物哈希与 `git diff --check`。
- [x] 创建可追溯的本地主线提交。
- [x] 推送到 GitHub 当前主线分支；外部数据导出确认后与后续 012 更新一并完成发布。

验收标准：远端主线包含本轮 012 源码、正式文档和验证资产；`011` 哈希保持不变；`package_012.wl/pdf` 分别与权威源码/手册一致；推送后本地 `HEAD` 与远端目标分支一致。此次发布不包含 Kira/Fermat reduction，也不提交 `.qoder/` 或临时 PDF 目视检查文件。

完成结论：发布前从 GitHub 获取远端状态并确认本地/远端均位于 `02de7e7`、无分叉；13 个 `012_*` 测试文件全部通过，`git diff --check` 通过，011 哈希保持 `DB91BB...EC22`，012 程序与版本化交付脚本、正式手册与版本化交付 PDF 的哈希分别一致。快照提交 `b07835b` 已随本轮后续 012 更新一并推送到 GitHub `main`；`.qoder/`、`results_test/` 和 Kira/Fermat reduction 输出未进入提交。

### 当前最高优先级任务：2026-07-21 common-theta/统一正则化等价性与指标链路定稿

- [x] 登记任务边界：这是 IBP boundary 正确性模块，不是表示优化；优先级高于 package 封装、性能与使用体验改进。
- [x] 在技术笔记正文给出共同-theta 方案从传播子乘积、单一 delta、odd-subset 展开，到逐线 `WT/shrinkTerms`、整数指标 shift、zero-point shift、coincident canonical 和最终 `J[...]` 的完整逻辑链路。
- [x] 在技术笔记附录分别完整推导共同-theta 方案与统一 Gaussian mollifier 逐线方案，并证明二者在所有共享同一时间差的 full-line bundle 上等价。
- [x] 在技术笔记附录加入三线 worked example：逐步展开 single/triple 项、Gaussian `1/12 -> 1/4`，并至少给出一个 massive/massless 线组到最终 `J[...]` 指标与 zero-point 的具体映射；用户手册保留简短示例。
- [x] 正文只保留推荐的共同-theta canonical 链路及关键公式；Gaussian/逐线分配的详细矩展开、`1/12 -> 1/4` 和一般系数证明统一移入附录。
- [x] 审计并列出所有受影响实现模块：bundle 识别、atomic massive/massless contact、`WT/shrinkTerms` 编译消费、simultaneous shrink、指标/零点映射、coincident canonical、可达 sector BFS、linearData/serializer 与测试。
- [x] 同步 plan、design、README、AGENTS、正式手册和专项 TODO，删除任何把 common-theta bundle 说成低优先级/future optimization 的现行表述。
- [x] 进一步明确 independent benchmark convention，尤其是 massive h/H 与 massless shrink 的整数指标、zero-point 和总物理幂次映射；不额外提示多线 family 的目标结论，让独立推导者继续只从传播子定义推导。
- [x] 更新 `independent-benchmark/package/`：删除旧无版本名程序和手册，交付 `package_012.wl` 与 `package_012.pdf`，同步 examples 的加载路径，并确认目录中不残留旧 package/manual。
- [x] 复核其它已确认错误均已由 012、文档和独立 expected 更正；重跑专项/继承/family 回归、TeX/PDF、哈希、矛盾扫描与 `git diff --check`。

验收标准：正文链路必须落到可执行的 `J[aList,linePacks,ispList]` 指标和 zero-point 变化，不能停在抽象 `WT`；附录必须从同一有限正则化定义证明两方案总 boundary 完全相同，并明确逐线分配不是独立的 canonical 选择。独立 benchmark 只补 convention，不泄露多线推导答案；正式交付只保留带版本号的 012 package 与手册。

完成结论：技术笔记正文现已给出 `bundle product -> single delta -> odd subset -> massive WT/massless contact -> integer/zero-point shift -> final J -> coincident canonical -> reachable sector` 的完整链路；附录分别证明共同-theta与统一 Gaussian 逐线方案，并用三线例子展示 `1/12 -> 1/4` 以及 mixed h/massless 的最终指标。代码审计确认 012 已正确消费 `WT/shrinkTerms`，本轮未发现需要新开 013 的实现缺陷；新增 `012_common_theta_wt_chain_test.wl` 通过 13/13。全部 13 个 012 专项/继承测试、10 个 family 回归和三个版本化 package examples 通过。独立 benchmark 只保留定义与通用指标记账 convention，不再给出特定多线答案；交付目录只含 `package_012.wl`、`package_012.pdf` 与 examples。两份 PDF 编译、引用和受影响页目视检查通过，未运行 Kira/Fermat reduction。

### 当前任务：2026-07-20 theta(0) 定义与可达 shrink sector 修正

- [x] 按用户追加要求修正版本边界：011 保持本轮开始时的内容；所有实现进入新版本 012；专项测试放入 `000_code/test/` 的 012 文件，不改乱 011/check 历史资产。
- [x] 从 SK 传播子与分布乘积出发，比较共同 theta、保留逐线 theta 的统一平滑正则化、点分裂等自洽方案，明确单个 `theta(0)=1/2` 与 `delta theta^m` 乘积规则的适用边界。
- [x] 用独立公式和最小可执行例子判定差异，不能仅以当前 package 输出作为真值。
- [x] 新建并修正 012：将同一代表顶点对之间的 theta-full 线视为共同边界 bundle，一次 contact 事件允许任意非空奇数子集同时 shrink；事件之间只合并不同代表类并形成 forest；补 massiveFull coincident endpoint canonical；011 不承载本轮修改。
- [x] 以既定 zero-point convention 核对每类 shrink 的“整数指标 shift + zero-point shift”总物理幂次：massive h 为 `1+2 nu`、H 为 `1+0`，massless 为 `0+0`；simultaneous contact 对所选线逐项求和。
- [x] 审计项目内 hand-derived/check 为何与旧程序同时通过：区分真正独立推导、共享 helper/同构假设、结构 smoke test 和硬编码 expected，并补防同源错误的测试。
- [x] 同步 benchmark 规范、手推 expected、examples、plan/design/tech/manual 和 benchmark package 快照，消除“禁止 delta^2”与“枚举所有子集”的矛盾。
- [x] 运行专项、全量受影响回归、TeX/PDF 及 Git hygiene 检查，并记录正确、错误和需保留条件的逐条结论。

验收标准：最终方案必须同时满足分布论自洽、统一正则化下的逐线 Leibniz 求导与共同-theta 合并等价、每次 contact 事件只含一个共同 delta、sector 仅含真实可达缩并、massive/massless coincident 状态 canonical 完整；结论必须有独立推导或执行证据支持。

完成结论：011 以 SHA-256 `DB91BB043BC9BCE3F5AE2296D2EDD061C0569F356E5AD8866D23B752EA27EC22` 原样保留，修正全部进入 `000_code/012_dS_ibp_general.wl`。推荐共同-theta odd-subset contact；逐线 theta 可用统一 Gaussian mollifier 自洽定义，且求和严格回到共同-theta 结果。massless shrink 的整数/零点总 shift 为 `0+0`，massive h/H 分别为 `1+2 nu` 与 `1+0`。正式文档、correctness TODO 和 benchmark package 快照均已同步；012 专项/继承测试与 10 个独立 family 对照全部通过，PDF 受影响页目视检查正常，未运行 Kira/Fermat reduction。

### 当前任务：2026-07-20 多重连边缩并后的 theta(0) 与二次 shrink 审计

- [x] 登记审计边界：两个顶点之间存在多条 massive/massless 传播子时，检查其中一条 shrink 后其余 full line 的端点是否变为 coincident，以及程序如何解释对应的 `theta(0)`。
- [x] 追踪 sector 构造、代表顶点重映射、time-IBP regular/theta-boundary 项和 coincident canonical 门禁，确认剩余传播子是否仍会被继续 shrink。
- [x] 用现有 parallel-line 验证资产或最小复现核对 massive、massless 及混合多重连边，区分数学 convention、当前实现和潜在缺陷。
- [x] 给出明确结论：是否会产生错误、错误出现在哪一层，以及需要怎样修正或补测。

验收标准：结论必须由 011 实际控制流和可执行最小例子支持，不能把此前仅针对多条 massless 共同 theta 的 future bundle 讨论代替本次一般多重连边问题。

审计结论：局部 time-IBP 路径会在 shrink 后把所有线端点重映射到代表顶点。剩余 full line 若变为 coincident，则 `lineEndpointSlotsAtVertex` 返回 `{1,2}`，`timeThetaBoundaryShrinkTerms` 因 slot 数不为 1 返回 0，不会继续产生 theta shrink；massive 线仍同时执行两个端点的普通 building-block 导数，massless `n=0` 的两个 regular 项相消，`n=1` 由目标 sector canonical 置零。massive、massless、mixed bubble 最小复现均得到 coincident endpoints `{1,1}`、二次 boundary `0`。但全局 `shrinkSectorSubsets` 仍用所有 theta-full 线的幂集，三种 bubble 都错误列出 `{1,2}`；`makeShrinkSectorSeedBatch` 把 mixed 双缩并 sector 纳入并报告 complete，而从单缩并 sector 实际生成的双缩并项数为 0。因此当前 canonical batch/linearData 含不可达的冗余多重缩并 sector；它与可达块通常解耦，不直接改写 top/single-sector 方程，但会引入无物理来源的 sector、冗余 masters 和错误 coverage 声明。另一个缺口是 massiveFull coincident 线尚未显式 canonical `n_{1},n_{2}` 交换对称性。正确修正应把 sector 枚举改为按代表顶点逐步扩展的可达 forest：只有候选线两端当前属于不同代表类时才允许 shrink，并补 massive coincident 对称 canonical；同时修订 benchmark 中“禁止 `delta^2`”与“枚举所有子集”的矛盾及相关 expected。本轮仅完成诊断，未修改 011。

### 当前任务：2026-07-20 动力学量导数统一编译入口与两项清理

- [x] 登记实施边界：删除无用途且与 canonical 门禁冲突的 `makeMomentumIBPLinearSystem`；修正技术笔记的 `xi/z` 链式法则；统一动力学量导数与 IBP 的函数系统 convention。
- [x] 将 external-vector/外不变量 massive building-block 导数改为只读取 line-local 最终 `AT -> derivativeTerms`，与 momentum/time IBP 共用同一编译后原子；明确 `WT -> shrinkTerms` 只用于 theta coincidence，不参与普通动力学量导数。
- [x] 增加非平凡 `T(x)` 的独立 expected，覆盖 external-vector 与外不变量导数、两个基底态和 h/H 回归；确认输出即时 canonical 且无 forbidden `n`。
- [x] 删除 `makeMomentumIBPLinearSystem` 定义及全部引用，确认正式入口只有 canonical batch/linear workflow。
- [x] 修正技术笔记的链式法则和含混指标说明，同步审计 plan/design/manual；运行受影响回归、TeX/PDF 与 Git hygiene 检查。

验收标准：任意可编译 `P,Q,T,W` 的 qIBP、tIBP 和动力学量导数都从同一个最终 `compiledFunctionSystem["AT"]/derivativeTerms` 得到 massive 导数；`WT/shrinkTerms` 的职责仅为 theta shrink；不存在 momentum-only linear helper；代码和文档统一使用 `partial_xi=2 xi partial_z` 与 `D=2 Sum c(v.Q) partial_z`，相关回归全部通过。

完成结论：`externalVectorBuildingBlockDerivativeTerms` 的 massive 分支现已复用 line-local `compiledMomentumEndpointDerivativeTerms`，因此 qIBP、tIBP、external-vector 与外不变量导数统一读取最终 `AT -> derivativeTerms`；普通动力学量导数不微分 theta ordering，故不调用 `WT/shrinkTerms`。无用途的 `makeMomentumIBPLinearSystem` 已从 011 与 benchmark 程序快照删除。新增非平凡 `T=DiagonalMatrix[{x,1}]` 专项回归通过 8/8，并确认两个目标基底态、external-vector/外不变量路径、无 forbidden `n`、Wronskian normalization 隔离及 h/H 行为。技术笔记的 `xi/z` 链式法则已修正，两份 TeX/PDF 已重新编译并完成受影响页面目视检查；全量受影响回归和 `git diff --check` 通过，未运行 Kira/Fermat reduction。

### 当前任务：2026-07-20 三项审计问题的指定修正方案

- [x] 为一般 `P,Q,T,W` 的外不变量导数指定最小修正：massive external-vector building-block 路径复用编译后的 `compiledMomentumEndpointDerivativeTerms`，不再直接假设 `n->n+1`。
- [x] 原方案曾为 `makeMomentumIBPLinearSystem` 指定兼容处理；后续用户明确要求无用途即删除，因此该项已由上一节的“彻底删除定义及引用”取代，不再保留 deprecated gate。
- [x] 为技术笔记的 `xi/z` 链式法则指定公式修正与全文一致性审计，统一到 `D=2 Sum[c (v.Q) partial_z]`。
- [x] 指定专项回归、全量受影响回归、文档编译和验收顺序；本轮只完成方案，不修改 011 或物理公式。

验收标准：实施者能直接按函数和测试文件定位修改；一般非平凡 `T(x)` 的 external-vector 导数有独立 expected；旧 h/H 行为无退化；momentum-only 入口不再产生含混结果；plan/design/tech/manual 的 `xi/z` 因子一致，且全量受影响检查通过。

指定方案：在 `externalVectorBuildingBlockDerivativeTerms` 的 massive 分支中，以 `extCoeff compiledMomentumEndpointDerivativeTerms[topo,int,e,endpointSlot,vDotQ]` 替换手写的 `n/a/b` 三次移位；新增非平凡 `T=DiagonalMatrix[{x,1}]` 的 massiveCross 外不变量导数专项，独立验证 `A_T` 第一行同时产生 `x^-1 F0` 与 `x F1`，并补 h/H `n=0,1` 回归。原方案中的 deprecated compatibility gate 已被用户后续要求覆盖，实施时直接删除 `makeMomentumIBPLinearSystem`。技术笔记中的错误 `xi/z` 等同关系改为 `partial_xi=2 xi partial_z`，并把复合算符写成 `2 Sum c(v.Q) partial_z = 2 Sum c(v.Q)/z partial_(log z)`；同步清理“通常 b->b+1”等含混说明。实施顺序固定为先加失败测试、再改实现、再改文档，最后运行函数系统、独立变量、atomic massive、mixed/pure massive、多圈、公开 API、serializer 及 TeX/PDF 检查。

### 当前任务：2026-07-20 项目全景通读与知识梳理

- [x] 登记阅读范围：总体 plan、design note、tech note、当前进度、README、正式用户手册、独立 benchmark 规范与当前主线 `011_dS_ibp_general.wl`。
- [x] 梳理项目所依赖的 dS in-in 图、统一积分族、EOM、Wronskian/shrink、time/momentum IBP、sector、ISP 与 Kira 线性输入等基础知识。
- [x] 对照文档与 `011` 的实际数据结构、初始化门禁、公开 API、canonical 流水线和 serializer，区分已实现能力、已验证范围与后续规划。
- [x] 形成面向后续开发的项目概况、关键 convention、代码导航、风险点和建议接手顺序，并把完成结论回填本节。

验收标准：能够从物理问题、数学表示、topology/function-system 输入、IBP 生成与 canonical、线性系统导出直到验证资产，连贯说明当前项目；结论以当前权威文档和 011 实现为依据，不把历史版本、future bundle 或尚未封装的功能误报为现状。

完成结论：已通读当前权威 Markdown/TeX 文档、边界条件短笔记、独立 benchmark 规范，并按模块核对 011 的 5698 行实现。当前主线已形成 `case -> topology/函数系统编译与 validation -> 全 sector qIBP+tIBP -> 即时 EOM/massless canonical -> linearData -> 可选 Kira serializer` 的闭环，但仍是可直接 `Get` 的科研脚本，不运行 reduction，也未实现边界条件求解、正式 package context、streaming/chunking 或共同-theta bundle。代码审计当时另记录了三个待专项修正点：外不变量 massive building-block 导数未复用最终 `derivativeTerms`、momentum-only linear helper 与 canonical 门禁冲突、技术笔记的 `xi/z` 链式法则有误；本节只负责记录，三项均已由上方最新任务修正并验证。

### 当前任务：2026-07-20 independent benchmark 程序快照与使用手册

- [x] 登记交付边界：`independent-benchmark/package/` 只放当前程序脚本、面向使用者的手册和少量典型应用 examples，不放 README、plan、design、开发进度、内部推导答案、expected 或结果对比脚本。
- [x] 建立无多版本并存的当前快照；后续更新必须先删除旧版内容，再放入新版程序脚本、手册和对应应用 examples。
- [x] 编写自包含使用手册，覆盖加载、topology/function-system 输入、统一 `J`、seed/IBP、EOM/canonical、独立变量导数、公开 API、线性系统、serializer、错误门禁和返回状态解释；不写程序完善计划。
- [x] 逐项运行手册命令和三个应用 examples，以执行结果反查手册缺项；完成文档、版本同步、目录内容和 Git hygiene 检查。
- [x] 以正式用户手册 TeX/PDF 为唯一权威来源：先补齐正式手册，再复制 PDF 到 benchmark；同步审计 README、AGENTS、plan/design/tech note、benchmark 任务书和目录说明是否对应 011 及本轮最新交付边界。

验收标准：独立验证者只读 `independent-benchmark/package/` 的程序、手册和典型应用 examples 即可知道如何调用全部已实现用户功能；如何组织手推结果和比较 actual/expected 由对方自行完成。目录中只有一个无版本歧义的当前程序快照，不含验证 adapter，也不泄露 benchmark 待推导公式或 package 开发材料。

实现结论：`independent-benchmark/package/` 已交付无版本名的 011 程序、由正式用户手册生成的 14 页 PDF，以及 mixed workflow、裸 Hankel 函数系统、两圈 ISP 三个应用例子。正式手册已补齐实际调用链并删除内部 hand-derived 目录、expected 说明和全量验证表；对外任务采用“先只读任务书并冻结手推，再打开 package 学习调用和自行对照”的两阶段隔离。

### 当前任务：2026-07-19 通用函数系统编译层与 Hankel 手推回归

- [x] 登记新默认语义：缺省模式为 h，缺省 `T=IdentityMatrix[2]`，原始 `W` 直接取 h 的 Wronskian，故缺省 `WT=Det[T] W=W_h`。
- [x] 在 010 之后新开 011，实现 `P,Q,T,W[,WT] -> A0,AT,WT -> derivativeTerms/shrinkTerms` 的初始化编译层；显式 `WT` 只作一致性校验。
- [x] 将 h/H 改成同一编译接口的 presets；IBP 导数与 massive theta shrink 只消费编译后的数据，旧 `bbType/eomCoefficients` 输入在初始化时转换到同一编译表示。
- [x] 为编译层补充独立检查：缺省 h、显式 h、裸 H（含 `nu^2/x^2` 二次 pole）、一般 `T`、Wronskian 一致性与非法输入门禁。
- [x] 在 hand-derived cross-check 中加入 Hankel 型覆盖，并运行受影响的原子、混合、多圈及公开 API 回归。
- [x] 同步 README、plan/design/tech note、用户手册与版本状态，重编译并目视检查两份 PDF。

验收标准：新版本中缺省 h 的 `T=I` 且 `WT=W_h`；Hankel preset 的 `AT` 明确含二次 pole；所有 massive 导数/shrink 只有编译后入口；h/H 手推 expected 独立于被测实现；既有回归无退化。

实现结论：011 已满足上述验收标准。缺省 massive 函数系统直接使用 h 的 `P_h=(2 nu+1)/x`、`Q_h=1`、`T=IdentityMatrix[2]` 和归一化 `W_h`，因此编译所得 `WT=Det[T] W=W_h`；裸 H preset 使用 `P_H=1/x`、`Q_H=1-nu^2/x^2`，其二次 pole 由统一编译得到的 `AT` 普通 Laurent 项进入 IBP，不含 H 专用 EOM 分支。

### 当前任务：2026-07-19 `P,Q,T,W -> A_T,W_T` 编译层规划

- [x] 登记目标数据流：输入二阶标准型的一阶导系数 `P`、零阶系数 `Q`、基变换 `T` 和原始 Wronskian `W`；`T` 缺省为单位矩阵。
- [x] 在总体 plan/design/tech note 中统一定义基础导数系统 `A0={{0,1},{-Q,-P}}`、变换后系统 `AT=T'.Inverse[T]+T.A0.Inverse[T]` 与 `WT=Det[T] W`。
- [x] 设计初始化编译结果 `compiledFunctionSystem`：IBP time/radial 导数只读取 `AT` 的指标移位数据，shrink 只读取 `WT` 的编译结果，不在 IBP 层重推 `P/Q/T/W`。
- [x] 明确 `W` 必须作为带归一化常数的原始输入；由 `P` 只能验证 `W'/W=-P`，不能确定归一化。允许显式给 `WT` 作交叉校验，但权威值由 `Det[T] W` 生成。
- [x] 明确可编译性门禁：`T` 对两个独立解相同且可逆；`AT` 矩阵元与 `WT` 必须能有限分解为 package 可吸收的系数和 `x=-xi tau` 幂，否则初始化失败，不进入 IBP。
- [x] 在迁移规划中把 h/H 规定为纯数据 presets：共同以裸 H 的 `P,Q,W` 为基础，H 使用 `T=IdentityMatrix[2]`，h 使用 `T_Htoh`；完成迁移后 IBP 核心不再按 `bbType` 分支。
- [x] 将本规划标为通用特殊函数接口的后续未完成功能；当前 010 的内置 h/H 路径继续作为迁移前基准，不在本轮直接替换。

验收标准：计划中只有一条权威链路 `P,Q,T,W -> AT,WT -> IBP`；导数与 shrink 分别只有一个编译后入口；`T=I`、显式 `WT` 校验、不可逆/不可分解输入和 h/H preset 均有明确行为。

### 当前任务：2026-07-19 Wronskian 的 `P,Q` 与 `T` 标准章节

- [x] 登记范围：只记录二阶 `P,Q` 方程的 Wronskian 关系和同一解空间经 `T(x)` 变换后的关系，不收入其它 `n=0,1` 特例讨论。
- [x] 在技术笔记合适位置新建独立 Wronskian 章节，简洁推导 `W'=-PW`、`W=C exp(-Integral[P])`、`Wtilde=det(T) W` 与 logarithmic derivative/trace 关系。
- [x] 清理相关笔记/手册中作为等价观点出现的反对称矩阵 `E` 表述，统一使用 fundamental matrix 与 determinant；保留一个简短 H 到 h 例子。
- [x] 重编译相关 TeX、渲染抽查新章节并完成公式/引用/Git hygiene 检查。

验收标准：章节逻辑只含“`P,Q` 标准型 -> W”和“`T` 变换 -> 新 W”两段主线；明确 `Q` 在 Abel 推导中抵消、`T` 对两个独立解相同且可逆；不引入无关基底标签讨论或重复等价形式。

### 当前任务：2026-07-19 零点 convention 与 public API `badstate` 说明

- [x] 登记任务边界：补全独立 benchmark 的幂次零点定义，并以代码/测试为依据解释 `dSIBPPublicAPI::badstate`。
- [x] 核对 010 中 `a0/b0/bS0` 的输入、sector 合并和 `J` 展开语义，补写 top/merged/shrunk/ISP 的零点 convention 与示例。
- [x] 核对 `010_public_api_check.wl` 的 14 项断言、非法离散态样例、消息触发位置和返回值，说明“没有静默隐藏”的准确含义。
- [x] 完成 benchmark 答案泄露扫描、Markdown/Git hygiene 检查并记录结论。

验收标准：零点与整数指标的职责、符号参数保留规则和 sector 派生量均无歧义；对 `badstate` 的说明区分 Mathematica 消息、函数返回值和测试是否通过，不把预期诊断误说成测试失败。

结论：零点是 family/sector metadata，`J` 只保存整数移位指标；实际幂次分别为 `a+a0`、`-(b+b0)`、`-(bS+bS0)`，ISP 无零点。010 缺规则时回退 0，但独立 benchmark 要求 top `a0/b0` 显式且唯一；merged `a0`/`bS0` 作为派生 sector metadata。public API 的非法样例是 masslessFull `{bv,nbad}`：`nbad` 不是字面 `0/1`，门禁发出 `dSIBPPublicAPI::badstate` 并返回 `$Failed`；第 14 项断言的是 `$Failed`，消息可见性未被 `Quiet` 隐藏但也没有被该断言单独捕获。实测消息随后仍输出 `14/14`，进程退出码 0。

### 当前任务：2026-07-19 独立 benchmark 的 `J` 逐 pack 被积函数定义

- [x] 登记补充边界：定义必须足以把每个 `J` 槽机械还原为物理被积函数，但不得加入导数、EOM、Wronskian 或 shrink 的答案。
- [x] 明确 masslessFull `{b,n}` 中 `b/b0/n`、有序端点、SK case 与 `M[0]/M[1]` 的对应关系。
- [x] 同步明确 massive `{b,n1,n2}`、masslessCross `{b}`、shrunk `{bS}`、`aList` 和 `ispList` 的被积函数含义。
- [x] 增加至少一个完整 `J -> integrand` 对接示例并完成答案泄露扫描与 Markdown/Git hygiene 检查。

验收标准：独立推导者只读 benchmark 输入即可无歧义展开任意 top/sector `J` 的各个物理因子；定义式可以给，任何由定义求导或 coincidence 得到的关系仍不得给。

补充结论：新增 `LineBlock` 定义把每种 pack 逐一还原为 `q` 幂和相应 massive SK、massless `M`、massless cross `D` 或 shrunk monomial；明确 massless `{b,n}` 中 `b+b0` 是完整固定+整数动量幂，`n` 是按第一有序端点定向的 `M[0]/M[1]` 基底选择器，不是幂次或 time 导数计数。标准 `1/(2q)` 的数值 `1/2` 可提出，而 `q^-1` 必须进入 `b0`。另加入 atomic massless `J -> integrand` 完整示例。

### 当前任务：2026-07-19 独立 benchmark 最小输入规范审计

- [x] 登记审计边界：benchmark 只给基础单元、费曼规则、物理 convention 和统一 `J` 指标槽位/顺序，不泄露待独立推导的答案。
- [x] 检查并删除裸 H/h 的微分方程、Wronskian 等式结果、具体 shrink 公式及其它会与 package 共享结论的提示。
- [x] 保留 H 为 Hankel 函数、h 基于 Hankel 的定义，并只要求独立推导 Wronskian 与特定 `n` 的缩并关系。
- [x] 补齐 `J` 的 `aList/linePacks/ispList` 顺序、逐类 line pack 槽位、端点顺序和物理对象映射，完成定向文本审计。

验收标准：独立推导者可无歧义构造同一积分族和逐项比较键，但不能从 benchmark 文档直接抄得 H/h EOM、Wronskian 值或 shrink 指标移位/系数；文档与当前 010 接口约定一致。

审计结论：原任务书不符合该标准，曾直接给出 h/H EOM、massless 端点导数、Wronskian shrink、zero-point 映射和 future bundle 导数公式。现已改为只给 Hankel/h 定义、四种 SK kernel、Heaviside/质量 convention、拓扑与 seed 输入；所有 EOM、Wronskian、特定 `n` coincidence/shrink、merged zero-point 和 prefactor 都列为独立推导输出。`J` 的三个顶层槽、`vertexOrder/lineOrder/ispData` 顺序和每种 line pack 内部槽位均已逐项映射。

### 当前任务：2026-07-19 删除 001--006 并修正 007--010 裸 H EOM

- [x] 清点并删除工作树中 001--006 代码、检查入口及只服务于这些版本的状态引用；Git 历史不改写。
- [x] 在 007、008、009、010 中把 H tag 从错误 `{2 nu,1}` 递推改为含 `nu^2/x^2` 的裸 Hankel 导数系统，并保持 h 行为不变。
- [x] 重写 `atomic_massive_line` 的 H expected/推导，使 H 恢复为独立物理 benchmark，而不是 legacy 回归。
- [x] 更新 README、AGENTS、plan/design/tech/manual/independent benchmark 与版本状态。
- [x] 运行 007--010 H 专项、010 全量相关回归、TeX/PDF 与 Git hygiene 检查。

验收标准：工作树入口从 007 起；007--010 的 H EOM 均显式包含二次 pole 对应的指标移位；h/H 独立 expected 通过；不运行 Kira/Fermat reduction，不改写 Git 历史。

### 当前任务：2026-07-19 h/H 一阶系统与 Wronskian 基变换复核

- [x] 从 tech note 中的 h/H 定义独立推导 `n=0,1` 基底的一阶系统，核对已写入的 $A_h,A_H$。
- [x] 推导一般 $2\times2$ 基变换 $\widetilde{\mathbf F}=T(x)\mathbf F$ 下连接矩阵与 Wronskian/bilinear 的变换律。
- [x] 倒查错误来源：定位代码中 legacy `"H" -> {2 nu,1}` 的首次引入版本，并扫描 plan/design/tech/manual/benchmark 中所有把它误称为裸 Hankel EOM 的传承位置。
- [x] 修正文档中任何把“裸 Hankel 导数基底”和当前硬编码递推混同的公式，并重新编译、核验。

验收标准：明确区分导数基底与一般二维基底；给出无歧义的 $A_h,A_H$ 和 Wronskian 变换公式；若发现错误，不能只在对话中说明，必须同步修正相关笔记与计划。

复核结论：$A_h=\left(\begin{smallmatrix}0&1\\-1&-(2\nu+1)/x\end{smallmatrix}\right)$ 正确；先前 $A_H=\left(\begin{smallmatrix}0&1\\-1&-2\nu/x\end{smallmatrix}\right)$ 错误。裸 Hankel 导数基底的正确矩阵为 $A_H=\left(\begin{smallmatrix}0&1\\\nu^2/x^2-1&-1/x\end{smallmatrix}\right)$。错误在初始 Git 提交 `cd7b9c9`（2026-07-11）的 004/005、tech note 与 plan 中已经同时存在，随后传承到 006--010、independent benchmark 和 atomic H expected；不是 010 新引入。H 的 Wronskian $1/x$、shrink power $-1$、prefactor 与零点结论始终正确，错误限于旧 EOM/导数系统。现已在所有保留版本 007--010 修复，001--006 及其专用检查从工作树删除。

### 当前任务：2026-07-19 bundle 低优先级设计、通用 2x2 EOM 规划与 010 公开 API

- [x] 登记任务边界、未完成项和验收标准。
- [x] 将平行 massless 共同-theta bundle 的原理、压缩记号、time/momentum IBP 公式和 shrink 难点写入 plan/design/tech/manual，明确只是低优先级潜在优化，不是当前版本门禁。
- [x] 审计特殊函数 EOM 是否仍硬编码 h/H；确认 010 仍使用 `bbEOMCoefficients`。早期方案登记为直接输入一阶 `2x2` 系统，后续已统一细化为输入 `P,Q,T,W`、初始化编译 `AT/WT`，并说明 h/H 是该接口的两个 presets。
- [x] 核对 `dtau/dqq/dqk`、`rep2innerform/rep2outform/rep2Integrand` 在 009 中确实未实现。
- [x] 保留 009，新开 `000_code/010_dS_ibp_general.wl`，实现六个公开 API，复用现有原子链式法则、EOM/canonical 和 topology context。
- [x] 增加 `010_public_api_check.wl`，覆盖线性表达式、生成元选择、inner/out round-trip、`J` 指标保持和 inert `Hh` integrand 展开，14/14。
- [x] 同步 README、AGENTS、plan/design/tech/manual，运行相关 Wolfram 回归与 TeX 编译；最终文本审计和 `git diff --check` 见本任务收尾记录。
- [x] 完成收尾审计：修正非历史语境中残留的 009 主线表述，确认六个 API 不再列作未实现，009 文件无差异；抽查两份 PDF 的新增公式页，并完成全工作区 `git diff --check`。

验收标准：bundle 设计完整但明确低优先级且不进入 010 canonical；通用 `2x2` EOM 输入作为真实未完成功能记录；010 成为主线且六个公开 API 有错误门禁与小型验证，009 保持不变；不运行 Kira/Fermat reduction。

实现结论：010 新增显式 topology 与已注册 context 两套调用方式。`dtau/dqq/dqk` 要求 full-line 离散态已显式取 `0/1`，并复用 009 原子 seed 与即时 EOM/canonical；`rep2innerform/rep2outform` 保护所有 `J` 后只转换系数中的 `sp/qq/qk/kk`、外不变量和 ISP 名；`rep2Integrand` 展开时间幂、顶点相位、分母、ISP，并用 inert `Hh[MassiveBlock|MasslessBlock|MasslessCrossBlock]` 保存非平凡传播子块。

### 当前任务：2026-07-19 平行无质量传播子 bundle 简化核查

- [x] 登记核查范围和验收标准。
- [x] 核对当时的 009 是否实际实现多条平行 massless 线的共同 theta/canonical 简化。
- [x] 核对当前逐线表示、metadata 和 future bundle 记号的准确边界。
- [x] 向用户说明简化原理、当前可用记号与真正 bundle 后的建议记号。

验收标准：明确回答该模块是否已加入；区分逐线 merged-two-theta 与多线共同-theta bundle；给出 theta 乘积为何从形式上的 `2^r` 状态降为两个共同区域，以及当前/未来积分记号和尚未实现的 canonical/IBP 规则。

核查结论：009/010 均未实现真正的多线共同-theta bundle canonical。`masslessBundleCandidates[topo]` 只按原始顶点对识别多条 `masslessFull` 候选线，在 topology/sector metadata 中返回 `bundleMode -> "futureOptionalTwoThetaSharedPair"`；seed、IBP、shrink 与 serializer 仍使用逐线 `{b[e],n[e]}`。`parallel_massless_bundle_guard` 的 242/242 也只验证 `perLineMergedTheta`。未来参考记号 `JBundle[...]` / `JMixedBundle[...]` 不进入 010 主线或正式 expected。

### 当前任务：2026-07-19 GitHub 拉取与对上原因复核

- [x] 检查本地工作树、远端和分支状态。
- [x] 从 GitHub 执行 `fetch`/`pull --ff-only`，确认是否存在合作者新提交。
- [x] 重跑 10 个 hand-derived package examples 与独立变量求导 example，复核“结果对上”是否可在本机复现。
- [x] 对照 009 主线实现与独立 expected helper，定位对上的主要技术原因。

验收标准：明确 GitHub 是否有新提交；给出当前本地未提交改动边界；列出复现实测结果；说明对上的关键实现点与仍未完成边界。

### 当前任务：2026-07-18 旧状态错误清理

- [x] 登记清理范围、未完成项和验收标准。
- [x] 扫描当前文档中的旧主线版本、已完成但未勾选项、未来时叙述和文件状态矛盾。
- [x] 核对 README/AGENTS，并修正进度文件、设计/计划、用户手册及相关入口文档中的错误状态；不改物理公式和主线代码。
- [x] 运行定向文本审计与 `git diff --check`，确认不再把已完成项写成未完成，也不误删真实后续能力缺口。

验收标准：当次清理统一以 009 为主线；后续 2026-07-19 已新开 010，当前状态以本文件最上方任务为准。

### 当前任务：2026-07-18 项目进展盘点

- [x] 记录本次盘点任务、检查范围和验收标准。
- [x] 核对 Git 工作区、当前分支与远端同步状态。
- [x] 核对主线代码、公开接口、examples、独立 benchmark 和文档的实际完成度。
- [x] 汇总已完成能力、仍未完成项、状态记录矛盾和建议接手顺序。

验收标准：给出基于当前仓库实况的进展摘要，明确已验证范围、未完成项、工作区状态和下一步优先级；不把历史待办误报为当前事实。

#### 盘点后的当前结论

本节是 2026-07-18 的仓库实况快照；下方分阶段清单已在同日完成状态对齐，历史验证记录中的旧版本号只表示当时实际测试对象。

- Git：盘点开始时 `main` 与 `origin/main` 同在 `02de7e7`；本次只修改本进度文件，未改主线代码。
- 主线：`000_code/010_dS_ibp_general.wl`，继承 009 的 topology-driven seed、统一 `J`、time/momentum IBP、EOM/canonical、ISP 导数、独立变量求导、`linearData` 与基础 Kira serializer，并加入六个公开 API。
- 验证资产：`hand-derived-v2` 有 10 个 family，对应 examples 有 10 个 cross-check；最近一次全量记录为 2026-07-18 全部通过。本次没有重跑 Wolfram 回归，因为 HEAD 自该次验证后只增加了进度发布记录。
- 当前功能缺口：用户输入 `P,Q,T,W` 并编译 `AT/WT` 的特殊函数系统及 h/H preset 迁移、正式 Mathematica package/context、streaming/chunking 和其它后端 serializer；parallel-massless bundle canonical 只作为低优先级潜在优化。
- 当前文档缺口：主线版本、symmetry 和 10 个 benchmark 状态已对齐；两份 TeX 已编译成功，但 PDF 目视验收未完成，日志仍有 overfull warning。
- 版本保留状态：010 为当前主线、009 为上一开发版，008/007 为保留的历史版本；001--006 不再保留。

### 当前任务：2026-07-18 文档补全与 GitHub 更新

- [x] 确认当前是否已有程序包功能手册。
- [x] 将独立变量/微分方程求导模块写入用户手册、总体 plan 和技术/设计相关文档。
- [x] 重新编译或运行必要检查，确认文档与 example 不破坏当前主线。
- [x] 将当前工作区状态提交并推送到 GitHub。

验收标准：手册明确说明程序包能力和微分方程求导接口；相关文档同步；验证命令通过；GitHub 远端包含本次更新。

### 当前任务：2026-07-16 项目最新状态盘点

- [x] 读取当前权威进度文件、README、AGENTS、主线代码目录、check 目录、note 目录和 benchmark 目录。
- [x] 核对当前主线版本、已完成验证、未完成 P0/P1 项和下一步接手顺序。
- [x] 向用户汇总最新状态，明确哪些结论来自本次文件盘点，哪些仍只是待验证项。

验收标准：给出一份可直接接手开发的状态摘要，包含当前主线、已验证范围、未完成函数族/API/文档项和下一步建议。

### 当前任务：2026-07-16 继续补全手推及验证

- [x] 建立 `mixed_bubble` 的 `hand-derived-v2` 目录，沿用已通过函数族的 README/family/expected/check 格式。
- [x] 独立写出 mixed massive+massless bubble 的小型手推 expected，不调用 009 seed 生成函数产生 expected。
- [x] 提权运行 `mixed_bubble/check.wl`，记录通过数、覆盖计数和失败分类。
- [x] 若发现 009 实现或手推公式不一致，先定位到具体 sector/sign/generator/state，不做大范围重构。

验收标准：`mixed_bubble` 至少完成全 SK、全可达 sector、全 active time 与 q-IBP 生成元的独立逐条比较，并把结果写回本文件验证记录。

### 当前任务：2026-07-17 全量补完手推并拆出 package examples

- [x] 将 `hand-derived-v2/<family>/check.wl` 中所有加载 `009_dS_ibp_general.wl` 的程序包交叉验证脚本迁出到 `000_code/examples/hand_derived_cross_checks/`。
- [x] `hand-derived-v2/<family>/` 只保留独立手推输入、expected、必要 derivation 和 README，不再放 package actual check。
- [x] 为已完成族建立 examples：`atomic_massless_line`、`atomic_massive_line`、`pure_massless_bubble`、`mixed_bubble`。
- [x] 补完剩余函数族手推 expected 与 package example cross-check：`mixed_triangle`、`mixed_sunrise`、`pure_massive_bubble_reference`、`two_loop_isp_toy`、`parallel_massless_bundle_guard`、`vertex_energy_signs`。
- [x] 每完成一个 family 立即运行对应 example check，写回本文件验证记录。
- [x] 在 `009_dS_ibp_general.wl` 补入 momentum-IBP 对 ISP/numerator 因子的导数项，并用两圈 ISP family 交叉验证。

验收标准：C2 全部函数族都有独立手推 expected，程序包交叉 check 全部作为 package examples 单列，且每个 example check 给出通过数和覆盖计数。

### 当前任务：2026-07-17 独立变量求导模块

- [x] 查文献确认对外部不变量求导如何转成外动量矢量导数与点积算子组合。
- [x] 明确本 package 中独立变量分两类：顶点外腿能量变量与 scalar-product/外不变量变量。
- [x] 增加对所有独立变量求导的模块；外腿能量变量只作用于顶点 e 指数，scalar-product 变量通过线性方程转成 `p_i . d/dp_j` 算子组合后复用矢量导数。
- [x] 增加小型 example/check，覆盖外腿能量、外不变量和 ISP/内线相关变量求导。

实现记录：外部不变量导数采用
`\partial_{x_a}=\sum_{ij} c^{(a)}_{ij}\,k_i\cdot\partial_{k_j}`，系数由
`\sum_{ij}c^{(a)}_{ij}(k_i\cdot\partial_{k_j})x_b=\delta_{ab}` 求解。完整 `K^2`
外动量矢量导数空间通常有零空间，解不唯一；当前实现默认使用上三角 canonical basis，
并在 `makeExternalInvariantDerivativeDecomposition` 返回矩阵、系数、残差、`nullity` 与 `nonUniqueQ`。

验收标准：文献公式、实现接口、测试结果都写回本文件；不运行 Kira/Fermat。

- 当前开发主线：`000_code/012_dS_ibp_general.wl`；011 为本轮审计冻结基线，010/009/008/007 为保留的历史版本。
- 012 继承 011 的统一函数系统与公开 API，并新增共同-theta contact、可达 sector 和 coincident massive canonical；缺省 massive 模式为 h，`T=IdentityMatrix[2]` 且 `W=WT=W_h`。
- h/H 都是 `P,Q,T,W` 接口的 presets；time/momentum IBP 只读取 `AT` 编译项，massive theta shrink 只读取 `WT` 编译项。
- 012 的 10 个 hand-derived family、专项检查、公开 API 与 serializer smoke 已全量通过；pure massive bubble 已同时覆盖 h/H，共 608/608。
- 当前积分表示：`J[aList, linePacks, ispList]`。
- massive full/cross：`{b[e], n[e,1], n[e,2]}`。
- massless full：有序端点 convention 下的 `{b[e], n[e]}`，`n[e]` 只取 `0/1`，统一使用双 theta 合并路线。
- massless cross：`{b[e]}`，没有 theta 离散指标。
- massive 或 massless full 线都可由共同-theta contact 进入可达 shrink sector；massive 使用 `bS=b+1`，massless 使用 `bS=b`。
- 所有 Hankel `n=2` 必须在 seed 层立即用 EOM 消去；massless 正式表示中禁止产生 `n=2`。
- seed 保持小型解析表达式；需要撒点的 linear/check 阶段使用代数数值规则。禁止大范围解析生成和大范围撒点。
- Kira 只是一种 serializer；package 不运行 Kira/Fermat，不保存本机路径，不给 seed 直接导出 Kira。

### 当前 P0：独立 benchmark convention 完整化

- [x] 文档自包含给出 dS SK 费曼规则、顶点相位、massive/massless full/cross 传播子结构。
- [x] 明确定义 $h$ building block、其一阶递推与二阶微分方程/EOM，不依赖 package note。
- [x] 完整固定 massless 有序端点、单 $n$、$++/--/+\!-/ -\!+$ 的正负号、regular 与 theta-delta shrink/cancellation。
- [x] 每个待测函数族先统一写出顶点、SK signs、线序、有序端点、动量路由、ISP、零点、$J$ notation、sector 和生成元，再发布推导任务。
- [x] 审计所有 family 任务不再使用未定义或相互冲突的 notation，并通过 Markdown/文本一致性检查。

### 当前 P0：GitHub 最新版发布

- [x] 当次发布主线为 `010`，`009` 为上一开发版，`008/007` 继续保留；001--006 及其专用 check 已删除，外部 reference 公式保留。后续开发已新开 011。
- [x] 已审定最新版 note、plan、design、用户手册、PDF、独立 benchmark、研究进度和必要验证文件的完整发布清单。
- [x] 已审计 `.gitignore`、远端、当前分支和私有仓库状态；临时产物、测试输出和本机路径均排除。SSH 可访问且未认证 API 返回 404，符合私有仓库状态。
- [x] 已将发布 commit `fc03df0` push 到现有 GitHub 私有仓库 `origin/main`。

### 1.0 已完成 P0：无质量 `{b_i,n_i}` 的 time-IBP 缩并与抵消

- [x] `note`：用户手册和技术笔记都写出有序两端点的 regular 项、仅 `n_i=1` 时的 theta-delta 项、第一/第二端点系数 `-2/+2`、缩并指标 `b_{S,i}=b_i`，以及两端点落到同一 active vertex 后 regular/shrink 相消且反对称 `n_i=1` 为零。
- [x] `plan`：把上述规则列为 seed pipeline 的强制步骤和验收门禁，不允许未处理边界项进入 `linearData` 或 serializer。
- [x] `design`：给出与代码一致的有序端点公式，并明确 sector 改变后必须用目标 sector 的代表顶点映射重新判定 coincident cancellation。
- [x] `code`：009 中 time-IBP 对 `masslessFull` 已实现并显式注释上述规则；专项回归直接检查 regular、theta-delta 与目标 sector canonical。
- [x] 已运行 atomic massless 与 pure massless bubble 小型解析回归；未做大范围撒点。

### 1.1 已完成并由 010 全族基准复核

- [x] 008 继承并通过 `sp[p,q]` 用户接口与内部标量积缓存。
- [x] 008 的有序 massless 单 `n` 端点规则。
- [x] 008 的 massless momentum 指数核导数。
- [x] 008 的 massive/massless theta-boundary shrink，含 `++/--` Vpm。
- [x] 008 的顶点 `+/-` 外部相位符号。
- [x] 008 的 coincident massless `n=1` canonical zero。
- [x] 008 的 backend-neutral `linearData` 与基础 Kira serializer。
- [x] 现有极小回归：massless 方向 27/27、`sp` 24/24、缓存 8/8、Kira serializer smoke 11/11。
- [x] 上述底层能力已由 010 的 10 个指定函数族覆盖全 sector/全生成元并逐项比较；这证明当前 benchmark 范围通过，不等于任意拓扑的数学穷尽证明。
- [x] 旧 50 项 expected 未作为当前证据；当前证据来自从零建立的 `hand-derived-v2`。

### 1.2 已解决历史缺口与当前未完成项

- [x] `independent-benchmark.md` 已更新到当前 convention：有序 massless 单 `n`、massless shrink、全 sector/全生成元和简化输出格式。
- [x] 旧 `hand-derived/` 已整体清空；`hand-derived-v2/` 已从零建立 10 个指定函数族，未迁移旧 expected。
- [x] 10 个指定函数族已有新 convention 下的全 sector、全 time/q-IBP 生成元 expected 与 package cross-check。
- [x] 008 的跨 sector canonical 缺陷已在 009 修复：从每个 `J` 的 shrunk packs 重建代表顶点映射后，`pure_massless_bubble` 70/70。
- [x] 007 缺陷已在 008 修复：`atomic_massive_line` 从 96/104 提升为 104/104；默认 offset 为 `++ -> 1`、`-- -> 0`，显式用户设置仍可覆盖。
- [x] 每个函数族已按各自 README 覆盖所有要求的顶点 `+/-` 组合和适用离散 `n=0/1` 状态。
- [x] 10 个新手推函数族均已建立独立 expected；012 的修正版 expected 与 comparison harness 位于 `000_code/test/012_hand-derived/` 和 `000_code/test/012_hand_derived_cross_checks/`，并全部通过。
- [x] 010 已实现 `dtau`、`dqq`、`dqk`、`rep2innerform`、`rep2outform`、`rep2Integrand` 公开原子 API。
- [x] 010 继承用户 `symmetryRules`、`repSymmetry0` 与 `symmetry[expr_,topo_]`；只单次应用用户规则，不自动检测图自同构。
- [x] 主用户手册 `000_note/01_dS_ibp_package/dS_ibp_package.tex` 已按 package 使用手册重写，旧 family tree、xq、旧 tq 与无关附录已删除。
- [x] 根目录 README 与 AGENTS 已改为 012 当前开发主线，并明确本 todolist 的最高任务更新优先级。
- [x] 技术笔记的 massless time-IBP convention 已统一；最新版入口文档的旧接口/旧 notation 已完成定向审计。
- [x] 本轮专项审计：design 已明确 `J[aList,linePacks,ispList]` 的三个正式槽位，第三槽 ISP 不是附注。
- [x] 本轮专项补齐：plan、design、tech note、用户手册与代码已一致写出 massless `{b,n}` 的 time regular 翻转、theta-delta shrink、coincident `n=1` 抵消和目标 sector canonical。
- [x] 用户手册开头已按“费曼规则 -> massive/massless/full/cross -> J pack”顺序重写。
- [x] `dtau/dqq/dqk`、`rep2innerform/outform`、`rep2Integrand` 已在 010 实现并通过 14/14。
- [x] 012 继承用户输入 `P,Q,T,W`、初始化编译 `AT/WT` 并供 IBP 导数/shrink 分别调用；h/H 均为 presets，其中缺省 h 有 `T=I`、`W=WT=W_h`，H 的 `AT` 显式含二次-pole 指标移位。
- [x] 两份 TeX 已最终编译通过，最新版入口文档的旧 notation/H/common-theta 公式审计与 PDF 目视检查均已完成。

## 2. 本轮任务顺序

严格按下列顺序推进。每完成一项立即更新勾选和验证记录，不在最后一次性补状态。

### A. 文档与任务边界

- [x] A1. 建立本文件，记录当前真实状态和交接顺序。
- [x] A2. 在 `dS_IBP_package_plan.md` 开头注明它是总体 plan，并链接本文件作为当前 todolist。
- [x] A3. 更新 `independent-benchmark/independent-benchmark.md` 到 010 convention：
  - 目标版本改为 010。
  - massiveFull 与 masslessFull 都产生可达 shrink sectors。
  - 明确 massless 有序端点、`n=0/1`、同端点二阶导数和 theta-delta 符号。
  - 删除过度复杂的多层 Association 输出要求，改为每个函数族一个 README 加简单 `.wl` 关系列表。
  - 明确外部 AI 的结果输出在它自己的目录，不写回 `independent-benchmark/`。
  - 增加所有函数族、全 sector、全生成元和易错点覆盖矩阵。
- [x] A4. 更新根 README 和 AGENTS：010 为当前开发主线，新增本 todolist 的优先级规则。
- [x] A5. 新手推基准稳定后已统一修订设计笔记、技术笔记和用户手册；旧 notation 全仓审计仍在 F 节单列。

### B. 旧手推数据清除

- [x] B1. 删除整个 `000_code/check/hand-derived/` 旧目录。
- [x] B2. 不迁移、不改名复用旧 `.wl` expected。
- [x] B3. 新建 `hand-derived-v2/` 空目录骨架并只写新 convention README；实际关系必须重新推导后再落盘。
- [x] B4. 旧 reference bubble 代码保留在 `reference/`，只作为独立参考，不复制其旧 Head/零点/sector notation。

### C. 新手推基准

新手推结果统一放入：

`000_code/check/hand-derived-v2/<family-name>/`

每个函数族目录只使用：

- `README.md`：定义拓扑、线序、端点顺序、动量路由、顶点符号范围、sector、生成元、离散态范围、特殊检查目的。
- `family.wl`：该函数族输入数据的独立定义。
- `expected.wl`：简单 relation 列表；每条只记录 `sector`、`vertexSigns`、`generator`、`seedRules`、`equation` 和简短 `tags`。
- 必要时 `derivation.md`：只记录确实需要人工审查的符号和一两条代表推导，不堆放所有展开式。

011 及更早版本的历史 comparison harness 保留在：

`000_code/examples/hand_derived_cross_checks/<family>_check.wl`

#### C1. 原子易错点

- [x] massless full：`n=0/1 × 第一/第二端点` 四种时间导数。
- [x] massless full：端点顺序反转，`n=0` 不变、`n=1` 变号。
- [x] massless full：同端点二阶导数回到原 `n` 并产生负号。
- [x] massless full：`n=1` 的第一/第二端点 theta-delta 系数 `-2/+2`。
- [x] massless full：`++/--` 的 regular 导数符号。
- [x] massless cross：`+-/-+` 两端点相位符号，无离散 `n`、无 shrink。
- [x] massive full/cross：两个端点的 `n=0/1` 导数和任何 `n=2` 的即时 EOM。
- [x] massive full：Wronskian shrink 的四个 `{n1,n2}` 状态、两端点和 `++/--` Vpm 符号。
- [x] massive/massless shrink：`bS` 整数移位、非零 zero-point 和合并后的单一 `a`。
- [x] 缩并后 coincident endpoints：同一 active vertex 上两个端点都参与求导；massless `n=1` 为零。
- [x] 顶点相位：`+` 给 `-I E`，`-` 给 `+I E`。
- [x] `sp` 的 Orderless 只表示标量积交换性 `sp[p,q]=sp[q,p]`，不是图或积分族对称性；用户任意动量名、外不变量命名与 `ke[i]` 另由接口检查覆盖。
- [x] ISP：`mixed_sunrise` 与 `two_loop_isp_toy` 已覆盖对角、交叉和外动量 q-IBP 对非零 ISP 指标的移动。
- [x] sector metadata：全 sector cross-check 已覆盖 compact `aList`、原顶点到代表顶点映射和 line slot；跨 sector coincident canonical 缺陷已由 009 修复。
- [x] 同一对顶点多条 full 线：012 的 `parallel_massless_bundle_guard` 已验证共同-theta single/triple contact、coincident canonical 与不存在 pair sector。

#### C2. 必做函数族

- [x] `atomic_massless_line`：22 条全 sector/全生成元关系与 8 个额外原子检查全部通过。
- [x] `atomic_massive_line`：h/H、四种 SK、全 sector/全生成元 104/104；H expected 独立包含 `nuM^2/x^2` 的 time/momentum 指标移位，007--010 全部通过。
- [x] `pure_massless_bubble`：两条 massless 线；所有 SK、所有可达 sector、每个 sector 的全部 active time 生成元和全部 q-IBP 生成元，64/64。
- [x] `mixed_bubble`：一 massive 加一 massless；四种 SK、可达 sector、全部 active time 与 `d/dq.q`/`d/dq.k`，132/132。
- [x] `mixed_triangle`：两条等质量 massive 加一 massless；三个顶点的 8 个 SK 组合、所有可达 sector、全部 time/q-IBP 生成元，1792/1792。
- [x] `mixed_sunrise`：两圈、一个 massive 加两个 massless；含 ISP，四个 SK 组合、所有可达 sector 和 6 个 q-IBP 生成元，1842/1842。
- [x] `pure_massive_bubble_reference`：保留与旧 reference 的可比较物理例子并使用统一 `J`；正式 expected 不启用额外 symmetry，reference-only 对称条件写入 README；h/H 各 304 条，共 608/608。
- [x] `two_loop_isp_toy`：专测 `dqq` 交叉生成元和一般 `sp` ISP，不依赖 bubble 硬编码，978/978。
- [x] `parallel_massless_bundle_guard`：两顶点三条 massless 线的共同-theta contact 与可达 sector，194/194。
- [x] `vertex_energy_signs`：覆盖 `++/--/+−/−+`、外腿能量 `ke[i]` 与外不变量复用；为闭合 `dqk` 坐标加入零幂辅助 ISP，90/90。

#### C3. 每个函数族的强制覆盖

- [x] 每个顶点的所有 `+/-` 组合。
- [x] 每个 SK case 中由 odd-subset contact/BFS 实际可达的 theta-full shrink 状态；cross 线不伪造 shrink sector。
- [x] 每个 sector 的全部 active `dtau` 生成元。
- [x] 每个 sector 的全部 `d/dq_l.q_m` 与 `d/dq_l.k_j` 生成元。
- [x] 每个 full massive pack 的所有 `n1,n2 in {0,1}`。
- [x] 每个 masslessFull pack 的所有 `n in {0,1}`；masslessCross 无 `n`。
- [x] 连续 seed 只取一个小基点，整数指标可取 0，但所有 `a0/b0/bS0` 保持非零符号参数。
- [x] 最终 seed 不含 massive `n>=2`，不含 massless 非 `0/1` 状态。
- [x] 每条关系带来源标签；任何因 canonical 变成零的关系仍保留并注明原因。
- [x] family README/expected 给出预期 sector、生成元、离散态和 relation 计数，package example 核对实际计数。

### D. 自动比较与验证

- [ ] D1. 抽取统一的 package-actual 比较 helper；当前 10 个 example 各自执行 `Expand[actual-expected]===0` 和计数检查，逻辑正确但仍有重复。
- [x] D2. expected 由独立手推 helper 或直接公式生成，不调用 011 主线 seed 生成函数。
- [x] D3. package actual 的 10 个 cross-check 均调用 011；公开 API 另由 `011_public_api_check.wl` 验证。
- [x] D4. 每个函数族的 example check 已运行并记录通过数，2026-07-19 又完成一次 011 全量回归。
- [x] D5. 检查保持小型符号 seed 并逐条流式比较，没有一次保留大范围解析展开式。
- [ ] D6. 为 sampled `linearData` 单列使用小型代数数值规则的专项验证记录；现有 serializer smoke 不替代该项。
- [x] D7. 未运行 Kira/Fermat，只检查 serializer 生成的基础信息文件。
- [x] D8. 已记录的 WolframScript 检查均显式输出结果，不使用 `Quiet` 隐藏消息。

### E. 010 公开原子 API

010 已通过 10 个 hand-derived family 与公开 API 14/14。

- [x] `dtau[i,expr,topo]` / 短签名：对单个 `J` 或线性组合作用指定顶点 time-IBP。
- [x] `dqq[i,j,expr,topo]`：作用 `d/dq_i . q_j`。
- [x] `dqk[i,j,expr,topo]`：作用 `d/dq_i . k_j`。
- [x] 三个复合算子内部调用同一组原子链式法则和即时 EOM/canonical。
- [x] `rep2innerform`：用户 `sp`、外不变量和 ISP 名到内部编号表示。
- [x] `rep2outform`：内部系数表示回到 `sp`/外不变量/用户 ISP 名。
- [x] 两个表示转换函数保护 `J`，不改变三个指标槽及任何指标值。
- [x] `rep2Integrand`：将 `J` 展开为被积函数表示；非平凡传播子 block 用 inert `Hh[...]`。
- [x] `rep2Integrand` 不积分、不生成 IBP、不应用 EOM。
- [x] 已添加 inner/out round-trip、指标保持和 integrand 内容检查。
- [x] 公开函数给出缺失 topology、错误生成元、shape 与未解析离散态消息。
- [x] topology 输入新增可选 `symmetryRules`；输入处给出一条指标交换替换规则示例。
- [x] `repSymmetry0[topo_]` 返回用户原始规则，`symmetry[expr_,topo_]` 只负责函数化单次应用并保持线性。
- [x] 不实现自动拓扑/参数对称性检测；没有输入规则时 `symmetry` 返回原表达式。
- [ ] pure massive bubble reference 的 README 已记录等质量内线交换与相等外腿参数下可选的额外对称性，但正式 expected 保持 `symmetryRules={}`；若要验证用户规则，需另建 symmetry 专项 example。

### F. 文档统一
本轮先完成以下一致性门禁，再继续扩展函数族：

- [x] design 的统一积分表示明确为三个正式槽位：`aList`、`linePacks`、`ispList`。
- [x] plan/design/tech note/manual 都写出 massless `{b,n}` 的两个端点、`n=0/1`、regular 翻转、`-2/+2` delta shrink、`bS=b` 与 coincident 抵消。
- [x] manual 第一章从 SK 费曼规则开始，并直接映射到 massive/massless full/cross packs。
- [x] 验证章节列出函数族目录、sector、生成元、SK signs、离散态与每个易错点，不使用复杂多层输出格式。
- [x] 原子 API 只写成“后续版本计划”，直到代码与独立 check 实际通过。

- [x] `dS_IBP_package_design_note.md`：权威实现、验证范围、公开 API 和 `P,Q,T,W -> AT,WT` 函数系统已更新到 011。
- [x] `dS_IBP_package_tech_note.tex`：已统一 massless 有序单 `n`、theta shrink、q/time IBP 和目标 sector 公式；其余章节仍按独立审计项继续整理。
- [x] `000_note/01_dS_ibp_package/dS_ibp_package.tex` 已重写为用户手册：
  - 开头先讲 dS SK 费曼规则及其到 `J` pack 的转换。
  - 复用现有图，但分别解释 massive/massless/full/cross。
  - 早期解释 massive EOM、massless `{10}=-{01}`、二阶导数和单 `n`。
  - 给出 topology、`sp`、ISP、external invariants、`ke[i]`、seed、`linearData` 和 serializer 的使用接口。
  - 删除 family tree、xq、tq-to-xq、旧纯 massless tq 第八章、详细 A/B 比较和全部无关附录。
  - B/单 theta 路线最多只在背景中说明一次，本 package 不采用。
- [x] README 只介绍 011 公开入口、已通过基准和真实未完成项。
- [x] AGENTS 记录本文件的最高任务更新优先级。
- [x] 两份 TeX 已编译且日志无 error/未定义引用；本轮新增/修正的 EOM、Wronskian、bundle 与公开 API 页面已完成目视检查，无截断、重叠、黑块或乱码；日志只剩既有的非致命 overfull/font warning。

## 3. 验收标准

在以下条件全部满足前，不宣称 package 主体完成：

- [x] 新手推目录不含旧 expected 的复制或 notation 迁移。
- [x] C2 所有函数族满足 C3 全覆盖。
- [x] 每个函数族的实际 relation 数与 expected/README 预期计数一致。
- [x] 全部 expected 与 package actual 逐项通过。
- [x] 全部 cross-check seed 均通过即时 EOM/canonical，不含 forbidden `n`。
- [x] 009 的 Vpm/symmetry 原子接口已通过。
- [x] `dtau/dqq/dqk` 与表示转换已在 010 实现，并由 011 的公开 API 检查继续验证。
- [ ] sampled `linearData` 在小型数值规则下可生成且 backend-neutral。
- [x] 011 Kira serializer smoke 只写基础文件并通过结构检查，11/11；未运行 Kira/Fermat。
- [x] 用户手册与技术笔记编译通过并完成目视检查。
- [x] 定向文本审计不再发现 006 被列为主线、旧 `xq/family-tree` 或 masslessFull 不 shrink 等错误；AGENTS 中的 `G/R1` 仅为明确标注的反例，masslessCross 无 shrink 是正确边界。
- [x] `git diff --check` 通过；未误删用户已有的无关修改。

## 4. 验证记录

| 日期 | 对象 | 命令/方式 | 结果 | 备注 |
|---|---|---|---|---|
| 2026-07-23 | 016 标准两圈 sunrise authority 纠正 | 模块入口与正式单文件 `check_016_gate_r5_fixes.wl`；标量积方向、ISP 数、精确反解、generator 数；残留扫描、哈希与 `git diff --check` | 通过 | 两条路径均 `22/22`；5 个标量积、3 个分母、2 个 ISP、6 个 momentum generators；核心与交付字节无需修改 |
| 2026-07-23 | 016 Gate R5 修复、公开 timeOnly 表示与正式交付 | 模块/候选/正式 `check_016_gate_r5_fixes.wl`；候选/正式 API coverage；candidate/formal example 05；手册两遍 `pdflatex` 与页面渲染；SHA-256、`git diff --check` | 通过 | Gate R5 `15/15`；coverage `4/4`、API `29/29`；example 05 两次退出码 0；程序 `6EA99C...A67A`，两份 PDF `59E6BC...2DCD`；未运行 Kira/reduction |
| 2026-07-20 | 011 动力学量导数函数系统统一与清理 | 新增 `011_external_function_system_derivative_check.wl`；重跑函数系统、h/H、独立变量、专项/API/serializer 和 10 个 hand-derived family；定向残留扫描；两份 TeX 两遍编译并渲染受影响页；SHA-256 与 `git diff --check` | 全部通过 | 新专项 8/8；函数系统 22/22、h/H 6/6、公开 API 14/14、symmetry 11/11、massless 27/27、`sp` 24/24、缓存 8/8、serializer 11/11 及 10 个 family 全通过；011 与 benchmark 程序哈希一致，正式手册与交付 PDF 哈希一致；`makeMomentumIBPLinearSystem` 已删除，普通动力学量导数只读最终 `AT/derivativeTerms`，`WT/shrinkTerms` 仅用于 theta/Wronskian shrink；`xi/z` 公式已修正；未运行 Kira/Fermat |
| 2026-07-20 | independent benchmark 程序、正式手册与应用 examples | 正式 TeX 两遍编译；PDF 全页及新增调用页渲染；手册应用 smoke；逐个运行 3 个 examples；SHA-256、目录边界、文档状态、Markdown fence 与 Git whitespace 审计 | 通过 | 手册 smoke 15/15；3 个 examples 均退出码 0；程序副本与 011 主线哈希一致，交付 PDF 与正式手册哈希一致；PDF 14 页，无截断/重叠/黑块/乱码；package 中无 expected、验证 adapter、plan/design；未运行 Kira/Fermat reduction |
| 2026-07-20 | 011 通用函数系统与 Hankel 手推回归 | `011_function_system_check.wl`、`011_hH_basis_transform_check.wl`、10 个 hand-derived cross-check、独立变量与 011 专项/API 检查；两份 TeX 各编译两遍并渲染函数系统及验证表页面 | 全部通过 | 函数系统 22/22；h/H 变换 6/6；atomic massive 104/104；pure massive h/H 620/620；其余 8 个 hand-derived family、独立变量、symmetry 11/11、massless 27/27、`sp` 24/24、缓存 8/8、serializer 11/11、public API 14/14 均通过。缺省为 h、`T=I`、`W=WT=W_h`；H 二次 pole 由 `AT` 编译项产生；未运行 Kira/Fermat |
| 2026-07-19 | GitHub 拉取与结果复核 | `git fetch origin main`；`git pull --ff-only origin main`；运行 `000_code/examples/hand_derived_cross_checks/*_check.wl`；`wolframscript -file 000_code\examples\independent_variable_derivatives_check.wl` | 通过 | GitHub `origin/main` 与本地 HEAD 同为 `02de7e7`，pull 为 `Already up to date`；10 个 package examples 全部通过，独立变量求导 4 项均为 True；本地仍有文档状态清理类未提交改动 |
| 2026-07-19 | `P,Q,T,W -> A_T,W_T` 规划与 Wronskian 标准章节 | plan/design/tech/manual/README 定向审计；两份 TeX 各编译两遍；渲染技术笔记第 6 页和用户手册第 5、12 页；旧 `E`/反向 `P,Q`/双入口扫描；`git diff --check` | 通过 | 权威链路固定为 `P,Q,T,W -> AT,WT -> derivativeTerms/shrinkTerms -> IBP`；`T` 缺省单位矩阵，显式 `WT` 只校验；PDF 无截断、重叠、黑块或乱码；该编译接口和 h/H preset 迁移仍是后续未完成功能，本轮未改代码 |
| 2026-07-19 | 零点 convention 与 public API `badstate` | 核对 010 `zeroPointRuleValue/linePowerIndex/sectorZeroPointRules` 和 public validation；沙箱外重跑 `wolframscript -file 000_code\examples\010_public_api_check.wl`；benchmark 禁用答案扫描、fence 平衡、`git diff --check` | 通过，14/14 | 实际先打印 `badstate`（line 1 masslessFull pack 2 的 `nbad`），再打印 14/14，退出码 0；第 14 项断言返回 `$Failed`，不单独断言消息标签；benchmark 已补 top/sector/ISP 零点职责与稳定分解规则 |
| 2026-07-19 | 独立 benchmark `J` 逐 pack 定义 | 通读统一表示与四类传播子输入；增加 `LineBlock` 和 atomic massless 展开例；扫描答案型公式；Markdown fence 平衡；`git diff --check` | 通过 | `{b,n}` 明确定义为 `q^(-(b+b0)) M[sigma,n;q,Delta]`；massive/cross/shrunk/a/ISP 同步逐项定义；澄清 `1/(2q)` 的 `q^-1` 进入 `b0`；未加入任何导数/EOM/Wronskian/shrink 答案 |
| 2026-07-19 | 独立 benchmark 最小输入规范 | 通读 `independent-benchmark.md`/README；定向扫描 H/h EOM、Wronskian/shrink 答案、`bS0` 派生赋值与 bundle 公式；Markdown fence 平衡；`git diff --check` | 通过 | 删除共享答案型公式；只保留函数定义、SK 费曼规则、convention、topology/seed 输入和逐槽 `J` 映射；所有 family 输入只含 unshrunk `a0/b0`；`derivation.md` 改为必交来源隔离记录 |
| 2026-07-19 | 裸 H EOM 修正最终验收 | 007--010 `atomic_massive_line_check.wl`；010 全量 hand-derived/专项/API/独立变量回归；两份 TeX 两遍编译并渲染 H 公式页；旧版本入口与 Git whitespace 审计 | 通过 | H expected 已独立包含二次 pole；007--010 各 104/104；010 其余回归全部通过；001--006 及其专用检查已删除；PDF 无截断/重叠/乱码；未运行 Kira/Fermat，未改写 Git 历史 |
| 2026-07-19 | h/H 一阶系统、Wronskian 与错误溯源 | `wolframscript -file 000_code\check\010_hH_basis_transform_check.wl`；Git `-S/grep/ls-tree`；两份 TeX 编译两遍并渲染抽查 | 6/6，通过 | 确认正确裸-H 矩阵含 `nu^2/x^2`；$A_H\to A_h$、`det T=x^(-2nu)`、Abel trace 与一般 Wronskian 变换均通过；错误自初始 004/005 传承；手册 13 页、技术笔记 26 页，无 fatal/未定义引用 |
| 2026-07-19 | 010 公开 API | `wolframscript -file 000_code\examples\010_public_api_check.wl` | 14/14 | 覆盖显式/已注册 topology、`dtau/dqq/dqk` 与底层一致、线性、动量符号索引、inner/out/round-trip、`J` 指标保持、inert `Hh` integrand 和非法离散态门禁；门禁断言返回 `$Failed`，消息在运行输出中可见但未单独捕获断言 |
| 2026-07-19 | 007--010 裸 H EOM 修正 | 对 `atomic_massive_line_check.wl` 分别传入 007/008/009/010；H expected 显式含 `nuM^2/x^2` 项 | 各 104/104 | 007 同时回补已知 `++ -> 1, -- -> 0` Vpm 默认值；四个保留版本的 h/H、SK、time/momentum 与 shrink 全通过 |
| 2026-07-19 | 010 hand-derived 与独立变量全量回归 | 运行 `hand_derived_cross_checks/*_check.wl` 和 `independent_variable_derivatives_check.wl`，均加载 010 | 全部通过 | atomic massive 104/104（h/H 均为物理 expected）；atomic massless 22/22+8/8；mixed bubble 138/138；mixed sunrise 2178/2178；mixed triangle 1800/1800；parallel massless 242/242；pure massive 310/310；pure massless 70/70；two-loop ISP 1230/1230；vertex energy 90/90；独立变量四项均 True |
| 2026-07-19 | 010 专项回归与 serializer | 运行 `000_code/check/010_*_check.wl` | 全部通过 | symmetry 11/11；massless 27/27；`sp` 24/24；缓存 8/8；Kira serializer smoke 11/11；未运行 Kira/Fermat |
| 2026-07-19 | 010 收尾文本/PDF/Git hygiene | 定向 `rg`；渲染并抽查新增公式页；`git diff --check`；`git diff -- 000_code/009_dS_ibp_general.wl` | 通过 | 非历史语境均以 010 为主线；六个 API 不再列作未实现；PDF 无截断/重叠/黑块/乱码；009 无差异；全工作区无 whitespace error |
| 2026-07-19 | bundle/2x2 EOM 文档与 TeX | plan/design/tech/manual/benchmark 定向审计；两份 TeX 各运行 `pdflatex -interaction=nonstopmode -halt-on-error` 两遍 | 通过 | 用户手册 12 页、技术笔记 25 页；无 fatal/未定义引用；抽查 EOM、bundle 与限制页显示正常；日志仍有非致命 overfull/font/hyperref warning |
| 2026-07-19 | 平行 massless bundle 简化核查 | 定向读取 009 的 `masslessBundleCandidates`/pack 生成、plan/design/tech/manual、independent benchmark 的 `JBundle/JMixedBundle` 和 `parallel_massless_bundle_guard` | 完成 | 009 只识别 metadata 候选并验证逐线 merged-theta；真正共同-theta bundle、单 `nBundle` 的 canonical/IBP/shrink 规则和 serializer 尚未实现 |
| 2026-07-18 | 旧状态错误清理 | 定向扫描旧主线/未来时/notation；`pdflatex -interaction=nonstopmode -halt-on-error dS_ibp_package.tex` 两遍；`git diff --check` | 通过 | 009、10 个 family、symmetry、006 保留状态已对齐；用户手册 11 页重新生成，无 fatal/未定义引用；公开 API、D1/D6、bundle canonical 和 PDF 目视 QA 等真实缺口保留 |
| 2026-07-18 | 项目进展盘点 | `git status/log/remote`；读取 README、009 API 定义、10 个 hand-derived family 与 examples、benchmark、设计/plan 和 TeX 日志 | 完成 | 盘点前 `main=origin/main=02de7e7`；确认 009 主线与 10/10 验证资产；未重跑 Wolfram 回归；发现公开 API、设计笔记旧版本叙述、历史清单和 PDF 版式验收缺口 |
| 2026-07-18 | 用户手册与技术笔记微分方程求导文档 | `pdflatex -interaction=nonstopmode -halt-on-error dS_ibp_package.tex`；`pdflatex -interaction=nonstopmode -halt-on-error dS_IBP_package_tech_note.tex`，均运行两遍 | 通过 | 用户手册位于 `000_note/01_dS_ibp_package/dS_ibp_package.tex`；PDF 已更新；仅有 overfull/font/hyperref 等非致命 warning |
| 2026-07-18 | 009 独立变量求导 example | `wolframscript -file 000_code\examples\independent_variable_derivatives_check.wl` | 通过 | 覆盖 `ke[i]`、`s11`、`s11` 分解和两外动量非唯一分解记录 |
| 2026-07-18 | 009 hand-derived package examples 全量回归 | `Get-ChildItem 000_code\examples\hand_derived_cross_checks -Filter '*_check.wl' | sort Name | foreach { wolframscript -file $_.FullName }` | 全部通过 | atomic massive 104/104；atomic massless 22/22+8/8；mixed bubble 138/138；mixed sunrise 2178/2178；mixed triangle 1800/1800；parallel massless 242/242；pure massive bubble reference 310/310；pure massless 70/70；two-loop ISP 1230/1230；vertex energy signs 90/90 |
| 2026-07-18 | 本次文档/example diff hygiene | `git diff --check -- 000_code/009_dS_ibp_general.wl 000_code/examples/independent_variable_derivatives_check.wl README.md 000_note/01_dS_ibp_package/dS_ibp_package.tex 000_note/01_dS_ibp_package/dS_ibp_package.pdf 000_note/dS_IBP_package_plan.md 000_note/dS_IBP_package_design_note.md 000_note/dS_IBP_package_tech_note.tex 000_note/dS_IBP_package_tech_note.pdf 研究计划与研究进度.md` | 通过 | 限定到本次相关文件，避免旧文件既有空白问题干扰 |
| 2026-07-18 | GitHub 更新 | `git add -A`；`git commit -m "Update dS IBP docs and derivative examples"`；`git push origin main` | 通过 | 主体发布 commit `f071575` 已推送到 `origin/main` |
| 2026-07-17 | 009 独立变量求导模块 | `wolframscript -file 000_code\examples\independent_variable_derivatives_check.wl` | 通过 | 覆盖 `ke[i]` 顶点能量标量求导、`s11` 通过 `k_i.d/dk_j` 分解求导、两外动量时完整算符空间 `nullity>0` 的非唯一性记录 |
| 2026-07-17 | 009 hand-derived examples 全量回归 | 运行 `000_code/examples/hand_derived_cross_checks/*_check.wl` | 全部通过 | atomic massless 22/22+8/8；atomic massive 104/104；pure massless 70/70；mixed bubble 138/138；mixed triangle 1800/1800；pure massive bubble reference 310/310；parallel massless 242/242；mixed sunrise 2178/2178；two-loop ISP 1230/1230；vertex energy signs 90/90 |
| 2026-07-17 | 009 momentum-IBP ISP 导数修复 | 在 `applyMomentumGeneratorSeed` 中加入 `momentumISPDerivativeTerms`，重跑 `mixed_sunrise` 与 `two_loop_isp_toy` | 通过 | 修复前 package actual 漏掉 numerator/ISP 因子导数；修复后两圈 ISP examples 全通过 |
| 2026-07-17 | hand-derived/package example 拆分 | `hand-derived-v2/<family>/` 只留 README/family/expected；package actual 比较脚本统一放到 `000_code/examples/hand_derived_cross_checks/` | 完成 | 新增独立 helper `_manual_ibp_engine.wl`，不加载 package；actual 对比只在 examples 中加载 `009` |
| 2026-07-16 | 009 mixed bubble 独立手推 | 提权运行 `000_code/check/hand-derived-v2/mixed_bubble/check.wl` | 138/138 | massive h + massless exp；全 SK、top/e1/e2/e1_e2、全部 active time 与 `d/dq.q`/`d/dq.k`；覆盖计数正确 |
| 2026-07-16 | 项目最新状态盘点 | 读取 progress/README/AGENTS/000_code/000_note/independent-benchmark/check 目录，`rg` 核对关键未完成项，`git status --short` | 完成 | 当前主线仍为 009；下一步为 `mixed_bubble`；发现 `hand-derived-v2/README.md` 仍写 007 convention，README/AGENTS 仍提到已不存在的 006 |
| 2026-07-15 | 独立 benchmark convention | 10 family 字段审计、74 个代码围栏、`git diff --check` | 通过 | 自包含 SK/h-EOM/massless signs；未运行解析 IBP 或 reduction |
| 2026-07-15 | 009 massless time-IBP 专项 | 提权运行 `009_massless_direction_check.wl` | 27/27 | 新增同点 regular 与 `-2/+2` theta-delta 抵消断言 |
| 2026-07-15 | 用户手册与技术笔记 | `latexmk -pdf` | 11 页 / 23 页，编译通过 | 日志无 error/未定义引用；目视检查因图像读取 ACL 待补 |
| 2026-07-15 | 009 pure massless bubble | 提权运行 70 条独立关系 | 70/70 | 全 SK、top/e1/e2/e1_e2、全部生成元 |
| 2026-07-15 | 009 atomic massless 回归 | `atomic_massless_line/check.wl` | 22/22 + 8/8 | 无回归 |
| 2026-07-15 | 009 atomic massive 回归 | `atomic_massive_line/check.wl` | 104/104 | 无回归 |
| 2026-07-15 | 009 Kira serializer smoke | 提权运行 `009_kira_export_smoke_check.wl` | 11/11 | 只检查基础 serializer，未运行 Kira/Fermat |
| 2026-07-15 | 008 pure massless bubble 缺陷判定 | 70 条全 SK/sector/generator 关系 | 66/70 | 4 个失败均为目标 subsector coincident `n=1` 未置零 |
| 2026-07-15 | 008 massless 原子回归 | `008_massless_direction_check.wl` | 25/25 | 无消息 |
| 2026-07-15 | 008 `sp` 接口 | `008_sp_interface_check.wl` | 24/24 | 含任意动量名与 Orderless |
| 2026-07-15 | 008 scalar-product cache | `008_scalar_product_cache_check.wl` | 8/8 | 无消息 |
| 2026-07-15 | 008 Kira serializer smoke | `008_kira_export_smoke_check.wl` | 11/11 | 未运行 Kira/Fermat |
| 2026-07-15 | 008 atomic massive 修复 | 提权运行 `atomic_massive_line/check.wl` | 104/104 | 无 Wolfram 消息 |
| 2026-07-15 | 008 symmetry/Vpm | 提权运行 `008_symmetry_check.wl` | 11/11 | 含 `SetAttributes[sp,Orderless]` 直接实现检查 |
| 2026-07-15 | 008 atomic massless 回归 | 提权运行 `atomic_massless_line/check.wl` | 22/22 + 8/8 | 无回归 |
| 2026-07-15 | 007 `atomic_massive_line` 缺陷判定 | 提权运行 104 条独立手推关系 | 96/104 | 8 个失败全为 `++` Wronskian shrink 反号；其余 96 条通过 |
| 2026-07-15 | `atomic_massless_line` | 提权运行 `hand-derived-v2/atomic_massless_line/check.wl` | 22/22 + 8/8 | expected 不调用 007；只在 check 中生成 actual |
| 2026-07-15 | 旧手推数据清除 | 删除 `hand-derived/`，建立 `hand-derived-v2/` 空骨架 | 完成 | 未迁移任何旧 `.wl` 或 README |
| 2026-07-15 | 任务与文档边界 | progress/plan/README/AGENTS/independent benchmark 一致性检查 | 通过 | 已明确 007 真实状态和未完成手推范围 |
| 2026-07-15 | 007 massless 原子检查 | `007_massless_direction_check.wl` | 25/25 | 现有实现回归，尚不是新手推全族基准 |
| 2026-07-15 | 007 `sp` 接口 | `007_sp_interface_check.wl` | 24/24 | 轻量检查 |
| 2026-07-15 | 007 scalar-product cache | `007_scalar_product_cache_check.wl` | 8/8 | 轻量检查 |
| 2026-07-15 | 007 Kira serializer smoke | `007_kira_export_smoke_check.wl` | 11/11 | 未运行 Kira/Fermat |

## 5. 接手者的下一步

1. 先完成并维持 `000_note/2026-07-21_common_theta_correctness_todo.md` 的全部正确性门禁；共同-theta、`WT`、指标/零点、coincident canonical 和可达 sector 不是可选优化。
2. 在不破坏 012 显式 topology API 的前提下，把当前可直接 `Get` 的科研脚本迁入正式 `BeginPackage/EndPackage` context，并为公开符号增加 `::usage`。
3. 抽取 10 个 package cross-check 的统一比较 helper，并为 sampled `linearData` 增加小型代数数值规则专项验证。
4. 不运行 Kira/Fermat；后续仍只在 seed/linear serializer 层做小规模验证。

### 当前任务：MadStree 无外腿顶点的辅助能量边界与显式奇点目标
- [x] 新版本前置门禁：从空结果目录 fresh 串行运行现行 MadStree v0.14 的全部 test 与 Examples；所有 Wolfram 入口退出码均为 0。核心 `58/58`、路径 `27/27`、制品 `24/24`、同步/周期图表 `22/22`、顶点约化 `19/19`，六个 examples 均通过，正规化 example `16/16`；测试产物已清理。
- [x] 建立 FlintNDE 0.5.0 与 MadStree v0.15 的计划/todolist；独立 FlintNDE 当前回归为
  `173/173`，Wolfram 接口为 `25/25`，Vendor 同步边界已核对。
- [x] 允许用户省略或输入 `externalLegEnergy -> 0`；内部改写为独立辅助能量符号，先生成无穷远边界，再把辅助能量输运回用户目标值 0。
- [x] 区分拓扑建族的辅助参数与数值 `pointSequence` 中的零值；私有辅助坐标不进入用户表头或固定参数完整性要求。
- [x] 完成中间奇点的 FlintNDE singularity-jump 路径：同一奇点局部基覆盖收敛域内双侧用户点，精确奇点不进入普通节点，真实发散输出文本 `Infinity`，并从出射普通点继续输运；MadStree 仍只负责单变量 affine 分段。
- [ ] 修复末端奇点匹配路线：由独立 FlintNDE 按目标奇点到最近其它奇点的距离选择域内隐藏普通匹配点，MadStree 只消费该计划；主链低阶值作为结果，参考链高阶值只做分类一致性和精度核验。
- [ ] 更新 MadStree v0.15/FlintNDE 0.5.0 接口文档、手册和测试；建立当前版本独立验证并从空结果目录 fresh 运行，清理不再适用的旧限制文字，完成 `git diff --check`。

验收标准：现行 v0.14 基线 fresh 测试全部记录；FlintNDE 0.5.0 独立测试和文档先完成，再与 MadStree Vendor 逐文件同步；新版本缺省/零外腿输入可自动构造辅助边界并回到零；显式奇点目标默认进入局部奇点模式并给出风险提示；自动绕行模式仍可选且明确可能绕开 apparent/removable singularity；无负幂且无零阶 log 时返回有限常数（允许为 0），有 pole 或 log 发散时返回可识别状态；普通点行为不回归。
### 当前任务：012 tadpole 对称性并入与批量独立变量导数
- [x] 自动识别 self-loop tadpole，并生成可与用户 `symmetryRules` 求并集的正则化对称性规则。
- [x] 让自动/用户对称性通过统一 `symmetry[expr, topo]` 在 seed canonical、全 sector 初始化及线性数据前一致生效。
- [x] 批量枚举独立外部不变量与顶点能量变量，生成微分方程 seed 及 decomposition/状态报告。
- [x] 新增独立 hand-derived tadpole symmetry 与 derivative-batch benchmark，并完成交叉检查。

验收标准：自动 tadpole 规则不覆盖用户规则；massive self-loop 端点态交换、massless 反对称态/odd ISP 归零可被独立 expected 复核；批量导数接口覆盖所有独立变量并明确失败原因；012 既有测试与新增 benchmark 全部通过。

完成结论：`012_dS_ibp_general.wl` 已新增原始 self-loop tadpole 识别、自动规则与用户规则并集、canonical/sector/linearData 接入，以及批量独立变量导数 API。odd ISP 只在 loop momentum 独占该 self-loop 时归零；`massiveCross/masslessCross` 与 shrink 后才 coincident 的普通线均有防误用门禁。13 个根目录 012 测试、11 个 hand-derived cross-check 全部通过；新增 tadpole/derivative benchmark 8/8，通过项包含 shared-loop 与 `G+-` guard。Kira 仅运行既有 export smoke 11/11，未运行 reduction。

### 当前任务：012 外部动力学变量总导数 `ds[expr,sij]`
- [x] 审计 reference 中 `*_de` 文件及 IBP 脚本的求导定义，提取系数项与指标项的乘积法则。
- [x] 实现公开入口 `ds[expr_, sij_]`：`expr` 可为单个 `J` 或带动力学量系数的线性组合，`sij` 使用 family 初始化约定的外部变量名。
- [x] 保留已有单积分导数核心，并补齐表达式系数的显式求导贡献、外部/内部变量映射和 canonical/symmetry 门禁。
- [x] 将参考公式与独立 expected 整理到专用 hand-derived/cross-check 目录，覆盖系数导数、积分导数及二者同时存在的情形。
- [x] 更新 README、plan、design note 与研究进度，并运行全部受影响回归。

验收标准：`ds[c(s) J_1+d(s) J_2,s]` 必须严格等于 `c'(s)J_1+c(s)ds[J_1,s]+d'(s)J_2+d(s)ds[J_2,s]`；不接受未初始化的外部变量名；结果无遗漏的系数导数、无 forbidden `n`，并与独立参考交叉检查一致。

完成结论：公开入口 `ds[expr,sij,topo]` 与 context 短签名 `ds[expr,sij]` 已实现。程序先将不同 `J` 替换为惰性 token，对显式动力学量系数执行 `D`，再逐积分加入指标导数并统一执行 EOM、symmetry 与 canonical；内部 `kk[i,j]`、未初始化变量及非线性 `J_i J_j` 均拒绝。参考 `001 bubble_ibp_sym.m` 的 `D[expr,ks]+积分导数` 结构和 `002 bubble_de.m` 的符号变量保留要求已整理到 `012-hand-derived/ds_total_derivative/`，独立 check 9/9。交叉检查同时确认并修复了旧 external-vector helper 对 `Sqrt[s11]` 未执行链式法则而产生 `sqrt(2)` 错因的问题。13 个根目录 012 测试和 12 个 hand-derived cross-check 全部通过；Kira export smoke 11/11，未运行 reduction。README、plan、design note、技术笔记和用户手册均已更新，两份 PDF 编译成功且受影响页渲染检查通过；正式 `package_012.wl/pdf` 与权威源文件 SHA-256 分别一致。
