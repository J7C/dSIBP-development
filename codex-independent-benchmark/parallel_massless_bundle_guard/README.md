# parallel_massless_bundle_guard

三条 massless 线均有序 `v1->v2`。固定 `--` 可达 `{top,e1,e2,e3,e1_e2_e3}`，不存在任何 double sector；固定 `+-` 只有 top。top 离散态 8，其余为 1；每 sector momentum generator 为 3。

总 relation 61、`s11/s12/s22` 总导数 18、zero relation 0。任务书与独立 oracle 均正式固定 upper-triangular `Dij (i<=j)`，重生后 package 对照为 18/18。triple contact 是本 family 的主要验收项。
