# parallel_massless_bundle_guard 推导

三线共同 theta 的 boundary 是 `A1A2A3-B1B2B3`。odd-subset 展开只含三个 single 与一个 triple。对 `n1=n2=n3=1`，single 项因两个 unselected `M1` coincidence 为零，triple 系数为 `(1/4)(-2)^3=-2`；这直接复现原始乘积差值 `(-1)^3-1=-2`。

动量闭合为

```text
q^2=D1,
k1.q=(s11+D1-D2)/2,
k2.q=(s22+D1-D3)/2.
```

四个外部 `D_ij` 反解三个不变量导数。
