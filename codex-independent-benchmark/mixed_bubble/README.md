# mixed_bubble

line 1 是 massive h，line 2 是有序 `v1->v2` massless。固定 `++` 的共同 theta odd subset 给 sector `{top,e1,e2}`；两线 simultaneous shrink 是 even subset，不可达。固定 `-+` 只有 top。

同分支 top `(time,momentum,discrete)=(2,2,8)`；`e1` 为 `(1,2,1)`，`e2` 为 `(1,2,4)`，其中未选中的 massive sector seed 遍历 `00/01/10/11`，关系输出把 `10` canonical 到 `01`。cross top 为 `(2,2,4)`。总 relation 63、`s11` 总导数 4、zero relation 0。
