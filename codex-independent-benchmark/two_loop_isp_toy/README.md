# two_loop_isp_toy

保留用户符号 `l3,k321,wdnmd`。三条 massless 线均以 `v1->v2` 为正方向；固定 `++` sector 为 `{top,e1,e2,e3,e1_e2_e3}`，固定 `-+` 只有 top。每 sector 有 6 条 momentum generators；同分支 top 离散态 8，其余为 1；另遍历 ISP 点 `{0,0},{1,0},{0,1}`。

总 relation 300、`s11` 总导数 6、zero relation 0。`sp` 只使用 Orderless，不重命名用户变量。
